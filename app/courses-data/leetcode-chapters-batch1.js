export const chapters = [
  { id: "lc-1", group: "数组", icon: "🟢", title: "1. 两数之和（简单）", content: `# 1. 两数之和\n\n## 题目描述\n给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出和为目标值 target 的那两个整数，并返回它们的数组下标。\n\n**示例：** nums = [2,7,11,15], target = 9 → [0,1]\n\n## 解题思路\n\n**方法一：暴力枚举** - 双重循环，O(n²)时间\n\n**方法二：哈希表（最优）** - 一次遍历，用哈希表存储已遍历元素及其索引\n\n## 解法一：暴力法\n\n\`\`\`python\ndef twoSum(nums, target):\n    n = len(nums)\n    for i in range(n):\n        for j in range(i+1, n):\n            if nums[i] + nums[j] == target:\n                return [i, j]\n    return []\n\`\`\`\n\n\`\`\`java\nclass Solution {\n    public int[] twoSum(int[] nums, int target) {\n        for (int i = 0; i < nums.length; i++) {\n            for (int j = i + 1; j < nums.length; j++) {\n                if (nums[i] + nums[j] == target) return new int[]{i, j};\n            }\n        }\n        return new int[0];\n    }\n}\n\`\`\`\n\n**复杂度**：时间 O(n²)，空间 O(1)\n\n## 解法二：哈希表\n\n\`\`\`python\ndef twoSum(nums, target):\n    d = {}\n    for i, num in enumerate(nums):\n        if target - num in d:\n            return [d[target-num], i]\n        d[num] = i\n    return []\n\`\`\`\n\n\`\`\`java\nimport java.util.HashMap;\nclass Solution {\n    public int[] twoSum(int[] nums, int target) {\n        HashMap<Integer, Integer> map = new HashMap<>();\n        for (int i = 0; i < nums.length; i++) {\n            if (map.containsKey(target - nums[i])) return new int[]{map.get(target-nums[i]), i};\n            map.put(nums[i], i);\n        }\n        return new int[0];\n    }\n}\n\`\`\`\n\n**复杂度**：时间 O(n)，空间 O(n)\n\n## 边界条件\n- 重复元素：先查找再插入，可正确处理如 [3,3] 的情况\n- 负数：天然支持\n\n## 相似题目\n- 15. 三数之和、18. 四数之和、167. 两数之和 II` },
  { id: "lc-26", group: "数组", icon: "🟢", title: "26. 删除有序数组中的重复项（简单）", content: `# 26. 删除有序数组中的重复项\n\n## 题目描述\n给你一个升序排列的数组 nums，请你原地删除重复出现的元素，使每个元素只出现一次，返回删除后数组的新长度。\n\n**示例：** nums = [1,1,2] → 返回 2，nums = [1,2,_]\n\n## 解题思路\n\n**双指针法**：慢指针 slow 指向已处理位置，快指针 fast 遍历数组。\n\n## 解法：双指针\n\n\`\`\`python\ndef removeDuplicates(nums):\n    if not nums: return 0\n    slow = 0\n    for fast in range(1, len(nums)):\n        if nums[fast] != nums[slow]:\n            slow += 1\n            nums[slow] = nums[fast]\n    return slow + 1\n\`\`\`\n\n\`\`\`java\nclass Solution {\n    public int removeDuplicates(int[] nums) {\n        if (nums.length == 0) return 0;\n        int slow = 0;\n        for (int fast = 1; fast < nums.length; fast++) {\n            if (nums[fast] != nums[slow]) nums[++slow] = nums[fast];\n        }\n        return slow + 1;\n    }\n}\n\`\`\`\n\n**复杂度**：时间 O(n)，空间 O(1)\n\n## 相似题目\n- 27. 移除元素、80. 删除有序数组中的重复项 II` },
  { id: "lc-27", group: "数组", icon: "🟢", title: "27. 移除元素（简单）", content: `# 27. 移除元素\n\n## 题目描述\n给你一个数组 nums 和一个值 val，你需要原地移除所有数值等于 val 的元素，并返回移除后数组的新长度。\n\n## 解法：双指针\n\n\`\`\`python\ndef removeElement(nums, val):\n    slow = 0\n    for fast in range(len(nums)):\n        if nums[fast] != val:\n            nums[slow] = nums[fast]\n            slow += 1\n    return slow\n\`\`\`\n\n\`\`\`java\nclass Solution {\n    public int removeElement(int[] nums, int val) {\n        int slow = 0;\n        for (int fast = 0; fast < nums.length; fast++) {\n            if (nums[fast] != val) nums[slow++] = nums[fast];\n        }\n        return slow;\n    }\n}\n\`\`\`` },
  { id: "lc-35", group: "数组", icon: "🟢", title: "35. 搜索插入位置（简单）", content: `# 35. 搜索插入位置\n\n## 题目描述\n给定一个排序数组和一个目标值，在数组中找到目标值，并返回其索引。如果目标值不存在于数组中，返回它将会被按顺序插入的位置。\n\n## 解法：二分查找\n\n\`\`\`python\ndef searchInsert(nums, target):\n    left, right = 0, len(nums) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target: return mid\n        elif nums[mid] < target: left = mid + 1\n        else: right = mid - 1\n    return left\n\`\`\`\n\n\`\`\`java\nclass Solution {\n    public int searchInsert(int[] nums, int target) {\n        int left = 0, right = nums.length - 1;\n        while (left <= right) {\n            int mid = left + (right - left) / 2;\n            if (nums[mid] == target) return mid;\n            else if (nums[mid] < target) left = mid + 1;\n            else right = mid - 1;\n        }\n        return left;\n    }\n}\n\`\`\`\n\n**复杂度**：时间 O(log n)，空间 O(1)\n\n## 相似题目\n- 704. 二分查找` },
  { id: "lc-53", group: "数组", icon: "🟢", title: "53. 最大子数组和（简单）", content: `# 53. 最大子数组和\n\n## 题目描述\n给你一个整数数组 nums，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。\n\n**示例：** nums = [-2,1,-3,4,-1,2,1,-5,4] → 6（[4,-1,2,1]）\n\n## 解法一：动态规划（Kadane算法）\n\n\`\`\`python\ndef maxSubArray(nums):\n    max_sum = curr_sum = nums[0]\n    for num in nums[1:]:\n        curr_sum = max(num, curr_sum + num)\n        max_sum = max(max_sum, curr_sum)\n    return max_sum\n\`\`\`\n\n\`\`\`java\nclass Solution {\n    public int maxSubArray(int[] nums) {\n        int maxSum = nums[0], currSum = nums[0];\n        for (int i = 1; i < nums.length; i++) {\n            currSum = Math.max(nums[i], currSum + nums[i]);\n            maxSum = Math.max(maxSum, currSum);\n        }\n        return maxSum;\n    }\n}\n\`\`\`\n\n**复杂度**：时间 O(n)，空间 O(1)\n\n## 解法二：分治法\n略，时间 O(n log n)\n\n## 相似题目\n- 152. 乘积最大子数组` },
  { id: "lc-66", group: "数组", icon: "🟢", title: "66. 加一（简单）", content: `# 66. 加一\n\n## 题目描述\n给定一个由整数组成的非空数组所表示的非负整数，在该数的基础上加一。\n\n## 解法\n\n\`\`\`python\ndef plusOne(digits):\n    for i in range(len(digits)-1, -1, -1):\n        if digits[i] < 9:\n            digits[i] += 1\n            return digits\n        digits[i] = 0\n    return [1] + digits\n\`\`\`\n\n\`\`\`java\nclass Solution {\n    public int[] plusOne(int[] digits) {\n        for (int i = digits.length - 1; i >= 0; i--) {\n            if (digits[i] < 9) { digits[i]++; return digits; }\n            digits[i] = 0;\n        }\n        int[] res = new int[digits.length + 1];\n        res[0] = 1;\n        return res;\n    }\n}\n\`\`\`` },
  { id: "lc-88", group: "数组", icon: "🟢", title: "88. 合并两个有序数组（简单）", content: `# 88. 合并两个有序数组\n\n## 题目描述\n给你两个按非递减顺序排列的整数数组 nums1 和 nums2，另有两个整数 m 和 n，分别表示 nums1 和 nums2 中的元素数目。请你合并 nums2 到 nums1 中，使合并后的数组同样按非递减顺序排列。\n\n## 解法：逆向双指针\n\n\`\`\`python\ndef merge(nums1, m, nums2, n):\n    p1, p2, p = m-1, n-1, m+n-1\n    while p2 >= 0:\n        if p1 >= 0 and nums1[p1] > nums2[p2]:\n            nums1[p] = nums1[p1]\n            p1 -= 1\n        else:\n            nums1[p] = nums2[p2]\n            p2 -= 1\n        p -= 1\n\`\`\`\n\n\`\`\`java\nclass Solution {\n    public void merge(int[] nums1, int m, int[] nums2, int n) {\n        int p1 = m-1, p2 = n-1, p = m+n-1;\n        while (p2 >= 0) {\n            if (p1 >= 0 && nums1[p1] > nums2[p2]) nums1[p--] = nums1[p1--];\n            else nums1[p--] = nums2[p2--];\n        }\n    }\n}\n\`\`\`` },
  { id: "lc-118", group: "数组", icon: "🟢", title: "118. 杨辉三角（简单）", content: `# 118. 杨辉三角\n\n## 题目描述\n给定一个非负整数 numRows，生成杨辉三角的前 numRows 行。\n\n## 解法\n\n\`\`\`python\ndef generate(numRows):\n    res = [[1]]\n    for i in range(1, numRows):\n        prev = res[-1]\n        curr = [1]\n        for j in range(1, i):\n            curr.append(prev[j-1] + prev[j])\n        curr.append(1)\n        res.append(curr)\n    return res if numRows else []\n\`\`\`\n\n\`\`\`java\nimport java.util.*;\nclass Solution {\n    public List<List<Integer>> generate(int numRows) {\n        List<List<Integer>> res = new ArrayList<>();\n        if (numRows == 0) return res;\n        res.add(new ArrayList<>(Arrays.asList(1)));\n        for (int i = 1; i < numRows; i++) {\n            List<Integer> curr = new ArrayList<>();\n            curr.add(1);\n            List<Integer> prev = res.get(i-1);\n            for (int j = 1; j < i; j++) curr.add(prev.get(j-1) + prev.get(j));\n            curr.add(1);\n            res.add(curr);\n        }\n        return res;\n    }\n}\n\`\`\`` },
  { id: "lc-121", group: "数组", icon: "🟢", title: "121. 买卖股票的最佳时机（简单）", content: `# 121. 买卖股票的最佳时机\n\n## 题目描述\n给定数组 prices，第 i 个元素 prices[i] 表示第 i 天的价格。只能选择某一天买入，未来某一天卖出，求最大利润。\n\n## 解法：一次遍历\n\n\`\`\`python\ndef maxProfit(prices):\n    min_price, max_profit = float('inf'), 0\n    for p in prices:\n        min_price = min(min_price, p)\n        max_profit = max(max_profit, p - min_price)\n    return max_profit\n\`\`\`\n\n\`\`\`java\nclass Solution {\n    public int maxProfit(int[] prices) {\n        int minPrice = Integer.MAX_VALUE, maxProfit = 0;\n        for (int p : prices) {\n            minPrice = Math.min(minPrice, p);\n            maxProfit = Math.max(maxProfit, p - minPrice);\n        }\n        return maxProfit;\n    }\n}\n\`\`\`\n\n**复杂度**：时间 O(n)，空间 O(1)` },
  { id: "lc-136", group: "数组", icon: "🟢", title: "136. 只出现一次的数字（简单）", content: `# 136. 只出现一次的数字\n\n## 题目描述\n给定一个非空整数数组，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现了一次的元素。\n\n**要求：** 线性时间复杂度，常数空间。\n\n## 解法：异或运算\n\n\`\`\`python\ndef singleNumber(nums):\n    res = 0\n    for num in nums:\n        res ^= num\n    return res\n\`\`\`\n\n\`\`\`java\nclass Solution {\n    public int singleNumber(int[] nums) {\n        int res = 0;\n        for (int num : nums) res ^= num;\n        return res;\n    }\n}\n\`\`\`\n\n**异或性质**：a^a=0, a^0=a, 满足交换律结合律\n\n## 相似题目\n- 137. 只出现一次的数字 II、260. 只出现一次的数字 III` }
];
