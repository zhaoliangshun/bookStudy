// =============================================================
// 面向开发者的计算机原理 - batch3
// 章节 11-15：并发与IO
// -------------------------------------------------------------
// 定位：纯内容阅读型教程（无代码执行），简单易懂，生活类比，对日常编程有实际帮助。
// 面向：已经会写代码但想理解原理的开发者。
// 转义规则：content 内部不使用反引号，用普通引号或加粗代替，避免 JS 语法问题。
// =============================================================

export const chapters = [
  // ============================================================
  // 第 11 章：进程与线程：并发的基石
  // ============================================================
  {
    id: "hw-process-thread",
    group: "并发与IO",
    icon: "🏭",
    title: "进程与线程：并发的基石",
    content: `## 从一个工厂的故事说起

想象一家大型工厂。工厂里有好几个独立的车间，每个车间有自己的工具、原料仓库和生产任务，车间之间互不干扰。在每个车间内部，又有好几个工人一起干活，他们共享这个车间的工具和原料，协作完成本车间的任务。

这个故事就是计算机里"进程和线程"的完美类比：

- **进程** = 车间。每个进程有自己的独立内存空间（工具和原料仓库），互不干扰。
- **线程** = 车间里的工人。同一进程内的多个线程共享这个进程的内存空间，可以协作完成任务。

理解这个类比，你就理解了并发的基石。后面所有的并发概念——锁、竞态、协程、事件循环——都是在这个基础上展开的。

## 进程：独立内存空间的程序实例

什么是进程？一句话：**进程是一个正在运行的程序实例，拥有独立的内存空间**。

你在电脑上同时打开浏览器、微信、音乐播放器，这就是三个进程。每个进程都有自己的一块内存，浏览器不能直接读写微信的内存，反之亦然。这种隔离保证了稳定性——一个程序崩溃了，不会把另一个程序也带崩。

生活类比：

- 车间A在生产手机，车间B在生产电脑。两个车间各有各的零件库，A车间的工人拿不到B车间的零件。
- 这种隔离让生产互不干扰，但也意味着两个车间要交换零件时，得通过"物流"（进程间通信，IPC）来运送，而不是直接走过去拿。

进程的几个关键特点：

1. **独立内存空间**：每个进程有自己的虚拟地址空间，互不侵犯。
2. **资源分配的基本单位**：操作系统给每个进程分配内存、文件句柄等资源。
3. **创建开销大**：创建一个进程要复制内存空间、分配资源，是个"重活"。
4. **切换开销大**：CPU 从一个进程切换到另一个进程，要切换内存映射、刷新缓存，代价不小。

在命令行里敲下 node app.js，操作系统就为你创建了一个 Node.js 进程。你用 ps 或任务管理器看到的每一个条目，都是一个进程。

## 线程：进程内的执行单元

线程是什么？**线程是进程内的执行单元，同一进程的多个线程共享该进程的内存空间**。

回到车间的类比：一个车间（进程）里雇了 5 个工人（线程）。他们共享车间的工具和零件库（共享内存），可以同时做不同的工序，协作完成生产任务。一个工人发现零件不够了，可以直接去仓库拿，因为仓库是大家共享的。

线程的关键特点：

1. **共享内存**：同进程的线程可以直接读写同一块内存，通信极其方便（但也容易出问题，下一章详谈）。
2. **CPU 调度的基本单位**：操作系统实际上是在调度线程，不是进程。
3. **创建开销小**：比进程轻得多，因为不需要复制内存空间。
4. **切换开销小**：线程切换不用换内存映射，比进程切换快。

一个进程至少有一个线程（主线程）。你在 Java 里 new Thread().start()、在 Python 里 threading.Thread().start()，就是在进程内创建新的线程。

## 为什么需要多线程

既然线程切换也要开销，为什么还要多线程？两个核心原因：

**原因一：充分利用多核 CPU**

现在的 CPU 都是 4 核、8 核、16 核甚至更多。一个线程同一时刻只能跑在一个核上。如果你只用单线程，那 8 核 CPU 你只用了一个核，另外 7 个核在"看戏"，太浪费了。

多线程可以把任务分给多个核同时执行。比如你要处理 1000 张图片，单线程要一张张来，8 线程可以 8 张并行处理，理论上快 8 倍。

生活类比：一个车间雇 8 个工人，8 道工序同时进行，比一个工人从头做到尾快得多。

**原因二：避免 IO 阻塞**

程序运行时不是一直在算数，很多时候在等 IO——等硬盘读数据、等网络返回、等用户输入。如果单线程，等 IO 的时候 CPU 就闲着了，白白浪费时间。

多线程可以让一个线程等 IO 时，另一个线程继续干活。比如 Web 服务器，一个线程在等数据库返回时，另一个线程可以处理别的用户请求，整体吞吐量大幅提升。

生活类比：车间里一个工人在等零件到货（IO 阻塞），其他工人可以继续手头的活，不会整个车间停工。

## 线程的代价：创建、切换、内存

多线程不是免费的午餐，它有三笔代价：

**代价一：创建开销**

虽然线程比进程轻，但创建线程仍要分配栈空间（默认通常是 1MB 到 8MB）、分配内核数据结构。频繁创建销毁线程会很费资源。这就是为什么有线程池——预先创建一批线程，重复使用，避免反复创建。

**代价二：切换开销**

CPU 从一个线程切到另一个线程时，要保存当前线程的寄存器状态、刷新 CPU 缓存、加载新线程的状态。虽然比进程切换快，但仍要消耗微秒级的时间。线程太多时，CPU 光是切换就来不及干活了，这种现象叫"颠簸"（thrashing）。

**代价三：内存占用**

每个线程都要有自己的栈空间。创建 1000 个线程，光栈就要占 1GB 到 8GB 内存。所以线程数量不能无限多。

## 协程：用户态的轻量线程

为了解决线程太"重"的问题，出现了**协程**（coroutine），也叫纤程（fiber）、绿色线程、用户态线程。

协程是什么？**协程是在用户态（而非操作系统内核）管理的轻量级"线程"**。它的切换不需要操作系统介入，由程序自己（或语言运行时）调度，所以极其轻量。

协程的特点：

1. **极轻量**：一个协程只占几 KB 内存，可以轻松创建几十万个。
2. **切换极快**：在用户态切换，不用进内核，不用刷新 CPU 缓存。
3. **协作式调度**：协程要主动"让出"执行权（yield/await），不像线程是被抢占的。

不同语言的协程实现：

- **Go 的 goroutine**：用 go 关键字启动，运行时自动调度，可以轻松开十万并发。Go 的哲学是"用通信代替共享内存"（CSP 模型）。
- **Python 的 async/await**：基于协程的异步编程，单线程处理高并发 IO。
- **JavaScript 的 async/await**：配合事件循环，是 Node.js 和前端异步的核心。
- **Java 的虚拟线程（Loom 项目）**：JDK 21 正式引入，让 Java 也能轻松开百万并发。

生活类比：如果说线程是工厂正式雇佣的工人（要签合同、占工位、有保险），协程就是"临时帮工"——叫来就干、干完就走、几乎没成本，所以可以叫来非常多。

## 多进程 vs 多线程 vs 协程怎么选

这是开发者常问的问题。一个简单的选择指南：

**选多进程**：

- 需要极高稳定性，一个崩了不能影响其他（如 Nginx 的 worker 进程模型）。
- 不需要频繁共享数据，或共享数据成本可以接受。
- 想充分利用多核，又不想处理复杂的线程同步问题。
- 典型：Nginx、Chrome 浏览器（每个标签页一个进程）、Python multiprocessing。

**选多线程**：

- 需要共享大量数据，共享内存比进程间通信方便得多。
- 任务是 CPU 密集型，要榨干多核性能。
- 能接受线程同步带来的复杂度。
- 典型：Java 后端服务、数据库、游戏引擎。

**选协程**：

- 主要是 IO 密集型任务（网络请求、数据库、文件 IO）。
- 需要极高的并发数（几万、几十万连接）。
- 能接受协程是单线程内调度（一个协程阻塞会卡住整个线程，除非用异步 IO）。
- 典型：Go 服务、Node.js、Python 异步框架。

注意：协程本身不解决 CPU 密集型任务的多核利用问题。Go 通过多线程加每个线程上跑多个 goroutine（GMP 模型）来同时获得多核和轻量并发；Python 和 JS 的协程默认是单线程的，要利用多核还得开多进程。

## 日常启示：为什么 Node.js 单线程也能高并发

这是初学者最困惑的问题之一：Node.js 主线程是单线程的，为什么能处理高并发请求？

答案的三个关键点：

1. **单线程指的是主执行线程，背后有线程池**：Node.js 的主线程跑事件循环，但文件 IO、加密等操作会交给底层的 libuv 线程池（默认 4 个线程）。

2. **事件循环加非阻塞 IO**：主线程不等待 IO，发起 IO 请求后立刻处理下一个请求，等 IO 完成后通过回调处理结果。这样单线程也能同时"管"很多请求。

3. **适合 IO 密集型，不适合 CPU 密集型**：如果某个请求要做大量计算（如图像处理、加密哈希），会卡住主线程，所有其他请求都得等。这是 Node.js 的软肋。

所以 Node.js 高并发的本质是：**用单线程加事件循环加非阻塞 IO，避免了多线程的切换开销和同步复杂度，特别适合 IO 密集型场景**。但代价是 CPU 密集型任务会阻塞整个事件循环。

这个原理同样适用于 Nginx、Redis——它们都是"单线程加事件循环加多路复用 IO"的模型，在 IO 密集型场景下表现极其出色。

## 小结

- 进程 = 独立内存空间的程序实例（车间），隔离性好但创建和切换重。
- 线程 = 进程内的执行单元（工人），共享内存，比进程轻，但仍需内核调度。
- 多线程的价值：利用多核 CPU、避免 IO 阻塞。
- 协程 = 用户态轻量线程，极轻量，适合高并发 IO。
- 选型：多进程求稳定、多线程求多核加共享、协程求高并发 IO。
- Node.js 单线程高并发的秘密：事件循环加非阻塞 IO 加适合 IO 密集型。

理解进程、线程、协程的区别，是写好多并发程序的第一步。下一章我们看多线程编程里最让人头疼的几个坑——竞态、死锁、可见性。`,
  },

  // ============================================================
  // 第 12 章：并发陷阱：竞态、死锁、可见性
  // ============================================================
  {
    id: "hw-concurrency-problems",
    group: "并发与IO",
    icon: "⚠️",
    title: "并发陷阱：竞态、死锁、可见性",
    content: `## 从一个银行转账的灾难说起

假设银行系统有个变量 balance 记录你的账户余额，初始是 1000 元。现在你同时用手机和电脑登录，都点了"取款 800 元"。如果代码是这样的：

第一步：读取 balance（1000）
第二步：计算 balance - 800 = 200
第三步：写回 balance = 200

如果两个线程（一个处理手机请求，一个处理电脑请求）同时执行，可能这样交错：

- 线程A 读到 balance = 1000
- 线程B 读到 balance = 1000（此时 A 还没写回）
- 线程A 算出 200，写回 balance = 200
- 线程B 算出 200，写回 balance = 200

结果：你取了 1600 元，账户还剩 200 元，银行亏了 600 元。这就是**竞态条件**（race condition）——多个线程同时访问共享数据，最终结果取决于执行的先后顺序。

这一章我们看并发编程里最经典的几个坑，以及怎么避免它们。理解这些坑，你才能写出在多核环境下也正确可靠的代码。

## 竞态条件：并发第一坑

竞态条件的定义：**当多个线程并发访问共享数据，且至少一个线程在写时，最终结果取决于线程执行的相对顺序，就产生了竞态条件**。

它的可怕之处在于：bug 不会每次都出现，而是"时有时无"——单线程测试时一切正常，一上多线程环境就偶尔出错，而且很难复现。这种 bug 被称为 Heisenbug（海森堡 bug，致敬测不准原理），调试起来让人抓狂。

竞态条件的典型场景：

- **读-改-写**：像上面的银行转账，"读出来、改一下、写回去"这三步不是原子的，中间可能被插入。
- **延迟初始化**：经典的单例模式双重检查，如果没正确同步，可能创建出两个实例。
- **复合操作**：比如"如果 map 里没有 key 就 put"，"判断加 put"两步之间可能被插入。

避免竞态的核心思路：让"读-改-写"变成不可分割的原子操作，或者干脆不共享可变数据。

## 原子性：操作不可分割

**原子性**（atomicity）是指一个操作或一组操作"不可分割"——要么全部执行完，要么一步都没执行，中间不会被任何其他线程打断。

"原子"这个词来自希腊语 atomos，意思是"不可分割的"。在并发里，原子操作就像一个"黑盒"，外部看不到中间状态。

怎么保证原子性？最常用的手段是**加锁**。锁的工作原理：线程进入临界区前先"上锁"，其他线程想进来时发现门锁了，就在门口排队等；当前线程干完活"解锁"，下一个线程才能进。

生活类比：银行柜台只有一个窗口（临界区），客户（线程）要办业务必须排队，一个办完下一个才能上。这样就不会出现两个客户同时改余额的情况。

不同语言都有锁的设施：Java 的 synchronized 和 ReentrantLock、Python 的 threading.Lock、Go 的 sync.Mutex。加锁的本质就是把一段代码变成"一次只能一个线程执行"的临界区。

## 死锁：互相等待的僵局

加锁能解决竞态，但加锁本身会带来一个新的大麻烦——**死锁**（deadlock）。

死锁的定义：**多个线程互相持有对方需要的锁，又互相等待对方释放，导致所有线程都卡住，永远无法继续**。

经典的"哲学家就餐问题"：5 个哲学家围坐圆桌，每两人之间放一根筷子（共 5 根）。每个人要吃饭需要同时拿起左右两根筷子。如果所有人同时拿起左手边的筷子，然后都等右手边的筷子——所有人都拿着一根筷子等另一根，永远等不到，全部饿死。这就是死锁。

死锁的四个必要条件（缺一不可）：

1. **互斥**：资源一次只能被一个线程用。
2. **持有并等待**：线程拿着一个资源，还在等另一个。
3. **不可剥夺**：资源不能被强行抢走，只能等持有者主动释放。
4. **循环等待**：A 等 B，B 等 C，C 等 A，形成环。

打破其中任何一个就能避免死锁。最常用的策略是**打破循环等待**：给所有锁排个顺序，所有线程都按相同顺序获取锁，就不会形成环。

生活类比：让所有哲学家都"先拿编号小的筷子，再拿编号大的"，就不会出现循环等待。又或者规定"拿了左手筷子拿不到右手时，放下左手筷子重新来"，这就是"尝试获取+超时回退"的策略。

## 活锁与饥饿：另一种僵局

死锁是"都卡住不动"，还有两种更隐蔽的问题：

**活锁**（livelock）：线程没卡住，一直在动，但做的是无用功。比如两个人在走廊迎面走来，都礼貌地让到同一边，又同时让到另一边，反复让来让去，谁也过不去。在并发里，线程不断重试一个失败的操作（比如抢锁失败就立刻重试，失败了再立刻重试），CPU 跑得很欢，但任务毫无进展。

**饥饿**（starvation）：某个线程一直抢不到资源，永远在等待。比如某个低优先级线程，每次想运行时都被高优先级线程抢先，永远轮不到它。就像食堂排队，总有人插队，老老实实排队的人永远吃不上饭。

解决饥饿的常见办法是**公平锁**——按先来后到的顺序分配锁，不让任何一个线程永远等下去。解决活锁的办法是引入"随机退避"——重试失败后随机等一会儿再试，避免多个线程同步重试又同步失败。

## 内存可见性：CPU 缓存惹的祸

这是一个更隐蔽、更难理解的问题。先看个 Java 经典例子：

线程A 执行：flag = true
线程B 在循环里：while (!flag) { ... }

按直觉，A 把 flag 设成 true 后，B 应该立刻跳出循环。但实际上 B 可能永远跳不出来——这就是**内存可见性问题**。

原因在于 CPU 缓存。现代 CPU 每个核都有自己的 L1/L2 缓存。线程A 在核1上把 flag = true 写进了核1的缓存，还没刷回主内存；线程B 在核2上读 flag，读到的是核2缓存里的旧值 false。于是 B 永远看不到 A 的修改。

生活类比：两个车间各有自己的小黑板（CPU 缓存），A 车间在小黑板上写了通知，但没抄到公司大公告栏（主内存）；B 车间只看大公告栏，永远看不到 A 的通知。

解决可见性的办法：

- **Java 的 volatile 关键字**：保证变量的写立刻刷回主内存，读都从主内存读。
- **内存屏障**：CPU 指令层面的"屏障"，强制刷新缓存。
- **加锁**：锁的获取和释放会自动插入内存屏障，保证可见性。

可见性问题在 Java（JMM，Java 内存模型）里特别突出，在 C/C++ 里更复杂，在 Go 和 Python 里相对少见（因为它们对并发的抽象更高）。但理解原理，你才能在排查"为什么这个变量改了别的线程看不到"时不至于一头雾水。

## 互斥锁、读写锁、自旋锁的区别

锁不只是"一把锁"，不同场景用不同的锁：

**互斥锁**（Mutex）：最基础的锁，同一时刻只允许一个线程进入临界区。简单可靠，但读多写少的场景性能差——明明读不冲突，却要排队。

**读写锁**（RWLock）：区分读和写。多个读可以同时进行（读共享），但写要独占（写排他），且写的时候不能有任何读。适合"读多写少"的场景，比如缓存系统。

生活类比：图书馆的阅览室，大家可以一起进去看书（读共享），但有人要整理书架（写）时，所有人都得出来，整理完再进。

**自旋锁**（Spinlock）：线程获取不到锁时不挂起（不睡眠），而是在循环里反复尝试（自旋）。适合临界区极短的场景——挂起和唤醒线程的开销比自旋等一会儿还大。但如果临界区长，自旋会白白烧 CPU。

生活类比：等电梯时你不坐下休息（不挂起），而是一直盯着电梯门（自旋），因为你知道马上就到了；但如果电梯要从 30 楼下来，你还站着盯就不如坐下等。

## 日常启示：用不可变数据和消息传递

并发问题这么复杂，有没有"治本"的办法？有两条现代并发编程的黄金法则：

**法则一：尽量用不可变数据**

如果数据是不可变的（创建后不能修改），那多个线程同时读它就永远不会出竞态——没有写，就没有冲突。

实践：

- 函数式编程强调不可变数据结构。
- Java 的 String、Integer 等是不可变的，可以安全共享。
- 修改数据时，不就地改，而是创建一个新副本（如 React 的 state 更新理念）。

不可变数据消除了竞态的根源，让并发编程简单得多。代价是要多创建对象，但在现代语言里这点开销通常可以接受。

**法则二：用消息传递代替共享内存**

Go 语言有句名言："不要用共享内存来通信，而要用通信来共享内存。"（Do not communicate by sharing memory; instead, share memory by communicating.）

意思是：与其让多个线程共享一块内存然后加锁访问，不如让每个线程有自己的私有数据，需要交换时通过"消息"（channel）来传递。

- Go 的 channel、Erlang 的 actor 模型、Akka 的 actor 都是这种理念。
- 共享内存加锁容易出 bug（死锁、竞态、可见性），而消息传递把并发问题转化为"消息顺序"问题，更易推理。

这就像：与其让多个工人共用一个工具箱（共享内存加锁），不如每个工人有自己的工具箱，需要工具时互相发消息借（消息传递）。

## 小结

- 竞态条件：多线程同时读写共享数据，结果取决于执行顺序，是并发第一坑。
- 原子性：操作不可分割，靠加锁保证。
- 死锁：互相持有对方需要的锁，打破循环等待即可避免。
- 活锁与饥饿：一直做无用功 / 一直轮不到。
- 内存可见性：CPU 缓存导致一个线程的修改另一个看不到，用 volatile 或内存屏障解决。
- 锁的家族：互斥锁（通用）、读写锁（读多写少）、自旋锁（临界区极短）。
- 现代并发的解法：不可变数据加消息传递。

并发编程很难，但掌握了这些原理，你就能写出更可靠的高并发代码。下一章我们看 Node.js 和 JS 是怎么用事件循环和异步来"绕开"这些并发难题的。`,
  },

  // ============================================================
  // 第 13 章：异步与事件循环：Node.js/JS的秘密
  // ============================================================
  {
    id: "hw-async-event-loop",
    group: "并发与IO",
    icon: "🔄",
    title: "异步与事件循环：Node.js/JS的秘密",
    content: `## 从一个外卖订单说起

你在手机上点了一份外卖，整个过程是这样的：

1. 下单，然后商家接单（要等几分钟）
2. 商家做饭，骑手取餐（要等十几分钟）
3. 骑手送餐，你收到（要等半小时）

如果全程你只能"干等"——盯着手机屏幕什么也干不了，这叫**同步阻塞**。现实中你当然不会这样：下完单你就去看电视、刷手机、和同事聊天，等外卖到了电话响了你再去取。这就是**异步**。

编程里的异步和这个一模一样。理解了它，你就理解了 Node.js、JavaScript、Python async 的核心机制——事件循环。这一章我们就把这个听起来很玄乎的概念彻底讲透。

## 同步 vs 异步：排队等 vs 先做别的回头来看

**同步**（synchronous）：调用一个操作后，调用方一直等它完成才继续往下走。就像你打电话订餐，电话不挂，一直等对方回复"好了"。

**异步**（asynchronous）：调用一个操作后，调用方不等待，立刻继续干别的；操作完成后，通过回调、Promise 或事件通知调用方。就像你下单后不盯着，去做别的事，外卖到了电话通知你。

同步代码简单直观，但遇到耗时操作（网络请求、文件读写）会让整个线程卡住。异步代码复杂（回调、状态管理），但能充分利用等待时间，大幅提升吞吐量。

## 阻塞 vs 非阻塞：干等 vs 立刻返回

**阻塞**（blocking）：调用一个操作时，如果数据没准备好，调用方线程被"挂起"（暂停），一直等到数据准备好才返回。期间线程什么都干不了。

**非阻塞**（non-blocking）：调用一个操作时，如果数据没准备好，立刻返回一个"还没好"的标识，调用方可以去做别的，过会儿再来问。

注意"同步/异步"和"阻塞/非阻塞"是两个维度：

- 同步/异步描述的是**调用方要不要等结果**。
- 阻塞/非阻塞描述的是**被调用方在数据没准备好时的行为**（挂起调用方 vs 立刻返回）。

经典组合：**异步非阻塞**——调用方不等，被调用方也不挂起调用方。这是 Node.js、Nginx 等高并发系统的核心模型。

## 事件循环：单线程高并发的核心

**事件循环**（Event Loop）是 Node.js 和浏览器 JavaScript 实现"单线程高并发"的核心机制。它的模型很简单：

1. 一个主线程，不停地转圈（循环）。
2. 一个事件队列（任务队列），里面排着各种待处理的任务（回调函数）。
3. 每转一圈，就从队列里取出一个任务执行；执行完再取下一个。

当遇到异步操作（如网络请求、定时器）时，主线程不亲自等，而是把它交给底层的异步设施（如 libuv、浏览器网络模块），同时登记一个回调函数，然后立刻继续执行下一个任务。等异步操作完成，结果和回调会被放进事件队列，主线程转过来时就会执行这个回调。

生活类比：事件循环像一个极其高效的快递分拣员。他不停地从传送带上拿起包裹（任务）处理；遇到需要等对方确认的包裹，他不自己等，而是放到"待回复"区，立刻处理下一个包裹；等"待回复"区的包裹有了回复，会被重新放回传送带，他转过来时再处理。

这就是为什么单线程也能"同时"处理很多请求——它不是真的同时执行多个任务，而是利用任务之间的等待时间（IO 等待）穿插处理，让 CPU 始终在干活。

## Node.js 的事件循环阶段

Node.js 的事件循环（基于 libuv）比浏览器的复杂，分为几个阶段（phase），每轮循环依次经过：

1. **timers 阶段**：处理到期的 setTimeout、setInterval 回调。
2. **pending callbacks 阶段**：处理一些系统操作的回调（如 TCP 错误回调）。
3. **idle、prepare 阶段**：内部使用，通常不用关心。
4. **poll 阶段**：获取新的 IO 事件，执行 IO 相关回调（最重要）。如果没到期的定时器，会在这里适当等待。
5. **check 阶段**：执行 setImmediate 的回调。
6. **close callbacks 阶段**：执行 close 事件的回调（如 socket.on('close')）。

此外还有微任务队列（process.nextTick 和 Promise），它们在**每个阶段切换之间**都会被清空，优先级高于宏任务。

理解这些阶段有助于解释一些"诡异"的执行顺序问题，比如 setTimeout(fn, 0) 和 setImmediate(fn) 谁先执行——答案取决于调用时所处的阶段：在主模块里调用，setImmediate 通常先；在 IO 回调里调用，setImmediate 一定先于下一个 setTimeout。

## 浏览器的事件循环：宏任务 vs 微任务

浏览器的事件循环模型相对简单，但有一个关键区分：**宏任务**和**微任务**。

**宏任务**（macrotask）：包括 setTimeout、setInterval、I/O 操作、UI 渲染、postMessage 等。每轮事件循环从宏任务队列里取一个执行。

**微任务**（microtask）：包括 Promise 的 then/catch/finally 回调、MutationObserver、queueMicrotask 等。每执行完一个宏任务，在进入下一个宏任务前，会**清空所有微任务**。

执行顺序：

1. 取一个宏任务执行。
2. 执行过程中可能产生新的微任务和宏任务，分别入队。
3. 这个宏任务执行完后，清空所有微任务（包括执行微任务时新产生的微任务）。
4. 必要时渲染 UI。
5. 回到第 1 步。

## Promise 为什么是微任务，setTimeout 为什么是宏任务

这是初学者常问的问题。答案在于设计目的：

**setTimeout 是宏任务**：它的回调需要"等一段时间"才执行，本质上是基于定时器的，应该和其他宏任务（IO、UI 渲染）一起排队，每轮取一个。如果它是微任务，会在当前任务后立刻清空，那 setTimeout(fn, 0) 就真成了"立刻执行"，和设计初衷（至少延迟到下一轮事件循环）不符。

**Promise 是微任务**：Promise 的 then 回调代表"前一步异步操作完成了，紧接着要做的事"，它和当前任务的关系很紧密，应该尽快执行，不应该排到下一轮宏任务后面。设计成微任务，保证它在当前宏任务结束后、下一个宏任务开始前就执行，让异步代码的行为更可预测、更接近"同步"的直觉。

举个例子：

    console.log('1');
    setTimeout(() => console.log('2'), 0);
    Promise.resolve().then(() => console.log('3'));
    console.log('4');

输出顺序是：1、4、3、2。因为：

- 1、4 是同步代码，先执行。
- 3 是微任务，在同步代码后、下一个宏任务前执行。
- 2 是宏任务，在下一轮事件循环才执行。

## async/await 的本质：Promise 的语法糖

async/await 是 ES2017 引入的异步语法，让异步代码看起来像同步代码一样。但它的本质是 Promise 的语法糖：

- **async 函数**：返回值自动被包装成 Promise。
- **await 表达式**：等价于拿到 Promise 的结果，然后用 then 的回调继续执行后面的代码。await 会"暂停"当前 async 函数的执行（但不阻塞主线程），把控制权交回事件循环，等 Promise resolve 后再继续。

关键理解：**await 不是阻塞整个线程，而是"暂停"当前 async 函数**。在 await 期间，主线程可以去处理其他任务。这是 async/await 比传统回调或纯 Promise 更易读的原因——代码看起来是"从上到下"的，但底层仍是异步的。

## 日常启示：循环里别乱 await，用 Promise.all

**坑一：在循环里串行 await**

新手常这样写：

    for (const url of urls) {
      const data = await fetch(url);
      process(data);
    }

这会导致请求一个接一个串行执行——第一个请求回来了才发第二个，白白浪费了并发的机会。如果每个请求要 1 秒，10 个请求就要 10 秒。

**正确做法**：如果请求之间没有依赖，应该用 Promise.all 并发：

    const datas = await Promise.all(urls.map(url => fetch(url)));
    datas.forEach(process);

这样 10 个请求同时发出，总耗时接近最慢的那个（约 1 秒），而不是 10 秒。

**坑二：忘记 await 一个 async 调用**

    async function save() { await db.write(...); }
    save();  // 忘了 await
    console.log('done');

输出"done"时数据可能还没写完，如果 save 抛错也不会被捕获。记得给 async 调用加 await，或者用 .catch 处理。

**坑三：在 forEach 里 await 无效**

    urls.forEach(async url => { await fetch(url); });
    console.log('all done');  // 不会等！

因为 forEach 的回调虽然标了 async，但 forEach 不会等回调的 Promise resolve。要用 for...of（串行）或 Promise.all 加 map（并发）。

## 小结

- 同步/异步：调用方等不等结果；阻塞/非阻塞：数据没准备好时挂起还是立刻返回。
- 事件循环：单线程加任务队列加回调，靠穿插利用等待时间实现高并发。
- Node.js 事件循环分多个阶段；浏览器分宏任务和微任务。
- Promise 是微任务（紧跟当前任务），setTimeout 是宏任务（下一轮才执行）。
- async/await 是 Promise 的语法糖，await 暂停当前函数但不阻塞线程。
- 实战：循环里用 Promise.all 并发，别串行 await；别忘了 await async 调用；forEach 不等 async 回调。

理解事件循环，你就能写出正确、高效的异步代码，也能看懂那些"诡异"的执行顺序面试题。下一章我们深入 IO 模型，看事件循环底层是怎么"同时盯住"成千上万个网络连接的。`,
  },

  // ============================================================
  // 第 14 章：IO模型：select/poll/epoll的区别
  // ============================================================
  {
    id: "hw-io-model",
    group: "并发与IO",
    icon: "📡",
    title: "IO模型：select/poll/epoll的区别",
    content: `## 从一个前台接待员说起

想象一家大酒店的前台，有一个接待员。客人来了要办入住，接待员要查电脑、登记、发房卡。这是个"IO 操作"——要等电脑响应。

**做法一（阻塞 IO）**：接待员给一个客人办入住时，全程盯着电脑等它响应，期间其他客人都得排队。如果电脑卡了 30 秒，后面所有客人都得干等 30 秒。这是传统多线程服务器（如 Tomcat 默认配置）的模型——一个请求一个线程，线程等 IO 时就卡住。

**做法二（非阻塞 IO 加轮询）**：接待员给客人发完查房请求后，立刻去接待下一个客人，过几秒回来问电脑"好没好"，没好就再去接待别人，过会儿再问。这样能服务更多客人，但反复问"好没好"也累。

**做法三（IO 多路复用）**：接待员装了个"提醒铃"，给电脑发请求后就去接待别人；哪个电脑好了就响铃，接待员听到铃去处理对应的客人。这样一个接待员就能同时管几十台电脑、几十个客人。这就是 Nginx、Node.js、Redis 高并发的秘密。

这一章我们就讲清楚这几个 IO 模型，以及为什么"单线程加多路复用"能吊打"多线程加阻塞 IO"。

## 什么是 IO

IO 就是**输入/输出**，指数据在内存和外部设备（网卡、硬盘、键盘等）之间的搬运。

- 从网卡读数据到内存：网络 IO（读 HTTP 请求、查数据库返回结果）。
- 从硬盘读数据到内存：文件 IO（读配置文件、读日志）。
- 从内存写数据到网卡/硬盘：输出 IO。

IO 的特点是**慢**——相比 CPU 的纳秒级运算，磁盘 IO 要毫秒级，网络 IO 要几十毫秒甚至几百毫秒。慢了上万倍。所以程序运行的大部分时间常常不是在算，而是在等 IO。

理解 IO 模型，就是要回答一个问题：**当数据还没准备好时，调用方该怎么办**？是干等、是反复问、还是装个铃铛？这三种回答，对应了三种 IO 模型。

## 阻塞 IO（BIO）：一直等到数据准备好

**阻塞 IO** 是最直观的模型：调用 read 时，如果数据没准备好，调用方线程被挂起，一直等到数据准备好并拷贝到内存，才返回。

生活类比：你打电话给餐厅问"饭好了吗"，对方说"还没，你等着"，你就一直拿着电话不挂，等到饭好了才挂。期间你什么都干不了。

BIO 的代码最简单直观，但有个致命问题：**一个线程只能盯一个连接**。如果服务器要同时处理 10000 个客户端连接，就得开 10000 个线程，每个线程大部分时间都在等 IO，白白占内存和 CPU 切换开销。

传统的 Java BIO（java.io 包）、Python 的 socket 默认都是阻塞 IO。Tomcat 的默认线程模型（一个请求一个线程）也是基于 BIO 思路。

## 非阻塞 IO（轮询）：反复问"好了没"

**非阻塞 IO** 的核心：调用 read 时，如果数据没准备好，立刻返回一个错误码（如 EAGAIN），不挂起调用方。调用方可以去做别的，过会儿再来 read 一次。

生活类比：你打电话问餐厅"饭好了吗"，对方说"没好"，你挂了；过 5 秒再打，还没好，又挂了；反复打，直到一次问出"好了"为止。

非阻塞 IO 解决了"线程被挂起"的问题，但带来了新问题：**反复轮询很费 CPU**。你得不停地问，大部分时候答案是"没好"，CPU 全花在"问"上了。而且效率不高——你不知道什么时候该问，只能机械地反复试。

实际中，纯非阻塞 IO 加轮询很少单独用，通常会配合"IO 多路复用"来避免忙等。

## IO 多路复用：一个线程盯多个连接

**IO 多路复用**（IO multiplexing）是高并发的关键。它的思路：让一个线程同时"盯住"多个连接，哪个连接有数据可读了，就通知应用程序去处理那个连接。这样一万个连接只需要一个线程就能管，彻底解决了 BIO 的线程爆炸问题。

生活类比：前台接待员装了"提醒铃"，他同时给 10000 个客人发了请求，然后干别的；哪个客人的电脑好了，对应的铃就响，接待员去处理那个客人。一个接待员就能搞定上万个客人。

IO 多路复用的核心是一个系统调用：应用程序把"我关心的所有连接"列表交给内核，然后阻塞等待；内核监视所有连接，一旦某个连接有事件（可读、可写、出错），就返回告诉应用程序"第 N 个连接有情况"，应用程序去处理。

实现 IO 多路复用的系统调用有三个：select、poll、epoll（Linux）。它们思路一样，但实现和性能差异巨大。下面分别讲。

## select：有 1024 限制的元老

**select** 是最古老的 IO 多路复用机制（1983 年）。它的工作方式：

1. 应用程序把关心的文件描述符（fd）集合打包，传给 select。
2. select 阻塞等待，内核监视这些 fd。
3. 有事件时，select 返回，但只告诉你"有事件"，不告诉你具体是哪个 fd。应用程序得**遍历整个集合**，逐个检查哪个有事件。

select 的两大缺陷：

- **容量限制**：默认最多监视 1024 个 fd（FD_SETSIZE 宏定义）。要支持更多连接得改内核参数重新编译，麻烦。
- **性能差**：每次调用都要把整个 fd 集合从用户态拷到内核态；返回后还要遍历整个集合找有事件的 fd。连接数一多，开销线性增长。

## poll：用链表打破 1024 限制

**poll**（1997 年左右）是 select 的改进版，思路几乎一样，主要改动：

- 用链表代替位图存储 fd 集合，**打破了 1024 的限制**，可以监视任意数量的 fd。
- 接口更清晰，把"关心的事件"和"返回的事件"分开。

但 poll 没解决 select 的性能问题：仍然每次要把整个集合拷到内核，返回后仍要遍历所有 fd 找有事件的。连接数多了，性能依然线性下降。所以 poll 在高并发场景用得不多。

## epoll：Linux 高并发的真正王者

**epoll**（Linux 2.6，2002 年）是真正解决高并发的方案。它用两个核心机制彻底解决了 select/poll 的性能问题：

**机制一：红黑树存储关心的 fd**

- 应用程序通过 epoll_ctl 把"关心的 fd"注册进内核，内核用红黑树存储。增删改查是 O(log n)，高效。
- 不像 select/poll 每次都要重新传整个集合，epoll 只在添加/删除 fd 时操作一次。

**机制二：就绪队列**

- 内核维护一个"就绪队列"。某个 fd 有事件时，内核直接把它放进就绪队列。
- epoll_wait 返回时，只把就绪队列里的 fd 拷贝回用户态——**只返回有事件的 fd，不返回全部**。
- 应用程序拿到的是"有事件的 fd 列表"，直接逐个处理，不用遍历所有 fd。

epoll 的优势：

- **没有数量限制**（只受系统资源限制）。
- **性能不随连接数下降**：epoll_wait 只返回有事件的 fd，时间复杂度是 O(活跃连接数)，而不是 O(总连接数)。10 万连接里只有 100 个活跃，epoll 只处理这 100 个，性能极高。
- **支持两种触发模式**：LT（水平触发，默认）和 ET（边缘触发，性能更高但编程复杂）。

## 为什么 Nginx/Node.js/Redis 能单线程高并发

答案就是 epoll（Linux）或类似机制（Mac 的 kqueue、Windows 的 IOCP）。

- **Nginx**：用 epoll 加单线程（或多进程，每进程单线程事件循环），一个 worker 能轻松处理几万并发连接。
- **Node.js**：底层 libuv 在 Linux 上用 epoll，在 Mac 上用 kqueue，在 Windows 上用 IOCP。主线程跑事件循环，靠多路复用同时盯住所有网络连接。
- **Redis**：单线程加 epoll，几万 QPS。Redis 6.0 引入多线程处理网络 IO，但命令执行仍单线程。

它们的共同模式：**单线程事件循环加 IO 多路复用**。优势：

- 没有线程切换开销。
- 没有锁竞争（单线程不需要锁）。
- 内存利用率高（不需要给每个连接分配线程栈）。

但前提是：**任务主要是 IO 密集型**。如果某个连接要长时间 CPU 计算，会卡住整个事件循环，所有其他连接都得等。这就是为什么 Node.js、Redis 不适合 CPU 密集型任务。

## 异步 IO（AIO）：内核帮你读完再通知

IO 多路复用虽然高效，但仍有一步：epoll 通知"数据准备好了"后，应用程序还要调用 read 把数据从内核缓冲区拷到用户缓冲区。这一步仍是同步的。

**真正的异步 IO（AIO）** 更进一步：应用程序发起 read 请求后立刻返回；内核负责把数据准备好、并拷贝到用户缓冲区、都弄完了再通知应用程序"数据已经在你的缓冲区里了，直接用"。应用程序全程不用等，也不用自己拷数据。

- **Windows IOCP**：成熟的真异步 IO 模型，.NET 的异步基础。
- **Linux io_uring**：Linux 5.1 引入的新一代异步 IO 框架，性能极高，正在取代老旧的 Linux AIO。
- **Node.js 的 libuv**：在 Linux 上其实用的是 epoll（模拟异步），并非真 AIO；在 Windows 上用 IOCP（真 AIO）。所以 Node.js 的"异步"在不同平台底层实现不同。

## 日常启示：为什么 Netty/Node.js 适合 IO 密集型

理解了 IO 模型，你就能明白为什么有些技术栈适合某些场景：

**适合 IO 密集型**（高并发网络请求、大量数据库/缓存查询）：

- Node.js、Nginx、Netty、Go、Python async。
- 它们用事件循环加多路复用，少量线程处理大量连接，IO 等待时穿插处理其他请求。

**适合 CPU 密集型**（图像处理、加密、大数据计算）：

- 多线程模型（Java 线程池、C++ 并发）、计算框架（Spark、Ray）。
- 它们靠多核并行加速计算，IO 等待少，重点是榨干 CPU。

选型启示：

- 写 Web API 网关、聊天服务器、实时推送：选事件循环型（Node.js、Go、Netty）。
- 写图像识别、密码学计算、数据压缩：选多线程型（Java 线程池、C++）。
- 两者混合：考虑 Go（goroutine 既能高并发 IO 又能用多核）、或 Node.js 加子进程/worker_threads 处理 CPU 任务。

另一个启示：**别在事件循环里做重计算**。Node.js 里如果有个请求要做 100ms 的同步计算，整个事件循环卡 100ms，期间几万连接都得不到响应。把重计算移到 worker 线程或子进程，主线程只做轻量的 IO 调度。

## 小结

- IO 是数据在内存和外部设备间的搬运，特点是慢，是程序性能瓶颈的常见来源。
- 阻塞 IO：等到数据准备好，简单但一个线程盯一个连接。
- 非阻塞 IO：立刻返回"没好"，要轮询，费 CPU。
- IO 多路复用：一个线程盯多个连接，select 有 1024 限制、poll 打破限制但仍 O(n)、epoll 用红黑树加就绪队列做到 O(活跃数)。
- Nginx/Node.js/Redis 高并发的秘密：单线程事件循环加 epoll。
- 真异步 IO：内核帮你读完再通知，Windows IOCP、Linux io_uring。
- 选型：IO 密集型选事件循环，CPU 密集型选多线程，混合用 Go 或 worker。

理解 IO 模型，你就能在设计高并发系统时做出正确选择，也能在性能调优时找到真正的瓶颈。下一章我们把视角拉到网络协议，看数据是怎么在网络上传的，以及 HTTP 各版本的演进。`,
  },

  // ============================================================
  // 第 15 章：网络通信：从TCP到HTTP
  // ============================================================
  {
    id: "hw-network-basics",
    group: "并发与IO",
    icon: "🌐",
    title: "网络通信：从TCP到HTTP",
    content: `## 从你打开一个网页说起

你在浏览器输入一个网址、按下回车，到页面显示出来，中间发生了什么？这是一道经典面试题，背后涉及的就是网络通信。简化一下：

1. **DNS 解析**：把域名翻译成 IP 地址（像查通讯录找对方电话）。
2. **TCP 连接**：和服务器建立连接（像拨通电话）。
3. **TLS 握手**（如果是 HTTPS）：建立加密通道。
4. **发送 HTTP 请求**：告诉服务器"我要这个页面"。
5. **服务器处理并返回**：服务器把 HTML 发回来。
6. **浏览器渲染**：解析 HTML、加载资源、绘制页面。

这一章我们聚焦其中最核心的部分：TCP 怎么可靠传输、HTTP 各版本的演进、HTTPS 怎么保证安全。理解了这些，你就能排查"接口为什么慢"、看懂 HTTP/2、HTTP/3 的设计。

## TCP 三次握手与四次挥手：为什么不是两次

TCP 是面向连接的可靠传输协议。建立连接要**三次握手**（three-way handshake），断开要**四次挥手**（four-way handshake）。

**三次握手**（建立连接）：

1. 客户端发到服务器：SYN（"我想连你，我的序号是 x"）。
2. 服务器发到客户端：SYN+ACK（"好的，我也想连你，我的序号是 y，确认你的 x"）。
3. 客户端发到服务器：ACK（"收到，确认你的 y"）。

为什么不是两次？如果是两次，第二步服务器发完 SYN+ACK 就认为连接建立了，但这个包可能丢了或延迟了。如果客户端根本没收到，服务器却一直在等，就浪费资源。更危险的是"旧的连接请求复活"——一个延迟的旧 SYN 到达服务器，服务器回个 SYN+ACK 就建了连接，但客户端根本没想连，服务器却傻等。三次握手让客户端最后确认一次，避免这种"僵尸连接"。

生活类比：A 打电话给 B。

- 两次：A 说"听见吗"，B 说"听见，你说吧"——B 就开始说话了。但如果 B 的"听见"没传到 A，B 一直在说，A 根本没在听。
- 三次：A 说"听见吗"，B 说"听见，你能听见我吗"，A 说"能听见"——双方都确认了对方能听见，才开始正式通话。

**四次挥手**（断开连接）：

1. 客户端发到服务器：FIN（"我说完了"）。
2. 服务器发到客户端：ACK（"收到，你说的我都收到了"）。
3. 服务器发到客户端：FIN（"我也说完了"）。
4. 客户端发到服务器：ACK（"收到，再见"）。

为什么是四次而不是三次？因为 TCP 是**全双工**的——双方都能同时发数据。客户端说"我不发了"不代表服务器也没话说了，服务器可能还有数据要发。所以服务器先回 ACK 确认收到客户端的 FIN，等自己数据发完了再发 FIN，客户端再回 ACK。中间的第 2、3 步不能合并（除非服务器刚好也没数据要发了）。

## TCP 的可靠传输：序列号、确认、重传、滑动窗口

TCP 之所以叫"可靠传输"，是因为它在不可靠的 IP 层之上做了一堆工作保证数据不丢、不乱、不重。

**序列号**：每个字节的数据都有序号。接收方按序号重排，即使数据包乱序到达也能拼回原样。

**确认（ACK）**：接收方收到数据后回一个 ACK，告诉发送方"我收到第 N 个字节了，下次从 N+1 发"。

**重传**：发送方发出数据后启动定时器，超时没收到 ACK 就重发。这应对了丢包。

**滑动窗口**：控制发送速度，避免发太快把接收方淹没。接收方在 ACK 里带上"我还能收多少"（窗口大小），发送方据此调整。窗口会随接收方处理能力滑动，所以叫"滑动窗口"。

生活类比：你给朋友寄一摞编号的书。

- 序列号：每本编号，朋友收到能按顺序摆好。
- 确认：朋友收到每本后告诉你"第 N 本收到了"。
- 重传：你寄出第 5 本后等回复，超时没回就重寄一本。
- 滑动窗口：朋友说"我家书架还能放 10 本"，你就一次最多寄 10 本；朋友看快满了说"只能放 3 本"，你就少寄点。

## TIME_WAIT 与粘包：两个常见现象

**TIME_WAIT**：四次挥手里，最后发 ACK 的那一方（通常是主动关闭方）发完 ACK 后要等一段时间（通常是 2 倍 MSL，约 1 到 2 分钟）才真正关闭。这个状态叫 TIME_WAIT。

为什么要等？两个原因：

1. 保证最后一个 ACK 能到达对方。如果 ACK 丢了，对方会重发 FIN，这边还能重发 ACK。如果直接关了，就没办法重发，对方会一直等。
2. 让旧连接的网络包"寿终正寝"。如果立刻用同样的端口建新连接，旧连接延迟到达的包可能被误认为新连接的，造成混乱。

TIME_WAIT 是高并发服务器的常见问题——短连接太多会堆积大量 TIME_WAIT，占满端口。解决：用长连接（keep-alive）、调小 TIME_WAIT 时长、或用 SO_REUSEADDR 复用端口。

**粘包**：TCP 是字节流协议，不保留消息边界。你发"ABC"和"DEF"，接收方可能一次收到"ABCDEF"，也可能分两次收到"AB"和"CDEF"——这就是"粘包"。

粘包不是 bug，是 TCP 的本质特性。解决要靠应用层自己定义消息边界：定长消息、用分隔符（如换行）、或用"长度前缀"（先发消息长度，再发消息体）。HTTP 用 Content-Length 或 chunked 编码就是这个目的。

## HTTP/1.1 的队头阻塞与 keep-alive

HTTP/1.1 是大家最熟悉的版本。它的两个关键特性：

**keep-alive（持久连接）**：HTTP/1.0 默认每个请求建一个 TCP 连接，用完就断。一个网页有几十个资源（图片、CSS、JS）就要建几十次 TCP 连接，每次都要三次握手加 TLS 握手，开销大。HTTP/1.1 默认开 keep-alive，一个 TCP 连接可以发多个请求，复用连接，省去重复握手。

**队头阻塞（Head-of-Line blocking）**：HTTP/1.1 在一个 TCP 连接上，请求必须按顺序发、按顺序收响应。如果第一个请求慢（比如是个大接口），后面的请求就算准备好了也得等。这叫"队头阻塞"。

浏览器为了缓解这个问题，会对同一域名开 6 个并发连接（不是无限，因为服务器扛不住太多连接）。但 6 个连接里每个仍有队头阻塞，治标不治本。

## HTTP/2：多路复用、头部压缩、服务器推送

HTTP/2（2015 年）针对 HTTP/1.1 的痛点做了大改进：

**多路复用**：在一个 TCP 连接上可以同时跑多个请求/响应，互不阻塞。HTTP/2 把数据拆成"二进制帧"（frame），每个帧标记属于哪个"流"（stream，对应一个请求），不同流的帧可以穿插发送，接收方按流重组。这样彻底解决了 HTTP 层的队头阻塞。

**头部压缩（HPACK）**：HTTP/1.1 每个请求都带一堆重复的 header（Cookie、User-Agent 等），浪费带宽。HTTP/2 用 HPACK 算法压缩头部，客户端和服务器各维护一张"头部表"，重复的字段只传索引，大幅减少冗余。

**服务器推送**：服务器可以在客户端请求 HTML 时，主动把 HTML 里要用的 CSS、JS 一起推过去，省去客户端再发请求的往返。不过这个特性实践中用得不多，HTTP/3 里甚至考虑废弃。

注意：HTTP/2 解决了 HTTP 层的队头阻塞，但**没解决 TCP 层的队头阻塞**——如果 TCP 层某个包丢了，所有流都得等重传。这是 HTTP/3 弃用 TCP 的原因。

## HTTP/3：弃用 TCP，改用 QUIC

HTTP/3（2022 年正式标准化）最大的变化：**底层不用 TCP 了，改用基于 UDP 的 QUIC 协议**。

为什么弃用 TCP？因为 TCP 层的队头阻塞。HTTP/2 多个流共用一个 TCP 连接，一旦某个 TCP 包丢了，TCP 要重传这个包，期间整个连接的所有流都被阻塞（哪怕其他流的数据已经到了）。连接越快、丢一个包影响越大。

QUIC（Quick UDP Internet Connections，Google 开发）在 UDP 之上自己实现了类似 TCP 的可靠传输、拥塞控制，但有几个改进：

- **流之间独立**：一个流丢包只影响这个流，其他流不受影响。彻底消除队头阻塞。
- **连接迁移**：QUIC 用一个"连接 ID"标识连接，不绑定 IP 加端口。你从 WiFi 切到 4G，IP 变了，连接不断，视频不会卡。
- **更快的握手**：QUIC 把传输层握手和 TLS 握手合并，第一次连接只要 1 个 RTT，重连只要 0 个 RTT（0-RTT），比 TCP 加 TLS 快很多。

代价：QUIC 在 UDP 上，一些防火墙/中间设备对 UDP 不友好，可能被限速；QUIC 的实现在用户态，性能优化比内核态的 TCP 难。但整体趋势是 HTTP/3 越来越普及。

## HTTPS：TLS 握手与证书防中间人

HTTPS = HTTP + TLS。TLS 提供加密、完整性、身份认证，让数据在网络上不被窃听、篡改、冒充。

**TLS 握手简化过程**（以 TLS 1.2 为例）：

1. 客户端发 ClientHello：告诉服务器"我支持哪些加密算法、TLS 版本"。
2. 服务器发 ServerHello 加证书加公钥：选定算法，把数字证书发给客户端。
3. 客户端验证证书：用受信任的 CA 公钥验证证书签名，确认"这证书确实是 CA 签发给 example.com 的"。
4. 双方协商出对称密钥（用非对称加密交换密钥材料）。
5. 后续通信用对称密钥加密（对称加密快）。

**证书为什么能防中间人**？核心是"非对称加密加 CA 信任链"：

- 服务器有一对公私钥，私钥只有服务器有，公钥通过证书公开。
- 证书里有服务器的公钥和域名，由权威 CA（证书颁发机构）用私钥签名。
- 客户端（浏览器/操作系统）内置了受信任的 CA 根证书。
- 中间人要伪造证书，没有 CA 私钥就签不出合法签名，客户端验证会失败。

简单说：**CA 的签名保证了"这个公钥确实是 example.com 的"，而公钥加密保证了"只有持有私钥的真服务器才能解密"**。中间人既改不了证书（没 CA 私钥），也解不了客户端用公钥加密的数据（没服务器私钥）。

TLS 1.3 简化了握手（1-RTT 甚至 0-RTT），更安全更快，是现在的主流。

## 日常启示：接口慢的原因排查

理解了网络通信，你就能系统地排查"接口慢"。一个 HTTP 请求的耗时可以拆成几段：

1. **DNS 解析**：域名到 IP。慢的话考虑减少域名数量、用 DNS 预解析、检查 DNS 服务器。
2. **TCP 连接**：三次握手。慢的话考虑用 keep-alive 复用连接、检查网络延迟。
3. **TLS 握手**（HTTPS）：慢的话考虑用 HTTP/2（一个连接复用）、TLS 1.3（握手更快）、用会话恢复减少握手。
4. **请求传输**：发请求报文的时间。慢的话考虑压缩请求体、减少请求大小。
5. **服务器处理**：服务器算出响应的时间。慢的话优化后端逻辑、加缓存、查数据库。
6. **响应传输**：响应报文传回的时间。慢的话考虑压缩（gzip/br）、用 CDN、减少响应大小。

排查工具：

- 浏览器开发者工具的 Network 面板，看每个请求的 Timing，能拆出 DNS、连接、TLS、等待（TTFB）、传输各段耗时。
- curl -w 可以打印各阶段耗时。
- 抓包工具（Wireshark）能看 TCP/TLS 细节。

常见优化：

- 用 HTTP/2 多路复用减少连接数。
- 用长连接避免反复握手。
- 用 CDN 就近返回静态资源。
- 用 gzip/br 压缩减少传输量。
- 接口慢优先查服务器处理（TTFB），别只盯着网络。

## 小结

- TCP 三次握手建连、四次挥手断连，保证双方都准备好、防止僵尸连接。
- TCP 靠序列号加确认加重传加滑动窗口实现可靠传输。
- TIME_WAIT 是主动关闭方的等待状态，粘包是 TCP 字节流无边界的副作用，都要应用层处理。
- HTTP/1.1 有 keep-alive 但有队头阻塞。
- HTTP/2 用多路复用加头部压缩解决 HTTP 层队头阻塞，但 TCP 层仍阻塞。
- HTTP/3 弃用 TCP 改用 QUIC（基于 UDP），消除 TCP 层队头阻塞，连接更快、可迁移。
- HTTPS 靠 TLS 加密，证书靠 CA 签名防中间人。
- 接口慢排查：拆成 DNS、连接、TLS、传输、处理各段分别看。

理解从 TCP 到 HTTP 的整个链条，你就能在接口慢、连接异常、协议选型时做出正确判断，而不是只会"重启试试"。这就是网络原理对日常开发的实际帮助。`,
  },
];
