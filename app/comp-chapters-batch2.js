// =============================================================
// Batch 2：数据表示（4 章）
// =============================================================

export const chapters = [
  // ============================================================
  // 第 1 章：二进制与十六进制
  // ============================================================
  {
    id: "comp-binary-hex",
    group: "数据表示",
    icon: "🔢",
    title: "二进制与十六进制",
    content: `
## 概述
计算机内部所有数据——文字、图片、视频、程序指令——归根结底都是一串 0 和 1。这一章从最基础的二进制讲起：为什么计算机偏爱二进制而不是十进制？十六进制又解决了什么问题？理解 bit、byte、word 这几个单位和大小端字节序，是后续看懂内存 dump、调试二进制协议、排查网络报文的前提。哪怕你写了十年业务代码，把这一层底座搞清楚后，很多「玄学 bug」会突然变得有迹可循。

## 核心要点
- **bit（位）**: 计算机最小单位，只有 0/1 两个值——对应电路里的「低电平/高电平」，是信息的最小颗粒。
- **byte（字节）**: 8 个 bit 组成 1 字节，取值范围 0~255——是内存编址的基本单位，每个内存地址指向 1 字节。
- **word（字）**: CPU 一次能处理的位数，32 位机器 word=4 字节，64 位机器 word=8 字节——决定寄存器宽度和「一次能算多大的数」。
- **二进制**: 逢 2 进 1，如 \`1010\` = 8+2 = 10——计算机的「母语」，所有运算最终都落在 bit 上。
- **十六进制**: 逢 16 进 1，用 0~9 和 A~F 表示，1 个十六进制位恰好对应 4 个 bit——是二进制的「简写」，让人读得下去。
- **换算关系**: 1 字节 = 8 bit = 2 个十六进制位，如 \`0xFF\` = \`11111111\` = 255；4 字节 int = 8 个十六进制位。
- **大端序（Big Endian）**: 高位字节存放在低地址——符合人类从高位读到低位的习惯，网络协议默认用大端（网络字节序）。
- **小端序（Little Endian）**: 低位字节存放在低地址——x86/ARM 主流 CPU 采用，日常 PC 和服务器几乎都是小端。
- **位运算**: \`&\` 与、\`|\` 或、\`^\` 异或、\`~\` 取反、\`<<\` 左移、\`>>\` 右移——直接操作 bit，常用于掩码和标志位，性能极高。
- **常见前缀**: 二进制 \`0b1010\`，十六进制 \`0xFF\`——C23 起原生支持 \`0b\` 前缀，老代码多用 \`0x\` 替代。

## 原理与机制
- **为什么偏偏是二进制**: 硬件层面，用电压高低表示状态最稳。十进制需要 10 个稳定电压档位，电路噪声稍大就会把 6 误判成 7；二进制只要区分「高/低」两档，抗干扰能力高出一个数量级，制造容错也大。生活比喻：硬币只有正反两面，简单可靠、识别成本低；若做成十面骰子，制造和判读都麻烦。所以「不是二进制选择了计算机，是物理规律选择了二进制」。
- **十六进制为什么是 16 而不是 8 或 32**: 因为 16 = 2⁴，1 个十六进制位恰好对应 4 个 bit（半个字节，也叫 1 个 nibble），2 个十六进制位正好是 1 字节——对齐关系完美。二进制太长，1 个 32 位整数写成 \`0011000001111010...\` 人眼数不清；十六进制把它压成 8 个字符，长度缩短 3/4 且一一对应。比喻：把 4 个硬币捆成一摞换成一张纸币，面额一目了然。
- **大小端的由来与取舍**: 不同 CPU 厂商历史选择不同——Motorola/PowerPC/SPARC 偏好大端，Intel x86 选择小端，ARM 双模可配。小端的优势是「低地址放低位字节」，把 int 强转成 char 数组时低字节就在下标 0，无需调整；大端的优势是字节流顺序就是数字的自然书写顺序，适合网络传输和文件格式。两者没有绝对优劣，只是「先存高位还是先存低位」的约定差异。
- **网络字节序统一为大端**: TCP/IP 规定网络上传输多字节整数一律用大端序。所以发送前要 \`htonl\`/\`htons\`（host to network long/short）转换，接收后 \`ntohl\`/\`ntohs\` 转回——这层转换屏蔽了本机差异，让不同架构的机器能互通。
- **位与字节的层级关系**: 1 bit < 1 byte(8bit) < 1 word(通常 4/8 字节) < 寄存器宽度。CPU 的「位宽」（32/64 位）指的就是寄存器和数据总线的宽度，决定了寻址空间上限（32 位最多 4GB）和单条指令能处理的字节数。

## 易错点与陷阱
- **陷阱**: 用 \`printf("%x", v)\` 打印时误以为输出顺序就是内存顺序——\`%x\` 输出的是「数值」的十六进制（高位在左），不是「字节序」。要看真实内存布局，必须把变量地址转成 \`unsigned char*\` 后逐字节遍历打印。
- **陷阱**: 大小端只在「多字节类型」（int/short/long/float/double）里有意义，单字节的 \`char\` 数组没有字节序问题——新手常把「字符串的字节顺序」和「整数的字节序」搞混，二者根本不是一回事。
- **陷阱**: 右移运算 \`>>\` 对有符号数是「算术右移」（高位补符号位），对无符号数是「逻辑右移」（高位补 0）——同一段代码在 int 和 unsigned int 上行为不同，处理位图/掩码时务必用无符号类型。
- **陷阱**: \`0b1010\` 二进制字面量是 C23/C++14 才支持，老编译器（MSVC 旧版、C89）不认——跨平台代码用 \`0x\` 十六进制或宏定义位掩码更稳妥。
- **陷阱**: 混淆「位（bit）」和「字节（byte）」——网络带宽常用 bit/s（如 100Mbps），文件大小用 byte（如 4MB），100Mbps 实际下载速度约 12.5MB/s，差了 8 倍。

## 实战建议
- **建议**: 抓内存 dump、看二进制协议、调试指针内容时，永远用十六进制思维——\`hexdump -C file\` 或 \`xxd\` 是看文件真实字节的标配，比 \`cat\` 直观得多。
- **建议**: 涉及网络协议字段（端口号、IP 头长度等）时，发送前一定 \`htonl/htons\`，接收后 \`ntohl/ntohs\`——别假设「我的机器就是大端」，代码迁移到小端服务器会出诡异 bug。
- **建议**: 位运算操作掩码时一律用无符号类型（\`uint32_t\`/\`uint64_t\`），避免符号位扩展带来非预期行为——这是 C/C++ 跨平台代码的安全铁律。
- **建议**: 阅读二进制时学会「4 位一组」心算转十六进制——记住 \`1010=A\`、\`1111=F\` 几个常见值，调试效率会大幅提升。
`,
    code: `// ===== 二进制与十六进制 演示 =====
// 演示：同一数据的二进制/十进制/十六进制输出，并检测本机大小端
#include <stdio.h>
#include <stdint.h>

// 打印 32 位整数的二进制（每 4 位加空格，便于对照十六进制）
void print_bin(uint32_t n) {
    for (int i = 31; i >= 0; i--) {
        putchar((n >> i) & 1 ? '1' : '0');
        if (i % 4 == 0 && i != 0) putchar(' ');   // 每 4 位分隔
    }
}

int main(void) {
    uint32_t v = 0x1234ABCD;          // 一个 32 位整数

    // 1. 同一数据的三种进制输出
    printf("=== 同一数据的三种进制 ===\\n");
    printf("十进制   : %u\\n", v);
    printf("十六进制 : 0x%08X\\n", v);
    printf("二进制   : ");
    print_bin(v);
    printf("\\n\\n");

    // 2. 大小端检测：按地址递增逐字节读取
    printf("=== 字节序（按内存地址递增）===\\n");
    unsigned char *p = (unsigned char *)&v;
    for (int i = 0; i < 4; i++) {
        printf("  地址+%d -> 0x%02X\\n", i, p[i]);
    }
    if (p[0] == 0xCD) {
        printf("→ 小端序 (Little Endian)：低位字节 0xCD 在低地址\\n");
    } else {
        printf("→ 大端序 (Big Endian)：高位字节 0x12 在低地址\\n");
    }

    // 3. 位运算演示：取高字节与低字节
    printf("\\n=== 位运算取字节 ===\\n");
    printf("v >> 24      = 0x%02X (取最高字节)\\n", (v >> 24) & 0xFF);
    printf("v & 0xFF     = 0x%02X (取最低字节)\\n", v & 0xFF);
    return 0;
}
`,
  },

  // ============================================================
  // 第 2 章：整数与浮点数
  // ============================================================
  {
    id: "comp-int-float",
    group: "数据表示",
    icon: "_decimal",
    title: "整数与浮点数",
    content: `
## 概述
数字在计算机里不是「理所当然」的——负数怎么存？小数怎么存？为什么 \`0.1 + 0.2\` 不等于 \`0.3\`？这些问题背后是原码、反码、补码和 IEEE 754 浮点标准。本章把整数和浮点数的存储机制讲透：补码如何统一了加减法、浮点数为何会丢精度、整数溢出又为何会「回绕」。理解这些，你就能解释那些「看起来不该出 bug 却出了」的诡异数字问题。

## 核心要点
- **原码（Sign-Magnitude）**: 最高位作符号位（0 正 1 负），其余位为绝对值——直观但 0 有两个表示（+0/-0），加减法要分情况，已淘汰。
- **反码（Ones' Complement）**: 正数同原码，负数逐位取反——解决了部分问题但 0 仍有两个表示，现代 CPU 不用。
- **补码（Two's Complement）**: 正数同原码，负数 = 取反后加 1——现代计算机统一用补码存有符号整数，0 唯一、加减法统一。
- **补码范围**: n 位有符号数范围 \`-2^(n-1) ~ 2^(n-1)-1\`，如 int8_t 是 -128~127——负数比正数多一个，因为 0 占了正数侧。
- **IEEE 754 浮点**: 由符号位（1 bit）+ 指数（exponent）+ 尾数（mantissa）三部分组成——类似科学计数法 \`(-1)^s × 1.m × 2^e\`。
- **单精度 float**: 1 位符号 + 8 位指数 + 23 位尾数，约 7 位有效十进制数字；双精度 double 多 29 位尾数，约 15~17 位有效数字。
- **精度丢失**: 0.1 在二进制下是无限循环小数（类似十进制的 1/3），存进有限尾数必然截断——这是 \`0.1+0.2≠0.3\` 的根因。
- **整数溢出**: 有符号溢出是未定义行为（UB），无符号溢出是「回绕」（mod 2^n）——别依赖有符号溢出的行为。
- **特殊浮点值**: +Inf/-Inf（溢出）、NaN（非法运算如 0/0）、+0/-0——比较时 \`NaN != NaN\` 是个经典坑。
- **舍入模式**: 默认「就近舍入到偶数」（round to nearest, ties to even）——减少累计误差，但仍非精确。

## 原理与机制
- **补码像钟表**: 12 点钟往前拨 3 小时和往后拨 9 小时停在同一个位置——因为 12 小时是一个「模」，3 和 -9 在模 12 下等价。计算机里 n 位整数的模是 2^n，所以 -1 用 \`2^n - 1\` 表示（全 1），-x 用 \`2^n - x\` 表示。这样 \`a + (-b)\` 就等于 \`a + (2^n - b)\`，溢出后高位自然丢弃，结果和 \`a - b\` 一致——于是「减法变成加法」，CPU 只需要一套加法器电路。
- **补码下 0 唯一**: 原码里 +0 = \`0000\`、-0 = \`1000\`，两个 0 让相等判断很别扭；补码里 0 就是 \`0000\`，而 \`1000\`（假设 4 位）表示 -8——所以负数比正数多一个。
- **浮点像科学计数法**: 十进制科学计数法 \`1.23×10^5\` 用有限位数表示大数；IEEE 754 是二进制版 \`1.m×2^e\`，用有限的指数位和尾数位覆盖从极小到极大的范围。float 指数 8 位（偏移 127）能表示 \`2^-126 ~ 2^127\`，范围巨大但精度有限——「范围」和「精度」是一对矛盾，位数固定时不可兼得。
- **为什么 0.1 存不下**: 十进制 0.1 转成二进制是 \`0.0001100110011...\` 无限循环，尾数只有 23/52 位，必须截断——存进去的就已经不是 0.1 而是一个近似值，再参与运算误差会累积。这和十进制下 1/3 = 0.3333... 截断后 3×0.333 ≠ 1 是同一个道理。
- **整数溢出回绕**: 无符号数溢出按模 2^n 回绕（255+1→0），是明确定义的行为；有符号溢出在 C 里是「未定义行为」，编译器可能基于「不溢出」的假设做优化，导致意料外的结果——所以循环变量用无符号更安全。

## 易错点与陷阱
- **陷阱**: 用 \`==\` 比较浮点数——\`0.1 + 0.2 == 0.3\` 几乎必然为 false。正确做法是判断差的绝对值是否小于 epsilon（如 \`fabs(a-b) < 1e-9\`）。
- **陷阱**: 用浮点数做循环变量或累加器——\`for (float f = 0; f != 1.0; f += 0.1)\` 可能永远停不下来。改用整数计数或 \`f < 1.0\`。
- **陷阱**: 有符号整数溢出是未定义行为，别指望它「回绕」——\`INT_MAX + 1\` 在优化开启时可能变成任意值，甚至导致死循环。
- **陷阱**: \`NaN\` 不等于任何值包括自己——\`x != x\` 为 true 时说明 x 是 NaN；用 \`isnan()\` 判断才靠谱。
- **陷阱**: 整数和浮点混算时隐式转换——\`3/2\` 等于 1（整数除法），\`3/2.0\` 才等于 1.5；\`int * float\` 会把 int 提升为 float，可能丢精度。

## 实战建议
- **建议**: 财务、金额计算一律用整数（以分为单位）或定点数库，绝不用 float/double——银行系统里 0.01 元的误差累积到亿级就是真金白银的损失。
- **建议**: 浮点比较永远用 epsilon：\`fabs(a - b) < 1e-9\`；浮点循环用 \`<\` 而非 \`!=\`，避免死循环。
- **建议**: 整数运算注意溢出——关键路径用 \`__builtin_add_overflow\`（GCC/Clang）或专门的大数库检测；无符号循环变量比有符号更可控。
- **建议**: 调试浮点问题时用 \`%.17g\` 打印 double 的完整精度（double 约 17 位有效数字），用 \`%x\` 配合 union 看其二进制位——能直接看到符号/指数/尾数布局。
`,
    code: `// ===== 整数与浮点数 演示 =====
// 演示：补码表示与运算、浮点精度丢失、整数溢出回绕
#include <stdio.h>
#include <stdint.h>

int main(void) {
    // 1. 补码：-1 在内存中是全 1
    printf("=== 补码演示 ===\\n");
    int8_t neg1 = -1;
    printf("int8_t  -1 的字节 = 0x%02X (二进制 11111111)\\n", (uint8_t)neg1);
    printf("  -1 + 1 = %d  (补码下减法即加法，溢出后自然归 0)\\n\\n",
           (int8_t)(neg1 + 1));

    // 2. 浮点精度丢失：0.1 + 0.2 ≠ 0.3
    printf("=== 浮点精度丢失 ===\\n");
    double x = 0.1, y = 0.2;
    printf("0.1 + 0.2 = %.17f\\n", x + y);
    printf("0.3       = %.17f\\n", 0.3);
    printf("0.1+0.2 == 0.3 ? %s\\n\\n",
           (x + y == 0.3) ? "true" : "false (精度丢失!)");

    // 3. 整数溢出
    printf("=== 整数溢出 ===\\n");
    int32_t imax = 2147483647;        // INT32_MAX
    uint8_t  umax = 255;              // UINT8_MAX
    printf("int32_t  MAX = %d\\n", imax);
    printf("int32_t MAX+1= %d (有符号溢出，行为未定义，此处为回绕)\\n", imax + 1);
    printf("uint8_t  MAX = %u\\n", umax);
    printf("uint8_t MAX+1= %u (无符号溢出按模 256 回绕)\\n",
           (uint8_t)(umax + 1));
    return 0;
}
`,
  },

  // ============================================================
  // 第 3 章：字符编码
  // ============================================================
  {
    id: "comp-char-encoding",
    group: "数据表示",
    icon: "🔤",
    title: "字符编码",
    content: `
## 概述
「乱码」是每个程序员都踩过的坑，而乱码的根源几乎都在字符编码。本章梳理 ASCII、Unicode、UTF-8、UTF-16 之间的关系：ASCII 是只装英文的小字典，Unicode 是给全世界字符编号的「世界语电话簿」，UTF-8/UTF-16 是把这些编号存进内存的不同方式。搞清楚「码点（编号）」和「编码（字节序列）」的区别，你就能从容处理中文、emoji 和跨平台文本。

## 核心要点
- **ASCII**: 7 位编码，0~127 共 128 个字符——英文字母、数字、标点和控制符，1 字节就够存（最高位留 0）。
- **Unicode**: 给世界上每个字符分配一个唯一编号（码点，Code Point），如 \`中\` = U+4E2D——目标是「一个编号对应一个字符」，覆盖所有语言加 emoji。
- **UTF-8**: Unicode 的一种变长编码，1~4 字节表示一个码点——ASCII 占 1 字节（与 ASCII 完全兼容），中文占 3 字节，emoji 占 4 字节。
- **UTF-16**: 变长 2 或 4 字节——大部分常用字符 2 字节，辅助平面（emoji 等）4 字节；Windows 和 Java 内部用它。
- **UTF-32**: 定长 4 字节，每个码点都占 4 字节——浪费空间，几乎不用于存储，偶用于内存处理。
- **BOM（Byte Order Mark）**: 文件开头的 \`U+FEFF\`，既标识编码类型又声明字节序——UTF-8 的 BOM 是 \`EF BB BF\`，常被误认为是乱码。
- **码点 vs 字节**: \`中\` 的码点是 1 个（U+4E2D），但 UTF-8 下占 3 字节——\`strlen\` 数的是字节不是字符，这是中文长度算错的常见原因。
- **变长编码的优势**: UTF-8 让英文 1 字节、中文 3 字节，英文为主的文本极省空间；同时与 ASCII 完全向后兼容——这是它成为 Web 标准的关键。
- **乱码根因**: 用 A 编码写入的字节，被 B 编码解读——如把 UTF-8 字节当 GBK 读，就会出现「锟斤拷」之类的经典乱码。
- **常用编码**: 中文环境常见 GB2312/GBK/GB18030（国标）、Big5（繁体）；国际通用 UTF-8——新项目一律 UTF-8。

## 原理与机制
- **ASCII 是只有英文字母的电话簿**: 早期计算机是英文世界的发明，128 个字符够用——26 个字母大小写、10 个数字、标点和控制符，每个字符一个编号（A=65, a=97）。中文、日文、阿拉伯文根本不在表里。比喻：ASCII 是一本只收录英文名的小电话簿，薄薄一本但只够一个国家用。
- **Unicode 是世界语电话簿**: 为了收纳全世界所有文字（包括 emoji、古文字、数学符号），Unicode 联盟给每个字符分配唯一编号（码点）。它只是一个「编号表」，不规定怎么存进内存——「\`中\` 是 U+4E2D」是事实，但存成几个字节、怎么存，由 UTF-8/16/32 决定。比喻：Unicode 是一本收录全世界所有人名字的电话簿并给每个人编号，但「怎么把编号写下来」是另一回事。
- **UTF-8 是高效编码方式**: 它按字符常用程度分配字节长度——ASCII 用 1 字节（且字节值与 ASCII 完全相同，向后兼容），欧洲文字 2 字节，中日韩 3 字节，emoji 4 字节。前缀位标记了字节数（\`0xxxxxxx\`=1字节、\`110xxxxx\`=2字节、\`1110xxxx\`=3字节），解码器能从任意位置自我同步——所以 UTF-8 截断半个字符不会影响后续解码。这是它比 UTF-16 更适合网络传输的原因。
- **UTF-16 的定长幻觉**: 早期以为 2 字节（65536 个码点）够装所有字符，后来发现不够（CJK 扩展、emoji），引入「代理对」用 4 字节表示辅助平面字符。所以 UTF-16 也是变长的，只是大部分情况 2 字节——这让它既不如 UTF-8 紧凑，又不如 UTF-32 简单，处境尴尬。
- **乱码的机制**: 文本 = 字节序列 + 编码规则。写入时用编码 A 把字符变成字节，读取时如果用编码 B 解释这些字节，字符就「翻译」错了。如 UTF-8 下「中」是 \`E4 B8 AD\` 三个字节，被 GBK 两两一组解读就会变成两个乱码字。解决乱码就是找回正确的编码规则。

## 易错点与陷阱
- **陷阱**: \`strlen("中国")\` 返回 6（字节数）而不是 2（字符数）——统计字符数要用专门的宽字符函数或按 UTF-8 规则手算。
- **陷阱**: UTF-8 字符串从中间截断可能切断一个多字节字符——\`strncpy\` 按字节截，中文可能剩半个乱码；要按字符边界截断。
- **陷阱**: Windows 记事本默认带 BOM，Linux 工具（如 shell 脚本）遇到 BOM 会报错——跨平台文件建议 UTF-8 无 BOM。
- **陷阱**: C 源文件里有中文时，编译器按不同编码解释源文件可能报「非法字符」——用 \`-finput-charset=UTF-8\` 指定，或源文件存为 UTF-8。
- **陷阱**: 字符串反转不能简单逐字节反——多字节字符反转后变成乱码；要按码点（字符）反转而非按字节。

## 实战建议
- **建议**: 新项目一律 UTF-8——数据库、文件、HTTP 响应、源代码全统一 UTF-8，能消除 90% 的编码问题。Windows 内部用 UTF-16 是历史包袱，新代码不必跟进。
- **建议**: 统计字符串「字符数」而非「字节数」时，用 UTF-8 感知函数（C 用 \`mbstowcs\` 或手算首字节），避免把 3 字节中文当成 3 个字符。
- **建议**: 处理用户输入做长度限制时，按「字符数」而非「字节数」限制——否则一个 emoji 可能占 4 字节，让用户少输好几个字。
- **建议**: 排查乱码先确认「字节是什么编码」——\`hexdump\` 看真实字节，再对照预期编码解读；别盲目转码，越转越乱。
`,
    code: `// ===== 字符编码 演示 =====
// 演示：ASCII/中文在 UTF-8 下的字节序列，字节数 vs 字符数
#include <stdio.h>
#include <string.h>

// 手动统计 UTF-8 字符串的字符数（按首字节判断该字符占几个字节）
size_t utf8_char_count(const char *s) {
    size_t count = 0;
    while (*s) {
        unsigned char c = (unsigned char)*s;
        if (c < 0x80)            s += 1;   // 0xxxxxxx     : ASCII，1 字节
        else if ((c & 0xE0) == 0xC0) s += 2;  // 110xxxxx: 2 字节
        else if ((c & 0xF0) == 0xE0) s += 3;  // 1110xxxx: 3 字节（中文常用）
        else if ((c & 0xF8) == 0xF0) s += 4;  // 11110xxx: 4 字节（emoji）
        else s += 1;                       // 非法首字节，跳过
        count++;
    }
    return count;
}

int main(void) {
    // 1. ASCII 字符
    printf("=== ASCII 字符 'A' ===\\n");
    char a = 'A';
    printf("'A' = %d (十进制) = 0x%02X (十六进制)，占 1 字节\\n\\n",
           a, (unsigned char)a);

    // 2. 中文字符在 UTF-8 下的字节序列
    printf("=== 中文字符 '中' (UTF-8) ===\\n");
    const char *zh = "中";
    printf("字节序列: ");
    for (size_t i = 0; i < strlen(zh); i++) {
        printf("%02X ", (unsigned char)zh[i]);
    }
    printf("\\n共 %zu 字节表示 1 个汉字（UTF-8 中文占 3 字节）\\n\\n",
           strlen(zh));

    // 3. 字节数 vs 字符数
    const char *s = "Hello中国";
    printf("=== 字符串 \\"%s\\" ===\\n", s);
    printf("字节数 (strlen) = %zu\\n", strlen(s));
    printf("字符数 (UTF-8)  = %zu\\n", utf8_char_count(s));
    printf("→ ASCII 1 字节/字符，中文 3 字节/字符；strlen 数的是字节\\n");
    return 0;
}
`,
  },

  // ============================================================
  // 第 4 章：数据结构与内存布局
  // ============================================================
  {
    id: "comp-data-layout",
    group: "数据表示",
    icon: "📦",
    title: "数据结构与内存布局",
    content: `
## 概述
写 C/C++ 的人迟早会遇到这样的困惑：明明结构体字段就那么几个，\`sizeof\` 出来却比想象的大；同一个结构体换种字段顺序，大小居然变了。这背后是「内存对齐」和「填充」在起作用。本章讲数组、结构体、联合体在内存中的真实布局，以及指针的本质——它就是一个地址。理解内存布局，是看懂反汇编、排查段错误、写出紧凑高效结构体的必备知识。

## 核心要点
- **数组（Array）**: 同类型元素连续存放——\`int arr[4]\` 占 16 字节紧挨着，\`arr[i]\` 的地址 = 起始地址 + i×sizeof(int)，随机访问 O(1)。
- **结构体（Struct）**: 不同类型字段打包在一起——但字段间可能有「填充字节」以满足对齐要求，\`sizeof\` 往往大于字段大小之和。
- **联合体（Union）**: 所有成员共享同一段内存——大小等于最大成员，任一时刻只存一个有效值，改一个会覆盖其他。
- **对齐（Alignment）**: 每种类型有「自然对齐」要求——int 通常 4 字节对齐（地址是 4 的倍数），double 8 字节对齐。CPU 读对齐数据更快。
- **填充（Padding）**: 编译器在字段间或末尾插入空白字节凑齐对齐——这是 \`sizeof\` 偏大的原因。
- **指针（Pointer）**: 本质是「内存地址」——32 位系统占 4 字节，64 位系统占 8 字节；\`int*\` 和 \`char*\` 大小相同，区别在指向的类型。
- **栈（Stack）**: 函数局部变量所在，由编译器自动分配释放——后进先出，分配快但大小有限（通常 1~8MB）。
- **堆（Heap）**: \`malloc\`/\`free\` 手动管理的内存——空间大但需自己负责释放，泄漏和野指针是主要风险。
- **offsetof**: 宏，返回结构体字段的偏移量——调试对齐问题时最直观的工具。
- **位域（Bit-field）**: 结构体里按 bit 切分字段——省空间但有可移植性问题，常用于协议头解析。

## 原理与机制
- **内存是一排带编号的储物柜**: 每个柜子 1 字节，编号就是地址。数组是「连续的柜子」，\`arr[3]\` 就是第 3+起始号 那个柜子，一步到位；链表是「分散的柜子」，每个柜子里除了数据还塞着「下一个柜子的编号」，要顺藤摸瓜。这是数组随机访问 O(1) 而链表 O(n) 的根本原因。
- **对齐就像超市货架摆齐**: 货架（CPU 总线）按固定宽度（4/8 字节）取货，如果商品（数据）摆得歪歪扭扭跨了货架边界，店员要分两次拿再拼起来——慢。所以编译器把数据对齐到货架边界，宁可留空也摆齐。CPU 读对齐数据通常一条指令搞定，读未对齐数据可能要两次内存访问甚至触发异常（某些架构直接崩溃）。
- **结构体填充规则**: 每个字段对齐到自身对齐要求的倍数地址；整个结构体大小是其最大成员对齐值的倍数（末尾也补齐）。所以 \`char a; int b; char c;\` 里 a 后补 3 字节、c 后补 3 字节，共 12 字节；重排成 \`int b; char a; char c;\` 后只要 8 字节——「大字段在前」是省空间的通用技巧。
- **联合体的共享机制**: 所有成员从同一地址开始，改任一成员实际是改同一段字节。所以把 int 和 float 放进 union，改 int 后读 float 看到的是同一组字节重新解释——这是 C 里做类型双关（type punning）的「合法」方式，比强制指针转换更安全。
- **栈 vs 堆的取舍**: 栈分配只需移动栈指针（一条 \`sub rsp\` 指令），极快且自动回收，但大小固定、不能跨函数返回；堆空间大、生命周期自由，但 \`malloc\` 要查空闲链表、可能触发系统调用，慢且容易泄漏。高频小对象优先用栈。

## 易错点与陷阱
- **陷阱**: 误以为 \`sizeof(struct)\` 等于各字段大小之和——忘了对齐填充，\`{char,int,char}\` 算成 6 实际是 12。永远用 \`sizeof\` 而非手算。
- **陷阱**: 结构体字段顺序随意排——\`{char,int,char}\` 占 12 字节，\`{int,char,char}\` 占 8 字节，同样的字段省了 1/3 空间。
- **陷阱**: 指针大小因架构而异——32 位程序 4 字节、64 位程序 8 字节；把指针强转成 int 在 64 位会截断，导致崩溃。用 \`intptr_t\`/\`uintptr_t\`。
- **陷阱**: 返回指向栈局部变量的指针——函数返回后栈帧被回收，指针指向的内存被复用，读到垃圾值。栈上的东西别跨函数返回。
- **陷阱**: 联合体读写不同成员是「未定义行为」（C 标准），但几乎所有编译器都支持——可移植代码用 \`memcpy\` 替代，或检查编译器的宽松设置。
- **陷阱**: \`malloc\` 返回的地址不一定对齐到你想的类型——标准保证对齐到 \`max_align_t\`，通常够用，但 SIMD 指令可能要 16/32 字节对齐，需用 \`aligned_alloc\`。

## 实战建议
- **建议**: 定义结构体时按「大字段在前、小字段在后」排列——既省空间又利于缓存行对齐，是零成本的性能优化。
- **建议**: 调试对齐问题时用 \`offsetof(type, member)\` 打印各字段偏移，配合 \`sizeof\` 一眼看出哪里有填充——比肉眼看源码靠谱。
- **建议**: 指针和整数互转用 \`intptr_t\`/\`uintptr_t\`，不要用 int/long——保证在 32/64 位下都不会截断。
- **建议**: 高频分配的小对象优先放栈上（局部变量），大对象或生命周期跨函数的才用堆——能极大减少 \`malloc/free\` 开销和内存泄漏风险。
`,
    code: `// ===== 数据结构与内存布局 演示 =====
// 演示：结构体对齐填充、字段重排省空间、联合体共享内存、指针本质
#include <stdio.h>
#include <stddef.h>

// 故意乱序排列以观察对齐填充
struct Bad {
    char a;      // 1 字节
    int  b;      // 4 字节
    char c;      // 1 字节
};

// 重排：大字段在前，减少填充
struct Good {
    int  b;      // 4 字节
    char a;      // 1 字节
    char c;      // 1 字节
};

// 联合体：所有成员共享同一段内存
union Data {
    int   i;
    float f;
    unsigned char bytes[4];
};

int main(void) {
    // 1. 结构体对齐：sizeof 对比
    printf("=== 结构体对齐与填充 ===\\n");
    printf("struct Bad  sizeof = %zu\\n", sizeof(struct Bad));
    printf("struct Good sizeof = %zu  (字段重排省了空间)\\n\\n",
           sizeof(struct Good));

    // 2. 打印各字段偏移，看清填充位置
    printf("=== struct Bad 各字段偏移 ===\\n");
    printf("  a (char) 偏移 %zu\\n", offsetof(struct Bad, a));
    printf("  b (int ) 偏移 %zu  (a 后填充 3 字节)\\n", offsetof(struct Bad, b));
    printf("  c (char) 偏移 %zu\\n", offsetof(struct Bad, c));
    printf("  → 末尾再填充 3 字节凑齐 4 的倍数，共 %zu 字节\\n\\n",
           sizeof(struct Bad));

    // 3. 联合体共享内存：改一个成员，其他成员跟着变
    printf("=== 联合体共享内存 ===\\n");
    union Data d;
    d.i = 0x41424344;       // 写 int
    printf("union Data sizeof = %zu (所有成员共享同一段内存)\\n",
           sizeof(union Data));
    printf("写入 i=0x%08X 后：\\n", d.i);
    printf("  i     = %d\\n", d.i);
    printf("  f     = %f\\n", d.f);
    printf("  bytes = %02X %02X %02X %02X\\n",
           d.bytes[0], d.bytes[1], d.bytes[2], d.bytes[3]);
    printf("  → 同一段字节，按 int/float/byte[] 解读结果不同\\n\\n");

    // 4. 指针本质就是地址
    printf("=== 指针本质（地址）===\\n");
    int x = 42;
    int *p = &x;
    printf("x 的值 = %d\\n", x);
    printf("x 的地址 = %p\\n", (void *)p);
    printf("指针 p 自身大小 = %zu 字节 (64 位系统为 8)\\n", sizeof(p));
    return 0;
}
`,
  },
];
