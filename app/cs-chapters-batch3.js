// =============================================================
// 计算机原理入门教程 - batch3
// 章节 17-24：程序运行原理
// -------------------------------------------------------------
// 风格：简单入门、生活例子、干货、对编程有帮助。
// 演示代码用 Python（仅标准库），通过 /api/run-py 调用系统 python3 执行。
// 转义规则：反引号写作 \`，\${ 写作 \$\{。
// =============================================================

export const chapters = [
  // ============================================================
  // 第 17 章：源代码到可执行：编译与解释
  // ============================================================
  {
    id: "cs-17",
    group: "程序运行原理",
    icon: "📜",
    title: "源代码到可执行：编译与解释",
    content: `## 从一份菜谱说起

你写了一份番茄炒蛋的菜谱，想让两个厨师照着做。

第一个厨师是个急性子，他拿到菜谱就开始做：看第一行"打两个鸡蛋"，立刻打鸡蛋；看第二行"热油"，立刻开火倒油……每看一行做一行，做完再读下一行。做得不快，但拿到菜谱立刻能开工，菜谱改了一行也能马上照新菜谱来。

第二个厨师是个较真的人，他先把整份菜谱"翻译"成自己最熟悉的一套简写动作卡片——比如"取蛋两枚打入碗中搅匀"翻译成"动作 A""动作 B"。翻译完了，他照着卡片飞快地做。第一次翻译花点时间，但之后每次做这道菜都直接用卡片，又快又稳。可一旦菜谱改了一个字，整份卡片要重新翻译。

第一个厨师就是**解释执行**，第二个厨师就是**编译执行**。你写的 Python 或 C 代码就是"菜谱"，CPU 是最终"动手"的人，但 CPU 只听得懂最简单的机器指令（"动作卡片"）。从菜谱到动作，中间要经过"翻译"，翻译方式不同，程序的行为就不同。本章讲清楚编译、解释、混合这三种翻译方式。

## CPU 只认机器码

不管你用什么语言写代码，最终到 CPU 那里都是**机器码**——一串 0 和 1 的指令。CPU 内部的电路只能识别几十种最基础的动作：加、减、移动、跳转。你写的 \`print("hello")\` 或 \`x = a + b\`，CPU 一概不认识。

所以从源代码到 CPU 真正执行，必须有一步"翻译"。翻译方式不同，就分出了编译型、解释型、混合型。

## 编译型：整体翻译成机器码

**编译型语言**（C、C++、Go、Rust）的流程：

\`\`\`text
源代码(.c) --编译器--> 机器码(.exe) --直接运行--> CPU 执行
\`\`\`

编译器一次性把整个源代码翻译成机器码，生成可执行文件。运行时直接执行机器码，不用再翻译。这就像第二个厨师提前翻译好整套动作卡片，做菜时飞快。

优点：运行快（已经是机器码，CPU 直接执行）；一次编译多次运行；编译时能做大量优化（类型检查、内联、死代码消除）。

缺点：改一行代码要重新编译（大项目编译几分钟到几小时）；跨平台要分别编译（Windows 版、Mac 版各编译一次）；编译错误要全部修完才能运行。

C/C++ 是典型代表。写完 \`.c\` 文件，\`gcc main.c -o main\` 编译出可执行文件，运行飞快。但改一个字要重新编译。

## 解释型：逐行翻译执行

**解释型语言**（Python、JavaScript、Ruby）的流程：

\`\`\`text
源代码(.py) --解释器--> 逐行翻译并执行 --> CPU
\`\`\`

解释器读一行源代码，翻译成机器码执行，再读下一行。不生成独立的可执行文件。这就像第一个厨师，看一行做一行。

优点：改完直接运行，不用编译（开发快、调试方便）；跨平台（只要有对应平台的解释器，同一份代码到处跑）；交互式（REPL，输一行看一行结果）。

缺点：运行慢（每次运行都要重新翻译，且翻译粒度粗、优化少）；运行时才发现类型错误（没有编译期类型检查）；部署要带解释器。

Python 是典型代表。写完 \`.py\`，\`python main.py\` 直接运行。改完再跑，不用编译。但比 C 慢 10~100 倍。

## 混合型：先编译成字节码再解释

**混合型语言**（Java、C#，Python 实际上也是）的流程：

\`\`\`text
源代码 --编译--> 字节码(.class/.pyc) --虚拟机解释/JIT--> 执行
\`\`\`

字节码不是机器码（不能直接给 CPU 执行），而是给**虚拟机**看的"中间代码"。虚拟机（JVM、Python 的 PVM）负责把字节码解释成机器码执行。

为什么搞中间层？为了**跨平台**。机器码跟 CPU 架构绑定（x86 的机器码 ARM 跑不了），但字节码是通用的。编译一次生成字节码，任何装了 JVM 的设备都能跑——"一次编写，到处运行"。

Java 的 JIT（即时编译）：JVM 运行时把频繁执行的字节码编译成机器码缓存起来，下次直接执行机器码。这样 Java 既有跨平台的好处，又接近 C 的速度。

Python 也有字节码：跑 \`.py\` 后会生成 \`.pyc\` 文件，就是编译后的字节码。下次运行直接加载 \`.pyc\` 省去编译步骤。但 Python 标准 CPython 没有 JIT，所以还是慢。

## 表格对比：三种翻译方式

| 维度 | 编译型（C） | 解释型（Python） | 混合型（Java） |
| --- | --- | --- | --- |
| 运行速度 | 最快 | 慢（10~100 倍） | 较快（JIT） |
| 开发速度 | 慢（要编译） | 快（直接跑） | 中等 |
| 跨平台 | 要分别编译 | 装解释器即可 | 装虚拟机即可 |
| 类型检查 | 编译期 | 运行期 | 编译期+运行期 |
| 部署 | 单个可执行文件 | 要带解释器 | 要带虚拟机 |
| 适合 | 系统/游戏/高性能 | 脚本/数据分析 | 企业后端/Android |

## 编程启发

1. **为什么 Python 慢**：每行代码运行时都要解释器翻译成字节码再执行，且没有 JIT（CPython）。同样一个循环，C 编译后是几条机器指令，Python 要跑一堆解释器逻辑。所以性能关键代码用 C 扩展（NumPy 就是 C 写的）。

2. **为什么部署方式不同**：C 程序编译成单个 .exe 直接发给用户就能跑；Python 要带整个解释器和依赖，所以有了 Docker、PyInstaller 打包；Java 要带 JVM。理解这点你就理解了各种打包工具的意义。

3. **静态类型快在哪**：编译器知道类型，能直接生成高效的机器码；动态类型每次运行都要判断类型、查找方法，开销大。这就是 TypeScript、Cython 流行的原因。

4. **.pyc 文件的作用**：第一次运行 .py 编译成 .pyc 缓存，第二次直接加载 .pyc 跳过编译，启动更快。改了 .py 会重新生成 .pyc。

## ⚠️ 常见坑

- **坑 1：以为 Python 没有编译**。Python 也有编译（.py → .pyc），只是编译成字节码不是机器码，且自动进行不用手动操作。用 \`dis\` 模块能看到字节码。
- **坑 2：以为编译型一定比解释型快**。大方向对，但有 JIT 的 Java/JS 在某些场景接近 C。而且算法复杂度比语言选择影响更大——O(n) 的 Python 比 O(n²) 的 C 快。
- **坑 3：以为字节码 = 机器码**。字节码是给虚拟机看的中间代码，还要虚拟机翻译成机器码。所以 Java/Python 比纯编译的 C 多一层翻译开销。

## 小结

- 编译型（C/C++/Go）：先编译成机器码再执行，运行快但开发慢。
- 解释型（Python/JS）：边翻译边执行，开发快但运行慢。
- 混合型（Java/Python）：先编译成字节码，再由虚拟机解释或 JIT 执行，兼顾跨平台和性能。
- Python 也有字节码（.pyc），用 dis 模块可查看。
- 部署方式因语言而异：C 发可执行文件，Python 带解释器，Java 带虚拟机。

## 常见疑问 Q&A

**Q1：为什么 Python 不加 JIT 让它变快？**
CPython 加 JIT 会增加复杂度和内存占用，且 Python 的动态特性让 JIT 优化效果有限。PyPy 是带 JIT 的 Python 实现，快几倍但兼容性不如 CPython。Python 3.13 在试验 JIT，未来可能提速。

**Q2：JavaScript 是编译型还是解释型？**
现代 JS 引擎（V8）是混合型：先解析成字节码，热点代码用 JIT 编译成机器码。所以 JS 比 Python 快（V8 有 JIT 和更激进的优化），但比 C 慢。

**Q3：为什么 C 编译那么慢？**
C 编译器要做大量优化（内联、循环展开、死代码消除、链接），还要处理头文件包含。大项目几百万行代码，编译要几十分钟。Go/Rust 用增量编译和模块化改善了这个问题。`,
    code: `# ===== 第 17 章：源代码到可执行——编译与解释 =====
# 演示：编译 vs 解释的类比、用 dis 看 Python 字节码

import dis
import sys

print("=== 1. 三种翻译方式 ===")
print("编译型 (C/C++/Go): 源码 --编译器--> 机器码 --直接执行-->")
print("  优点: 运行快   缺点: 改一次要重新编译")
print("解释型 (Python/JS): 源码 --解释器--> 边翻译边执行-->")
print("  优点: 开发快   缺点: 运行慢")
print("混合型 (Java): 源码 --编译--> 字节码 --JVM/JIT--> 机器码")
print("  兼顾跨平台和性能")

print()
print("=== 2. 厨师类比 ===")
print("编译 = 厨师先把菜谱全翻译成动作卡片，做菜飞快")
print("解释 = 厨师看一行菜谱做一步，立刻能开工但慢")
print("CPU 只认机器码(动作卡片)，源码(菜谱)必须翻译")

print()
print("=== 3. Python 也有编译 ===")
print("Python 版本:", sys.version.split()[0])
print(".py --编译--> .pyc 字节码 --解释器(PVM)--> 执行")
print("Python 是混合型，只是编译成字节码不是机器码")

print()
print("=== 4. 用 dis 看字节码 ===")
def add(a, b):
    return a + b

print("源码: def add(a, b): return a + b")
print("对应的字节码:")
dis.dis(add)
print("-> 一行 a+b 对应 LOAD_FAST + LOAD_FAST + BINARY_ADD")

print()
print("=== 5. for 循环的字节码 ===")
def loop_demo():
    total = 0
    for i in range(3):
        total += i
    return total

print("源码: total=0; for i in range(3): total+=i")
dis.dis(loop_demo)
print("-> 循环靠跳转指令实现，比 C 多很多解释开销")

print()
print("=== 6. 速度差异的本质 ===")
print("C:    源码预编译成机器码，运行时直接执行，最快")
print("Java: JIT 把热点字节码编译成机器码，接近 C")
print("Python: 每次运行都解释字节码，慢约 10-100 倍")
print("所以系统软件用 C++，数据分析用 Python(开发快)")
print("性能关键部分用 C 扩展，如 NumPy")
`,
  },

  // ============================================================
  // 第 18 章：指令周期：取指-译码-执行
  // ============================================================
  {
    id: "cs-18",
    group: "程序运行原理",
    icon: "⚙️",
    title: "指令周期：取指-译码-执行",
    content: `## 厨师按菜谱做菜的三个动作

你让一个厨师照着菜谱做菜。注意观察他的工作方式——他不会一口气把整本菜谱背下来，而是反复做三个动作：

1. **抬头看菜谱下一行**："哦，下一步是热油"。这一步叫**取指（Fetch）**——把下一条指令从"菜谱"（内存）取到眼前。
2. **理解这行说什么**："热油就是倒油开火等油冒烟"。这一步叫**译码（Decode）**——搞清楚这条指令要干什么、对谁干。
3. **照做**：倒油、开火、等油冒烟。这一步叫**执行（Execute）**——真正动手完成指令。

做完这一行，他抬头看下一行，重复这三个动作，直到菜谱读完或遇到"出锅"。CPU 干活的方式和这个厨师一模一样：**取指、译码、执行**，循环往复，这叫**指令周期**。理解了这个循环，你就理解了 CPU 是怎么跑程序的，也理解了调试器断点到底在干什么。

## 指令周期：CPU 的工作循环

CPU 一直在做这个循环：

\`\`\`text
1. 取指(Fetch)：从内存取出下一条指令，放进 CPU
2. 译码(Decode)：分析这条指令是什么操作、操作数在哪
3. 执行(Execute)：完成这个操作（加法、移动、跳转等）
4. 回到第 1 步，处理下一条
\`\`\`

这个循环快得惊人——现代 CPU 一秒能做几十亿次。你感觉程序"瞬间"跑完，其实是 CPU 闪电般地重复了几十亿次取指-译码-执行。

## 寄存器：CPU 的"手边"

厨师做菜时，油盐酱醋不会每次都跑去厨房角落拿，而是放在手边的调料盒里，随手就能取。CPU 也有"手边"——**寄存器（Register）**。

寄存器是 CPU 内部极少量、极快的存储空间，离运算单元最近，读写几乎零延迟。常见寄存器：

- **程序计数器（PC）**：记着"做到第几条指令了"，下一条要取的指令地址
- **指令寄存器（IR）**：当前正在执行的那条指令
- **累加器（ACC）**：暂存运算的中间结果
- **栈指针（SP）**：指向当前调用栈的栈顶

为什么要有寄存器？因为内存虽然大但慢（相对 CPU 而言），每次都去内存取数据太耽误事。CPU 把最常用的数据放在寄存器里，用完再写回内存。这就像厨师把当前要用的食材放在手边，而不是每次都去仓库取。

## 程序计数器：记着做到第几步

程序计数器（PC，Program Counter）是整个指令周期的心脏。它存的就是"下一条指令的地址"。

- 取指时：CPU 根据 PC 里的地址，去内存取指令
- 取完一条：PC 自动 +1（指向下一条）
- 遇到跳转指令：直接把 PC 改成目标地址（这就是 if/while/for 的底层原理）

你写的 \`if x > 0:\` 在 CPU 那里就是一条"条件跳转"——满足条件就改 PC 跳到某处，不满足就 PC+1 继续往下。\`while\` 循环就是执行完循环体后，跳回循环开头（改 PC 回退）。理解了 PC 和跳转，你就理解了所有控制流的本质。

## 表格对比：内存 vs 寄存器

| 维度 | 寄存器 | 内存 |
| --- | --- | --- |
| 位置 | CPU 内部 | CPU 外部 |
| 容量 | 极小（几十个，每个几十字节） | 大（GB 级） |
| 速度 | 极快（0 周期） | 慢（几十~上百周期） |
| 用途 | 暂存当前运算数据 | 存程序和数据 |
| 程序员能控制 | 汇编能直接用 | 高级语言操作 |

## 编程启发：理解指令周期才能理解调试

1. **断点的原理**：调试器在你设断点的那行，把原指令换成一条"中断"指令。CPU 执行到这里触发中断，暂停程序，调试器接管。本质就是在指令周期的"取指"后插入一个停顿。

2. **单步执行**：调试器的"单步步过"就是让 CPU 执行一条指令（一个指令周期）后暂停。理解了指令周期，你就理解了为什么单步能逐行观察程序状态。

3. **程序卡死的本质**：程序卡住往往是卡在某个指令周期——可能是死循环（PC 一直在几条指令间跳），或者等 IO（执行阶段卡住等数据）。

4. **性能优化从减少指令数入手**：CPU 跑得快慢本质是"单位时间完成了多少指令周期"。优化算法减少指令数、用更高效的数据结构减少寻址开销，都是在这个层面优化。

## ⚠️ 常见坑

- **坑 1：以为 CPU 能同时执行多条指令**。单核 CPU 任何时刻只执行一条指令，"同时"是快速切换的错觉。多核才能真正同时执行多条。
- **坑 2：以为程序是"一次性加载执行"**。程序是一条条指令取指-执行地跑，遇到跳转就改变执行顺序。所以程序的执行顺序不一定等于代码书写顺序。
- **坑 3：以为寄存器和内存一样快**。寄存器比内存快几十倍，编译器会尽量把热点变量放在寄存器里。这就是为什么 \`register\` 关键字（C 语言）曾经有用。

## 小结

- CPU 的工作循环是：取指（取下一条指令）→ 译码（理解指令）→ 执行（照做），无限循环。
- 寄存器是 CPU 的"手边"，极快但极小；程序计数器（PC）记着做到第几条。
- 跳转指令就是改 PC，这就是 if/while/for 的底层原理。
- 断点/单步调试本质是在指令周期中插入暂停。
- 程序性能 = 指令数 × 每条指令耗时，优化从减少指令数入手。

## 常见疑问 Q&A

**Q1：CPU 怎么知道程序从哪开始执行？**
操作系统加载程序时，把入口地址（如 C 的 main 函数）写进 PC，CPU 从那里开始取指。程序结束时执行一条特殊指令（如 HALT 或系统调用）把控制权还给操作系统。

**Q2：取指和执行能同时做吗？**
现代 CPU 用**流水线（Pipeline）**技术——第一条指令在执行时，第二条已经在取指了，像工厂流水线一样重叠。这样整体吞吐量大增。但本质还是一条条指令顺序处理，只是各阶段重叠了。

**Q3：为什么死循环会让 CPU 占满？**
死循环里 CPU 一直在取指-译码-执行那几条跳转指令，永不暂停，所以占满一个核心。正常程序会等 IO（CPU 空闲）或睡眠（主动让出），CPU 占用就降下来了。`,
    code: `# ===== 第 18 章：指令周期——取指-译码-执行 =====
# 演示：用 Python 模拟 CPU 的取指-译码-执行循环

print("=== 1. 指令周期：取指-译码-执行 ===")
print("取指(Fetch): 从内存取出下一条指令")
print("译码(Decode): 理解这条指令要做什么")
print("执行(Execute): 照做")

print()
print("=== 2. 模拟 CPU 执行 ===")
# 模拟内存：数据区
memory = [5, 3, 0]  # memory[2] 用来存结果
# 模拟程序：指令列表
program = [
    ("LOAD", 0),     # 累加器 = memory[0]
    ("ADD", 1),      # 累加器 += memory[1]
    ("STORE", 2),    # memory[2] = 累加器
    ("HALT", None),  # 停机
]

acc = 0       # 累加器（寄存器）
pc = 0        # 程序计数器：记着做到第几条
steps = 0
print("程序: LOAD 0, ADD 1, STORE 2, HALT (算 5+3)")
while True:
    op, arg = program[pc]              # 取指
    steps += 1
    print("  第%d步 PC=%d 取指:[%s %s] 译码:%s 执行..." % (
        steps, pc, op, arg if arg is not None else "", op))
    if op == "LOAD":                    # 执行
        acc = memory[arg]; pc += 1
    elif op == "ADD":
        acc += memory[arg]; pc += 1
    elif op == "STORE":
        memory[arg] = acc; pc += 1
    elif op == "HALT":
        print("    -> 停机"); break
print("结果 memory[2] =", memory[2])

print()
print("=== 3. 程序计数器 PC 的作用 ===")
print("PC 记着下一条指令的位置，每执行一条 PC+1")
print("跳转指令就是直接改 PC，实现循环和 if")

print()
print("=== 4. 用跳转模拟循环：1+2+3 ===")
total = 0
i = 1
while i <= 3:   # 等价于: 条件跳转实现的循环
    total += i
    print("  循环: i=%d, total=%d (靠跳转回退 PC)" % (i, total))
    i += 1
print("循环结果:", total)

print()
print("=== 5. 寄存器：CPU 的手边 ===")
print("寄存器是 CPU 内部的小存储，比内存快几十倍")
print("常见寄存器: PC(程序计数器) ACC(累加器) SP(栈指针)")
print("调试时看的'断点'就是让 CPU 在某条指令前停下")
print("-> 理解指令周期才能理解调试器的工作原理")
`,
  },

  // ============================================================
  // 第 19 章：内存管理：栈与堆
  // ============================================================
  {
    id: "cs-19",
    group: "程序运行原理",
    icon: "🥞",
    title: "内存管理：栈与堆",
    content: `## 从摞盘子和仓库说起

食堂里有两种存放东西的方式。

**摞盘子**：洗好的盘子一个个往上叠，拿的时候从顶上拿。特点是**后放的最先拿**（后进先出），而且只能动顶部——你要拿中间的盘子得先把上面的全挪开。这种"叠起来"的方式适合临时存放、用完就走的东西，比如客人用完的餐盘。

**仓库**：一个大房间，东西随便放，哪里有空位放哪里，每个东西贴个标签（地址）。要取某个东西按地址找，不用管别的。特点是**灵活但乱**，需要有人管理（仓库管理员记账：哪个位置放了什么、什么时候该清理）。

计算机的内存管理也是这两套：

- **栈（Stack）**= 摞盘子：函数调用、局部变量放这里，后进先出，自动管理（函数返回自动清理）。
- **堆（Heap）**= 仓库：动态分配的对象放这里，灵活但靠垃圾回收（GC）或手动管理。

本章讲清楚为什么内存要分栈和堆，以及栈溢出、内存泄漏这些坑从哪来。

## 栈：后进先出，自动管理

栈是一种"后进先出"（LIFO）的数据结构，只能从顶部压入（push）和弹出（pop）。内存里的栈区专门用来存：

- **函数调用的信息**：每次调用函数压一个"栈帧"，包含参数、局部变量、返回地址
- **局部变量**：函数里定义的变量

栈的最大特点是**自动管理**——函数调用时压栈，函数返回时栈帧自动弹出，局部变量随之销毁。你不用手动释放，系统全包了。所以栈快且省心。

但栈有局限：**大小有限**（Linux 默认 8MB，Python 递归深度默认 1000），而且**必须后进先出**——你不能保留一个函数的局部变量到函数返回之后。所以栈不适合存"长期存活"的数据。

## 堆：灵活分配，手动或自动回收

堆是一大块可以灵活分配的内存区域。程序运行中需要存"大小不定、存活时间不定"的数据时，就在堆上申请一块。

Python 里**所有对象**（列表、字典、自定义类的实例）都存在堆上。变量名只是栈上的一个"标签"，指向堆里的对象。

堆的特点：

- **灵活**：随时申请、随时释放，大小不限（只要内存够）
- **大**：堆的空间远大于栈（可以用几个 GB）
- **管理复杂**：要记录哪些内存被用了、哪些空闲；用完要释放，否则内存泄漏

Python 用**垃圾回收（GC）**自动管理堆——一个对象没人引用了，GC 自动回收它的内存。C/C++ 要程序员手动 \`malloc/free\`，忘了 free 就泄漏。

## 为什么分两种：用途不同、管理方式不同

为什么内存不搞一种？因为栈和堆的用途完全不同：

| 维度 | 栈 | 堆 |
| --- | --- | --- |
| 存什么 | 函数调用、局部变量 | 动态分配的对象 |
| 管理方式 | 自动（函数返回自动清理） | 手动(C) / GC(Python) |
| 速度 | 快（移动指针即可） | 慢（要查找空闲块） |
| 大小 | 小（MB 级） | 大（GB 级） |
| 顺序 | 后进先出 | 任意 |
| 生命周期 | 函数返回即销毁 | 由程序员/GC 决定 |

函数调用的信息必须"后进先出"（最后调用的最先返回），天然适合栈。而对象的大小、存活时间不确定，必须灵活分配，所以用堆。分开管理，各司其职。

## 编程启发：栈溢出和内存泄漏的根源

1. **栈溢出（Stack Overflow）**：递归太深，栈帧累积超过栈空间上限。Python 抛 RecursionError，C/C++ 直接段错误。解决办法：递归改循环（不压栈帧），或谨慎调大栈限制。

2. **内存泄漏（Memory Leak）**：堆上的对象不再使用，但还有引用指向它，GC 不回收，内存越占越多。Python 里常见于：全局列表不断 append 不清理、缓存不设上限、循环引用。C 里忘了 free 直接泄漏。

3. **Python 的引用计数**：Python 主要靠引用计数——一个对象被引用的次数归零时立刻回收。但循环引用（A 引用 B，B 引用 A）引用计数都不为零，要靠分代 GC 周期性清理。

4. **大对象放堆别放栈**：C 里在函数里定义超大数组 \`int arr[10000000]\` 会栈溢出，要用 \`malloc\` 放堆上。Python 不用担心，所有对象都在堆上。

## 表格对比：栈溢出 vs 内存泄漏

| 问题 | 发生在 | 原因 | 后果 | 解决 |
| --- | --- | --- | --- | --- |
| 栈溢出 | 栈 | 递归太深/局部变量太大 | 程序崩溃 | 递归改循环 |
| 内存泄漏 | 堆 | 对象不用了没释放 | 内存越占越多 | 及时释放/设上限 |
| 悬空指针 | 堆(C) | 释放后还在用 | 访问非法内存 | Python 用 GC 避免 |

## ⚠️ 常见坑

- **坑 1：以为 Python 不会内存泄漏**。Python 有 GC，但循环引用、全局容器无限增长仍会泄漏。用 \`gc\` 模块或内存分析工具排查。
- **坑 2：递归改循环还是慢**。改循环避免了栈溢出，但如果算法复杂度没变（如 O(2ⁿ)），还是慢。栈溢出和性能是两个问题。
- **坑 3：以为局部变量函数返回还能用**。局部变量在栈上，函数返回栈帧弹出就没了。返回局部变量的引用（C 里）是悬空指针。Python 返回的是堆上对象的引用，没事。

## 小结

- 栈像摞盘子，后进先出，存函数调用和局部变量，自动管理，快但小。
- 堆像仓库，灵活分配，存动态对象，手动(C)或 GC(Python)管理，大但慢。
- 分两种是因为用途不同：调用信息要后进先出（栈），对象要灵活分配（堆）。
- 栈溢出源于递归太深，内存泄漏源于堆对象没释放。
- Python 所有对象在堆上，变量名在栈上是指向堆对象的引用。

## 常见疑问 Q&A

**Q1：Python 变量到底在栈还是堆？**
变量名（标签）在栈上，变量指向的对象在堆上。比如 \`x = [1,2,3]\`，\`x\` 这个标签在栈上，列表对象 \`[1,2,3]\` 在堆上。函数返回时 \`x\` 这个标签弹出消失，但列表对象还在堆上（如果有人引用的话）。

**Q2：为什么栈比堆快？**
栈只需移动栈顶指针就能分配/释放，纯指针操作。堆要维护空闲链表、查找合适大小的块、处理碎片，开销大。所以栈的分配是 O(1)，堆的分配更复杂。

**Q3：GC 会不会影响程序性能？**
会。GC 运行时要暂停程序（Stop The World）扫描对象，可能造成卡顿。Python 的引用计数是实时的（对象归零立即回收，开销小），但分代 GC 周期运行有一定停顿。对延迟敏感的场景要注意 GC 调优。`,
    code: `# ===== 第 19 章：内存管理——栈与堆 =====
# 演示：函数调用栈、对象在堆上

print("=== 1. 栈 vs 堆 ===")
print("栈: 像摞盘子，后放先拿，存函数调用和局部变量")
print("堆: 像仓库，随便放随便取，存动态分配的对象")

print()
print("=== 2. 函数调用压栈 ===")
def func_a():
    x = 10
    print("    func_a 栈帧: x=%d" % x)
    func_b()
    print("    func_a 栈帧还在，x=%d" % x)

def func_b():
    y = 20
    print("      func_b 栈帧: y=%d" % y)
    func_c()
    print("      func_b 栈帧还在，y=%d" % y)

def func_c():
    z = 30
    print("        func_c 栈帧: z=%d" % z)

print("调用 func_a():")
func_a()
print("调用结束，所有栈帧弹出")

print()
print("=== 3. 局部变量在栈上，函数结束就没了 ===")
def make_local():
    local_var = [1, 2, 3]   # 列表对象在堆上
    print("  函数内 local_var =", local_var)
    return local_var        # 返回的是堆对象的引用

result = make_local()
print("  函数返回后 result =", result, "(列表对象在堆上，引用返回)")

print()
print("=== 4. 对象在堆上 ===")
a = [1, 2, 3]
b = {"name": "Tom"}
print("列表 a 的 id =", id(a), "(对象在堆上)")
print("字典 b 的 id =", id(b), "(对象在堆上)")
print("变量名 a/b 在栈上，指向堆里的对象")

print()
print("=== 5. 栈自动管理，堆靠 GC ===")
def temp():
    x = 42          # x 在栈上，函数结束自动回收
    big = [0]*1000  # 列表对象在堆上，没人引用时 GC 回收
    print("  temp 内: x=%d, big 长度=%d" % (x, len(big)))

temp()
print("  temp 结束，栈帧弹出，x 没了")
print("  堆上的列表没人引用，等待 GC 回收")

print()
print("=== 6. 栈溢出 vs 内存泄漏 ===")
print("栈溢出: 递归太深，栈帧超过栈空间上限")
print("  C/C++ 段错误，Python 抛 RecursionError")
print("内存泄漏: 堆上对象不再使用但没被回收")
print("  C 要手动 free，Python 靠引用计数+GC 自动处理")
`,
  },

  // ============================================================
  // 第 20 章：函数调用与栈帧
  // ============================================================
  {
    id: "cs-20",
    group: "程序运行原理",
    icon: "🍽️",
    title: "函数调用与栈帧",
    content: `## 从委托任务说起

你在公司里负责一个项目，遇到一个技术问题搞不定，于是你**委托**同事小王帮忙。这个"委托"就是**函数调用**：

- 你把问题描述和资料（**参数**）交给小王
- 小王放下手头的活，开始处理这个问题（**压栈帧**）
- 小王处理完，把结果（**返回值**）告诉你
- 你拿着结果继续你之前的工作（**弹栈帧，恢复现场**）

关键在于：小王处理问题时，**你之前做到哪一步了**得记下来，不然他回来你不知道接着干啥。而且小王可能又委托别人（函数里再调函数），一层层委托下去，每一层都要记住"我做到哪了、我的临时数据放哪了"。

计算机里，这些"委托记录"就是**栈帧（Stack Frame）**。每次函数调用压一个栈帧，记录这次调用的参数、局部变量和返回地址。理解了栈帧，你才能理解调试、递归、闭包。

## 栈帧：每次调用压一个

**栈帧**是调用栈上的一个"区块"，每次函数调用就压入一个栈帧，函数返回时弹出。一个栈帧里装着：

- **参数**：调用者传进来的值
- **局部变量**：函数内部定义的变量
- **返回地址**：函数执行完要回到调用者的哪一行继续
- **一些寄存器的备份**：恢复现场用

调用 A → A 调用 B → B 调用 C，栈的样子：

\`\`\`text
栈顶 →  C 的栈帧(参数、局部变量、返回地址)
        B 的栈帧
        A 的栈帧
栈底 →  main 的栈帧
\`\`\`

C 返回时弹出 C 的栈帧，控制权回到 B；B 返回弹出 B 的栈帧，回到 A。后调用的先返回，完美的后进先出。

## 参数怎么传：值传递 vs 引用传递

函数调用时，参数怎么从调用者传到函数里？这是编程里最容易踩坑的地方。

**值传递（C 默认）**：把参数的值复制一份传进去。函数内改这个副本，不影响外面的原变量。

**引用传递（Python/Java 的对象）**：把对象的地址（引用）传进去。函数内通过这个引用修改对象，外面的变量能看到变化。

Python 的规则是"传引用"，但表现因类型而异：

\`\`\`python
def modify(x, lst):
    x = x + 1        # 不可变对象，创建新对象，外部不变
    lst.append(99)   # 可变对象，原地修改，外部也变

n = 10
mylist = [1, 2]
modify(n, mylist)
print(n)       # 10，没变
print(mylist)  # [1, 2, 99]，变了
\`\`\`

记忆口诀：**不可变对象传进去像值传递（改了是新对象，外部不变），可变对象传进去像引用传递（原地改，外部变）**。

## 递归为什么可能栈溢出

递归是函数调用自己。每递归一层就压一个栈帧，深度 N 就压 N 个栈帧。栈空间有限，深度太大就栈溢出。

\`\`\`python
def recurse(n):
    if n == 0: return
    recurse(n - 1)
recurse(10000)  # RecursionError
\`\`\`

Python 默认递归深度限制 1000 层。这不是栈真的满了，是 Python 提前保护防止 C 层栈崩溃。

**为什么循环不会栈溢出？** 循环只用一份变量，不压新栈帧；递归每层都压一个栈帧。把递归改成循环（迭代）就能处理任意深度——这是避免栈溢出的根本方法。

## 表格对比：递归 vs 循环

| 维度 | 递归 | 循环 |
| --- | --- | --- |
| 栈帧 | 每层压一个 | 只用一个 |
| 深度限制 | 受栈空间限制 | 无限制 |
| 性能 | 有压栈弹栈开销 | 开销小 |
| 可读性 | 树/分治问题更清晰 | 线性问题更清晰 |
| 适用 | 树遍历、分治、回溯 | 线性遍历、计数 |

## 编程启发：理解调用栈才能调试和写闭包

1. **调试器的调用栈**：程序崩溃或断点暂停时，调试器显示"调用栈"——一层层栈帧，告诉你"谁调用了谁，当前在哪一层"。看懂调用栈才能定位问题根源。

2. **异常的传播**：异常抛出时沿调用栈向上传播——当前函数没 catch 就弹栈帧到上一层，层层往上直到被 catch 或弹到顶（程序崩溃）。这就是"栈展开"。

3. **闭包的原理**：闭包是"函数 + 它定义时的环境"。内层函数引用了外层函数的局部变量，外层函数返回后，那些局部变量因为被引用而留在堆上（Python 实现），不会被回收。理解栈帧和作用域才能理解闭包。

4. **尾递归优化**：如果函数最后一步是调用自己（尾递归），编译器可以优化成循环不压栈帧。但 Python 不支持，所以深层递归必须手动改循环。

## ⚠️ 常见坑

- **坑 1：递归没写终止条件**。\`def f(): return f()\` 永远递归必栈溢出。递归必须有明确的 base case。
- **坑 2：以为可变对象传参是值传递**。列表、字典传进函数被原地修改，外部也变。要不影响外部用 \`copy()\` 或 \`deepcopy()\`。
- **坑 3：闭包延迟绑定的坑**。在循环里创建多个闭包引用循环变量，所有闭包拿到的是循环结束后的值。要加默认参数 \`lambda i=i: ...\` 捕获当前值。

## 小结

- 函数调用像委托任务：调用压栈帧（记现场），返回弹栈帧（恢复现场）。
- 栈帧装着参数、局部变量、返回地址；后调用的先返回（后进先出）。
- 参数传递：不可变对象像值传递（改了是新的），可变对象像引用传递（原地改外部变）。
- 递归每层压栈帧，太深会栈溢出；循环不压栈帧，能处理任意深度。
- 理解调用栈才能调试、理解异常传播、理解闭包。

## 常见疑问 Q&A

**Q1：Python 是值传递还是引用传递？**
严格说是"传引用"——传的是对象的引用（地址）。但因为不可变对象改了会创建新对象，所以表现上像值传递；可变对象原地改，表现像引用传递。术语叫"传对象引用"或"传共享"。

**Q2：栈帧有多大？**
取决于函数的参数和局部变量数量。简单函数几十字节，局部变量多的函数几百字节。1000 层递归大概几十 KB 到几百 KB，8MB 栈空间足够，Python 限制 1000 是保守保护。

**Q3：为什么尾递归优化能省栈帧？**
尾递归（最后一步是调用自己）时，当前栈帧的局部变量后面不再用了，可以直接复用这个栈帧给下一次调用，不新压。这样递归不管多深都只用一个栈帧。Python 不支持这个优化，所以深层递归必须改循环。`,
    code: `# ===== 第 20 章：函数调用与栈帧 =====
# 演示：调用栈深度、递归、参数传递

import sys

print("=== 1. 栈帧：每次调用压一个 ===")
def greet(name):
    msg = "Hi, " + name
    return msg

def main():
    user = "Tom"
    result = greet(user)
    print("  main 调用 greet，得到:", result)

print("调用 main():")
main()
print("调用时: 压 main 栈帧 -> 压 greet 栈帧 -> greet 返回弹出 -> main 返回弹出")

print()
print("=== 2. 调用栈深度 ===")
print("Python 默认递归深度限制:", sys.getrecursionlimit())

def show_depth(n):
    if n == 0:
        return
    show_depth(n - 1)

print("递归调用 show_depth(50) 成功")
show_depth(50)

print()
print("=== 3. 递归栈溢出 ===")
def recurse(n):
    if n == 0:
        return "done"
    return recurse(n - 1)

try:
    recurse(5000)
except RecursionError:
    print("recurse(5000) -> RecursionError (栈帧太多)")

print()
print("=== 4. 值传递 vs 引用传递 ===")
# Python 传引用，但不可变对象表现得像值传递
def modify_num(x):
    x = x + 1   # 不可变，创建新对象，不影响外部
    print("    函数内 x =", x)

n = 10
print("调用前 n =", n)
modify_num(n)
print("调用后 n =", n, "(不可变对象，外部不变)")

def modify_list(lst):
    lst.append(99)  # 可变，原地修改，影响外部
    print("    函数内 lst =", lst)

mylist = [1, 2, 3]
print("调用前 mylist =", mylist)
modify_list(mylist)
print("调用后 mylist =", mylist, "(可变对象，外部也变)")

print()
print("=== 5. 改用循环避免栈溢出 ===")
def sum_recursive(n):
    if n == 0:
        return 0
    return n + sum_recursive(n - 1)

def sum_loop(n):
    total = 0
    for i in range(n + 1):
        total += i
    return total

print("递归求和 sum_recursive(100) =", sum_recursive(100))
print("循环求和 sum_loop(100)      =", sum_loop(100))
print("循环求和 sum_loop(10000)    =", sum_loop(10000), "(循环不压栈，不溢出)")
`,
  },

  // ============================================================
  // 第 21 章：变量与地址：名字与位置
  // ============================================================
  {
    id: "cs-21",
    group: "程序运行原理",
    icon: "🏷️",
    title: "变量与地址：名字与位置",
    content: `## 从门牌号和经纬度说起

你想去朋友家，朋友告诉你"我家在幸福路 88 号"——这是**门牌号**，好记、好用。但地图 App 内部用的是"东经 116.4°，北纬 39.9°"——这是**经纬度**，精确到地球上的唯一一个点。

门牌号是给人看的，经纬度是给系统用的。两者指向同一栋房子，只是表达方式不同。

变量也是一样：

- **变量名**（如 \`age\`）= 门牌号，给人看的，方便你写代码
- **内存地址**（如 \`0x7ffeeb40\`）= 经纬度，给 CPU 用的，精确到内存某个字节
- **值**（如 \`25\`）= 房子里住的人

你写 \`age = 25\`，背后是：在内存某个地址（经纬度）存了 25（房子里的人），并给它贴了个标签叫 \`age\`（门牌号）。CPU 找数据时用的是地址，你写代码用的是名字，名字和地址之间有一张"对照表"。

## 变量 = 名字 + 地址 + 值

一个变量完整地看有三要素：

\`\`\`text
变量 = 名字（age）+ 地址（0x...）+ 值（25）
\`\`\`

- **名字**：你在代码里用的标识符
- **地址**：值在内存中的位置
- **值**：实际存的数据

不同语言对这三者的处理不同。C 语言里变量名直接绑定一个固定大小的内存格子，里面装的就是值；Python 里变量名只是一个"标签"，贴在某个对象上，对象才存在内存里。

## 为什么有地址：CPU 只认地址

CPU 访问内存时，**只认地址**——它不知道什么叫 \`age\`、\`name\`，只知道"去 0x7ffeeb40 这个地址取 4 个字节"。变量名是给程序员用的，编译器/解释器负责把名字翻译成地址。

所以从代码到执行，有一层"名字 → 地址"的映射：

\`\`\`text
你写: age + 1
编译器/解释器: 查 age 对应的地址 0x7ffeeb40，从那里取值，加 1
CPU: 从 0x7ffeeb40 取数，做加法
\`\`\`

理解了这一点，你就能理解指针和引用——它们存的就是地址。

## 指针/引用：存的是地址

**指针（C）**和**引用（Python/Java）**本质都是"存地址的变量"。

C 语言的指针很直接：

\`\`\`c
int x = 42;       // 在某地址存了 42
int *p = &x;      // p 是指针，存的是 x 的地址
printf("%d", *p); // 解引用：顺着地址找到值，打印 42
\`\`\`

- \`\`&x\`\` 取 x 的地址
- \`\`*p\`\` 解引用，顺着地址取值
- 指针能加减运算，直接操作内存

Python 把指针藏起来了，叫**引用**。你不用写 \`&\` 和 \`*\`，但底层还是地址在运作：

\`\`\`python
a = [1, 2, 3]   # a 是引用，指向堆上的列表对象
b = a           # b 也是引用，指向同一个列表
b.append(4)
print(a)        # [1, 2, 3, 4] —— a 也变了，因为共用同一个对象
\`\`\`

\`a\` 和 \`b\` 就是指针（引用），指向同一个列表对象。\`id()\` 能看到这个地址（CPython 里 id 就是地址）。

## 编程启发：Python 的引用本质

1. **\`b = a\` 是共享不是拷贝**：可变对象（列表、字典）改一个另一个也变。要拷贝用 \`.copy()\`（浅拷贝）或 \`copy.deepcopy()\`（深拷贝）。

2. **\`is\` vs \`==\`**：\`is\` 比地址（是不是同一个对象），\`==\` 比值（内容是否相等）。判断 \`None\` 用 \`is None\`，判断值用 \`==\`。

3. **小整数缓存**：Python 缓存了 -5 到 256，所有指向 5 的变量其实指向同一个对象（\`id\` 相同）。大整数不缓存，\`id\` 不同。

4. **可变 vs 不可变**：不可变对象（int、str、tuple）改了会创建新对象（\`id\` 变）；可变对象（list、dict）原地改（\`id\` 不变）。这影响函数传参的行为。

## 表格对比：C 指针 vs Python 引用

| 维度 | C 指针 | Python 引用 |
| --- | --- | --- |
| 能否直接操作地址 | 能（&、*） | 不能（隐藏） |
| 指针运算 | 支持（p+1） | 不支持 |
| 内存管理 | 手动（malloc/free） | 自动（GC） |
| 悬空指针 | 有（危险） | 无（GC 保护） |
| 传递大数据 | 传指针高效 | 传引用高效 |

## ⚠️ 常见坑

- **坑 1：以为 \`b = a\` 是拷贝**。Python 里这是引用赋值，可变对象改一个另一个也变。要拷贝用 \`.copy()\` 或 \`deepcopy\`。
- **坑 2：用 \`is\` 比较值**。\`a is b\` 比的是地址不是值，大整数、长字符串可能为 False 即便值相同。比较值用 \`==\`。
- **坑 3：以为 id() 永远不变**。对象销毁后地址可能被新对象复用。别把 id 存下来长期用。

## 小结

- 变量 = 名字 + 地址 + 值。名字给人看，地址给 CPU 用。
- CPU 只认地址，变量名是给程序员用的，编译器/解释器负责名字到地址的映射。
- 指针/引用存的是地址；C 能直接操作，Python 隐藏在引用背后。
- Python 的 \`b = a\` 是共享不是拷贝；\`is\` 比地址，\`==\` 比值。
- 不可变对象改了创建新对象（id 变），可变对象原地改（id 不变）。

## 常见疑问 Q&A

**Q1：Python 里能看到真正的内存地址吗？**
\`id()\` 返回对象的唯一标识，在 CPython 里就是地址。但你不能像 C 那样对地址做运算或直接读写，Python 不允许。所以 Python 有引用的概念但没有"指针操作"。

**Q2：为什么 Python 用引用而不让程序员操作指针？**
安全。C 的指针能访问任意内存，写错了就崩溃或被黑客利用。Python 引用只能指向合法对象，对象由 GC 管理，不会访问到已释放内存。代价是失去了指针的灵活性和极致性能。

**Q3：a += 1 是修改原对象还是创建新对象？**
看类型。整数（不可变）会创建新对象，\`a += 1\` 后 id 变了；列表（可变）会原地修改，\`a += [1]\` 后 id 不变。这就是可变 vs 不可变的区别。`,
    code: `# ===== 第 21 章：变量与地址——名字与位置 =====
# 演示：id()、is vs ==、引用赋值

print("=== 1. 变量 = 名字 + 地址 + 值 ===")
age = 25
print("age = %d" % age)
print("id(age) = %d (对象在内存中的地址)" % id(age))
print("名字(age)好记，地址(id)精确，CPU 只认地址")

print()
print("=== 2. 小整数缓存：id 相同 ===")
a = 5
b = 5
print("a=5, b=5 -> a is b:", a is b, "(小整数-5~256被缓存，同一对象)")

print()
print("=== 3. 大整数不缓存：id 不同 ===")
a = 1000
b = 1000
print("a=1000, b=1000")
print("a is b:", a is b, "(不同对象)")
print("a == b:", a == b, "(但值相等)")

print()
print("=== 4. is vs == ===")
lst1 = [1, 2, 3]
lst2 = [1, 2, 3]
print("lst1=[1,2,3], lst2=[1,2,3]")
print("lst1 is lst2:", lst1 is lst2, "(两个不同对象)")
print("lst1 == lst2:", lst1 == lst2, "(内容相同)")
print("is 比地址，== 比值")

print()
print("=== 5. 引用赋值：共享不是拷贝 ===")
a = [1, 2, 3]
b = a  # 引用赋值
print("a=[1,2,3]; b=a -> a is b:", a is b)
b.append(4)
print("b.append(4) 后 a =", a, "(a也变了!)")

print()
print("=== 6. 可变 vs 不可变 ===")
s = "hello"
print("字符串 s='hello' id =", id(s))
s = s + " world"
print("s += ' world' 后 id =", id(s), "(变了，新对象)")

lst = [1, 2]
print("列表 lst=[1,2] id =", id(lst))
lst.append(3)
print("lst.append(3) 后 id =", id(lst), "(没变，原地修改)")

print()
print("=== 7. 判断 None 用 is ===")
x = None
print("x = None")
print("x is None:", x is None, "(判断None用is，规范)")
print("x == None:", x == None, "(能用但不推荐)")
`,
  },

  // ============================================================
  // 第 22 章：作用域：名字的有效范围
  // ============================================================
  {
    id: "cs-22",
    group: "程序运行原理",
    icon: "🗺️",
    title: "作用域：名字的有效范围",
    content: `## 公司里"张总"是谁

假设你在一家大公司上班。你在财务部，平时说"张总"大家都知道是财务部的张总。但当你去总部开会，说"张总"可能指的是集团副总张总——因为总部那个范围里，"张总"默认指级别更高的那位。

同一个名字"张总"，在不同范围（部门）里指不同的人。如果跨部门沟通不说明白，就会搞混。这就是**作用域**的问题——名字只在一定的范围内有效。

编程里也一样：你在一个函数里定义了 \`x = 10\`，在另一个函数里也定义了 \`x = 20\`，这两个 \`x\` 互不干扰，因为它们的作用域不同。作用域规定了"一个名字在哪些范围内能被访问"，防止名字冲突、隔离各部分的逻辑。

## 局部变量：只在函数里有效

在函数内部定义的变量叫**局部变量**，只在这个函数内有效，函数外访问不到。

\`\`\`python
def func():
    x = 10        # 局部变量
    print(x)      # 函数内能用

func()
print(x)          # 报错！x 在函数外不存在
\`\`\`

为什么这样设计？如果所有变量都全局可见，函数 A 里的 \`i\` 和函数 B 里的 \`i\` 会冲突，写大程序根本没法管理。局部变量让每个函数有自己的"私有空间"，互不干扰。

## 全局变量：到处能用

在所有函数外面定义的变量叫**全局变量**，整个文件都能访问。

\`\`\`python
count = 0   # 全局变量

def add():
    global count   # 要修改全局变量得声明 global
    count += 1

add()
print(count)   # 1
\`\`\`

注意 Python 的规则：**函数内可以读全局变量，但要修改必须声明 \`global\`**。不声明直接改会创建一个同名的局部变量，而不是改全局。

## LEGB 规则：Python 的查找顺序

Python 找一个名字时，按 **LEGB** 顺序逐层查找：

- **L（Local）**：当前函数内的局部变量
- **E（Enclosing）**：外层嵌套函数的变量（闭包用）
- **G（Global）**：模块级的全局变量
- **B（Built-in）**：Python 内置的名字（如 \`print\`、\`len\`）

找到就停，找不到报 \`NameError\`。所以局部变量会"遮蔽"同名的全局变量——内层优先。

\`\`\`python
x = "global"
def outer():
    x = "enclosing"
    def inner():
        x = "local"
        print(x)    # local，最内层优先
    inner()
\`\`\`

## 为什么有作用域：防止冲突、隔离逻辑

1. **防止名字冲突**：不同函数可以用相同的变量名（\`i\`、\`temp\`、\`result\`），互不干扰。没有作用域，几万行代码起名字会崩溃。

2. **隔离逻辑**：函数内部的状态不会泄漏到外部，调试时知道"问题一定在这个函数内"，范围缩小。

3. **内存管理**：局部变量函数返回就销毁，自动释放内存。全局变量活到程序结束，占内存更久。

## 编程启发：避免全局变量、理解闭包

1. **少用全局变量**：全局变量到处能改，出了 bug 很难追踪是谁改的。能用局部就用局部，需要共享用参数传或用类封装。

2. **闭包的原理**：内层函数引用了外层函数的局部变量，外层函数返回后，那些变量因为被引用而"存活"（留在堆上），不会被回收。这就是闭包——函数带着它的定义环境。

\`\`\`python
def make_counter():
    n = 0
    def step():
        nonlocal n   # 改外层函数的 n
        n += 1
        return n
    return step

c = make_counter()
c()  # 1
c()  # 2 —— n 被记住了
\`\`\`

3. **\`nonlocal\` 改外层函数变量**：\`global\` 改全局，\`nonlocal\` 改外层嵌套函数的变量（闭包场景）。

4. **作用域与生命周期**：局部变量函数返回就销毁；闭包捕获的变量活到闭包销毁；全局变量活到程序结束。理解生命周期才能避免"用了一个已销毁的变量"。

## 表格对比：四种作用域

| 作用域 | 在哪定义 | 哪能访问 | 生命周期 |
| --- | --- | --- | --- |
| Local | 函数内 | 函数内 | 函数返回销毁 |
| Enclosing | 外层函数 | 内层函数 | 外层返回(无闭包)销毁 |
| Global | 模块顶层 | 整个模块 | 程序结束 |
| Built-in | Python 内置 | 所有地方 | 永久 |

## ⚠️ 常见坑

- **坑 1：函数内改全局变量忘加 \`global\`**。直接改会创建同名局部变量，全局没变。要在函数内改全局必须 \`global\` 声明。
- **坑 2：闭包延迟绑定**。循环里创建多个闭包引用循环变量，所有闭包拿到的是循环结束后的值。要 \`lambda i=i: ...\` 捕获当前值。
- **坑 3：以为局部变量能影响外部**。局部变量函数外访问不到。要传出结果用 \`return\`，别靠副作用改全局。

## 小结

- 作用域规定名字的有效范围，防止冲突、隔离逻辑。局部变量只在函数内有效。
- Python 查找顺序 LEGB：Local → Enclosing → Global → Built-in，内层优先。
- 全局变量到处能读，函数内改要 \`global\` 声明；改外层函数变量用 \`nonlocal\`。
- 闭包 = 函数 + 定义环境，引用的外层变量因被引用而存活。
- 少用全局变量（难追踪），需要共享用参数或类封装。

## 常见疑问 Q&A

**Q1：为什么 Python 改全局变量要声明 global，读却不用？**
读全局只是查找名字（LEGB 找到 Global 层），不改变什么。但赋值在 Python 里默认是"创建/修改局部变量"，不声明 global 会在函数内创建一个同名局部变量，全局不变。声明 global 明确告诉解释器"我要改的是全局那个"。

**Q2：闭包里的变量为什么不会随函数返回销毁？**
普通局部变量在栈上，函数返回栈帧弹出就销毁。但闭包引用的变量会被 Python"捕获"放到堆上（实现细节），只要闭包还活着，那些变量就不回收。这就是闭包能"记住"状态的原因。

**Q3：全局变量到底有什么不好？**
全局变量任何函数都能改，出了 bug 你不知道是谁改的，调试困难。而且全局变量让函数之间产生隐式依赖（函数行为依赖全局状态），难以测试和复用。所以软件工程原则是"尽量缩小作用域"。`,
    code: `# ===== 第 22 章：作用域——名字的有效范围 =====
# 演示：作用域、global/nonlocal、闭包

print("=== 1. 局部 vs 全局 ===")
message = "我是全局变量"

def show():
    message = "我是局部变量"
    print("  函数内 message:", message)

show()
print("  函数外 message:", message, "(局部不影响全局)")

print()
print("=== 2. 读全局可以，写要 global ===")
count = 0

def read_global():
    print("  读取全局 count =", count)  # 只读可以

def write_global():
    global count  # 要修改得声明 global
    count = count + 1

read_global()
write_global()
print("  write_global 后 count =", count)

print()
print("=== 3. LEGB 查找规则 ===")
print("L(Local) -> E(Enclosing) -> G(Global) -> B(Built-in)")
x = "global"

def outer():
    x = "enclosing"
    def inner():
        x = "local"
        print("  inner 里 x =", x)
    inner()
    print("  outer 里 x =", x)

outer()
print("  最外层 x =", x)

print()
print("=== 4. nonlocal 改外层函数变量 ===")
def make_counter():
    n = 0
    def step():
        nonlocal n  # 改外层函数的 n
        n += 1
        return n
    return step

counter = make_counter()
print("  counter():", counter())
print("  counter():", counter())
print("  counter():", counter(), "(闭包记住了 n)")

print()
print("=== 5. 闭包：函数带着它的环境 ===")
def make_adder(base):
    def adder(x):
        return base + x  # 记住了外层的 base
    return adder

add10 = make_adder(10)
add20 = make_adder(20)
print("  add10(5) =", add10(5))
print("  add20(5) =", add20(5), "(各自记住不同的 base)")

print()
print("=== 6. 避免滥用全局变量 ===")
print("全局变量到处能改，难追踪 bug")
print("原则: 能用局部就用局部，需要共享用参数传或用类")
`,
  },

  // ============================================================
  // 第 23 章：并发与并行：同时做多件事
  // ============================================================
  {
    id: "cs-23",
    group: "程序运行原理",
    icon: "⚡",
    title: "并发与并行：同时做多件事",
    content: `## 一个人边吃饭边看手机 vs 两个人同时干活

想象两个场景：

**场景一**：你一个人吃饭，吃一口饭，刷一下手机，再吃一口饭，再刷一下手机……你其实**没有同时**做两件事，只是快速交替——这叫**并发（Concurrency）**。看起来同时，实际是切换。

**场景二**：你和另一个人一起干活，你吃饭他刷手机，**真正同时**进行——这叫**并行（Parallelism）**。需要多个人（多核 CPU）才能做到。

两者的区别：并发是"同时处理多件事"（交替），并行是"同时做多件事"（真正同时）。单核 CPU 只能并发（快速切换），多核 CPU 才能并行。理解了这个区别，你才能理解为什么多线程有时候快、有时候不快。

## 并发：交替执行，看起来同时

单核 CPU 任何时刻只能做一件事。但它可以快速切换——做一会儿任务 A，切到任务 B 做一会儿，再切回 A……切换速度快到你感觉不到，看起来像"同时"。这就是并发。

并发的好处：**等待时不浪费时间**。比如任务 A 在等网络数据（CPU 空闲），可以切到任务 B 干活，等 A 的数据到了再切回来。这样整体效率高很多。

并发的代价：切换有开销（保存/恢复现场），而且多个任务访问共享数据要加锁，容易出 bug。

## 并行：真正同时，需要多核

多核 CPU（现在手机都 8 核了）能真正同时执行多个任务——核心 1 跑任务 A，核心 2 跑任务 B，物理上同时进行。这就是并行。

并行的好处：**CPU 密集任务能加速**。一个任务要算 10 秒，两个核心并行算只要 5 秒。

并行的限制：任务必须能拆分（有些任务天生串行），且核心数有限（8 核最多 8 倍速）。

## 进程 vs 线程

实现并发/并行有两种基本单位：

**进程（Process）**：一个运行的程序实例。每个进程有独立的内存空间，进程间互不干扰。打开浏览器是一个进程，再打开微信是另一个进程。

- 优点：内存隔离，一个崩了不影响别的
- 缺点：创建开销大，进程间通信麻烦

**线程（Thread）**：进程内的执行单元。一个进程可以有多个线程，共享该进程的内存。

- 优点：创建开销小，共享内存通信方便
- 缺点：共享内存要加锁，一个线程崩可能拖垮整个进程

类比：进程像两家不同的公司（各自独立办公），线程像同一家公司的不同员工（共享办公室，协作方便但容易互相打扰）。

## 编程启发：为什么多线程能提升性能

1. **IO 密集任务多线程有效**：程序大部分时间在等网络、磁盘（CPU 空闲），多线程在等待时切换去干别的，整体快很多。比如下载 10 个网页，串行要 10 秒，10 线程并发约 1 秒。

2. **CPU 密集任务要多进程**：纯计算的任务，多线程在 Python 里因为 GIL 不能真正并行，要多进程（\`multiprocessing\`）才能用上多核。

3. **Python 的 GIL**：CPython 有全局解释器锁（GIL），同一时刻只允许一个线程执行 Python 字节码。所以 Python 多线程不能真正并行 CPU 任务，但 IO 任务（会释放 GIL）仍有效。这是 Python 多线程的最大限制。

4. **共享数据要加锁**：多线程访问同一份数据要加锁（\`threading.Lock\`），否则数据竞争出 bug。加锁要小心死锁（互相等锁）。

## 表格对比：并发 vs 并行，进程 vs 线程

| 维度 | 并发 | 并行 |
| --- | --- | --- |
| 本质 | 交替执行 | 真正同时 |
| 需要 | 单核即可 | 多核 |
| 适合 | IO 密集 | CPU 密集 |

| 维度 | 进程 | 线程 |
| --- | --- | --- |
| 内存 | 独立 | 共享 |
| 开销 | 大 | 小 |
| 通信 | 麻烦(管道/队列) | 方便(直接访问) |
| 安全 | 隔离，一个崩不影响 | 共享，一个崩拖垮全部 |

## ⚠️ 常见坑

- **坑 1：以为多线程一定快**。CPU 密集任务在 Python 里因 GIL 多线程不快，要多进程。IO 密集任务多线程才有效。
- **坑 2：共享数据不加锁**。多线程同时改一个变量会数据竞争，结果不可预测。必须加锁。
- **坑 3：以为核数越多越快**。任务不能无限拆分，且有线程切换、锁竞争开销。8 核跑到 4-6 倍就不错了，不是线性提升。

## 小结

- 并发是交替执行（看起来同时，单核可做），并行是真正同时（需多核）。
- 进程内存独立（安全但通信麻烦），线程共享内存（高效但要加锁）。
- IO 密集任务多线程有效（等待时切换），CPU 密集任务要多进程（绕过 GIL 用多核）。
- Python 有 GIL，多线程不能真正并行 CPU 任务，但 IO 任务仍有效。
- 多线程访问共享数据要加锁，否则数据竞争。

## 常见疑问 Q&A

**Q1：并发和并行能同时存在吗？**
能。一个 8 核 CPU 上跑 100 个线程，既有并行（8 个核心同时跑 8 个线程）又有并发（100 个线程在这 8 个核心上切换）。实际系统都是"多核 + 多线程"混合。

**Q2：为什么 Python 有 GIL？**
CPython 的内存管理（引用计数）不是线程安全的，加 GIL 是最简单的保护方式。去掉 GIL 要重写内存管理，难度大且可能降低单线程性能。PyPy、Jython 没有 GIL。Python 3.13 在试验移除 GIL（no-GIL 模式）。

**Q3：协程是什么，和线程什么区别？**
协程是用户态的轻量级"线程"，由程序自己调度（不是操作系统）。一个线程里可以跑成千上万个协程，切换开销极小。Python 的 \`asyncio\` 就是协程。协程适合 IO 密集场景（如高并发网络服务），但不能利用多核（要并行还是得多进程）。`,
    code: `# ===== 第 23 章：并发与并行——同时做多件事 =====
# 演示：串行 vs 并发的时间差异

import time
import threading

print("=== 1. 并发 vs 并行 ===")
print("并发: 一个人边吃饭边看手机，交替进行(看起来同时)")
print("并行: 两个人同时干活，真正同时(需要多核)")

print()
print("=== 2. 串行执行：一件做完再做下一件 ===")
def task(name, seconds):
    print("  开始 %s..." % name)
    time.sleep(seconds)
    print("  完成 %s" % name)

start = time.time()
task("烧水", 1)
task("切菜", 1)
serial_time = time.time() - start
print("串行耗时: %.2f 秒" % serial_time)

print()
print("=== 3. 并发执行：交替进行 ===")
start = time.time()
t1 = threading.Thread(target=task, args=("烧水", 1))
t2 = threading.Thread(target=task, args=("切菜", 1))
t1.start()
t2.start()
t1.join()
t2.join()
concurrent_time = time.time() - start
print("并发耗时: %.2f 秒" % concurrent_time)

print()
print("=== 4. 时间对比 ===")
print("串行: %.2f 秒 (一件做完再做下一件)" % serial_time)
print("并发: %.2f 秒 (等待时切换做别的)" % concurrent_time)
print("-> 等待类任务(IO)并发能省时间")

print()
print("=== 5. 进程 vs 线程 ===")
print("进程: 独立的程序实例，内存隔离，开销大")
print("线程: 进程内的执行单元，共享内存，开销小")
print("例子: 浏览器开多个标签页=多进程")
print("      一个标签页里同时下载图片=多线程")

print()
print("=== 6. Python 的 GIL ===")
print("GIL(全局解释器锁): 同一时刻只允许一个线程执行 Python 字节码")
print("所以 Python 多线程不能真正并行 CPU 密集任务")
print("但 IO 任务(网络/文件)会释放 GIL，多线程仍有效")
print("要真正并行 CPU 任务: 用 multiprocessing 多进程")
`,
  },

  // ============================================================
  // 第 24 章：IO 与程序：等待的艺术
  // ============================================================
  {
    id: "cs-24",
    group: "程序运行原理",
    icon: "🌊",
    title: "IO 与程序：等待的艺术",
    content: `## 烧水时你可以切菜

你准备做一顿饭，要烧水、切菜、煮饭。如果**同步**做：先烧水，盯着水壶等它开（5 分钟），再切菜（3 分钟），再煮饭（20 分钟）——总共 28 分钟，其中烧水的 5 分钟你啥也没干就干等。

但聪明人会**异步**做：先把水壶放上烧水，然后**不等它**，立刻去切菜；切完菜水可能还没开，再去淘米煮饭。水开了水壶会"叫"通知你，你再去处理。这样三件事的时间重叠了，可能 22 分钟就搞定。

这就是**异步 IO** 的思想——等待时不干等，去做别的事，等的事情完成了再回来处理。

程序里大量时间花在"等"上：等磁盘读文件、等网络传数据、等数据库返回结果。CPU 比磁盘快几万倍，等磁盘时 CPU 空闲太浪费。异步 IO 让程序在等待时去干别的，大幅提升效率。

## 什么是 IO

**IO = Input/Output（输入输出）**，指程序和外部的数据交换：

- **读文件、写文件**：磁盘 IO
- **网络请求**：网络 IO（爬虫、API 调用）
- **数据库查询**：数据库 IO
- **键盘输入、屏幕输出**：标准 IO

IO 的共同特点：**慢，要等**。CPU 执行一条指令纳秒级，磁盘读一次毫秒级，网络请求可能几百毫秒。这中间 CPU 如果干等，就白白浪费了几百万条指令的时间。

## 阻塞 vs 非阻塞

**阻塞（Blocking）**：程序发起 IO 后，**停下来等**，直到 IO 完成。等的时候这个线程啥也干不了。

\`\`\`python
data = request("http://example.com")  # 阻塞等 0.5 秒
print(data)  # 0.5 秒后才执行
\`\`\`

**非阻塞（Non-blocking）**：程序发起 IO 后**不等待**，立刻返回（可能数据还没好），程序继续干别的，过一会儿再来看看好了没。

阻塞写起来简单（一行代码等到底），但浪费 CPU；非阻塞效率高，但写起来复杂（要轮询检查）。

## 同步 vs 异步

**同步（Synchronous）**：调用后**等结果**，结果回来了才继续。\`requests.get()\` 是同步——调一行卡住等响应。

**异步（Asynchronous）**：调用后**不等结果**，立刻返回去做别的，结果好了通过**回调**或**事件通知**你。\`asyncio\` 的 \`await\` 是异步——发起后让出 CPU，结果好了再回来。

异步的本质：**等待时不占着 CPU，让别的任务用**。这样单个线程就能同时处理很多 IO 任务（高并发）。

## 编程启发：为什么有 async/await

1. **回调地狱**：早期的异步用回调函数——IO 完了调用一个函数。但多层嵌套的回调让代码极难读（"回调地狱"）。

2. **async/await 的优雅**：\`async/await\` 让异步代码**看起来像同步**——顺序写、好读，但底层是异步的。\`await\` 处会"暂停"当前函数，让出 CPU 给别的任务，结果好了再恢复。

3. **IO 密集用异步**：高并发的网络服务（Web 服务器、爬虫、聊天程序）大部分时间在等网络，用异步单线程就能处理成千上万个连接。CPU 密集任务用异步没用（没在等 IO），要多进程。

4. **同步简单异步复杂**：简单脚本用同步（\`requests\`）够用，好写好调试。高并发场景才值得上异步（\`aiohttp\` + \`asyncio\`），但要小心整个调用链都要异步，混用会阻塞。

## 表格对比：同步 vs 异步，阻塞 vs 非阻塞

| 维度 | 同步 | 异步 |
| --- | --- | --- |
| 等不等结果 | 等 | 不等，好了通知 |
| 写法 | 简单 | 复杂(回调/async) |
| 效率 | 低(干等) | 高(等待时干别的) |
| 适合 | 简单脚本 | 高并发服务 |

| 维度 | 阻塞 | 非阻塞 |
| --- | --- | --- |
| 调用后 | 停下等 | 立刻返回 |
| CPU | 空闲浪费 | 可做别的 |
| 数据没好时 | 卡住 | 返回"没好"，稍后再问 |

## ⚠️ 常见坑

- **坑 1：异步代码里用同步调用**。\`async def\` 里调 \`time.sleep(1)\` 或 \`requests.get()\` 会阻塞整个事件循环，异步白搭。要用 \`asyncio.sleep\` 和 \`aiohttp\`。
- **坑 2：以为异步一定快**。异步只对 IO 密集有效，CPU 密集任务异步没用（没在等 IO），要多进程。
- **坑 3：异步代码难调试**。异步的执行顺序不是代码书写顺序，报错栈也不直观。初学者先用同步，需要高并发再上异步。

## 小结

- IO 是输入输出（文件、网络、数据库），特点是慢、要等。
- 阻塞 = 等 IO 时卡住，非阻塞 = 不等立刻返回。
- 同步 = 等结果，异步 = 不等好了通知（async/await）。
- 异步的本质：等待时不占 CPU，让别的任务用，单线程高并发。
- IO 密集用异步（Web 服务/爬虫），CPU 密集用多进程，简单脚本用同步。

## 常见疑问 Q&A

**Q1：异步和多线程什么区别？**
多线程靠操作系统切换线程（每个线程占内存），线程多了开销大。异步单线程靠事件循环切换任务（协程），开销极小，能处理成千上万个连接。但异步不能利用多核（要并行还是多进程）。高并发 IO 用异步，要并行计算用多进程。

**Q2：为什么 \`await\` 后面只能跟协程？**
\`await\` 是"暂停当前协程，等这个异步操作完成"。只有协程（async 函数）才能被 await——它们知道怎么让出 CPU。普通同步函数不会让出 CPU，await 它没意义还会阻塞。

**Q3：Node.js 为什么天生异步？**
Node.js 设计初衷就是高并发网络服务，所以 IO 全异步（单线程事件循环）。Python 的 \`asyncio\` 是后来加的，所以标准库里有同步和异步两套 IO，容易混用。Go 用 goroutine + channel，写起来像同步但底层是异步调度，算是另一种解法。`,
    code: `# ===== 第 24 章：IO 与程序——等待的艺术 =====
# 演示：同步 vs 异步的时间差异

import time
import asyncio

print("=== 1. 什么是 IO ===")
print("IO = 输入输出: 读写文件、网络请求、数据库查询")
print("IO 的特点: 慢，要等(CPU 比磁盘快几万倍)")

print()
print("=== 2. 同步: 等完一个再下一个 ===")
def io_task(name, seconds):
    print("  开始 %s..." % name)
    time.sleep(seconds)  # 模拟 IO 等待
    print("  完成 %s" % name)

start = time.time()
io_task("下载图片", 1)
io_task("下载音乐", 1)
sync_time = time.time() - start
print("同步耗时: %.2f 秒" % sync_time)

print()
print("=== 3. 异步: 等待时去做别的 ===")
async def aio_task(name, seconds):
    print("  开始 %s..." % name)
    await asyncio.sleep(seconds)  # 非阻塞等待
    print("  完成 %s" % name)

async def main():
    start = time.time()
    await asyncio.gather(
        aio_task("下载图片", 1),
        aio_task("下载音乐", 1),
    )
    async_time = time.time() - start
    print("异步耗时: %.2f 秒" % async_time)
    return async_time

async_time = asyncio.run(main())

print()
print("=== 4. 时间对比 ===")
print("同步: %.2f 秒 (干等，一个完再下一个)" % sync_time)
print("异步: %.2f 秒 (等待时切换做别的)" % async_time)
print("-> IO 密集任务用异步能大幅省时间")

print()
print("=== 5. 阻塞 vs 非阻塞 ===")
print("阻塞: 等 IO 时整个程序卡住，啥也干不了")
print("非阻塞: 等 IO 时程序继续干别的，IO 完了再回来")
print("async/await 就是写非阻塞代码的语法")

print()
print("=== 6. 为什么有回调、async/await ===")
print("回调: IO 完了调用一个函数，但层层嵌套难读(回调地狱)")
print("async/await: 看起来像同步，实际是异步，好读好用")
print("适合: 网络爬虫、Web 服务器、聊天程序等 IO 密集场景")
`,
  },
];
