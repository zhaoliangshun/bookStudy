// =============================================================
// Batch 4：程序执行（4 章）
// =============================================================

export const chapters = [
  // ============================================================
  // 第 1 章：编译与链接
  // ============================================================
  {
    id: "comp-compile-link",
    group: "程序执行",
    icon: "🔨",
    title: "编译与链接",
    content: `
## 概述
C 源代码变成可执行文件并非一步到位，而是经过 预处理 → 编译 → 汇编 → 链接 四个阶段。每个阶段有独立的工具和产物，理解它们能帮你读懂编译报错、排查链接失败、合理选择静态库与动态库。本章用「翻译一本书」的比喻串联全流程，并用 gcc 的 -E/-S/-c 选项逐一演示每个阶段的产物，最后解释为什么会出现经典的 undefined reference 错误。

## 核心要点
- **预处理（Preprocess）**: \`gcc -E\` 处理所有以 \`#\` 开头的指令——展开 \`#include\`（把头文件内容原样粘贴进来）、替换 \`#define\` 宏、删除注释、执行 \`#if/#ifdef/#endif\` 条件编译。产物仍是合法的 C 代码，但体积可能膨胀几十倍（一个 \`#include <stdio.h>\` 就展开上千行）。
- **编译（Compile）**: \`gcc -S\` 把预处理后的 C 翻译成汇编语言，生成 \`.s\` 文件。这一步做词法分析、语法分析、语义检查、中间代码生成与优化，最终输出人类可读的汇编指令。
- **汇编（Assemble）**: \`gcc -c\` 把汇编代码翻译成机器码，生成目标文件（\`.o\`）。目标文件是二进制，但还不能运行——其中外部函数（如 printf）的地址还是占位符 0，等待链接器填入真实地址。
- **链接（Link）**: \`gcc\`（不带 -E/-S/-c）把一个或多个 \`.o\` 与 C 运行库（crt0、libc 等）合并，完成「符号解析」和「重定位」，输出最终可执行文件。
- **符号解析（Symbol Resolution）**: 链接器扫描所有 \`.o\` 的符号表，把「引用」（如调用了 printf）与「定义」（printf 的实现在 libc）一一配对。配不上就报 \`undefined reference\`。
- **重定位（Relocation）**: 把每个符号的最终内存地址回填到调用处。\`.o\` 中调用 printf 的位置最初只是占位符，链接后才填入真实地址——这就是「重定位」的字面含义。
- **静态库（.a）**: 把多个 \`.o\` 打包成一个归档文件，链接时把被引用的 \`.o\` 整个复制进可执行文件——产物大但独立，不依赖外部库。
- **动态库（.so / .dylib）**: 链接时只在可执行文件里记录「需要哪个动态库的哪个符号」，运行时由动态链接器（ld-linux / dyld）加载并绑定地址。产物小但依赖运行环境。
- **undefined reference**: 链接阶段错误，意思是「你用了某个符号但没提供它的定义」。常见原因：忘了 \`-l\` 选项、函数名拼写错误、头文件声明了但没实现、库版本不匹配。
- **查看动态依赖**: Linux 用 \`ldd ./prog\` 查看程序运行时依赖哪些动态库；macOS 用 \`otool -L ./prog\` 达到同样目的。
- **头文件 vs 库文件**: 头文件（\`.h\`）告诉编译器「有这个函数」供编译阶段检查类型；库文件（\`.a\`/\`.so\`）提供函数的实际机器码供链接阶段使用。只 include 头文件不链接库 → 编译通过但链接报错。
- **编译优化级别**: \`-O0\` 不优化（调试用）、\`-O1\` 基础优化、\`-O2\` 标准优化（发布默认）、\`-O3\` 激进优化（可能增大体积）、\`-Os\` 优化体积。优化级别影响代码质量但可能改变调试信息准确性。
- **编译器前端 vs 后端**: 前端做词法/语法分析生成中间表示（IR），后端把 IR 优化并生成目标机器码。Clang/LLVM 是多阶段架构——前端生成 LLVM IR，后端针对不同 CPU 生成机器码，所以能支持多种语言和多种目标平台。
- **交叉编译**: 在一个平台上编译另一个平台运行的程序——如 x86 上编译 ARM 的二进制。需指定 \`--target\` 或用专门的交叉编译工具链（如 arm-linux-gnueabihf-gcc）。Docker 多阶段构建常用于交叉编译。
- **增量编译**: 只重新编译修改过的源文件及其依赖，未改动的 \`.o\` 直接复用。Make/CMake 通过比较文件时间戳实现增量编译——大幅缩短大型项目的构建时间，但头文件修改会触发级联重编译。
- **编译期 vs 运行期**: 编译期是编译器把源码翻译成机器码的阶段（语法检查、类型检查、常量折叠、宏展开）；运行期是程序实际执行的阶段（变量赋值、函数调用、I/O）。编译期错误（语法错误）一定无法运行，运行期错误（除零、空指针）编译时无法发现。
- **GCC vs Clang/LLVM**: GCC 是老牌开源编译器，单体架构；Clang 是 LLVM 项目的前端，模块化、错误信息更友好、编译速度更快。macOS 默认用 Clang（命令名仍叫 gcc/clang），Linux 两者都常见。LLVM 的 IR 设计让多语言多平台复用后端成为可能。
- **PIE（位置无关可执行文件）**: \`-fPIE -pie\` 让可执行文件的代码段也能被加载到任意地址（配合 ASLR 地址空间随机化），提升安全性——攻击者难以预测关键函数地址。现代发行版默认开启 PIE。
- **STRIP 去符号**: \`strip\` 删除可执行文件中的符号表和调试信息，体积可减小 30~70%。但 crash 后 backtrace 失去函数名信息——发布版保留未 strip 版本用于线上 crash 排查（用 \`addr2line\` 还原行号）。
- **-MMD -MP 自动依赖**: 让编译器在生成 \`.o\` 的同时输出 \`.d\` 文件（记录该源文件依赖哪些头文件），Make 读取 \`.d\` 即可精确知道改了哪个头文件要重编哪些源文件——避免漏重编导致的诡异 bug。

## 原理与机制
- **翻译一本书的比喻**: 预处理 = 先整理批注和引用清单（展开 #include 和 #define）；编译 = 把正文翻译成目标语言（C → 汇编）；汇编 = 把译文排版成印刷稿（汇编 → 机器码）；链接 = 把多个章节合并并补上交叉引用页码（符号解析 + 重定位）。每阶段独立工作，前一步产物是后一步输入。
- **静态链接 vs 动态链接的取舍**: 静态链接像把引用的内容直接抄进书里——独立但笨重，升级库要重新编译全书；动态链接像附一张「需要时去某书架查阅」的便条——轻便但依赖书架（运行环境），书架上的书换了你就自动读新版。静态链接省心但体积大、升级难；动态链接省空间、易升级但部署时要确保环境一致。
- **目标文件结构（ELF / Mach-O）**: \`.o\` 文件不是纯机器码，而是分段存储的——\`.text\` 段放代码、\`.data\` 放已初始化全局变量、\`.bss\` 放未初始化全局变量、\`.symtab\` 放符号表、\`.rel.text\` 放重定位表。链接器正是靠符号表和重定位表完成跨文件拼接的。
- **动态链接的延迟绑定（PLT/GOT）**: 现代动态链接默认用「延迟绑定」——函数第一次被调用时才去动态库查地址并填入 GOT 表（全局偏移表），后续直接跳转。这样程序启动更快（不解析用不到的符号），代价是第一次调用稍慢。
- **链接顺序很重要**: \`gcc main.o -lfoo -lbar\` 中，如果 main 依赖 foo、foo 依赖 bar，顺序必须是「调用者在前、被依赖者在后」。写反了链接器在处理 main 时还没看到 foo 的定义，就会报 undefined reference——这是静态链接的固有限制。
- **为什么编译能过但链接报错**: 编译器只看当前编译单元（一个 .c 文件 + 它 include 的头文件），头文件里有函数声明就够了；链接器要看全局，必须找到每个被调用函数的定义。所以「声明在头文件、实现在库文件」的模式下，漏链接库必然编译通过但链接失败。
- **运行时库初始化**: 可执行文件入口不是 main 而是 \`_start\`（由 crt0 提供）——它设置运行环境、调用全局构造函数、调用 main、退出时调用析构函数。链接时除了你的 .o 还要链接 crt0.o、crti.o、crtn.o 等启动文件。
- **链接脚本（Linker Script）**: 链接器按脚本决定各段在内存中的布局地址。默认脚本由编译器提供，嵌入式开发常需自定义链接脚本控制内存映射——如把代码放 Flash、变量放 RAM。
- **LTO（链接时优化）**: \`-flto\` 让链接器跨编译单元做全局优化（内联、死代码消除），比单文件优化效果更好但链接变慢。现代项目发布版常用 LTO 获得额外 5~10% 性能提升。
- **链接器的两遍扫描**: 第一遍收集所有 \`.o\` 的符号定义表和未解析引用表；第二遍把未解析引用与定义配对，从静态库中拉取需要的 \`.o\`。这就是为什么静态库必须放在调用者之后——第二遍扫描时才知道需要哪些 \`.o\`。
- **弱符号（weak symbol）**: 用 \`__attribute__((weak))\` 声明的符号允许被同名强符号覆盖——链接器优先选强符号。常用于：可选功能（没实现就用弱符号的默认版本）、库的钩子函数、跨平台兼容（不同平台提供不同强实现）。
- **地址空间布局随机化（ASLR）**: 每次运行时随机化栈、堆、动态库、可执行文件的加载地址——攻击者无法预测关键代码/数据地址，缓冲区溢出攻击难度大增。PIE（位置无关可执行文件）让可执行文件本身也能随机加载。
- **静态链接的 -static 选项**: \`gcc -static\` 强制全部静态链接（包括 libc），产物完全独立。但 glibc 静态链接有诸多限制（NSS、DNS 等运行时动态加载功能会异常），musl libc 静态链接则无此问题——Alpine 镜像就靠 musl 实现真正的静态链接友好。

## 易错点与陷阱
- **陷阱**: \`undefined reference to 'foo'\` 不是编译错误而是链接错误——编译器只管语法和声明（头文件），链接器才找定义。看到这个错误先检查：是否漏了 \`-l\` 选项、库名是否正确、库路径是否在 \`-L\` 搜索范围内、链接顺序是否正确。
- **陷阱**: C++ 的符号会做 name mangling（名字改写），用 C 的方式调用 C++ 函数会报 undefined reference——C++ 函数需用 \`extern "C"\` 导出供 C 调用，头文件要加 \`extern "C"\` 包裹防止名字改写。
- **陷阱**: 静态链接看似「独立」但 glibc 等系统库不建议静态链接——glibc 依赖运行时动态加载（如 NSS 名称服务、DNS 解析），静态链接后可能出现运行时 "Segmentation fault" 或功能异常（如 gethostbyname 失败）。
- **陷阱**: 动态库版本不匹配会导致运行时报 \"version GLIBC_2.28 not found\"——在低版本系统编译、在高版本系统运行一般没问题，反之则可能因缺少新符号而崩溃。Docker 镜像用低版本基础镜像构建可提高兼容性。
- **陷阱**: \`gcc -c\` 默认生成与源文件同名的 \`.o\`（hello.c → hello.o），多个目录下同名文件合并时会互相覆盖——用 \`-o\` 显式指定输出名避免冲突。
- **陷阱**: 头文件循环依赖导致 \"redefinition\" 或 \"unknown type name\"——用 \`#ifndef / #define / #endif\` 头文件保护宏（include guard）避免重复包含，或用 \`#pragma once\`（主流编译器都支持）。

## 实战建议
- **建议**: 遇到链接错误时按「三步排查法」：1) \`nm foo.o\` 查看自己定义了哪些符号；2) \`nm libbar.a\` 或 \`nm -D libbar.so\` 查看库提供了哪些符号；3) 确认 \`-l\` 选项在 \`-L\` 路径下能找到库，且链接顺序是「调用者在前、被依赖者在后」。
- **建议**: 发布命令行工具优先静态链接（方便用户即下即用，不依赖运行环境）；发布长期运行的服务用动态链接（省内存、易升级安全补丁，多个进程共享一份库代码）。
- **建议**: 编译时养成 \`-Wall -Wextra -g\` 习惯——\`-Wall\` 捕获常见警告、\`-Wextra\` 更严格、\`-g\` 保留调试信息供 gdb/lldb 调试。发布版加 \`-O2\` 优化但保留 \`-g\`（可用于线上 crash 排查）。
- **建议**: 大型项目用 CMake/Make 管理编译流程，不要手写 gcc 命令——构建系统自动处理头文件依赖追踪和增量编译，避免「改了头文件但没重新编译」导致的诡异 bug。
- **建议**: 用 \`ldd\`（Linux）或 \`otool -L\`（macOS）在部署前检查可执行文件的动态库依赖，确保目标机器上所有依赖库都存在且版本匹配——避免「在我机器上能跑」的经典问题。
`,
    code: `# ===== 编译与链接四阶段演示 =====
# 用 gcc 的 -E/-S/-c 选项分别产出 预处理/汇编/目标文件，
# 最后链接成可执行文件；并查看动态库依赖、演示 undefined reference

CC=gcc
command -v "$CC" >/dev/null 2>&1 || CC=clang
echo "使用编译器: $CC"

WORKDIR=/tmp/compile_demo
rm -rf "$WORKDIR"; mkdir -p "$WORKDIR"
cd "$WORKDIR" || exit 1

# --- 准备示例 C 源文件 ---
cat > hello.c <<'CEOF'
#include <stdio.h>
#define GREETING "Hello"
int main(void) {
    printf("%s, 编译链接!\\n", GREETING);
    return 0;
}
CEOF

echo ""
echo "===== 阶段 1：预处理 ($CC -E) ====="
echo "展开 #include 和 #define，产物仍是 C 代码"
$CC -E hello.c 2>/dev/null | tail -n 4
echo "...（stdio.h 展开后有数百行，这里只看末尾几行）"

echo ""
echo "===== 阶段 2：编译 ($CC -S) ====="
echo "C 代码 → 汇编语言，生成 hello.s"
$CC -S -o hello.s hello.c
head -n 6 hello.s
echo "...（后面是 main 函数的汇编实现）"

echo ""
echo "===== 阶段 3：汇编 ($CC -c) ====="
echo "汇编 → 机器码，生成 hello.o（二进制不可读）"
$CC -c -o hello.o hello.c
file hello.o
echo "用 nm 查看 .o 中的符号（_main 是定义，_printf 是未解析引用）："
nm hello.o 2>/dev/null | head -n 5

echo ""
echo "===== 阶段 4：链接（$CC，不带 -E/-S/-c）====="
echo "合并 .o 与 C 运行库，补上 printf 的真实地址"
$CC -o hello hello.o
echo "运行结果: $(./hello)"

echo ""
echo "===== 动态库依赖 ====="
if command -v ldd >/dev/null 2>&1; then
  ldd ./hello | head -n 4
else
  echo "（macOS 用 otool -L 查看动态库依赖）"
  otool -L ./hello 2>/dev/null | head -n 4
fi

echo ""
echo "===== 附加：undefined reference 错误演示 ====="
cat > undef.c <<'CEOF'
extern void missing_func(void);
int main(void) { missing_func(); return 0; }
CEOF
echo "1) 编译阶段（-c）成功——符号未定义只留占位："
$CC -c -o undef.o undef.c && echo "   -> 编译 OK，undef.o 已生成"
echo "2) 链接阶段失败——找不到 missing_func 的定义："
$CC -o undef undef.o 2>&1 | head -n 2
echo "   ^ 这就是经典的 undefined reference 错误"
`,
  },

  // ============================================================
  // 第 2 章：进程的诞生与生命周期
  // ============================================================
  {
    id: "comp-process-lifecycle",
    group: "程序执行",
    icon: "🐣",
    title: "进程的诞生与生命周期",
    content: `
## 概述
程序躺在硬盘上只是一堆字节，被操作系统加载进内存、分配了执行上下文后才成为「进程」。本章讲解进程如何通过 fork/exec 诞生、经历哪些状态变迁、最终如何消亡，以及僵尸进程和孤儿进程的成因与处理。理解进程生命周期是编写守护进程、管理子进程、排查服务异常退出的基础。

## 核心要点
- **程序 vs 进程**: 程序是磁盘上的可执行文件（静态的指令与数据）；进程是程序运行时的实例（动态的，包含内存空间、寄存器、打开的文件等执行上下文）。一个程序可以同时有多个进程实例。
- **fork（创建子进程）**: \`fork()\` 创建当前进程的副本——子进程获得父进程内存空间、文件描述符的拷贝（写时复制 Copy-On-Write）。fork 在父进程返回子进程 PID，在子进程返回 0，失败返回 -1。通过返回值区分父子执行路径。
- **exec（替换程序映像）**: \`exec\` 系列函数（execl/execlp/execv/execvp）把当前进程的代码和数据替换为新程序，但 PID 不变、文件描述符大部分保留。fork 产生副本，exec 让副本「换灵魂」变成新程序——二者配合是 Unix 启动新程序的标准模式。
- **wait（回收子进程）**: \`wait()\`/\`waitpid()\` 让父进程等待子进程结束并获取退出状态。子进程退出后，内核保留其退出信息（PID、退出码、资源使用量）直到父进程调用 wait 取走——这之前子进程就是「僵尸」。
- **进程状态**: \`R\` 运行/就绪、\`S\` 可中断睡眠（等待事件）、\`D\` 不可中断睡眠（通常在等磁盘 I/O）、\`Z\` 僵尸（已退出但未被回收）、\`T\` 停止（被信号暂停，如 Ctrl+Z）。\`ps -o stat\` 可查看。
- **僵尸进程（Zombie）**: 子进程已退出但父进程还没调用 wait 回收，此时子进程的退出信息仍占着进程表条目。大量僵尸会耗尽 PID 资源导致无法 fork 新进程。
- **孤儿进程（Orphan）**: 父进程先于子进程退出，子进程被 init（PID=1，macOS 为 launchd）收养。孤儿进程不是问题——init 会自动 wait 回收它们，不会变僵尸。
- **进程控制块（PCB）**: 内核为每个进程维护一个 task_struct（Linux）/ proc 结构（macOS），记录 PID、状态、寄存器、内存映射、打开的文件、调度优先级、信号处理表等——这是进程存在的「身份证」。
- **CFS 调度器**: Linux 默认用完全公平调度器（CFS），用红黑树按虚拟运行时间（vruntime）排序，每次选 vruntime 最小的进程运行。nice 值（-20~19）影响时间片权重，值越低优先级越高。
- **退出流程**: 进程调用 \`exit()\` 或从 main 返回 → 刷新 stdio 缓冲 → 关闭文件描述符 → 释放内存 → 内核保留退出状态和 PID → 进入 Z 状态 → 等父进程 wait 后彻底销毁。
- **进程组与会话**: 进程组是一组相关进程的集合（如管道连接的命令），会话是更大的单位（如一个终端登录会话）。\`setsid()\` 创建新会话脱离 controlling terminal——守护进程的必备操作。
- **环境变量继承**: 子进程继承父进程的环境变量（environ），\`exec\` 后环境变量默认保留。\`putenv\`/\`setenv\` 修改的是当前进程的环境，不影响父进程——这是配置子进程行为的常用方式。
- **文件描述符继承**: fork 后子进程继承父进程的文件描述符表（指向同一文件表项）。不用的 fd 应及时关闭避免泄漏；\`FD_CLOEXEC\` 标志让 fd 在 exec 时自动关闭，避免泄漏给新程序。
- **nice 与优先级**: \`nice\` 值范围 -20~19，值越低优先级越高（只有 root 能设负值）。\`renice\` 调整已运行进程的优先级。CFS 调度器用 nice 值计算权重，影响时间片分配比例。
- **进程的内存布局**: 进程地址空间分段——\`.text\` 代码段（只读）、\`.data\` 已初始化全局变量、\`.bss\` 未初始化全局变量、堆（向上增长，malloc 分配）、内存映射区（mmap/动态库）、栈（向下增长，局部变量、函数调用帧）。每个段权限不同。
- **进程间通信（IPC）**: 管道（pipe，父子进程间）、命名管道（FIFO，任意进程间）、消息队列、共享内存（shmget/mmap，最快但需同步）、信号量、socket（跨机器通用）。共享内存最快但必须配合同步原语避免竞态。
- **进程上下文**: 进程运行所需的全部状态——CPU 寄存器（PC、SP、通用寄存器）、内存映射（页表）、打开的文件描述符表、信号处理表、当前工作目录、umask。fork 时这些大部分被复制，exec 时大部分被重置。
- **prctl / PR_SET_PDEATHSIG**: Linux 特有，让子进程在父进程退出时自动收到指定信号——避免子进程变孤儿后无监管运行。macOS 无此接口，需用 kqueue 监听父进程 fd 或周期性调用 \`getppid() == 1\` 检测。
- **daemon 守护进程**: 长期运行在后台的进程，无控制终端。标准创建流程：fork → 父进程 exit → setsid（脱离原会话）→ fork（防止重新获取终端）→ chdir("/") → umask(0) → 关闭继承的 fd → 进入工作循环。

## 原理与机制
- **细胞分裂的比喻**: fork 像细胞分裂——产生一个一模一样的副本（内存、文件描述符都拷贝），但分裂后两者独立运行，通过 fork 的返回值区分「原来的」和「新生的」。exec 像副本换灵魂——身体（PID、文件描述符）不变，但灵魂（代码和数据）换成了新程序。
- **Copy-On-Write（写时复制）**: fork 时并不真的复制整个内存空间，而是让父子共享同一份物理内存页，标记为只读。任一方尝试写入时，内核才复制那一页——这大幅减少了 fork 的开销。所以 fork 后立即 exec 是最高效的（根本不会触发复制）。
- **僵尸进程的比喻**: 像孩子死了但父母不来收尸——内核保留了孩子的「死亡证明」（退出码和资源统计），占着一个进程表条目。父母（父进程）必须来领（调用 wait）才能释放。父母不管，孩子就一直是僵尸，占着坑不拉屎。
- **为什么孤儿不是问题**: 孤儿的父进程先死了，内核会把孤儿重新指定给 init（PID=1）当养父。init 是个尽责的家长，会定期 wait 回收收养的孤儿——所以孤儿不会变僵尸。这就是为什么推荐「守护进程 fork 后父进程立即退出」让 init 收养。
- **进程状态转换图**: R↔S（时间片用完→睡眠，事件到来→就绪）、S→D（进入不可中断的磁盘 I/O）、R→Z（执行完毕退出）、Z→销毁（父进程 wait 后）。T 是被 SIGSTOP 暂停，可被 SIGCONT 恢复到 R/S。
- **信号与进程**: \`SIGCHLD\` 是子进程退出时内核发给父进程的信号。默认被忽略，但可以注册处理函数在其中调用 waitpid 异步回收子进程——这是守护进程避免僵尸的常用模式。
- **vfork vs fork**: \`vfork\` 创建子进程但不复制内存——子进程共享父进程地址空间，必须立即 exec 或 _exit（不能修改父进程数据）。现代 fork 的写时复制已很快，vfork 基本被淘汰。
- **Copy-On-Write 的实现**: fork 时内核把父子进程的所有页表项标记为只读，物理页不变。任一方写入时触发缺页异常，内核分配新物理页、复制内容、修改页表项为可写——按需复制，延迟开销。
- **进程资源限制**: \`ulimit\`/\`setrlimit\` 设置进程的资源上限——最大文件数、最大内存、CPU 时间、core dump 大小等。容器（Docker）的 cgroups 在此基础上做更精细的资源隔离和限制。

## 易错点与陷阱
- **陷阱**: fork 后不 wait 导致僵尸进程堆积——长期运行的服务（如 Web 服务器 fork 子进程处理请求）必须回收每个子进程，否则僵尸会逐渐耗尽 PID 资源（系统 PID 上限通常几万）。
- **陷阱**: fork 返回值不检查就使用——fork 可能失败（如进程数超限、内存不足），返回 -1。不检查就当成功用会导致父进程错误地执行子进程逻辑。务必检查 \`pid < 0\` 分支。
- **陷阱**: exec 失败不处理——exec 只在失败时返回（成功就不会返回了，因为代码已被替换）。fork 的子进程里 exec 失败后如果没 \`exit\`，会继续执行父进程的代码逻辑，产生混乱。exec 后紧跟 \`perror\` 和 \`_exit(127)\` 是标准做法。
- **陷阱**: fork 前有未刷新的 stdio 缓冲——printf 的内容在缓冲区里没 flush，fork 后子进程拷贝了缓冲区副本，导致同一行输出被打印两次。fork 前调用 \`fflush(stdout)\` 可避免。
- **陷阱**: 用 \`exit()\` 而非 \`_exit()\` 退出 fork 的子进程——\`exit()\` 会 flush 并关闭父进程继承来的 stdio 缓冲，可能导致父进程的缓冲数据被提前写出或重复输出。fork 的子进程应用 \`_exit()\` 直接走系统调用退出。
- **陷阱**: 在子进程里修改了父进程的文件描述符但不关闭——子进程继承了父进程打开的文件描述符，不用的应及时关闭避免资源泄漏（尤其是网络 socket，不关闭会占着端口）。
- **陷阱**: 在 signal handler 中调用非异步信号安全函数——如 printf、malloc 都不是信号安全的，在 handler 中调用可能导致死锁或数据损坏。handler 中只能用 write 等异步信号安全函数，或只设标志位在主循环中处理。
- **陷阱**: waitpid 在 SIGCHLD handler 中和主循环中竞争——如果主循环也调 waitpid，可能与 handler 中的 waitpid 抢夺子进程退出信息。应统一在 handler 中用 WNOHANG 循环回收，主循环不直接 wait。
- **陷阱**: 误以为 fork 后子进程的线程也会被复制——fork 只复制调用 fork 的那个线程，其他线程不会出现在子进程中。子进程持有的锁可能永远无人释放（锁的持有者线程已不存在）。

## 实战建议
- **建议**: fork 后立即在父进程检查返回值，在子进程分支里执行完任务就 \`_exit()\`，在父进程用 \`waitpid\` 回收。形成「fork → 子进程干活 → 父进程等」的清晰模式。
- **建议**: 长期运行的服务注册 \`SIGCHLD\` 处理函数，在其中用 \`while (waitpid(-1, NULL, WNOHANG) > 0)\` 循环回收所有已退出的子进程——\`WNOHANG\` 非阻塞，没有子进程退出时立即返回，不会卡住主循环。
- **建议**: 需要运行外部命令时用 fork + exec + wait 三连，或直接用 \`system()\`/\`popen()\`（它们内部就是这么做的）。注意 \`system()\` 有 shell 注入风险，处理不可信输入时用 execvp 直接执行避免经过 shell。
- **建议**: 排查僵尸进程用 \`ps aux | grep 'Z'\` 或 \`ps -eo pid,ppid,stat,cmd | grep Z\` 找到僵尸的父进程 PID，然后让父进程调用 wait 或发送信号让其回收；父进程不配合就只能 kill 父进程让 init 接手。
- **建议**: 守护进程的标准创建流程：fork → 父进程 exit → 子进程 setsid（脱离终端）→ fork → 父进程 exit → 子进程 chdir + umask + 关闭继承的 fd → 进入工作循环。两次 fork 确保进程不会再获取终端。
`,
    code: `// ============================================================
// 进程的诞生与生命周期演示
// 演示 fork 创建子进程、wait 回收、僵尸进程的产生与清除、exec 替换
// ============================================================
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>

int main(void) {
    printf("===== 1. fork + wait 基本流程 =====\\n");
    printf("父进程 PID=%d，准备 fork（细胞分裂）\\n", getpid());
    fflush(stdout);  // fork 前刷新缓冲，避免子进程拷贝到重复输出

    pid_t pid = fork();
    if (pid < 0) {
        perror("fork 失败");
        exit(1);
    }
    if (pid == 0) {
        // 子进程分支（fork 在子进程返回 0）
        printf("  [子] 我是副本 PID=%d，父 PID=%d\\n", getpid(), getppid());
        printf("  [子] 任务完成，退出码 42\\n");
        return 42;  // 子进程退出码
    }
    // 父进程分支（fork 在父进程返回子进程 PID）
    int status;
    waitpid(pid, &status, 0);  // 等子进程结束并收尸（回收）
    if (WIFEXITED(status)) {
        printf("  [父] 子进程 PID=%d 已退出，退出码=%d（已回收，不成僵尸）\\n",
               pid, WEXITSTATUS(status));
    }

    printf("\\n===== 2. 僵尸进程演示（子先退出，父故意不 wait）=====\\n");
    fflush(stdout);
    pid_t zpid = fork();
    if (zpid == 0) {
        printf("  [子2] PID=%d，马上退出，但父进程不收尸 -> 变僵尸\\n", getpid());
        return 0;
    }
    // 父进程故意 sleep 不调 wait，此时子2 已死但未回收 -> 僵尸(Z)
    sleep(2);
    printf("  [父] 刚才 2 秒内子2 处于僵尸(Z)状态\\n");
    printf("  [父] 可在另一终端执行: ps -o pid,ppid,stat,comm -p %d\\n", zpid);
    waitpid(zpid, NULL, 0);  // 现在 wait，僵尸消除
    printf("  [父] 已 wait 回收子2，僵尸消除\\n");

    printf("\\n===== 3. exec 换灵魂（子进程替换为 echo 程序）=====\\n");
    fflush(stdout);
    pid_t epid = fork();
    if (epid < 0) { perror("fork"); exit(1); }
    if (epid == 0) {
        // 子进程调用 execl：代码段被 /bin/echo 替换，PID 不变
        execl("/bin/echo", "echo",
              "  [exec后] 我是 echo 程序，原 C 代码已被完全替换",
              (char *)NULL);
        // 只有 exec 失败才会执行到这里
        perror("exec 失败");
        _exit(127);
    }
    waitpid(epid, NULL, 0);  // 等待 exec 后的子进程结束
    printf("  [父] 子进程 exec 执行完毕，已回收\\n");
    return 0;
}
`,
  },

  // ============================================================
  // 第 3 章：系统调用与用户态内核态
  // ============================================================
  {
    id: "comp-syscall",
    group: "程序执行",
    icon: "🌀",
    title: "系统调用与用户态内核态",
    content: `
## 概述
你的程序不能直接操作硬件——读写文件、收发网络、分配内存都要请操作系统代劳。程序运行在权限受限的「用户态」，内核运行在特权「内核态」，两者之间的桥梁就是「系统调用」。本章讲解用户态/内核态的隔离机制、系统调用的执行流程与开销，以及为什么带缓冲的 I/O 比直接系统调用快得多。

## 核心要点
- **用户态 vs 内核态**: 现代 CPU 有特权级别（x86 的 Ring 0 / Ring 3）。用户程序运行在 Ring 3（用户态），不能直接访问硬件、不能修改页表；内核运行在 Ring 0（内核态），拥有全部权限。这种隔离保证了安全性和稳定性。
- **系统调用（syscall）**: 用户程序通过约定的接口（如 read/write/open/mmap/brk）请求内核服务。执行时 CPU 触发特权级切换，从用户态进入内核态执行内核代码，完成后返回用户态。
- **进入内核的方式**: 1) 系统调用（主动请求内核服务）；2) 异常/陷阱（如缺页、除零）；3) 硬件中断（如磁盘 I/O 完成通知 CPU）。三者都触发用户态→内核态切换。
- **syscall 指令**: 现代 x86-64 用 \`syscall\` 指令（x86-32 用 \`int 0x80\`，ARM 用 \`svc\`）进入内核。参数通过寄存器传递（系统调用号放 rax，参数放 rdi/rsi/rdx...），比旧的中断方式快。
- **上下文切换开销**: 每次系统调用都要：保存用户态寄存器 → 切换到内核栈 → 执行内核逻辑 → 恢复寄存器 → 返回用户态。这个开销约 1~10 微秒，频繁系统调用会成为性能瓶颈。
- **read vs fread**: \`read()\` 是系统调用（每次调用都陷入内核）；\`fread()\` 是 C 库函数，内部维护用户态缓冲区，攒满一块（通常 4096 字节）才调一次 \`read()\`。读 1MB 数据，read(1) 要 100 万次系统调用，fread 只需约 256 次。
- **printf 为什么「慢」**: printf 不是直接系统调用，它先写到用户态缓冲区，缓冲区满或遇到 \\n（行缓冲模式）或 fflush 时才调 write() 陷入内核。所以 printf 频繁小输出并不慢——缓冲帮你摊薄了系统调用开销。
- **mmap**: \`mmap()\` 把文件映射到进程内存空间，之后访问内存即读写文件，内核通过缺页异常按需加载——适合大文件随机访问，避免每次 read 的系统调用和拷贝。
- **strace/dtruss**: Linux 用 \`strace -e trace=read,write ./prog\` 追踪程序的系统调用；macOS 用 \`dtruss\`（需 root）。能看到每次系统调用的参数和返回值，是排查 I/O 性能问题的利器。
- **上下文切换 vs 模式切换**: 模式切换（用户态↔内核态）发生在同一次系统调用中，开销较小；上下文切换发生在不同进程/线程间，需切换页表、TLB 刷新、缓存失效，开销大得多（约 1~10 微秒 vs 几十纳秒）。
- **/proc 文件系统**: Linux 的 \`/proc\` 是内核暴露的虚拟文件系统——\`/proc/<pid>/\` 查看进程信息（内存映射、fd、状态），\`/proc/cpuinfo\`、\`/proc/meminfo\` 查看硬件。读取 /proc 不产生磁盘 I/O，数据由内核动态生成。
- **select/poll/epoll**: 三种 I/O 多路复用机制。select 有 fd 数量限制（默认 1024）且每次扫描全部 fd；poll 无限制但仍全扫描；epoll 用红黑树管理 fd，只返回就绪的——高并发首选。
- **brk/mmap 与内存分配**: \`malloc\` 内部用 \`brk()\` 扩展堆顶（小内存）或 \`mmap()\` 申请独立内存区域（大内存）。\`free\` 不一定立即归还内核，而是放进空闲链表供下次 malloc 复用。
- **io_uring**: Linux 5.1+ 引入的异步 I/O 接口，用共享内存环形队列在用户态和内核态之间传递 I/O 请求，减少系统调用次数——比 epoll + 非阻塞 I/O 性能更高。
- **用户态栈 vs 内核态栈**: 每个线程在用户态有自己的栈（默认 8MB，malloc 分配），进入内核态后切换到内核栈（通常 8KB 或 16KB，内核静态分配）。系统调用的局部变量在内核栈上，所以内核代码不能在栈上分配大数组。
- **vDSO（虚拟动态共享对象）**: 内核把部分只读的系统调用（如 gettimeofday、clock_gettime）的实现放进一块共享内存，用户进程直接调用无需陷入内核——这是为什么 gettimeofday 几乎零开销。
- **系统调用号**: 每个系统调用有唯一编号（x86-64 Linux: read=0, write=1, open=2, fork=57, execve=59）。不同架构编号不同——这就是系统调用不跨架构的原因。glibc 的包装函数帮我们把函数名映射到编号。
- **O_NONBLOCK 与 EAGAIN**: 非阻塞 I/O 标志——read/write 在没有数据可读/不能立即写时立即返回 -1 并设 errno=EAGAIN（而非阻塞等待）。配合 epoll/select 实现高并发——事件就绪再读写，不会卡住线程。
- **直接系统调用 vs 库函数**: read/write/open 是薄包装（glibc 直接对应 syscall 指令）；fopen/fread/fwrite/malloc 是厚包装（内部维护缓冲、内存池等用户态逻辑）。库函数通常更易用、更高效，但底层仍依赖系统调用。

## 原理与机制
- **餐厅的比喻**: 用户态是顾客在餐厅大堂就餐，内核态是后厨备餐。顾客不能自己进后厨拿菜，必须通过服务员点单——服务员就是系统调用。频繁点单（频繁系统调用）就像每次只点一口菜，服务员跑来跑去效率极低；一次点一大桌（用缓冲批量 I/O）服务员只需跑一趟。
- **特权级隔离的意义**: 如果用户程序能直接读写硬件，一个 bug 就能覆盖整个内存或破坏磁盘数据。特权级隔离让用户程序只能通过内核暴露的接口（系统调用）间接操作硬件，内核负责权限检查和资源仲裁——这是多任务操作系统安全的基石。
- **系统调用的执行流程**: 1) 用户程序把参数放进寄存器；2) 执行 \`syscall\` 指令，CPU 切到内核态并跳到内核的系统调用入口；3) 内核查系统调用号找到对应函数；4) 执行内核逻辑（如读文件）；5) 把结果放进寄存器，执行 \`sysret\` 返回用户态；6) 用户程序拿到结果。
- **缓冲的分层**: 用户态缓冲（stdio 缓冲区，如 fopen/fread）→ 内核缓冲（page cache，read/write 先到内核缓冲，异步刷盘）→ 硬件缓冲（磁盘控制器缓存）。多层缓冲都是为了减少昂贵的底层操作（系统调用、磁盘 I/O）。
- **为什么 write 到文件不是立即落盘**: \`write()\` 只是把数据从用户态拷贝到内核的 page cache，函数就返回了。真正写盘由内核的 flush 线程延迟执行（或 fsync 强制立即刷盘）。所以程序 crash 但 write 成功的数据可能丢失——数据库用 fsync 保证持久化。
- **零拷贝技术**: 传统读文件再发网络要 4 次拷贝（磁盘→内核缓冲→用户缓冲→socket 缓冲→网卡）。用 sendfile/mmap+write/splice 可减少到 2 次甚至 1 次，避免用户态↔内核态的数据拷贝，大幅提升高并发场景的吞吐量。
- **系统调用号表**: 内核维护一张系统调用号到函数的映射表（sys_call_table）。每个系统调用有固定编号（如 x86-64 Linux 的 read=0, write=1, open=2）。不同架构编号不同——这就是系统调用不跨架构的原因。
- **内核栈**: 每个线程有自己的内核栈（通常 8KB 或 16KB）。进入内核态后使用内核栈，系统调用嵌套过深或内核栈溢出会导致 panic。内核代码应避免在栈上分配大数组。
- **异步 I/O（AIO）**: 传统 read/write 是同步的——发起后阻塞直到完成。异步 I/O 发起后立即返回，完成后通知（信号或回调）。io_uring 是现代 Linux 的高性能异步 I/O 方案。

## 易错点与陷阱
- **陷阱**: 频繁单字节 \`read()\`/\`write()\` 是性能杀手——每次只读 1 字节但系统调用开销固定（约 1~10 微秒）。读 100 万字节就是 100 万次系统调用，可能耗时 1~10 秒；用 fread 缓冲只需几毫秒。
- **陷阱**: 混用 \`fread()\`（带缓冲）和 \`read()\`（不带缓冲）操作同一文件描述符——两者各自维护读写位置，缓冲区与实际文件位置不同步，会导致数据错乱。要么全用 stdio，要么全用系统调用。
- **陷阱**: printf 不加 \\n 又不 fflush 时输出「消失」——行缓冲模式下，输出先在用户态缓冲区里，没遇到 \\n 或缓冲区没满就不调 write()。程序 crash 时缓冲区里的数据就丢了。调试时可用 \`setvbuf(stdout, NULL, _IONBF, 0)\` 关闭缓冲立即看到输出。
- **陷阱**: 以为 \`fsync()\` 没必要——write 成功只保证数据进了内核 page cache，断电仍会丢。数据库、配置文件等需要持久化保证的场景必须 \`fsync(fd)\` 强制刷盘，否则系统崩溃时数据丢失。
- **陷阱**: 用 \`printf\` 做高频日志拖慢程序——虽然 printf 有缓冲，但缓冲区满后每次都要 write() 系统调用。高频日志场景考虑用更大的缓冲区、批量写、或异步日志（先写内存队列，后台线程批量刷盘）。
- **陷阱**: strace 本身有性能开销——strace 拦截每次系统调用，可能让程序慢 2~10 倍。在压测环境用 strace 测出的性能不代表真实性能，只用于排查问题。

## 实战建议
- **建议**: I/O 操作优先用带缓冲的 stdio（fopen/fread/fwrite）而非裸 read/write，除非有特殊需求（如需要精确控制每次 I/O、或与非 stdio 的接口交互）。缓冲能自动帮你摊薄系统调用开销。
- **建议**: 用 strace（Linux）或 dtruss（macOS）排查「程序在干什么」「为什么卡住」——\`strace -e trace=openat,read,write -p <pid>\` 能看到程序正在读写哪些文件，是定位 I/O 问题的第一步。
- **建议**: 大文件随机访问用 \`mmap()\` 代替 read()——内核按需通过缺页异常加载文件页，避免显式 read 的系统调用和数据拷贝，随机访问效率更高。但小文件或顺序读 mmap 的缺页开销可能不如 read。
- **建议**: 高并发网络服务用 epoll/kqueue（Linux/macOS）代替每连接一线程的阻塞模型——epoll 一次系统调用可获取多个就绪事件，避免每个连接都阻塞在 read 上，大幅减少系统调用次数。
- **建议**: 需要持久化保证的关键数据，write 后务必 \`fsync(fd)\`——这会强制把 page cache 中的数据刷到磁盘。注意 fsync 后还应检查返回值，磁盘满或硬件故障时 fsync 会返回错误。
`,
    code: `// ============================================================
// 系统调用与用户态内核态演示
// 对比 write()（每次1次系统调用）vs fwrite()（带缓冲）的性能差异
// ============================================================
#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <unistd.h>
#include <time.h>

#define N 100000  // 每种方式写入的字节数

// 计时辅助：返回当前单调时钟秒数（wall-clock）
static double now(void) {
    struct timespec ts;
    clock_gettime(CLOCK_MONOTONIC, &ts);
    return ts.tv_sec + ts.tv_nsec / 1e9;
}

int main(void) {
    const char *path1 = "/tmp/syscall_unbuf.txt";
    const char *path2 = "/tmp/syscall_buf.txt";
    char byte = 'x';
    double t0, t1;

    printf("===== 对比 write()（每次1次系统调用）vs fwrite()（带缓冲）=====\\n");
    printf("各写入 %d 个字节，每字节调一次写入函数\\n\\n", N);

    // --- 1. 无缓冲：每字节一次 write() = 一次系统调用 ---
    int fd = open(path1, O_WRONLY | O_CREAT | O_TRUNC, 0644);
    if (fd < 0) { perror("open"); return 1; }
    t0 = now();
    for (int i = 0; i < N; i++) {
        // 每次都是一个完整的系统调用：用户态 -> 内核态 -> 用户态
        write(fd, &byte, 1);
    }
    t1 = now();
    close(fd);
    printf("write()  无缓冲: %.4f 秒  -> 约 %d 次系统调用\\n", t1 - t0, N);

    // --- 2. 带缓冲：fwrite 先写进用户态缓冲区，攒满 4096 字节才调一次 write() ---
    FILE *fp = fopen(path2, "w");
    if (!fp) { perror("fopen"); return 1; }
    t0 = now();
    for (int i = 0; i < N; i++) {
        // 先写进 stdio 缓冲区（用户态），攒满 4096 字节才陷入内核
        fwrite(&byte, 1, 1, fp);
    }
    fflush(fp);  // 把缓冲区残余刷到内核
    t1 = now();
    fclose(fp);
    // 缓冲区默认 4096，N=100000 -> 约 ceil(100000/4096)=25 次系统调用
    printf("fwrite() 带缓冲: %.4f 秒  -> 约 %d 次系统调用（缓冲区 4096）\\n",
           t1 - t0, (N + 4095) / 4096);

    printf("\\n结论：带缓冲的 I/O 系统调用次数从 %d 降到 %d，速度差距巨大。\\n",
           N, (N + 4095) / 4096);
    printf("这就是为什么 read() 单字节是性能杀手，而 fread() 同样单字节却不慢。\\n");

    unlink(path1);  // 清理临时文件
    unlink(path2);
    return 0;
}
`,
  },

  // ============================================================
  // 第 4 章：并发同步与锁
  // ============================================================
  {
    id: "comp-concurrency-lock",
    group: "程序执行",
    icon: "🔒",
    title: "并发同步与锁",
    content: `
## 概述
当多个执行流（线程或进程）同时访问共享数据时，如果不加保护就会产生「竞态条件」——结果取决于不可控的时序。本章讲解原子操作、互斥锁、自旋锁、读写锁、条件变量等同步原语，以及死锁的成因与预防。理解这些是编写正确并发程序的前提——并发 bug 难以复现、难以调试，必须在设计阶段就考虑周全。

## 核心要点
- **竞态条件（Race Condition）**: 多个执行流同时读写同一数据，至少一个是写操作，且操作不是原子的——结果取决于执行顺序。例如两个线程同时对 \`counter++\` 做 100 万次，结果可能只有 130 万而非 200 万。
- **原子操作（Atomic）**: 不可被中断的操作——要么全部完成，要么没开始。CPU 提供原子指令（如 x86 的 \`lock inc\`、CAS 即 compare-and-swap）。C11 的 \`_Atomic\` 类型保证对该变量的读写是不可分割的。
- **互斥锁（Mutex）**: 同一时刻只允许一个执行流进入临界区——像厕所门锁，进去的人锁门，外面的人排队等。获取锁失败时线程被挂起（不占 CPU），锁释放时内核唤醒等待者。
- **自旋锁（Spinlock）**: 获取锁失败时不停循环检查（「自旋」）——像不停敲门等人出来。不挂起线程、不进内核，响应快但空耗 CPU。适合临界区极短（几十纳秒）的场景。
- **读写锁（RWLock）**: 区分读和写——多个读者可同时进入，写者独占。适合读多写少的场景（如缓存、配置）。注意写者饥饿问题：持续有读者时写者一直等不到。
- **条件变量（Condition Variable）**: 让线程「拿号排队等叫号」——等待某个条件成立。配合互斥锁使用：检查条件不满足时 \`wait\`（自动释放锁并睡眠），另一线程改变条件后 \`signal\`/\`broadcast\` 唤醒等待者。
- **死锁（Deadlock）**: 多个执行流互相等待对方持有的资源，永远无法继续。经典场景：A 持有锁1等锁2，B 持有锁2等锁1。四个必要条件：互斥、持有并等待、不可抢占、循环等待。
- **内存序（Memory Ordering）**: CPU 和编译器会重排指令以提高性能，但在多线程下重排可能导致数据竞争。C11 的原子操作默认用 \`memory_order_seq_cst\`（顺序一致，最安全但最慢），可用更弱的序（acquire/release）提升性能。
- **volatile 的误区**: C 的 \`volatile\` 只保证「不做编译优化、每次从内存读」——它不保证原子性、不插入内存屏障。多线程同步必须用原子操作或锁，volatile 不是同步工具（Java 的 volatile 不同，它有 happens-before 语义）。
- **内存屏障（Memory Barrier）**: 阻止编译器和 CPU 对屏障前后的指令做重排。原子操作的 acquire/release 语义本质上就是在合适位置插入内存屏障，保证「释放前的写入对获取后的读取可见」。
- **信号量（Semaphore）**: 计数器同步原语——\`wait\` 减 1（为 0 则阻塞）、\`post\` 加 1（唤醒等待者）。互斥锁是信号量初值为 1 的特例；初值为 N 可控制同时访问的最多 N 个资源。
- **生产者-消费者模式**: 经典并发模式——生产者往队列放数据，消费者取数据。用互斥锁保护队列 + 条件变量通知「有数据了」/「队列空了」。阻塞队列是它的标准实现。
- **无锁数据结构**: 用 CAS 等原子操作实现的数据结构（如无锁队列、无锁链表），避免锁的阻塞和上下文切换。实现复杂、调试困难，但高并发下性能优于加锁版本。
- **RCU（Read-Copy-Update）**: Linux 内核用的读端无锁同步机制——读者直接访问旧数据，写者创建副本修改后原子切换指针，等所有读者退出旧数据后再释放。适合读多写极少的场景。
- **memory_order_acquire/release**: C11 原子操作的弱内存序——acquire 读取保证之后的访问不会重排到它之前；release 写入保证之前的访问不会重排到它之后。比默认的 seq_cst 快，但配对使用才正确——是高性能无锁代码的基础。
- **futex（Fast Userspace Mutex）**: Linux 的同步原语基石——用户态先用 CAS 尝试获取锁（快速路径，无内核参与）；竞争失败时通过 futex 系统调用陷入内核挂起（慢速路径）。所以无竞争的互斥锁几乎零开销。
- **锁的粒度**: 粗粒度锁（一把大锁保护所有数据）简单但并发度低；细粒度锁（每个数据结构单独的锁）并发度高但易死锁、开销大。设计时要权衡——常见策略是分片锁（如哈希表按桶加锁）。
- **乐观锁 vs 悲观锁**: 悲观锁先加锁再操作（mutex，冲突频繁时高效）；乐观锁先操作再提交时检查冲突（CAS/version，冲突少时高效无锁）。数据库的 MVCC、无锁数据结构都是乐观策略。
- **happens-before 关系**: 多线程可见性的形式化定义——如果 A happens-before B，则 A 的写入对 B 可见。锁的获取/释放、原子操作、线程创建/join 都建立 happens-before 关系。这是判断并发代码是否正确的理论依据。

## 原理与机制
- **厕所的比喻**: 互斥锁是厕所门锁——一次只进一人，进去锁门，外面的人排队等；自旋锁是不停敲门等人出来——耗体力（CPU）但响应快，适合等的时间很短；条件变量是拿号排队等叫号——等待期间可以坐着（不占 CPU），叫到号再起来。
- **死锁的比喻**: A 拿着厕所钥匙等 B 拿洗手液，B 拿着洗手液等 A 的钥匙——两人都不肯先放手，永远卡住。解决方法：规定所有人必须先拿钥匙再拿洗手液（固定加锁顺序），或设超时（等不到就放手）。
- **counter++ 不是原子的**: 它是三步操作——1) 从内存读 counter 到寄存器；2) 寄存器加 1；3) 写回内存。两个线程同时执行时可能都读到旧值、各自加 1、写回——结果只加了 1 而非 2。这就是「丢失更新」。
- **CAS（Compare-And-Swap）**: 原子操作的核心原语——\`CAS(addr, old, new)\`：如果 addr 的值等于 old，则写入 new 并返回成功，否则返回失败。互斥锁、无锁数据结构都基于 CAS 实现。失败时重试（自旋）直到成功。
- **互斥锁的实现**: 用户态先尝试 CAS 获取锁（快速路径）；失败时陷入内核（futex/Linux 或 ulock/macOS）挂起线程，避免空转占 CPU。所以 mutex 在无竞争时几乎零开销（一次 CAS），有竞争时才付出内核切换代价。
- **自旋锁 vs 互斥锁的取舍**: 临界区小于约 50 纳秒用自旋锁（等的时间短，挂起/唤醒开销比自旋还大）；大于约 1 微秒用互斥锁（挂起释放 CPU 给别人用）。临界区持锁时间不确定时优先 mutex，自旋锁多用于内核态。
- **内存序与可见性**: 没有同步原语时，线程 A 的写入可能永远不被线程 B 看到——编译器优化（寄存器缓存）、CPU 缓存一致性、指令重排都会导致不可见。原子操作和锁通过内存屏障保证「释放锁前的写入，对获取同一把锁的线程可见」。
- **缓存一致性协议（MESI）**: 多核 CPU 每核有 L1/L2 缓存，MESI 协议保证各核缓存数据一致——Modified（已修改）、Exclusive（独占）、Shared（共享）、Invalid（无效）。原子操作通过锁总线或缓存锁保证跨核可见。
- **False Sharing（伪共享）**: 两个变量在同一缓存行（通常 64 字节）内，不同核分别写不同变量——缓存行频繁在核间失效传递，导致性能急剧下降。用 \`alignas(64)\` 对齐或填充消除伪共享。
- **锁的公平性**: 互斥锁分公平锁（FIFO，先到先得）和非公平锁（允许插队）。公平锁避免饥饿但吞吐量低；非公平锁吞吐量高但可能导致个别线程长时间等待。
- **Amdahl 定律与并行加速比**: 程序中串行部分（1-p）决定了并行能带来的最大加速比——1/((1-p) + p/n)。即使无限增加 CPU，串行部分占比 10% 的程序最多只能加速 10 倍。这就是为什么优化并发要先找串行瓶颈，而非盲目加线程。
- **锁分离与数据结构优化**: 减少锁竞争比优化锁本身更重要——把一把大锁拆成多把小锁（分片锁）、用读写锁替代互斥锁（读多写少场景）、用无锁数据结构（CAS 队列）。设计阶段就要考虑数据布局让共享最小化。
- **per-cpu 数据**: Linux 内核常用技巧——每个 CPU 有自己的数据副本，访问本 CPU 的数据无需锁。统计计数器、内存分配缓存（slab）都用 per-cpu 设计，把锁竞争降到几乎为零。用户态可用 \`thread_local\`（C11/C++11）达到类似效果。
- **内存模型与硬件架构**: x86 是强内存模型（TSO，基本只有 store-load 重排）；ARM/POWER 是弱内存模型（各种重排都可能）。所以在 x86 上「碰巧正确」的无锁代码在 ARM 上可能出错——必须用原子操作和内存屏障保证跨架构正确。

## 易错点与陷阱
- **陷阱**: 以为 \`volatile int\` 能保证线程安全——C 的 volatile 只防编译器优化（不缓存到寄存器），不防 CPU 重排、不保证多核可见性。多线程计数必须用 \`_Atomic\` 或锁，volatile 不是同步工具。
- **陷阱**: 忘记释放锁——异常/提前 return 导致锁没释放，其他线程永远阻塞。C 没有 RAII（C++ 有 lock_guard），必须手动确保每条路径都 unlock；用 goto 统一清理或记录「持锁状态」避免遗漏。
- **陷阱**: 持锁时调用未知代码（回调函数）——回调可能尝试获取同一把锁（递归死锁）或别的锁（与外部形成循环等待）。持锁期间尽量只做简单操作，不调用外部代码。
- **陷阱**: 条件变量不用 while 循环检查条件——\`wait\` 可能被虚假唤醒（spurious wakeup）唤醒，醒来后条件可能仍不满足。必须用 \`while (条件不满足) wait()\` 而非 \`if\`，防止虚假唤醒后误入临界区。
- **陷阱**: 加锁顺序不一致导致死锁——线程 A 先锁1再锁2，线程 B 先锁2再锁1，交叉执行时互相等待。解决方案：全局规定加锁顺序（如按地址排序），所有线程按同一顺序获取多把锁。
- **陷阱**: 读写锁的写者饥饿——大量读者持续进入时写者永远等不到独占访问。解决方案：读写锁通常有「写者优先」模式，有写者等待时阻止新读者进入，让写者能在有限时间内获得锁。
- **陷阱**: 误以为单核 CPU 不需要同步——单核上虽然不会真正并行，但信号/中断可能在指令间隙打断，导致同样的竞态。只要存在异步执行流就需要同步。
- **陷阱**: 自旋锁在单核上使用导致死锁——单核上持锁线程被调度出去后，等待锁的线程自旋占用 CPU，持锁线程永远得不到运行机会。单核环境必须用可挂起的互斥锁。
- **陷阱**: 条件变量 signal 在 wait 之前调用——如果 signal 在 wait 之前执行，信号丢失，wait 会一直阻塞。解决：signal 必须在持锁状态下调用，确保 wait 已注册到等待队列。

## 实战建议
- **建议**: 能用原子操作就不用锁——简单的计数器、标志位用 \`_Atomic\` 类型（C11）或 \`__atomic\` 内建函数（GCC/Clang），开销远小于互斥锁，且无需手动释放。
- **建议**: 必须用锁时优先选互斥锁（mutex），临界区尽量短——只在访问共享数据时持锁，I/O、计算、调用外部函数都放到锁外。持锁时间越短，竞争越少，并发度越高。
- **建议**: 需要多把锁时全局规定加锁顺序——如按锁的内存地址从小到大获取，所有线程遵守同一顺序，从源头杜绝循环等待死锁。或者用 \`trylock + 超时\`：获取失败就释放已持有的锁重试。
- **建议**: 检测死锁可用 helgrind（Valgrind 工具）或 ThreadSanitizer（\`-fsanitize=thread\`）——它们在运行时检测数据竞争和锁顺序问题，能发现难以复现的并发 bug。
- **建议**: 优先使用更高层的并发抽象——如消息队列、actor 模型、通道（channel）等「消息传递」方式，避免共享内存。共享内存 + 锁容易出错，消息传递天然无数据竞争（「不要通过共享内存来通信，而应通过通信来共享内存」）。
`,
    code: `// ============================================================
// 并发同步与锁演示
// 用 fork + mmap 共享内存演示竞态条件，
// 对比 非原子 int 与 _Atomic int 的累加差异（不依赖 pthread）
// ============================================================
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/mman.h>
#include <sys/wait.h>
#include <stdatomic.h>

#define NUM_CHILDREN  4       // 并发子进程数
#define INCREMENTS    100000  // 每个子进程累加次数

// 共享内存布局：放两个计数器方便对比
typedef struct {
    int normal_counter;           // 非原子，多进程并发写会丢失更新
    _Atomic int atomic_counter;   // 原子操作，不丢失更新
} SharedCounters;

int main(void) {
    printf("===== 竞态条件演示：fork 多进程并发累加 =====\\n");
    printf("子进程数=%d，每个累加 %d 次\\n", NUM_CHILDREN, INCREMENTS);
    printf("期望结果：%d × %d = %d\\n\\n",
           NUM_CHILDREN, INCREMENTS, NUM_CHILDREN * INCREMENTS);

    // 用 mmap 创建父子共享内存（MAP_SHARED 让子进程的修改对父进程可见）
    SharedCounters *sc = mmap(NULL, sizeof(SharedCounters),
                              PROT_READ | PROT_WRITE,
                              MAP_SHARED | MAP_ANONYMOUS, -1, 0);
    if (sc == MAP_FAILED) { perror("mmap"); return 1; }
    sc->normal_counter = 0;
    atomic_store(&sc->atomic_counter, 0);

    // fork 多个子进程并发累加（模拟多线程竞态）
    for (int i = 0; i < NUM_CHILDREN; i++) {
        pid_t pid = fork();
        if (pid < 0) { perror("fork"); return 1; }
        if (pid == 0) {
            // 子进程：同时累加两个计数器
            for (int j = 0; j < INCREMENTS; j++) {
                sc->normal_counter++;   // 非原子：读-改-写三步，可能被打断
                sc->atomic_counter++;   // 原子：单条不可中断的指令（lock inc）
            }
            _exit(0);  // 子进程直接退出（不用 exit 避免刷缓冲）
        }
    }

    // 父进程等待所有子进程结束
    for (int i = 0; i < NUM_CHILDREN; i++) {
        wait(NULL);
    }

    // 读取结果（原子变量用 atomic_load 取值）
    int normal_val = sc->normal_counter;
    int atomic_val = atomic_load(&sc->atomic_counter);

    printf("非原子 int 累加结果: %d\\n", normal_val);
    printf("原子   int 累加结果: %d\\n", atomic_val);
    printf("\\n结论：非原子计数器因竞态丢失更新，结果 < 期望值 %d；\\n",
           NUM_CHILDREN * INCREMENTS);
    printf("      原子计数器每次 ++ 不可分割，结果恰好等于期望值。\\n");
    printf("      这就是「互斥锁/原子操作」存在的意义——保证操作不被穿插。\\n");

    munmap(sc, sizeof(SharedCounters));
    return 0;
}
`,
  },
];
