// =============================================================
// Batch 3：内存与存储（4 章）
// =============================================================

export const chapters = [
  // ============================================================
  // 第 1 章：虚拟内存与分页
  // ============================================================
  {
    id: "comp-virtual-memory",
    group: "内存与存储",
    icon: "🧠",
    title: "虚拟内存与分页",
    content: `
## 概述
你同时打开 10 个应用，每个都以为自己独占 4GB 内存，而机器物理内存才 8GB —— 这是怎么做到的？答案就是「虚拟内存」。本章讲解虚拟内存如何用「页表 + MMU」给每个进程画一张独立的地址空间大饼，TLB 如何加速地址翻译，缺页中断如何实现按需加载，以及 LRU、写时复制这些让系统既安全又高效的机制。理解虚拟内存，是理解进程隔离、内存泄漏、性能调优的基石。

生活比喻：虚拟内存像酒店给每位客人一张专属房卡，客人只看到自己的房间号（虚拟地址），实际房间位置（物理地址）由酒店后台（MMU + 页表）映射。客人 A 的 301 和客人 B 的 301 指向不同物理房间，互不打扰；缺页中断像客人到房间发现还没打扫，前台临时叫人打扫（从磁盘调入页）后再入住。

为什么这套机制如此重要？因为它是进程隔离的安全基石：A 进程无法直接读写 B 进程的内存，根源就是各自有独立页表；它又是「按需加载」的前提，让你能跑动一个声明了 10GB 内存但实际只用 100MB 的程序；它还是 fork、exec、mmap、malloc 这些核心系统调用的底层支撑。掌握虚拟内存，等于掌握了理解现代操作系统内存行为的「钥匙」。

## 核心要点
- **虚拟内存（Virtual Memory）**: 每个进程拥有独立的虚拟地址空间（64 位下通常 48 位有效，约 256TB），程序看到的地址都是虚拟地址，需经 MMU 翻译成物理地址。
- **分页（Paging）**: 内存被切成固定大小的「页」（通常 4KB），虚拟页通过页表映射到物理页框；页是内存管理的最小单位。
- **页表（Page Table）**: 多级页表（x86-64 四级：PGD→PUD→PMD→PTE）记录虚拟页→物理页映射；每个进程一份，由内核维护、MMU 查询。
- **TLB（Translation Lookaside Buffer）**: CPU 内的页表缓存，缓存最近用过的映射；TLB 命中可避免一次访存查表，未命中则触发「页表漫步」。
- **缺页中断（Page Fault）**: 访问的页不在物理内存时触发；次要缺页（从 swap/文件读回）vs 主要缺页（首次分配）；内核处理后重新执行指令。
- **按需加载（Demand Paging）**: 程序启动时只加载少量页，用到时才缺页调入 —— 所以 \`malloc\` 只是预约地址，真正分配发生在写访问时。
- **页面置换算法**: 内存不足时选谁淘汰？LRU（最久未用）、LFU（最少使用）、Clock（时钟/NRU）；Linux 默认用近似 LRU 的双指针策略。
- **写时复制（Copy-on-Write）**: \`fork()\` 后父子进程共享同一物理页，直到某方写入才复制 —— 这是 fork 快速且省内存的关键。
- **swap**: 物理内存不足时把冷页写入磁盘 swap 区；频繁换入换出（thrashing）会导致性能断崖式下跌。
- **mmap**: 把文件映射到虚拟地址空间，读写像访问内存一样；大文件处理、共享内存都依赖它。
- **大页（HugePages）**: 把页大小从 4KB 提升到 2MB/1GB，减少页表项数量与 TLB 压力；适合大内存数据库、JVM。
- **地址空间布局**: 64 位进程典型布局：代码段（低地址）→ 数据段 → 堆（向上生长）→ ... → 栈（高地址向下生长）；\`/proc/<pid>/maps\` 可见全貌。

## 原理与机制
- **地址翻译全流程**: CPU 产生虚拟地址 → TLB 查缓存 → 命中则得物理地址；未命中则 MMU 走多级页表 → 找到页框号 → 拼成物理地址 → 访问内存；若页表项标记「不在内存」则触发缺页中断。
- **多级页表为什么省内存**: 一个进程的虚拟空间巨大但大多未用；单级页表需为所有虚拟页建表项（巨量浪费）；多级页表只对「用到」的范围建中间表，未用的中间项直接置空 —— 用「按需建表」换来了空间节省。
- **TLB 为什么关键**: 每次访存都要翻译，若每次都查内存中的页表（多级则 4 次访存）性能崩溃；TLB 把热点映射留在 CPU 内，命中率通常 >99%；切换进程需刷 TLB（或用 PCID 标记），这就是上下文切换开销的一部分。
- **缺页中断的开销**: 一次缺页中断涉及 trap 到内核、可能触发磁盘 I/O、调度其他进程；耗时约微秒到毫秒级，比正常访存（纳秒级）慢上千倍 —— 所以频繁缺页是性能杀手。
- **Copy-on-Write 流程**: \`fork()\` 时只复制页表，物理页标只读共享；子进程写时触发保护异常 → 内核分配新页 → 复制内容 → 改页表为可写 —— 父子修改的部分才真正复制，未改的部分始终共享。
- **页面置换的代价**: 淘汰干净页直接丢弃（下次重新从文件读）；淘汰脏页需先写回磁盘；选错淘汰页会导致「刚淘汰又马上要用」，反复缺页 —— 这就是 LRU 的价值：基于「最近用过的很可能再用」的局部性原理。
- **swap 与 thrashing**: 内存远不够时，内核不断在内存和 swap 间换页，CPU 大量时间花在 I/O 上，系统近乎停滞 —— 此时加内存是唯一解，调参数收效甚微。
- **mmap 的两种模式**: 匿名映射（无文件，分配零页内存）与文件映射（按需从文件读页）；\`MAP_SHARED\` 修改写回文件、可跨进程共享，\`MAP_PRIVATE\` 则用 COW 隔离 —— 二者各有适用场景。
- **NUMA 与地址翻译**: 多 socket 机器上物理内存分布在不同节点，访问远端内存延迟更高；NUMA 感知的页分配策略让进程优先用本地内存，是大型服务器调优的关键。

## 易错点与陷阱
- **陷阱**: 以为 \`malloc\` 返回非 NULL 就一定有物理内存 —— 错！过度 commit 后真正写访问时才 OOM（Linux 默认允许超量 commit，由 OOM Killer 收场）。
- **陷阱**: \`mmap\` 大文件后顺序访问看似内存操作，实则每次跨页都触发缺页中断 —— 大量缺页会让「内存读写」退化成「磁盘 I/O」量级。
- **陷阱**: 频繁 \`fork()\` 的服务（如 prefork 的 Apache）在内存压力下 COW 复制爆发，瞬间内存翻倍 —— 别在内存吃紧时疯狂 fork。
- **陷阱**: swap 用量大不等于有问题，要看 si/so 速率；真正危险的是持续高速换页（thrashing），此时系统几乎瘫痪。
- **陷阱**: TLB shootdown 在多核间发 IPI 同步刷 TLB，是 NUMA/多核场景下不可忽视的开销 —— 大页（HugePages）能减少 TLB 项数量缓解压力。
- **陷阱**: 32 位进程默认只有 3GB 用户地址空间，分配大数组很容易失败；不要拿老代码的假设套 64 位环境。
- **陷阱**: \`mmap\` 用 \`MAP_POPULATE\` 预先填充页表可减少运行时缺页，但启动时一次性大量分配会拖慢启动 —— 适合长时间运行的服务而非短命令。
- **陷阱**: 容器里看到的内存是 cgroup 限制的，但 \`/proc/meminfo\` 显示的是宿主机全局值；容器内程序按 \`/proc\` 判断可用内存会误判 —— 用 cgroup 文件或 \`sysconf\` 替代。

## 实战建议
- **建议**: 用 \`/proc/<pid>/maps\` 看进程虚拟地址空间布局（代码段/堆/栈/共享库），用 \`/proc/meminfo\` 看系统内存分布 —— 这是排查内存问题的第一步。
- **建议**: 大内存服务（数据库、JVM）开启 HugePages 减少 TLB miss，尤其当 RSS 达几十 GB 时收益明显。
- **建议**: 关键服务设 \`oom_score_adj\` 保护，避免 OOM Killer 杀错进程；同时监控 si/so 速率，发现 thrashing 立刻扩容或限流。
- **建议**: 理解「局部性原理」指导代码：顺序访问数组、紧凑数据结构、避免随机跳转 —— 让 LRU/缓存/预取都能为你工作。
- **建议**: 调 \`vm.swappiness\`（默认 60）：数据库/JVM 类服务调到 10 左右减少换出，桌面可保持默认；但别设 0，留一点 swap 防 OOM 直杀。
- **建议**: 用 \`perf stat -e page-faults,minor-faults,major-faults\` 量化程序的缺页情况，定位频繁缺页的代码段；用 \`mincore\` 查询哪些页已驻留内存。
- **建议**: \`madvise()\` 显式告诉内核你对内存区的访问模式（\`MADV_SEQUENTIAL\`/\`MADV_RANDOM\`/\`MADV_WILLNEED\`），内核据此预读或调整置换策略 —— 这是高级程序员优化大内存程序的利器。
`,
    code: `# ===== 虚拟内存与分页 演示 =====
# 读取系统内存信息与进程虚拟地址空间映射（macOS 无 /proc，做兼容处理）

echo "=== 1. 系统内存信息 ==="
if [ -f /proc/meminfo ]; then
  echo "[Linux /proc/meminfo 前 8 行]"
  head -n 8 /proc/meminfo
else
  echo "[macOS 无 /proc/meminfo，用 vm_stat 替代]"
  vm_stat | head -n 8
  echo "（Linux /proc/meminfo 含 MemTotal/MemFree/MemAvailable/SwapTotal 等）"
fi

echo ""
echo "=== 2. 虚拟内存页大小（地址映射的最小粒度）==="
echo "页大小 = $(getconf PAGE_SIZE) 字节（通常 4096 = 4KB）"
echo "→ MMU 按页映射虚拟地址到物理地址，缺页中断以页为单位"

echo ""
echo "=== 3. 进程虚拟地址空间映射 ==="
PID=$$
echo "当前 Shell PID = \${PID}"
if [ -f /proc/\${PID}/maps ]; then
  echo "[/proc/\${PID}/maps 前 12 行]"
  head -n 12 /proc/\${PID}/maps
else
  echo "[macOS 无 /proc/<pid>/maps，Linux 下输出示例：]"
  echo "55a1b2c00000-55a1b2c01000 r-xp 00000000 08:01 1001  /bin/bash   # 代码段(只读可执行)"
  echo "55a1b2e00000-55a1b2e01000 rw-p 00000000 08:01 1001  /bin/bash   # 数据段(可读写)"
  echo "7f8a00000000-7f8a00021000 rw-p 00000000 00:00 0      [heap]      # 堆"
  echo "7ffc12340000-7ffc12361000 rw-p 00000000 00:00 0      [stack]     # 栈"
  echo "→ 列：起止虚拟地址 | 权限(rwx) + p私有/s共享 | 偏移 | 设备:inode | 映射文件"
  echo "→ p=私有(写时复制 COW)  s=共享；不同段隔离 = 进程间互不干扰"
fi

echo ""
echo "=== 4. 缺页/换页活动 ==="
if [ -f /proc/vmstat ]; then
  echo "[Linux /proc/vmstat 缺页/换页计数]"
  grep -E '^(pgfault|pgmajfault|pswpin|pswpout)' /proc/vmstat
else
  echo "[macOS] vm_stat 中的 page faults（Linux 对应 /proc/vmstat）"
  vm_stat | head -n 5
fi
echo "→ pgfault=缺页次数  pgmajfault=磁盘缺页  pswpin/out=swap 换入换出"

echo ""
echo "=== 演示结束 ==="
`,
  },

  // ============================================================
  // 第 2 章：堆栈与内存管理
  // ============================================================
  {
    id: "comp-stack-heap",
    group: "内存与存储",
    icon: "📚",
    title: "堆栈与内存管理",
    content: `
## 概述
程序运行时的数据放在哪里？答案分两块：栈和堆。栈负责函数调用的自动管理，堆负责运行时的动态分配。本章讲解栈的 LIFO 与函数调用帧、堆的分配释放机制、\`malloc/free\` 的原理，以及内存泄漏、悬垂指针这两大经典内存 bug。理解堆栈是写出无内存 bug、高性能代码的前提。

生活比喻：栈像弹夹压子弹，最后压入的最先弹出（函数返回时栈帧自动弹出）；堆像停车场，你自己找位置停车和取车，忘了取车就泄漏了（占着车位不放），凭旧票据去找已被清理的车位就是悬垂指针。

栈和堆的分工体现了「便利 vs 灵活」的权衡：栈由硬件指令直接管理（移动 SP 即可），分配释放极快但生命周期死板（随函数返回结束）；堆由库/运行时管理，灵活但要自己负责释放，错一步就是泄漏或悬垂。GC 语言（Java/Go/JS）用垃圾回收器替你管堆，但理解 C 的手动管理更能看清内存的本质 —— 这也是为什么系统编程、嵌入式、内核开发绕不开 C。

## 核心要点
- **栈（Stack）**: 自动分配释放，LIFO 结构；每次函数调用压入一个「栈帧」（存放局部变量、参数、返回地址），返回时弹出。
- **栈帧（Stack Frame）**: 包含返回地址、保存的寄存器、局部变量、参数；栈帧的连续压入/弹出对应函数的调用/返回。
- **栈生长方向**: x86/ARM 上栈向低地址生长（SP 递减）；压栈 SP 减，出栈 SP 加 —— 本章 demo 会实测验证。
- **堆（Heap）**: 运行时动态分配，由程序员（C）或运行时（GC 语言）管理；生命周期不受函数作用域限制。
- **malloc/free 原理**: \`malloc\` 向内核申请/扩展堆区（brk/mmap），内部用空闲链表或伙伴系统管理；\`free\` 标记块为可用但不一定立刻归还内核。
- **内存泄漏（Memory Leak）**: 分配的堆内存失去引用却没释放，无法回收；长期累积导致内存占用持续增长直至 OOM。
- **悬垂指针（Dangling Pointer）**: 指向已释放内存的指针；解引用是未定义行为（UB），可能读到脏数据或崩溃。
- **双重释放（Double Free）**: 同一块内存 free 两次，破坏堆元数据，常导致崩溃或可被利用的安全漏洞。
- **缓冲区溢出（Buffer Overflow）**: 写越界覆盖相邻内存，是栈溢出攻击的根源；用栈保护（canary）、ASLR 缓解。
- **内存碎片**: 频繁 malloc/free 后堆中出现大量不连续小空洞；外部碎片（空闲但不连续）vs 内部碎片（分配块内的浪费）。
- **栈溢出（Stack Overflow）**: 递归过深或局部变量过大撑爆栈空间（默认 8MB）；常见于无终止条件的递归 bug。
- **RAII 与智能指针**: C++ 用 RAII 模式让对象出作用域自动释放；\`std::unique_ptr\`/\`shared_ptr\` 把「谁分配谁释放」编译期固化，从根源消除多数内存 bug。

## 原理与机制
- **函数调用的栈机制**: \`call\` 指令把返回地址压栈并跳转；被调函数 prologue 保存旧 BP、开辟局部变量空间；epilogue 恢复 BP、\`ret\` 弹出返回地址跳回 —— 这就是 LIFO 的硬件实现。
- **栈vs堆的取舍**: 栈分配只需移动 SP（几条指令，O(1)且无碎片），但大小受限（默认 8MB）、生命周期随函数结束；堆灵活但慢（要查空闲块）、易碎片、易泄漏。能用栈就别用堆。
- **malloc 的实现层次**: 小块用 brk 维护的堆顶区（glibc 的 fastbin/smallbin），大块用 mmap 直接映射；free 时相邻空闲块会合并，顶部空闲块可能通过 sbrk 负方向收缩归还内核。
- **为什么 free 不立刻还内存**: 频繁向内核申请/归还开销大；malloc 实现把空闲块留在自己管理的堆池里复用，只有大块空闲或顶部连续空闲才 trim 给内核 —— 所以 RSS 往往不随 free 立刻下降。
- **内存泄漏的累积性**: 单次泄漏可能只有几十字节，但长跑服务（Web/数据库）运行数月后累积成 GB 级 —— 这类 bug 只在长期监控中暴露，开发阶段难发现。
- **悬垂指针的危险**: free 后内存可能被立即复用给别的分配，此时旧指针读写的是别人的数据；更糟的是攻击者可构造数据控制程序流（use-after-free 漏洞）。
- **glibc 的 bin 结构**: free 后的块按大小进 fastbin（小，单链表 LIFO）、smallbin/largebin（大，按大小分桶）、unsorted bin（暂存）；分配时先查 fastbin 再查合适的 bin —— 这是 ptmalloc 性能的关键。
- **栈保护（Canary）**: 编译器在栈帧返回地址前插入随机值（canary），函数返回前检查是否被改写；被改写说明发生栈溢出，立即终止进程 —— 阻止了经典的栈溢出攻击。

## 易错点与陷阱
- **陷阱**: 返回指向栈局部变量的指针 —— 函数返回后栈帧失效，指针指向的内存被后续调用覆盖，读到垃圾值。务必用堆或 static，或让调用者传入缓冲区。
- **陷阱**: \`free\` 后继续使用指针（use-after-free）；正确做法是 free 后立即置 \`NULL\`，后续解引用会立刻段错误而非埋雷。
- **陷阱**: \`malloc\` 后不检查返回值（可能返回 NULL），直接解引用导致段错误；虽罕见但 OOM 场景会触发。
- **陷阱**: 忘记对齐或越界写：\`char buf[4]; strcpy(buf, "hello");\` 写入 6 字节（含 \\0）越界覆盖相邻变量 —— 用 \`strncpy\` 或指定长度的拷贝。
- **陷阱**: 以为 \`free\` 会清空内存或置指针 NULL —— 它只标记块可用，指针值不变（变成悬垂），数据可能还在。
- **陷阱**: 在循环里 malloc 却只在部分分支 free，泄漏路径隐蔽；复杂逻辑用「谁分配谁释放」的对称结构。
- **陷阱**: \`realloc\` 可能返回新地址（原块不够大时搬移），旧指针立即失效；务必用返回值覆盖原指针，别假设原地扩展。
- **陷阱**: 递归深度与栈大小：默认 8MB 栈，每层栈帧几百字节，理论上万层就溢出；深度不可控的递归要改迭代或显式用堆模拟栈。

## 实战建议
- **建议**: 能用栈就不用堆；局部数组、临时变量都用栈，性能最好且零泄漏风险。
- **建议**: C 代码配 Valgrind/\`fsanitize=address\` 检测泄漏与越界；gcc/clang 加 \`-fsanitize=address -g\` 能在开发期抓住绝大多数内存 bug。
- **建议**: free 后立即置 NULL；分配后立刻想好释放点（构造/析毁配对），避免「忘记释放」。
- **建议**: 大对象用 \`calloc\`（清零）而非 \`malloc\`+memset；检查 \`malloc\` 返回值，关键路径要有 OOM 降级。
- **建议**: 高频分配场景用内存池/对象池，减少 malloc 次数和碎片；游戏、网络服务器普遍采用池化策略。
- **建议**: 用 \`ulimit -s\` 查看栈大小，递归深或局部变量大的程序可临时调大；但更好的是改用堆分配大缓冲区。
- **建议**: 线程栈独立：多线程程序每个线程有自己的栈（默认 8MB，pthread 可调），N 个线程就是 N×8MB 虚拟空间 —— 高并发要调小线程栈或用协程。
`,
    code: `// ===== 堆栈与内存管理 演示 =====
// 演示：栈 LIFO 与生长方向、栈帧、堆分配/释放、内存泄漏与悬垂指针
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// 递归压栈：每层分配一个栈帧，最后压入的最先弹出
static void check_stack(int depth, int *prev_addr) {
    int local = depth;
    printf("  depth=%d  本层 &local=%p  上层 addr=%p\\n", depth, (void*)&local, (void*)prev_addr);
    if (depth <= 0) {
        if (&local < prev_addr) printf("  -> 栈向低地址生长（x86/ARM 常见）\\n");
        else printf("  -> 栈向高地址生长\\n");
        return;
    }
    check_stack(depth - 1, &local);   // 压入下一帧（LIFO：最后压入的最先返回）
}

// 模拟内存泄漏：分配后不释放
static void leak_demo(void) {
    char *p = malloc(256);
    strcpy(p, "这块内存忘了 free，泄漏 256 字节");
    printf("  [泄漏] %s\\n", p);
    // 故意不 free(p) —— 函数返回后指针丢失，内存却仍占用
}

int main(void) {
    printf("=== 1. 栈：LIFO 与生长方向 ===\\n");
    int top = 0;
    check_stack(3, &top);

    printf("\\n=== 2. 堆：手动分配与释放 ===\\n");
    int *arr = malloc(5 * sizeof(int));
    for (int i = 0; i < 5; i++) arr[i] = i * 10;
    for (int i = 0; i < 5; i++) printf("  arr[%d]=%d (堆地址 %p)\\n", i, arr[i], (void*)&arr[i]);
    free(arr);
    arr = NULL;                    // 释放后置 NULL，避免悬垂指针

    printf("\\n=== 3. 内存泄漏演示 ===\\n");
    leak_demo();
    printf("  -> 泄漏的内存无法回收，长期累积会导致 OOM\\n");

    printf("\\n=== 4. 悬垂指针演示 ===\\n");
    int *dangling = malloc(sizeof(int));
    *dangling = 42;
    free(dangling);
    // *dangling = 99;  // 危险！写已释放内存（UB），这里仅注释说明
    printf("  free 后 dangling 仍指向 %p（悬垂），正确做法：dangling=NULL\\n", (void*)dangling);
    dangling = NULL;
    return 0;
}
`,
  },

  // ============================================================
  // 第 3 章：磁盘与文件系统
  // ============================================================
  {
    id: "comp-filesystem",
    group: "内存与存储",
    icon: "💽",
    title: "磁盘与文件系统",
    content: `
## 概述
内存断电即失，真正持久保存数据的是磁盘，而文件系统则在裸磁盘之上组织数据、提供「文件」这一抽象。本章讲解机械硬盘与 SSD 的差异、扇区/块/inode 的层次、ext4/XFS/FAT32 等文件系统的取舍、挂载的概念，以及顺序读与随机读之间巨大的性能鸿沟。理解这些，才能解释为什么数据库要顺序写 WAL、为什么小文件多会拖慢系统。

生活比喻：文件系统像图书馆，inode 是图书目录卡（记录书的位置和属性），文件名是书名（书名只在目录索引里，目录卡才有书的真实位置）。顺序读是按书架顺序一本本读，随机读是满馆跑着找散落各处的书 —— 后者累死人。

磁盘与内存的速度差距高达十万倍（内存纳秒级，磁盘毫秒级），这个鸿沟是数据库、日志、缓存设计绕不开的核心约束。理解磁盘特性，你才能看懂数据库为什么用 B+ 树（减少随机 I/O 次数）、为什么 LSM-Tree 用追加写（顺序写对 SSD 友好）、为什么 Kafka 把消息存文件还能那么快（顺序读 + page cache）。掌握文件系统，等于掌握了「数据如何落地」的底层逻辑。

## 核心要点
- **机械硬盘（HDD）**: 靠磁头在旋转盘片上寻道读写；随机访问需机械寻道（毫秒级），顺序访问吞吐高；便宜、容量大但慢且易损。
- **固态硬盘（SSD）**: 靠闪存芯片电信号读写；无机械寻道，随机 IOPS 远超 HDD（万倍量级）；但写入有磨损，需 FTL/TRIM 延长寿命。
- **扇区（Sector）**: 磁盘物理最小读写单位，传统 512 字节，现代高级格式 4KB。
- **块（Block）**: 文件系统逻辑最小单位，通常是扇区的整数倍（ext4 默认 4KB）；读写以块为粒度。
- **inode**: 文件元数据结构，记录文件大小、权限、时间戳、数据块位置；每个文件一个 inode，**文件名不在 inode 里**，而在目录项中。
- **文件系统类型**: ext4（Linux 默认，日志型）、XFS（大文件/高并发强）、FAT32（跨平台但无权限、单文件 4GB 上限）、NTFS（Windows）、APFS（macOS）。
- **挂载（Mount）**: 把一个文件系统接到目录树的某个节点（挂载点）；\`mount\` 命令完成接入，\`/etc/fstab\` 配置开机自动挂载。
- **日志（Journal）**: ext4/XFS 写数据前先写日志，崩溃后能快速恢复一致性；用少量性能换可靠性。
- **顺序读 vs 随机读**: 顺序读磁头连续移动/SSD 顺序写高效，吞吐可达 GB/s 级；随机读需反复寻道/擦写，IOPS 受限，机械盘差距可达百倍。
- **page cache**: 文件读写经过内核页缓存；重复读同一文件命中缓存极快，写先到缓存再异步落盘（\`fsync\` 强制落盘）。
- **direct I/O（直写）**: 绕过 page cache 直接读写磁盘，数据库常用（自己管缓存更高效）；需对齐到块大小，否则报错。
- **IOPS 与吞吐**: IOPS = 每秒读写次数（随机小 I/O 的指标）；吞吐 = MB/s（顺序大 I/O 的指标）；二者是评估磁盘性能的两个维度。

## 原理与机制
- **inode 与目录的关系**: 目录本质是一个特殊文件，内容是「文件名 → inode 号」的映射表；打开文件时内核先查目录项得到 inode 号，再读 inode 找数据块。这就是为什么硬链接（多个文件名指向同一 inode）能存在 —— 文件名只是目录里的别名。
- **HDD 随机为什么慢**: 每次随机访问都要「寻道（磁头移动）+ 旋转延迟（等扇区转到磁头下）」，约 5-10ms；而顺序访问磁头一旦定位就连续读写，吞吐可达 100-200MB/s。IOPS ≈ 1000/寻道时间，机械盘随机 IOPS 仅约 100。
- **SSD 随机为什么快但有限**: 无机械部件，随机读 IOPS 可达 10 万级；但写入需先擦除（闪存不能直接覆写），FTL 把随机写映射到空闲块并做磨损均衡；写入放大（WA）使实际写入量大于逻辑量。
- **日志文件系统如何保证一致性**: 写操作分三步：写日志 → 写数据 → 提交日志点；崩溃后若日志已提交则重放，未提交则丢弃 —— 避免了传统 fsck 扫描整盘的慢启动。
- **顺序写为什么是数据库的宠儿**: 顺序写避开随机寻道/擦写，吞吐稳定且对 SSD 寿命友好；WAL（预写日志）、LSM-Tree（追加写）都是利用顺序写的设计。
- **小文件多的性能问题**: 每个文件至少占一个 inode + 一个数据块，元数据开销大；随机访问大量小文件导致频繁寻道/inode 查找 —— 这就是「小文件灾难」。
- **page cache 的读写路径**: 读文件先查 page cache，命中则零拷贝返回，未命中则触发磁盘 I/O 并缓存；写文件先写 page cache（标记脏页），由 pdflush/flusher 线程异步落盘 —— \`fsync\` 强制把脏页写回磁盘。
- **SSD 的写入放大（WA）**: 闪存擦除以块为单位（如 4MB），改 4KB 也要擦整块再重写；FTL 用日志结构映射缓解，但 WA 仍可达 2-10 倍 —— 所以 SSD 看的「写入寿命」要按物理写量算，不是逻辑写量。

## 易错点与陷阱
- **陷阱**: FAT32 单文件上限 4GB，往 U 盘拷大文件报错「文件过大」却不是空间不足 —— 换 exFAT/NTFS。
- **陷阱**: \`df\` 显示空间够但 \`No space left on device\` —— 可能是 inode 耗尽（大量小文件），用 \`df -i\` 查 inode 使用率。
- **陷阱**: 写文件后不 \`fsync\` 就断电，数据可能在 page cache 里没落盘而丢失；关键数据必须显式 fsync。
- **陷阱**: SSD 频繁全盘擦写会加速磨损；避免无意义的反复写（如日志轮转太频繁），开启 TRIM 让 FTL 回收块。
- **陷阱**: 以为删除文件就释放了空间 —— 若有进程仍打开该文件，inode 不会回收（占用保留到进程关闭）；用 \`lsof\` 找这类「已删未释放」文件。
- **陷阱**: 跨文件系统硬链接会失败（inode 是文件系统局部的）；硬链接不能跨分区，软链接（符号链接）可以。
- **陷阱**: \`rename\` 跨文件系统会失败（底层是不同 inode 表），需改成「复制 + 删除」；同分区 rename 是原子的，常用作写文件的原子提交。
- **陷阱**: \`fsync\` 只刷文件数据，不刷目录项；新建文件后要同时 fsync 目录 fd，否则崩溃可能丢文件名。

## 实战建议
- **建议**: 数据库/日志分区选 XFS（大文件强）或 ext4（通用稳）；挂载时按场景加选项：\`noatime\`（不写访问时间，减少写）、\`data=writeback\`（性能优先）。
- **建议**: 评估随机 IOPS 看 \`iostat -x\` 的 \`r/s w/s await\`；随机负载重时优先上 SSD，机械盘只适合顺序归档。
- **建议**: 大量小文件场景考虑合并（如打包成单文件再用索引访问）、或换对象存储；避免单目录放十万级文件（目录项线性查找慢），分子目录存放。
- **建议**: 关键写入用 \`fsync\` 或 \`O_SYNC\` 保证落盘；但别滥用（性能骤降），按事务粒度批量 fsync。
- **建议**: 定期监控 inode 使用率（\`df -i\`）和磁盘健康（SSD 看 SMART 磨损指标），防患于未然。
- **建议**: 数据库用 direct I/O（\`O_DIRECT\`）绕过 page cache 自管缓存，避免双重缓存浪费内存；普通应用则享受 page cache 即可。
- **建议**: SSD 开启 TRIM（\`fstrim.timer\` 或挂载 \`discard\` 选项）让 FTL 回收空闲块，维持长期写入性能；但 \`discard\` 在线回收有性能开销，定时 fstrim 更稳妥。
`,
    code: `# ===== 磁盘与文件系统 演示 =====
# 用 dd 对比顺序写 vs 随机写速度，并查看 inode 信息

TMPFILE=$(mktemp 2>/dev/null || echo "/tmp/fsdemo_$$.tmp")
echo "测试文件: \${TMPFILE}"

echo ""
echo "=== 1. 顺序写：一次性写 256KB ==="
dd if=/dev/zero of="\${TMPFILE}" bs=256k count=1 2>&1 | tail -n 1
echo "→ 顺序写：磁头连续移动 / SSD 顺序写，吞吐高"

echo ""
echo "=== 2. 随机写：64 次 × 4KB 跳跃写（共 256KB）==="
for i in $(seq 0 63); do
  dd if=/dev/zero of="\${TMPFILE}" bs=4k count=1 seek=$i conv=notrunc 2>/dev/null
done
echo "→ 随机写：64 次独立小 I/O，机械盘需反复寻道，远慢于顺序写"

echo ""
echo "=== 3. inode 信息（文件的「目录卡」）==="
ls -li "\${TMPFILE}"          # 第 1 列 = inode 号
echo "→ inode 存：大小/权限/时间戳/数据块位置；文件名在目录项里，不在 inode"

echo ""
echo "=== 4. 文件系统信息 ==="
df -h "\${TMPFILE}" | head -n 2
echo "（macOS 多为 apfs；Linux 常见 ext4/xfs；用 df -T 或 findmnt 查类型）"
echo "（FAT32 不支持符号链接/权限，单文件上限 4GB）"

rm -f "\${TMPFILE}"
echo ""
echo "=== 演示结束 ==="
`,
  },

  // ============================================================
  // 第 4 章：缓存策略与性能优化
  // ============================================================
  {
    id: "comp-cache-strategy",
    group: "内存与存储",
    icon: "🚀",
    title: "缓存策略与性能优化",
    content: `
## 概述
从 CPU L1/L2 到内存 page cache、到 Redis、到 CDN，缓存无处不在。本章把「缓存」作为通用模式讲解：LRU/LFU/FIFO 等淘汰算法、缓存穿透/击穿/雪崩三大经典问题、多级缓存与一致性，以及它们对 Redis 设计、HTTP 缓存头的实战影响。理解缓存策略，是用最小成本撬动最大性能的关键。

生活比喻：缓存像你办公桌上的常用文件架，LRU 是「最久没看的文件拿走」给新文件腾地方；缓存穿透是有人反复要一份根本不存在的文件，你每次都去仓库白跑一趟；缓存击穿是某个热门文件突然过期，所有人同时冲去仓库找同一份；缓存雪崩是整个文件架塌了（大批缓存同时失效），所有人都去仓库找文件，仓库门口堵死。

缓存是计算机科学中「用空间换时间」最经典的体现。从硬件到软件，每一层缓存都在利用「局部性原理」缩小速度鸿沟：CPU 缓存弥补 CPU 与内存的差距，page cache 弥补内存与磁盘的差距，Redis 弥补内存与数据库的差距，CDN 弥补边缘与中心的网络差距。掌握缓存策略，你就能在性能优化的每个层次都找到杠杆 —— 这也是高级工程师与初级工程师的分水岭之一。

## 核心要点
- **缓存（Cache）本质**: 用更快但更贵的存储暂存热点数据，换取访问速度；基于「局部性原理」（最近用过的很可能再用、空间相邻的很可能一起用）。
- **命中率**: 命中数 / 总访问数；命中率是缓存价值的核心指标，90% 命中通常意味着平均访问时间接近缓存速度。
- **FIFO**: 先进先出，最早进入的先淘汰；实现简单（队列）但不考虑访问模式，可能淘汰热点。
- **LRU（Least Recently Used）**: 淘汰最久未访问的；用双向链表 + 哈希表实现 O(1)；命中率优于 FIFO，是工程最常用算法。
- **LFU（Least Frequently Used）**: 淘汰访问频率最低的；适合稳定热点（热门 key 持续高频），但对新 key 不友好（刚加入频率低易被淘汰）。
- **TTL 过期**: 给缓存设过期时间，保证最终一致；但大批同时过期会引发雪崩。
- **缓存穿透**: 查询根本不存在的 key，每次都打到后端；用布隆过滤器或缓存空值（短 TTL）防御。
- **缓存击穿**: 单个热点 key 过期瞬间，海量请求同时打到后端；用互斥锁（只放一个请求回源）或热点永不过期。
- **缓存雪崩**: 大批缓存同时失效或缓存整体宕机；用过期时间加随机抖动、多级缓存、熔断限流防御。
- **CDN**: 边缘节点缓存静态资源，让用户就近访问；回源是 CDN 未命中时回源站取数据。
- **HTTP 缓存**: \`Cache-Control\`（max-age 强缓存）、\`ETag\`/\`If-None-Match\`（协商缓存）；合理设置能省大量回源流量。
- **Write-through / Write-back**: 写穿透（同步写缓存+后端，一致但慢）vs 写回（先写缓存异步刷后端，快但有丢数据风险）；按一致性需求选择。

## 原理与机制
- **LRU 的 O(1) 实现**: 哈希表存 key→节点指针（O(1) 定位），双向链表维护访问顺序（O(1) 摘除与插入）；访问时把节点移到头部，淘汰时删尾部。本章 demo 用 C 完整实现这一结构。
- **多级缓存层次**: CPU L1/L2/L3 → 内存 page cache → 进程内缓存 → Redis → 数据库；每一级容量更大但更慢，命中率层层递进。设计时要决定每一层缓存什么、失效策略如何。
- **缓存与数据库一致性**: 写数据时先写库还是先删缓存？常见策略：先删缓存再写库（可能有读请求把旧值回填）、延迟双删（删缓存→写库→延迟再删缓存）；强一致需分布式锁或 binlog 订阅，成本高。
- **缓存预热**: 服务启动或大促前主动加载热点到缓存，避免冷启动期击穿；按历史访问日志统计热点 key 预加载。
- **LFU 的衰减问题**: 传统 LFU 频次只增不减，老热点即使不再访问也长期占位；改进版（如 Redis 的 LFU）给频次做指数衰减，让新热点有机会上位。
- **CDN 回源与刷新**: 未命中回源会放大源站压力；用预热（主动推送）、合理 TTL、回源率监控（<5% 为佳）控制；刷新（purge）用于内容更新后强制失效。
- **布隆过滤器原理**: 用 k 个哈希函数映射到 bit 数组，存在则全为 1（可能有误判），不存在则至少一个 0（一定无误判）；空间极省，是防穿透的利器，但删除困难。
- **Cache aside（旁路缓存）模式**: 最常用模式：读时先查缓存，miss 则查库并回填；写时先更库再删缓存（不是更新缓存，避免并发覆盖）。简单可靠，是 Redis + MySQL 的标配。

## 易错点与陷阱
- **陷阱**: 缓存与库不一致：先更新库再更新缓存，并发下可能 A 覆盖 B 的更新；多数场景用「先删缓存再更新库 + 延迟双删」更稳。
- **陷阱**: 大量 key 同一时刻过期（如批量预生成时设了相同 TTL）引发雪崩；过期时间务必加随机量（如 \`ttl + random(0,300s)\`）。
- **陷阱**: 把大对象塞缓存（如 10MB 的 JSON），单 key 过大导致 Redis 阻塞、网络拥塞；大对象拆分或只缓存摘要。
- **陷阱**: LRU 在「全表扫描」式访问下失效 —— 一次遍历把缓存全换成冷数据（缓存污染）；可改用 LFU 或限制扫描走旁路缓存。
- **陷阱**: 以为缓存命中率高就万事大吉 —— 还要看延迟（缓存本身是否慢）、错误率（缓存节点是否健康）、容量是否逼近上限。
- **陷阱**: HTTP 强缓存（max-age）期间内容更新用户看不到；需要版本化 URL（\`app.js?v=2\`）或主动 purge CDN。
- **陷阱**: Redis 的 \`KEYS *\` 在生产是禁用命令（O(N) 阻塞全库）；扫描用 \`SCAN\`，删除大量 key 用 \`UNLINK\`（异步删除）。
- **陷阱**: 缓存击穿用互斥锁时，锁本身要带超时和重试，否则持有锁的请求挂了会导致后续全部阻塞 —— 用 Redis 的 \`SET NX EX\` 实现带超时的锁。

## 实战建议
- **建议**: Redis 默认淘汰策略 \`noeviction\`（写满报错），生产改成 \`allkeys-lru\` 或 \`volatile-lru\`；热点稳定场景可试 \`allkeys-lfu\`。
- **建议**: 防穿透用布隆过滤器（内存占用小，可判断 key 一定不存在）；防击穿用互斥锁或热点 key 永不过期 + 异步刷新。
- **建议**: HTTP 静态资源用强缓存 + 文件指纹（hash 文件名），动态接口用协商缓存（ETag）；区分不可变资源（长 max-age）与可变资源（短 max-age + ETag）。
- **建议**: 多级缓存要分级失效：进程内缓存（秒级 TTL，本地最快）+ Redis（分钟级，共享）+ DB；进程内缓存注意一致性松紧，强一致场景慎用。
- **建议**: 监控命中率、内存使用、淘汰速率、慢查询；Redis 用 \`INFO stats\` 看 \`keyspace_misses/hits\`、\`evicted_keys\`，命中率低于 95% 就该排查。
- **建议**: 缓存是「锦上添花」不是「救命稻草」；设计时假设缓存会失效，后端必须扛得住回源流量（降级、限流、熔断）。
- **建议**: 热点 key 拆分：把一个超高 QPS 的 key 拆成多个副本（如 \`user:1:cache_0..9\`），读写随机选一个，分摊单 key 的压力到多分片。
`,
    code: `// ===== 缓存策略与性能优化：LRU 缓存演示 =====
// 双向链表(访问顺序) + 哈希表(O(1)定位) = O(1) 的 LRU 缓存
#include <stdio.h>
#include <stdlib.h>
#define CAP 3
#define HTSZ 128

typedef struct Node { int key, value; struct Node *prev, *next; } Node;
static Node *head, *tail;      // head=最近使用, tail=最久未用
static Node *table[HTSZ];      // 简易哈希表：key 直接做下标
static int sz = 0;

// 把节点移到头部（标记为最近使用）
static void touch(Node *n) {
    if (head == n) return;
    if (n->prev) n->prev->next = n->next;
    if (n->next) n->next->prev = n->prev;
    if (tail == n) tail = n->prev;
    n->prev = NULL; n->next = head;
    if (head) head->prev = n;
    head = n;
    if (!tail) tail = n;
}

static int lru_get(int key) {
    Node *n = (key < 0 || key >= HTSZ) ? NULL : table[key];
    if (!n) { printf("  get(%d) -> miss\\n", key); return -1; }
    touch(n);
    printf("  get(%d) -> hit, value=%d\\n", key, n->value);
    return n->value;
}

static void lru_put(int key, int value) {
    Node *n = (key < 0 || key >= HTSZ) ? NULL : table[key];
    if (n) { n->value = value; touch(n); return; }   // 已存在则更新
    n = malloc(sizeof(Node));
    n->key = key; n->value = value; n->prev = n->next = NULL;
    table[key] = n;
    n->next = head;
    if (head) head->prev = n;
    head = n;
    if (!tail) tail = n;
    if (++sz > CAP) {                                 // 超容量淘汰尾部(最久未用)
        Node *e = tail;
        printf("  -> 容量满(%d)，淘汰 LRU key=%d\\n", CAP, e->key);
        if (e->prev) e->prev->next = NULL;
        tail = e->prev;
        if (head == e) head = NULL;
        table[e->key] = NULL;
        free(e);
    }
}

static void lru_print(void) {
    printf("  缓存(最近->最久): ");
    for (Node *n = head; n; n = n->next) printf("[%d:%d] ", n->key, n->value);
    printf("\\n");
}

int main(void) {
    printf("=== LRU 缓存演示 (容量=%d) ===\\n", CAP);
    lru_put(1,10); lru_put(2,20); lru_put(3,30); lru_print();
    lru_get(1); lru_print();              // 命中1，key=2 成最久未用
    lru_put(4,40); lru_print();           // 容量满，淘汰 key=2
    lru_get(2); lru_get(3); lru_print();  // miss / hit
    return 0;
}
`,
  },
];
