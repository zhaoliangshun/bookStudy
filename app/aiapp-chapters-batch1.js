// =============================================================
// AI 应用编程教程 —— 第 1 批章节（AI编程入门组，共 5 章）
// -------------------------------------------------------------
// 章节范围：
//   1. aiapp-intro        AI 编程新时代总览
//   2. aiapp-capabilities AI 能为程序员做什么
//   3. aiapp-concepts     必懂核心概念：LLM/Token/上下文窗口/温度
//   4. aiapp-boundaries   AI 的能力边界与局限
//   5. aiapp-mindset      AI 时代的程序员思维转变
//
// 每个章节对象的结构：
//   id      : 唯一标识
//   icon    : 展示用 emoji
//   group   : 分组名
//   title   : 章节标题
//   content : Markdown 格式的详细讲解
//   code    : 可在 Node.js 沙箱运行、带详细中文注释的示例代码
// =============================================================

export const chapters = [
  {
    id: "aiapp-intro",
    icon: "🚀",
    group: "AI编程入门",
    title: "AI 编程新时代总览",
    content: `
# 第1章：AI 编程新时代总览

## 1.1 什么是 AI 编程

AI 编程（AI-Assisted Programming）是指利用大语言模型（LLM）等人工智能技术，在软件开发的各个环节辅助甚至替代人类完成编程任务的一类工作方式。它不是某一款具体的产品，而是一种新的开发范式：开发者把一部分原本需要手工敲击键盘的工作——查文档、写样板代码、调试、写测试、补注释——交给模型来完成，自己则把注意力集中在需求理解、架构设计、价值判断这些更高层次的事情上。

理解 AI 编程，最重要的是要破除两个极端的迷思。第一种极端是“AI 无用论”，认为模型生成的代码不可靠、容易出 bug，所以坚持纯手工写代码。这种态度忽略了工具的杠杆效应，就像拒绝使用 IDE 自动补全一样，长期下来一定会被采用新工具的同行拉开效率差距。第二种极端是“AI 万能论”，认为只要把需求丢给模型，就能拿到可上线的代码，开发者可以完全“躺平”。这种态度则严重低估了软件工程的复杂度，忽略了需求拆解、架构权衡、领域知识、合规约束这些模型短期内无法替代的环节。

正确的认知介于两者之间：**AI 编程是放大器，不是替代品**。它放大的是开发者已有的能力——一个有扎实工程功底的人，配合 AI 可以把产出提升数倍；而一个缺乏基础的人，配合 AI 也只能拿到一堆似是而非、无法落地的代码。本教程的目标，就是帮助你建立这种“放大”的能力。

下表概括了 AI 编程与传统编程在几个关键维度上的差异：

| 维度 | 传统编程 | AI 编程 |
| --- | --- | --- |
| 主要输入 | 键盘敲击的字符 | 自然语言意图 + 上下文 |
| 主要产出 | 自己一行行写的代码 | 模型生成 + 人工审查修改 |
| 思考重心 | “怎么实现” | “要实现什么 + 实现得对不对” |
| 知识获取 | 主动查文档/搜索 | 模型直接给出，需验证 |
| 错误来源 | 拼写、语法、逻辑 | 幻觉、过时 API、误解意图 |
| 协作单位 | 函数/模块 | 任务/Prompt |
| 学习曲线 | 学语法、学框架 | 学提问、学审查、学编排 |

## 1.2 AI 编程的发展历程：从代码补全到自主 Agent

AI 编程并不是 2023 年才出现的概念，它有一条清晰的演进路径。理解这条路径，能帮助你判断今天的工具处在什么阶段、未来又会朝哪个方向走。

**第一阶段：基于规则的代码补全（2010 年前后）**

早期的 IDE（如 Eclipse、IntelliJ、Visual Studio）就已经支持代码补全，但本质是基于语法树和符号表的“查表”，只能补全当前作用域内已定义的变量和方法。它没有任何“猜你下一步想干什么”的能力。

**第二阶段：统计模型的智能补全（2018 年前后）**

GitHub 与微软研究开始尝试用统计学习模型预测下一行代码，TabNine 是这一阶段的代表产品。它基于 GPT-2 之类的小模型，能在本地预测短小的代码片段，但准确率有限，且只能看当前文件、几行的上下文。

**第三阶段：Copilot 与云端大模型（2021 年）**

2021 年 6 月，GitHub Copilot 技术预览版发布，背后是 OpenAI 的 Codex 模型（GPT-3 的代码微调版本）。这是 AI 编程第一次以“IDE 插件”的形态大规模进入开发者日常工作。Copilot 不再是“补全下一行”，而是能根据注释生成整段函数，根据函数签名生成实现。这一阶段的关键词是 **Inline 补全**——你在编辑器里写代码，模型在旁边默默提建议，按 Tab 接受。

**第四阶段：Chat 模式与对话式编程（2023 年）**

2023 年 ChatGPT 爆发后，开发者开始把“写代码”这件事搬到对话框里。GitHub Copilot Chat、Cursor 的 Chat 面板、JetBrains AI Assistant 纷纷出现。这一阶段的特征是：开发者不再只是“接受或拒绝”补全建议，而是能用自然语言和模型来回讨论——“这段代码为什么报错？”“帮我把这个函数改成异步的”“给我加个边界测试”。Chat 模式让 AI 编程从“单向建议”变成了“双向协作”。

**第五阶段：Agent 模式与自主执行（2024-2026 年）**

2024 年开始，AI 编程进入 Agent 时代。代表产品包括 Cursor 的 Composer/Agent 模式、Cline、Trae 的 Agent 模式、Claude Code、OpenAI Codex CLI 等。Agent 模式的核心区别是：模型不再只是“回答”或“建议”，而是能**自主地读文件、改文件、跑命令、跑测试、根据报错自我修正**，循环多步直到完成任务。开发者从“一行行指挥”变成了“提一个目标，监督执行过程”。

\`\`\`text
2010  规则补全      ──>  2018  统计补全       ──>  2021  Copilot
                                                          │
                                                          ▼
2024  Agent 模式    <──  2023  Chat 模式       <──  Inline 补全
                                                          │
                                                          ▼
                                                    持续演进中
\`\`\`

## 1.3 主流应用形态：四种典型产品形态

今天市面上的 AI 编程工具，大致可以归为四种形态。每一种形态对应不同的使用场景和思维模式。

**1. 聊天助手（Chat Assistant）**

代表：ChatGPT、Claude.ai、Gemini Web。形态是网页对话框，开发者把代码贴进去提问。优点是通用、不绑定特定 IDE，适合做“独立问题求解”——查一个算法怎么写、解释一段陌生代码、做技术选型咨询。缺点是脱离了代码上下文，每次都要手动复制粘贴，不适合做大型项目内的连续修改。

**2. IDE 集成（IDE Integration）**

代表：GitHub Copilot、Cursor、Trae、JetBrains AI Assistant、Continue。形态是插件或一体化的 AI IDE，能直接读取项目文件、当前打开的编辑器内容、终端输出等上下文。这是目前主力开发场景里最高效的形态，因为它和开发者的工作流深度绑定——你在哪儿写代码，AI 就在哪儿出现。

**3. 终端 CLI（Terminal CLI）**

代表：Claude Code、OpenAI Codex CLI、Aider、gh copilot。形态是命令行工具，开发者在终端里用自然语言下达指令，工具直接读写本地文件、执行 git/构建/测试命令。CLI 形态的优势是“脚本化”和“可自动化”——你可以把一段 AI 编程任务写进 CI 流程，或者批量处理几十个仓库。它也更受资深开发者的喜爱，因为终端是他们最熟悉的环境。

**4. 代码生成平台（Code Generation Platform）**

代表：v0.dev、bolt.new、Lovable、Replit Agent。形态是网页表单 + 在线预览，用户描述需求，平台直接生成一个可运行的应用（通常是前端项目）。这类产品适合做原型验证、Demo 搭建、个人想法快速落地，但生成的代码如果要进入生产环境，仍然需要人工迁移和重构。

| 形态 | 典型产品 | 最适合场景 | 主要限制 |
| --- | --- | --- | --- |
| 聊天助手 | ChatGPT、Claude.ai | 独立问题、学习咨询 | 无代码上下文 |
| IDE 集成 | Copilot、Cursor、Trae | 日常项目开发 | 受限于 IDE 体验 |
| 终端 CLI | Claude Code、Codex CLI | 自动化、批量任务 | 学习曲线较陡 |
| 代码生成平台 | v0、bolt.new | 原型、Demo | 难直接进生产 |

## 1.4 三种 AI 编程范式对比：Chat、Inline、Agent

无论是哪种产品形态，开发者真正和 AI 交互的方式可以归纳为三种“范式”。它们各有适用场景，理解差异是高效使用 AI 编程的前提。

**范式一：Chat 模式（对话式）**

开发者用自然语言在对话框里描述需求或问题，模型给出文字回答 + 代码块，开发者自己决定是否复制到项目中。特点：**回合制、上下文由开发者显式提供**。适合复杂问题讨论、技术方案设计、调试思路分析。不适合大量机械的代码改动。

\`\`\`text
开发者：帮我写一个防抖函数，要求支持取消
AI：     [给出代码 + 解释]
开发者：再改成 TypeScript，加上泛型
AI：     [给出修改后的代码]
\`\`\`

**范式二：Inline 补全（行内补全）**

开发者在编辑器里正常写代码，模型根据上下文（当前文件、光标位置、注释）预测接下来的若干行，灰色显示在光标处，按 Tab 接受。特点：**零摩擦、低延迟、开发者始终在主导**。适合样板代码、重复模式、小范围补全。不适合跨文件、需要重新设计的大改动。

\`\`\`javascript
// 你输入注释：
// 计算两个日期之间的天数差
function daysBetween(start, end) {
  // 模型在光标处灰色预测：
  //   const ms = end - start;
  //   return Math.floor(ms / (1000 * 60 * 60 * 24));
}
\`\`\`

**范式三：Agent 模式（自主执行）**

开发者下达一个目标，模型自主规划步骤、读写文件、执行命令、观察结果、自我修正，循环多步直到完成或需要人工介入。特点：**多步自主、能动手改代码**。适合明确但步骤多的任务——加一个功能并写测试、批量重构、修一个复现步骤清晰的 bug。不适合模糊需求、高风险操作、需要业务判断的任务。

\`\`\`text
开发者：给 utils/date.js 加一个工作日计算函数，并补单元测试
Agent：
  1. 读取 utils/date.js，了解现有结构和导出方式
  2. 实现函数 addBusinessDays(date, n)
  3. 创建 tests/date.test.js，编写 5 个测试用例
  4. 运行 npm test，发现 1 个用例失败
  5. 修改实现，重新运行，全部通过
  6. 报告完成，等开发者确认是否提交
\`\`\`

三者的对比：

| 维度 | Chat 模式 | Inline 补全 | Agent 模式 |
| --- | --- | --- | --- |
| 交互单位 | 一段对话 | 一次按键 | 一个任务 |
| 上下文来源 | 开发者提供 | 编辑器自动 | 项目+命令+反馈 |
| 自主程度 | 低 | 极低 | 高 |
| 修改代码 | 不直接改 | 改光标处 | 跨文件改 |
| 适用粒度 | 中-大 | 小 | 中-大 |
| 出错成本 | 低（自己贴） | 低（看一眼） | 中-高（自动改） |
| 典型时机 | 方案讨论 | 写代码时 | 任务委派 |

实战中三种范式是**互补**的：Inline 用于日常书写，Chat 用于思考讨论，Agent 用于委派整块任务。一个高效的 AI 程序员会根据任务在三种范式间灵活切换，而不是固守某一种。

## 1.5 2023-2026 关键节点

理解最近几年的关键节点，能帮你建立对 AI 编程发展速度的直觉。

| 时间 | 事件 | 意义 |
| --- | --- | --- |
| 2021.06 | GitHub Copilot 技术预览 | AI 编程首次大规模进入 IDE |
| 2022.11 | ChatGPT 发布 | 大模型进入大众视野，编程是高频用法之一 |
| 2023.03 | GPT-4 发布，Copilot X 公布 | 模型能力跃升，AI 编程进入 Chat 时代 |
| 2023.06 | Claude 2 发布，上下文 100K | 长上下文让“喂整个项目”成为可能 |
| 2024.03 | Cursor 完成 B 轮融资，用户激增 | AI 优先 IDE 成为一种独立产品形态 |
| 2024.06 | Claude 3.5 Sonnet 发布 | 编程能力大幅领先，Cursor 默认模型 |
| 2024.10 | Cursor Composer 多文件编辑普及 | Agent 模式从概念走向日常 |
| 2025.02 | Claude Code 发布 | 终端原生 Agent 进入主流 |
| 2025.05 | OpenAI Codex CLI 开源 | CLI Agent 生态加速 |
| 2025-2026 | Trae、Cline、Windsurf 等多 Agent 工具成熟 | Agent 成为 AI 编程默认范式 |

从这张时间线可以看到三件事：第一，模型能力的提升是台阶式的，每一次新模型都会刷新“AI 能做什么”的上限；第二，产品形态在模型能力推动下不断演化，从补全到 Chat 再到 Agent；第三，速度极快——三年里 AI 编程从“尝鲜玩具”变成了“主力工具”，这种节奏在软件工程史上是罕见的。

## 1.6 对软件工程的整体影响

AI 编程对软件工程的影响远不止“写代码变快”，它在多个层面引发结构性变化。

**开发效率层面**：样板代码、CRUD 接口、单元测试、文档生成等“机械工作”的边际成本趋近于零。开发者的有效产出从“代码行数”转向“功能价值”。这也意味着用代码行数评估绩效的传统做法彻底失效。

**代码质量层面**：AI 生成的代码风格统一、注释完整、测试覆盖率高，但也存在“表面光鲜、暗藏 bug”的风险。Code Review 的重心从“写得对不对”转向“想得对不对”——审 reviewer 要判断 AI 是否真的理解了需求，而不是只看代码是否符合语法。

**团队结构层面**：初级开发者借助 AI 能更快地产出代码，但也容易绕过基础训练。资深开发者的角色从“写关键代码”转向“做架构决策 + 审 AI 输出 + 指导初级”。团队对“能指挥 AI 完成大任务”的人需求上升。

**知识传承层面**：以往靠老员工口口相传的隐性知识（踩过的坑、为什么这么设计）开始被 AI 部分替代——AI 能解释大部分公开知识。但只属于团队的领域知识、业务上下文，AI 仍然无法获取，这些知识的价值反而上升。

**风险与安全层面**：AI 可能引用过时 API、引入有漏洞的依赖、生成看似正确实则不安全的代码。供应链安全、License 合规、敏感信息泄露（代码贴到云端模型）成为新的工程话题。

## 1.7 本教程的学习路线图

本教程定位为“AI 应用编程”的入门到实战路线，目标是让开发者从“会用 Copilot 补全”进阶到“能编排 Agent 完成完整功能”。整体路线分四个阶段：

**阶段一：建立认知（第 1-5 章，本批次）**

理解 AI 编程是什么、能做什么、不能做什么、需要哪些核心概念、应该用什么心态面对。这一阶段不写复杂代码，但决定了你后续学习的方向是否正确。

**阶段二：掌握工具（后续批次）**

系统学习主流 AI 编程工具——Cursor/Trae 等 AI IDE、Claude Code/Codex CLI 等终端 Agent、v0 等代码生成平台。每个工具讲清楚它的范式、最佳实践、典型陷阱。

**阶段三：Prompt 与工作流（后续批次）**

学习如何写好提示词、如何为不同任务选择合适的范式、如何把 AI 嵌入到现有开发流程（需求→设计→编码→测试→Review→上线）。这一阶段是从“会用工具”到“高效用工具”的飞跃。

**阶段四：实战与进阶（后续批次）**

通过完整项目案例，演示如何用 AI 编程完成一个真实功能——从前期需求拆解、架构讨论，到代码实现、测试、文档、上线。同时涉及 AI 编程的陷阱、伦理、团队协作规范。

每个阶段都配有可运行的示例代码，建议边学边练，把每个章节的 code 字段在 Node.js 沙箱里跑一遍，观察输出，加深理解。

## 1.8 延伸学习

本教程聚焦“AI 应用编程”这一主题，要真正用好 AI，还需要了解更广的知识体系。完成本章后，推荐你结合本站的其他教程一起学习，形成完整的能力地图：

- **/ai**：AI 编程方法教程。侧重“怎么和 AI 协作”——提示词工程、AI 辅助编码、AI 工作流、陷阱与伦理。与本教程互补：本教程讲“AI 应用怎么写”，/ai 讲“写代码时怎么用 AI”。
- **/ai-agent**：AI Agent 应用开发实战。如果你想自己开发一个 Agent（而不是只用现成工具），这是必读教程。涵盖 LLM 原理、工具调用、记忆机制、多 Agent 编排等。
- **/ts**：TypeScript 教程。AI 生成的现代前端代码几乎都是 TypeScript，看懂和审查 AI 输出需要扎实的 TS 基础。
- **/py**：Python 教程。AI/数据/后端领域的 AI 输出大多以 Python 为主，掌握 Python 能让你更自如地审查和改造 AI 生成的代码。
- **/nextjs**：Next.js 教程。AI 代码生成平台（v0、bolt）默认产出的就是 Next.js 项目，理解 Next.js 是把 AI 生成的原型落地为生产应用的关键。

建议的学习顺序：本教程（AI 应用编程入门）→ /ai（编程方法）→ /ai-agent（Agent 开发）→ 选一门语言深入（/ts 或 /py）→ /nextjs（落地框架）。

下一章我们将系统盘点“AI 能为程序员做什么”——把 AI 在开发流程中的八大应用场景一次讲透，每个场景都配示例 prompt，帮你在实际工作中知道什么时候该召唤 AI。
`,
    code: `
// =============================================================
// 第1章示例代码：AI 编程范式识别器
// 演示如何根据任务特征，自动推荐使用 Chat / Inline / Agent 三种范式
// 可在 Node.js 沙箱中直接运行，无外部依赖
// =============================================================

// ---- 任务描述类型 ----
// 一个开发任务包含：意图、规模、是否需要跨文件、是否需要执行命令等特征
const taskExamples = [
  {
    name: "解释闭包",
    intent: "想搞懂 JavaScript 闭包到底是什么",
    scope: "single-file",      // 单文件级
    needsExecution: false,      // 不需要执行命令
    needsMultiStep: false,      // 单步即可
  },
  {
    name: "补全工具函数",
    intent: "正在写一个 formatDate 函数，写到一半",
    scope: "single-file",
    needsExecution: false,
    needsMultiStep: false,
  },
  {
    name: "为新模块加测试",
    intent: "给 utils/date.js 里所有导出函数补单元测试，并跑通",
    scope: "multi-file",        // 需要读源码 + 写测试文件
    needsExecution: true,       // 需要运行 npm test
    needsMultiStep: true,       // 写测试 + 跑测试 + 修复
  },
  {
    name: "技术选型讨论",
    intent: "新项目该选 Next.js 还是 Remix，列优劣",
    scope: "none",              // 不涉及具体代码
    needsExecution: false,
    needsMultiStep: false,
  },
  {
    name: "全仓库升级到 ESM",
    intent: "把整个仓库从 CommonJS 改成 ESM，并保证测试通过",
    scope: "repo",              // 仓库级
    needsExecution: true,
    needsMultiStep: true,
  },
];

// ---- 范式推荐器 ----
// 根据任务特征判断应该用哪种 AI 编程范式
function recommendParadigm(task) {
  // 规则 1：需要执行命令 + 多步骤 + 跨文件 -> Agent 模式
  // 这类任务开发者一处处改太慢，让 Agent 自主循环更高效
  if (task.needsExecution && task.needsMultiStep && task.scope !== "single-file") {
    return {
      paradigm: "Agent",
      reason: "任务需要执行命令、多步循环、跨文件修改，适合让 Agent 自主完成",
    };
  }

  // 规则 2：不涉及具体代码修改（scope=none）-> Chat 模式
  // 主要是讨论、咨询、解释，对话框最合适
  if (task.scope === "none") {
    return {
      paradigm: "Chat",
      reason: "任务以讨论/解释为主，不直接改代码，Chat 模式最合适",
    };
  }

  // 规则 3：单文件 + 不需要执行 + 单步 -> 看是否在“写代码过程中”
  // 如果是写代码时遇到的小补全 -> Inline；如果是独立求解 -> Chat
  // 这里用一个 heuristic：如果 intent 包含“正在写”/“写到一半”判定为 Inline
  if (/正在写|写到一半|接着写|继续/.test(task.intent)) {
    return {
      paradigm: "Inline",
      reason: "任务发生在编码过程中，Inline 补全零摩擦、不打断思路",
    };
  }

  // 规则 4：单文件、求解类问题 -> Chat 模式
  // 例如“解释这段代码”“帮我想个算法”，适合来回讨论
  if (task.scope === "single-file" && !task.needsMultiStep) {
    return {
      paradigm: "Chat",
      reason: "任务规模小但需要解释/讨论，Chat 模式便于来回交流",
    };
  }

  // 兜底：复杂任务都建议先 Chat 讨论方案，再 Agent 执行
  return {
    paradigm: "Chat -> Agent",
    reason: "复杂任务建议先用 Chat 讨论方案，确认后再用 Agent 执行",
  };
}

// ---- 执行推荐 ----
console.log("========================================");
console.log("  AI 编程范式识别器");
console.log("========================================\\n");

taskExamples.forEach((task, idx) => {
  const rec = recommendParadigm(task);
  console.log(\`[\${idx + 1}] 任务：\${task.name}\`);
  console.log(\`    意图：\${task.intent}\`);
  console.log(\`    特征：scope=\${task.scope}, exec=\${task.needsExecution}, multiStep=\${task.needsMultiStep}\`);
  console.log(\`    推荐范式：\${rec.paradigm}\`);
  console.log(\`    理由：\${rec.reason}\\n\`);
});

// ---- 统计：三种范式各覆盖多少任务 ----
const counter = { Chat: 0, Inline: 0, Agent: 0, "Chat -> Agent": 0 };
taskExamples.forEach((t) => {
  const p = recommendParadigm(t).paradigm;
  counter[p] = (counter[p] || 0) + 1;
});

console.log("========================================");
console.log("  范式覆盖统计");
console.log("========================================");
Object.entries(counter).forEach(([k, v]) => {
  if (v > 0) console.log(\`  \${k}：\${v} 个任务\`);
});

console.log("\\n✅ 范式识别完成！");
console.log("实战中三种范式是互补的：Inline 用于日常书写，Chat 用于讨论方案，Agent 用于委派整块任务。");
`
  },
  {
    id: "aiapp-capabilities",
    icon: "💡",
    group: "AI编程入门",
    title: "AI 能为程序员做什么",
    content: `
# 第2章：AI 能为程序员做什么

## 2.1 八大场景全景

要让 AI 真正帮上忙，前提是知道它“在哪些环节能帮”。本章把 AI 在软件开发流程中的能力拆成八大场景，每个场景都给出：输入是什么、输出是什么、典型 prompt、注意事项。读完之后你会形成一个清晰的“什么时候该召唤 AI”的反射。

八大场景分别是：代码生成、代码补全、代码审查、代码重构、测试生成、调试辅助、文档生成、学习辅助。它们覆盖了从“写新代码”到“维护老代码”再到“自我提升”的完整链路。

\`\`\`text
需求阶段        编码阶段                维护阶段        成长阶段
   │              │                       │              │
   ▼              ▼                       ▼              ▼
[代码生成] ──> [代码补全] ──> [代码审查] ──> [文档生成]
                  │              │
                  ▼              ▼
              [测试生成]      [调试辅助]
                  │              │
                  └──> [代码重构]<──┘
                                 │
                                 ▼
                             [学习辅助]
\`\`\`

## 2.2 代码生成（Code Generation）

**输入**：自然语言描述的需求（功能、输入输出、约束）。
**输出**：一段可运行的代码（函数/类/模块）。
**典型 prompt**：

> 帮我写一个 JavaScript 函数 \`formatBytes(bytes)\`，把字节数格式化成人类可读的字符串，例如 1536 -> "1.5 KB"，要求：1) 支持 B/KB/MB/GB/TB；2) 保留 1 位小数；3) 0 字节返回 "0 B"。

**注意事项**：

1. 给出函数签名和命名约定，避免模型起名风格混乱。
2. 列出明确的约束（边界值、单位、格式），减少来回。
3. 大段生成后立刻让模型解释“为什么这么写”，便于审查。
4. 对涉及安全/并发的代码，必须人工二次审查，不能直接合并。

代码生成是 AI 编程最直观的价值点，但要警惕“看起来对”的代码。下一章会专门讲幻觉问题，这里先记住一条：**生成不等于正确**。

## 2.3 代码补全（Code Completion）

**输入**：编辑器里的当前文件、光标位置、注释或已写的部分代码。
**输出**：接下来的若干行代码（灰色提示）。
**典型 prompt**（实际是注释或上下文，开发者不直接发 prompt）：

\`\`\`javascript
// 根据用户 ID 查询订单，按时间倒序，最多 20 条
async function getRecentOrders(userId) {
  // ← 此处按 Tab，AI 会补全实现
}
\`\`\`

**注意事项**：

1. 写清晰的注释和函数签名是给 AI 最好的“prompt”，注释越具体，补全越准。
2. 不要无脑接受补全，要扫一眼逻辑，特别是涉及边界值和异常处理的部分。
3. 长补全（多行）容易“跑偏”，如果前几行就不对，直接 Esc 拒绝比硬改更快。
4. 不同模型补全风格不同，正式项目里固定一个模型，避免风格混乱。

## 2.4 代码审查（Code Review）

**输入**：一段已有代码（自己写的或别人写的）。
**输出**：问题清单 + 改进建议（性能、可读性、安全、最佳实践）。
**典型 prompt**：

> 帮我审查下面这段 Node.js 中间件代码，重点看：1) 是否有安全漏洞；2) 错误处理是否完整；3) 是否有性能隐患；4) 是否符合 Express 最佳实践。请按问题严重程度排序，每条给出具体修改建议。
>
> \`\`\`javascript
> app.use(async (req, res, next) => {
>   const token = req.headers.authorization;
>   const user = await db.query('SELECT * FROM users WHERE token = ' + token);
>   req.user = user;
>   next();
> });
> \`\`\`

**注意事项**：

1. 明确审查维度（安全/性能/可读性/规范），不然模型容易泛泛而谈。
2. 让模型按严重程度排序，方便你抓重点。
3. AI 能发现大部分“模式化”问题（SQL 注入、未处理 Promise、硬编码等），但对业务逻辑的合理性判断较弱。
4. AI Review 不能完全替代人工 Review，但能大幅减少低级问题的来回。

## 2.5 代码重构（Code Refactoring）

**输入**：一段待重构的代码 + 重构目标（提升可读性/性能/解耦）。
**输出**：重构后的代码 + 改动说明。
**典型 prompt**：

> 把下面这个 100 多行的 \`processOrder\` 函数重构成更小的、职责单一的函数。要求：1) 每个函数不超过 30 行；2) 保持外部行为不变；3) 给出每个新函数的命名理由。

**注意事项**：

1. 重构前确保有测试覆盖，重构后立刻跑测试验证行为不变。
2. 让 AI “先列出改动计划，再执行改动”，避免一次性大改难以审查。
3. 性能重构要先让 AI 给出“为什么这样改更快”的推理，再人工判断。
4. 重命名时让 AI 一次只改一个符号，避免批量改引入冲突。

## 2.6 测试生成（Test Generation）

**输入**：被测代码 + 测试框架（Jest/Vitest/Pytest 等）。
**输出**：单元测试/集成测试代码。
**典型 prompt**：

> 给下面这个 \`addBusinessDays(date, n)\` 函数写 Vitest 单元测试。要求覆盖：1) 正常情况（n=1~10）；2) 边界值（n=0、跨周末、跨月底）；3) 异常输入（n 为负、date 非法）；4) 每个测试用例名要描述清“输入-预期”关系。

**注意事项**：

1. 明确测试框架和命名风格，否则 AI 可能给 Jest 风格但你项目用的是 Vitest。
2. 要求覆盖正常/边界/异常三类，AI 默认容易只写正常路径。
3. 测试代码也要审查——AI 经常写出“测试永远通过”的无效断言。
4. 测不到的代码（如纯 IO）让 AI 标注“需要 mock”，不要硬写。

## 2.7 调试辅助（Debugging Assistance）

**输入**：报错信息 + 相关代码 + 复现步骤。
**输出**：可能的根因 + 修复方案。
**典型 prompt**：

> 运行 \`npm run build\` 时报这个错：\`SyntaxError: Cannot use import statement outside a module\`。相关代码是 \`src/utils/date.js\` 第一行 \`import dayjs from 'dayjs'\`。package.json 里 type 字段没设置。帮我分析根因，给出 3 个可能的解决方案，并说明各自的副作用。

**注意事项**：

1. 把**完整报错信息**和**复现步骤**都给 AI，不要只贴一句话错误。
2. 让 AI 给多个方案而非一个，便于你权衡副作用。
3. AI 给的方案要先理解再执行，盲执行可能让问题更复杂。
4. 复杂 bug 让 AI “先列出排查思路”，而不是直接给答案，避免被误导。

## 2.8 文档生成（Documentation Generation）

**输入**：代码 + 文档类型（JSDoc/README/API 文档）。
**输出**：符合规范的文档字符串或文档文件。
**典型 prompt**：

> 给下面这个工具函数生成 JSDoc 注释，要求：1) 描述清晰；2) 列出 @param 的类型和含义；3) @returns 写明返回值；4) @throws 列出可能的异常；5) 给一个使用示例。

**注意事项**：

1. 文档生成是 AI 最擅长的场景之一，质量通常很高，但仍要检查“描述是否准确”。
2. 让 AI 基于代码生成文档，而不是凭空写——避免文档和代码不一致。
3. 大型 README 让 AI “先列大纲，再分节写”，避免一次生成过长失控。
4. API 文档要给出请求/响应示例，AI 能基于类型定义自动生成。

## 2.9 学习辅助（Learning Assistance）

**输入**：一个概念/一段陌生代码/一个问题。
**输出**：解释、类比、由浅入深的讲解。
**典型 prompt**：

> 我是一个有 3 年 JavaScript 经验的开发者，刚接触 Rust。请用 JavaScript 开发者能理解的方式，解释 Rust 的所有权（ownership）概念，给出一个 JavaScript 中类似但不同的对照例子，并列出 3 个新手常踩的坑。

**注意事项**：

1. 告诉 AI 你的背景（会什么、不会什么），它才能给出适合你水平的解释。
2. 用“费曼方式”让 AI “像教新手一样讲”，比直接问“X 是什么”效果好。
3. 学习类对话要主动让 AI “出 3 道题考考我”，主动练习比被动看更有效。
4. AI 讲解有少量幻觉，关键概念用官方文档交叉验证。

## 2.10 八大场景对比表

把八大场景横向对比，便于你在实际工作中快速判断“这个任务该用哪个场景”：

| 场景 | 输入 | 输出 | 适合范式 | 高频坑 |
| --- | --- | --- | --- | --- |
| 代码生成 | 需求描述 | 函数/类/模块 | Chat/Agent | 看似对实则错 |
| 代码补全 | 上下文+光标 | 几行代码 | Inline | 无脑接受 |
| 代码审查 | 已有代码 | 问题清单 | Chat | 泛泛而谈 |
| 代码重构 | 代码+目标 | 重构后代码 | Chat/Agent | 行为漂移 |
| 测试生成 | 被测代码 | 测试用例 | Chat/Agent | 无效断言 |
| 调试辅助 | 报错+代码 | 根因+方案 | Chat | 信息不全 |
| 文档生成 | 代码+类型 | 文档 | Chat/Inline | 与代码不符 |
| 学习辅助 | 概念/问题 | 讲解+类比 | Chat | 关键概念幻觉 |

## 2.11 八大场景之外的“灰色地带”

除了上面八个明确场景，还有一些“灰色地带”也值得用 AI：

1. **技术选型咨询**：让 AI 列出几个候选方案的优劣对比，但最终决策必须自己做。
2. **代码翻译**：把 Python 翻成 Go、把 Class 组件翻成 Hooks，AI 表现不错。
3. **正则/SQL 生成**：这两类“声明式”代码 AI 写得比人快，但必须用真实数据测试。
4. **commit message / PR 描述**：让 AI 基于改动生成草稿，再人工润色。
5. **配置文件**：Dockerfile、CI YAML、tsconfig 等，AI 能基于需求生成初版。

灰色地带的共同特点是：**输出是“草稿”，不是“终稿”**。把 AI 当成“快速起草器”，把人当成“终审法官”，是这些场景的正确姿势。

## 2.12 本章小结

AI 能为程序员做的事情，远不止“写代码”这一项。从需求到维护再到学习，开发流程的每个环节都有 AI 的用武之地。但要记住：

1. **每个场景都有它的“坑”**——不存在“无脑用”的场景。
2. **AI 输出永远是草稿**——人必须做终审。
3. **场景之间是衔接的**——一段代码可能经历“生成→审查→重构→测试→文档”全链路，每个环节都可以让 AI 介入，但要保持人在环里。

下一章我们将深入 AI 的“内功”——LLM、Token、上下文窗口、温度这些核心概念。理解了它们，你才能从“会用 AI”进阶到“懂 AI”，在遇到怪异输出时知道背后的原因。
`,
    code: `
// =============================================================
// 第2章示例代码：八大场景任务路由器
// 输入一段任务描述，自动判断属于八大场景中的哪一个，
// 并给出该场景的“使用要点”和“典型 prompt 模板”
// 可在 Node.js 沙箱中直接运行，无外部依赖
// =============================================================

// ---- 八大场景的定义 ----
// 每个场景包含：名称、关键词、使用要点、prompt 模板
const scenarios = [
  {
    name: "代码生成",
    keywords: ["写一个", "实现一个", "帮我写", "生成一个", "新建函数"],
    tips: [
      "给出函数签名和命名约定",
      "列出明确约束（边界、单位、格式）",
      "生成后让 AI 解释思路便于审查",
    ],
    template: "帮我写一个 {函数签名}，要求：1) {约束1}；2) {约束2}；3) {边界情况}",
  },
  {
    name: "代码补全",
    keywords: ["写到一半", "接着写", "继续", "下一步", "下面应该"],
    tips: [
      "写清晰的注释和签名作为隐性 prompt",
      "前几行不对就立刻 Esc 拒绝",
      "正式项目固定一个模型避免风格混乱",
    ],
    template: "// {清晰的注释描述意图}\\nfunction {签名} {{\\n  // ← Tab 触发补全\\n}}",
  },
  {
    name: "代码审查",
    keywords: ["审查", "review", "看看这段", "有没有问题", "有什么隐患"],
    tips: [
      "明确审查维度（安全/性能/可读性/规范）",
      "让 AI 按严重程度排序",
      "AI 发现模式化问题强，业务逻辑判断弱",
    ],
    template: "帮我审查下面这段代码，重点看 {维度}，按严重程度排序，每条给出修改建议。\\n\\n\`\`\`{lang}\\n{code}\\n\`\`\`",
  },
  {
    name: "代码重构",
    keywords: ["重构", "改写", "拆分", "优化结构", "解耦"],
    tips: [
      "重构前确保有测试覆盖",
      "让 AI 先列计划再执行",
      "重命名一次只改一个符号",
    ],
    template: "把下面这段代码重构成 {目标}，要求：1) {约束1}；2) 保持外部行为不变；3) 给出改动说明。\\n\\n\`\`\`{lang}\\n{code}\\n\`\`\`",
  },
  {
    name: "测试生成",
    keywords: ["写测试", "单元测试", "test", "测试用例", "覆盖率"],
    tips: [
      "明确测试框架和命名风格",
      "要求覆盖正常/边界/异常三类",
      "警惕“永远通过”的无效断言",
    ],
    template: "给下面这个 {函数名} 写 {框架} 单元测试，覆盖正常/边界/异常三类，每个用例名描述“输入-预期”关系。\\n\\n\`\`\`{lang}\\n{code}\\n\`\`\`",
  },
  {
    name: "调试辅助",
    keywords: ["报错", "bug", "为什么", "失败", "异常", "崩溃"],
    tips: [
      "贴完整报错信息和复现步骤",
      "让 AI 给多个方案便于权衡副作用",
      "复杂 bug 让 AI 先列排查思路",
    ],
    template: "运行 {命令} 时报这个错：\\n{完整报错}\\n\\n相关代码：\\n\`\`\`{lang}\\n{code}\\n\`\`\`\\n\\n帮我分析根因，给出 3 个可能的解决方案，说明各自的副作用。",
  },
  {
    name: "文档生成",
    keywords: ["注释", "文档", "jsdoc", "readme", "说明"],
    tips: [
      "让 AI 基于代码生成而非凭空写",
      "大型 README 先列大纲再分节写",
      "API 文档要让 AI 给请求/响应示例",
    ],
    template: "给下面这个函数生成 {文档类型}，要求：1) 描述清晰；2) 列出 @param 类型；3) 给使用示例。\\n\\n\`\`\`{lang}\\n{code}\\n\`\`\`",
  },
  {
    name: "学习辅助",
    keywords: ["解释", "什么是", "怎么理解", "讲讲", "为什么这样"],
    tips: [
      "告诉 AI 你的背景",
      "用费曼方式让 AI 像教新手一样讲",
      "关键概念用官方文档交叉验证",
    ],
    template: "我是 {你的背景}，刚接触 {概念}。请用我能理解的方式解释，给出对照例子，并列出 3 个新手常踩的坑。",
  },
];

// ---- 路由器：根据任务描述匹配场景 ----
function routeScenario(taskDesc) {
  // 把任务描述转小写做匹配
  const lower = taskDesc.toLowerCase();
  let best = null;
  let bestScore = 0;

  for (const s of scenarios) {
    let score = 0;
    for (const kw of s.keywords) {
      if (lower.includes(kw.toLowerCase())) {
        // 关键词命中加分，长关键词权重更高
        score += kw.length;
      }
    }
    if (score > bestScore) {
      bestScore = score;
      best = s;
    }
  }

  // 兜底：如果没命中任何关键词，默认归到“代码生成”（最高频场景）
  if (!best) {
    best = scenarios[0];
  }
  return { scenario: best, score: bestScore };
}

// ---- 测试用例：模拟开发者真实输入 ----
const tasks = [
  "帮我写一个防抖函数 debounce，支持取消",
  "这段代码 review 一下，看看有没有 SQL 注入",
  "运行 npm test 报错：SyntaxError: Cannot use import statement outside a module",
  "给这个函数加 JSDoc 注释",
  "把这个 100 行的 processOrder 函数重构成小函数",
  "我是 3 年 JS 经验，解释一下 Rust 的所有权",
  "给 addBusinessDays 写 vitest 单元测试",
  "// 根据用户 ID 查订单，按时间倒序，下面接着写",
];

// ---- 执行路由 ----
console.log("========================================");
console.log("  八大场景任务路由器");
console.log("========================================\\n");

tasks.forEach((task, idx) => {
  const { scenario, score } = routeScenario(task);
  console.log(\`[\${idx + 1}] 任务：\${task}\`);
  console.log(\`    匹配场景：\${scenario.name}（命中权重 \${score}）\`);
  console.log("    使用要点：");
  scenario.tips.forEach((t) => console.log(\`      - \${t}\`));
  console.log(\`    Prompt 模板：\\n      \${scenario.template.replace(/\\n/g, "\\n      ")}\`);
  console.log("");
});

console.log("========================================");
console.log("  场景覆盖统计");
console.log("========================================");
const counter = {};
tasks.forEach((t) => {
  const s = routeScenario(t).scenario.name;
  counter[s] = (counter[s] || 0) + 1;
});
Object.entries(counter).forEach(([k, v]) => {
  console.log(\`  \${k}：\${v} 次\`);
});

console.log("\\n✅ 路由完成！");
console.log("实战中可以基于这套思路，把团队常见任务整理成“场景-Prompt”对照表，加速 AI 协作。");
`
  },
  {
    id: "aiapp-concepts",
    icon: "🧠",
    group: "AI编程入门",
    title: "必懂核心概念：LLM/Token/上下文窗口/温度",
    content: `
# 第3章：必懂核心概念：LLM/Token/上下文窗口/温度

## 3.1 为什么要懂这些概念

很多开发者把 AI 当黑盒用——输入需求，输出代码，能用就行。这种态度在“写小工具”时没问题，但一旦遇到下面这些情况就会卡住：

- 模型说“上下文过长”，但你不知道哪里超了。
- 同一个 prompt 多次运行结果不一样，你以为是 bug。
- 生成的代码风格过于“发散”，想让它更稳定。
- 不同模型表现差异巨大，不知道怎么选。

这些问题都指向同一组底层概念：**LLM 的工作原理、Token、上下文窗口、温度、top_p、系统提示词、stop 序列**。本章把这些概念讲透，让你从“会用 AI”升级到“懂 AI”。

## 3.2 LLM 工作原理简介

LLM（Large Language Model，大语言模型）的本质极其简单：**根据已经看到的文本，预测下一个最可能出现的 token**。就这么一遍遍重复，每次预测一个 token，拼起来就成了生成的文本。

给它“今天天气真”，它预测下一个 token 大概率是“好”；给它“function add(a, b) { return”，它预测“a +”，再预测“b”，再预测“}”。代码生成的“智能”，本质上是这种 token 预测在编程语料上学到的模式。

\`\`\`text
输入: "function add(a, b) { return"  ──▶ LLM ──▶ 预测: " a"
输入: "function add(a, b) { return a" ──▶ LLM ──▶ 预测: " +"
输入: "function add(a, b) { return a +" ──▶ LLM ──▶ 预测: " b"
... 不断重复，直到生成结束符或达到长度上限
\`\`\`

理解这一点能解释很多现象：

1. **LLM 没有“真正的思考”**：它只是概率预测，所以会一本正经地编造看起来合理的内容（幻觉）。
2. **LLM 是无状态的**：每次调用都是独立的，所谓“记忆对话”只是把历史消息一起塞进输入。
3. **LLM 不会“查资料”**：除非接了工具（RAG、搜索），它只能基于训练数据回答，所以会有“知识截止日期”。
4. **LLM 的能力来自训练数据 + 模型规模**：在大量代码上训练的大模型，才能写出符合工程规范的代码。

## 3.3 Token 是什么

**Token 是 LLM 处理文本的最小单位**。它既不是字符也不是词，而是介于两者之间的一种“子词”单元。模型内部所有计算都以 token 为单位——输入是 token 序列，输出也是 token 序列。

**英文 token 估算**：

- 1 个 token ≈ 4 个英文字符。
- 1 个常见英文单词 ≈ 1.3 个 token。
- 1000 个 token ≈ 750 个英文单词。

\`\`\`text
"hello"           -> 1 token
"function"        -> 1 token
"understanding"   -> 1 token（常见词）
"ununderstandable"-> 3 token（un + understand + able）
\`\`\`

**中文 token 估算**：

中文的 token 化因模型而异，但大致规律是：1 个汉字 ≈ 1.5-2 个 token。也就是说，一段 1000 字的中文，可能消耗 1500-2000 个 token。这也是为什么中文长文本更容易触达上下文窗口上限。

\`\`\`text
"你好"     -> 大约 2 token
"大语言模型" -> 大约 6-8 token（每个字 1-2 token）
\`\`\`

**代码 token 估算**：

代码的 token 化对开发者更友好——常见关键字（function、return、if）通常是 1 个 token，符号（(){};）也较省。但变量名、字符串字面量会按子词切分。粗略估算：1 行 JavaScript 代码 ≈ 10-20 个 token。

**为什么 token 对开发者重要**：

1. **计费按 token**：API 价格通常是“每 1K token 多少钱”，输入输出分别计费。一个超长 prompt 可能比你想象贵 10 倍。
2. **上下文窗口以 token 为单位**：模型一次能处理的最大 token 数叫“上下文窗口”，超了就会截断或报错。
3. **优化 token 能省钱省时间**：把无关代码从 prompt 里删掉、用引用代替贴长代码，都能减少 token 消耗。

## 3.4 上下文窗口（Context Window）

**上下文窗口**是模型一次调用能处理的 token 数上限，包含输入（你的 prompt + 历史 + 文件）和输出（模型生成的回答）。可以把它想象成模型的“短期记忆容量”。

\`\`\`text
┌─────────────── 上下文窗口（例如 200K token）───────────────┐
│  系统提示词 │  历史对话 │  当前文件 │  你的问题 │  模型输出  │
└──────────────────────────────────────────────────────────┘
                          ↑
                  所有部分共享这个上限
\`\`\`

上下文窗口的意义：

1. **决定了能喂多少代码**：128K 窗口大约能装下 3-5 万行代码，足以覆盖一个中型项目的多个核心模块。但 8K 窗口连一个稍大的文件都装不下。
2. **影响多轮对话长度**：每轮对话都会累积进上下文，对话太长后期会“忘”前面的内容（被截断）。
3. **影响 Agent 任务复杂度**：Agent 多步执行时，每步的“观察结果”都进上下文，复杂任务可能很快撑满窗口。

下表是主流模型在 2025-2026 年的上下文窗口大小对比（数据为公开标称值，实际可用因厂商策略可能略有差异）：

| 模型 | 上下文窗口 | 大约能装下的代码量 |
| --- | --- | --- |
| GPT-4o | 128K | 3-4 万行代码 |
| GPT-4.1 | 1M | 25-30 万行代码 |
| Claude 3.5 Sonnet | 200K | 5-6 万行代码 |
| Claude 4 Sonnet（含长上下文） | 200K-1M | 5-30 万行代码 |
| Gemini 2.5 Pro | 2M | 50-60 万行代码 |
| DeepSeek V3 | 128K | 3-4 万行代码 |
| Qwen3-Max | 256K-1M | 6-30 万行代码 |

**上下文窗口不是越大越好**：

1. **越长越贵**：长上下文的 API 调用费用成比例上升。
2. **越长越慢**：处理 1M token 比 128K 慢数倍，影响响应体验。
3. **“中间被遗忘”现象**：研究表明，超长上下文里位于中间位置的信息，被模型引用的概率会下降（lost in the middle 现象）。
4. **真正需要长上下文的场景不多**：大部分日常开发任务，10-30K token 就够了。需要 200K+ 的场景主要是“喂整个代码库做问答”。

## 3.5 温度（Temperature）与 top_p

**温度（temperature）** 是控制生成随机性的参数，取值范围通常是 0 到 2（不同厂商略有差异）。温度越高，输出越“发散”；温度越低，输出越“确定”。

\`\`\`text
temperature = 0.0  ->  每次都选概率最高的 token，输出几乎确定
temperature = 0.7  ->  默认值，平衡稳定性和多样性
temperature = 1.0  ->  完全按概率分布采样，输出多样
temperature = 1.5+ ->  偶尔选低概率 token，输出可能"放飞自我"
\`\`\`

**top_p**（核采样）是另一个控制随机性的参数，它从概率累积的角度限制候选 token：只从累积概率达到 p 的最可能 token 中采样。temperature 和 top_p 通常二选一调整，不要同时改。

**温度对代码生成的影响**：

| 温度 | 行为特征 | 适合场景 | 风险 |
| --- | --- | --- | --- |
| 0.0 | 几乎确定，每次输出一致 | 生产代码、Bug 修复、单元测试 | 容易陷入次优解，缺乏灵活性 |
| 0.2-0.4 | 稳定但略有变化 | 日常编码、文档生成 | 较低，推荐区间 |
| 0.5-0.7 | 多样性较高 | 头脑风暴、写示例、创意任务 | 可能生成不规范代码 |
| 0.8-1.0 | 高度发散 | 探索性提问、写诗、写文案 | 代码不可控，慎用 |
| >1.0 | 极度发散 | 几乎不用于编程 | 输出可能完全无效 |

**实战建议**：

1. **写生产代码用 0-0.3**：保证稳定可复现，便于审查。
2. **写测试用例用 0.3-0.5**：既要稳定，也要覆盖不同思路。
3. **写文档/示例用 0.5-0.7**：让语言更自然，避免千篇一律。
4. **遇到“模型老给同一种错答案”时调高到 0.5**：可能跳出局部最优。
5. **遇到“模型每次都不一样难复现”时调低到 0**：便于定位问题。

## 3.6 系统提示词（System Prompt）

**系统提示词**是调用 LLM API 时一条特殊角色的消息，用于设定模型的行为框架。它对用户不可见，但会全程影响模型的所有回答。

\`\`\`text
messages: [
  { role: "system",    content: "你是一个资深 Node.js 工程师..." },
  { role: "user",      content: "帮我写一个中间件..." },
  { role: "assistant", content: "..." },
  ...
]
\`\`\`

系统提示词的作用：

1. **设定角色**：“你是一个 TypeScript 专家”“你是一个 Rust 资深工程师”等，让模型用相应领域的最佳实践回答。
2. **设定风格**：“代码必须有 JSDoc 注释”“输出用中文，代码用英文”等，统一输出格式。
3. **设定约束**：“不要使用第三方库”“只回答与 X 相关的问题”等，限制模型行为边界。
4. **设定流程**：“先列出改动计划，再执行改动”“回答前先复述我的需求”等，强制模型按步骤来。

**系统提示词的写作要点**：

1. **简洁明确**：长系统提示词会占用上下文，且模型可能“忘”后半部分。
2. **用肯定句**：“用 JSDoc 注释”比“不要没注释”更有效。
3. **关键约束放前面**：模型对前面的内容更敏感。
4. **示例比规则强**：给一个目标输出的例子，胜过写十条规则。

在 Cursor、Trae 等工具里，.cursorrules、.trae/rules 等项目级规则文件本质上就是项目级系统提示词，所有对话都会带上它。

## 3.7 stop 序列

**stop 序列**是一组字符串，模型生成时一旦遇到这些字符串就立即停止。它用于控制输出边界。

\`\`\`text
prompt: "写一个 add 函数，只输出代码："
stop: ["\\n\\n", "\`\`\`"]
// 模型生成到第一个空行或代码块结束符就停
\`\`\`

stop 序列的典型用途：

1. **限制输出长度**：让模型只生成代码不生成解释，遇到 \`\`\` 就停。
2. **结构化输出**：让模型按 JSON 格式输出，遇到“}”就停（注意要小心嵌套）。
3. **多轮对话控制**：让模型生成到“Human:”就停，防止它自己编造下一轮用户输入。

## 3.8 综合实战：一次 API 调用都包含什么

把上面所有概念串起来，看一次典型的 AI 编程 API 调用：

\`\`\`javascript
const response = await llm.chat({
  model: "claude-3.5-sonnet",          // 选模型
  messages: [
    { role: "system", content: "你是 TypeScript 专家，代码必须有 JSDoc 注释" },
    { role: "user",   content: \`帮我写一个 \${userReq}\` },
  ],
  temperature: 0.3,                     // 偏稳定，便于审查
  top_p: 1,                             // 不用 top_p 控制
  max_tokens: 2000,                     // 限制输出长度
  stop: ["\\n\\n\\n"],                  // 遇到三个空行就停
});
\`\`\`

这次调用涉及的概念：

- 系统提示词：设定 TypeScript 专家角色 + JSDoc 约束。
- 上下文窗口：system + user 消息 + 输出，都不能超过模型的窗口（如 200K）。
- token 计费：system + user 是输入 token，输出是输出 token，分别计费。
- 温度 0.3：偏稳定，适合写生产代码。
- max_tokens：限制输出不超过 2000 token，防止失控。
- stop 序列：遇到三个空行停，避免模型继续编造后续内容。

理解这次调用的每一个参数，你就掌握了 AI 编程的“控制面板”。

## 3.9 不同模型的取舍

主流模型在编程场景下各有特点，选型时要综合考虑：

| 模型 | 编程能力 | 上下文 | 速度 | 价格 | 适合场景 |
| --- | --- | --- | --- | --- | --- |
| Claude 3.5/4 Sonnet | 顶级 | 200K-1M | 中 | 中 | 复杂项目、长上下文、Agent |
| GPT-4o/4.1 | 顶级 | 128K-1M | 快 | 中 | 通用编程、Chat 模式 |
| Gemini 2.5 Pro | 强 | 2M | 中 | 低 | 超长上下文、跨文件分析 |
| DeepSeek V3 | 强 | 128K | 快 | 极低 | 成本敏感场景 |
| Qwen3-Max | 强 | 256K-1M | 快 | 低 | 中文场景、国内部署 |

选型建议：

1. **日常 IDE 编码**：Claude Sonnet 或 GPT-4o，质量最稳。
2. **长项目分析**：Gemini 2.5 Pro（2M 窗口）或 Claude 长上下文版。
3. **批量任务**：DeepSeek/Qwen，成本只有头部模型的 1/10。
4. **Agent 自主执行**：Claude Sonnet，工具调用稳定性最好。
5. **中文场景**：Qwen/DeepSeek，中文理解和生成更自然。

## 3.10 本章小结

| 概念 | 一句话理解 | 实战要点 |
| --- | --- | --- |
| LLM | 概率预测下一个 token | 没有真正思考，会幻觉 |
| Token | 文本最小单位 | 中文比英文费 token，影响计费和窗口 |
| 上下文窗口 | 一次能处理多少 token | 不是越大越好，够用即可 |
| 温度 | 控制随机性 | 生产代码 0-0.3，创意任务 0.5-0.7 |
| top_p | 核采样控制 | 和温度二选一调整 |
| 系统提示词 | 设定模型行为框架 | 简洁、肯定句、关键约束放前 |
| stop 序列 | 控制输出边界 | 用于限制长度和结构化输出 |

掌握这些概念后，你就能解释 90% 的“AI 为什么这样回答”的疑惑，也能在遇到怪异输出时知道该调哪个参数。下一章我们将直面 AI 的“阿喀琉斯之踵”——能力边界与幻觉，告诉你什么时候该信、什么时候绝不能信。
`,
    code: `
// =============================================================
// 第3章示例代码：Token 估算器与上下文窗口模拟器
// 演示如何估算文本的 token 数，并模拟上下文窗口的截断行为
// 可在 Node.js 沙箱中直接运行，无外部依赖
// =============================================================

// ---- Token 估算函数 ----
// 真实 token 化由模型分词器决定，这里用一个启发式估算：
// - 英文：约 4 字符 / token
// - 中文：约 1.5-2 字符 / token（这里按 0.6 token/字符，即 1 字符约 1.7 token）
// - 代码：常见关键字算 1 token，其余按子词估算
function estimateTokens(text) {
  if (!text) return 0;

  let tokens = 0;
  // 用正则把文本切成“英文单词 / 中文字符 / 代码符号”三类
  // [\u4e00-\u9fa5] 匹配单个中文字符
  // [a-zA-Z_][a-zA-Z0-9_]* 匹配英文单词或标识符
  // \\s+ 匹配空白
  // [^\\s\\w\u4e00-\u9fa5] 匹配其它符号（代码符号、标点）
  const pieces = text.match(/[\u4e00-\u9fa5]|[a-zA-Z_][a-zA-Z0-9_]*|\\s+|[^\\s\\w\u4e00-\u9fa5]/g) || [];

  // 常见代码关键字集合，命中算 1 token
  const keywords = new Set([
    "function", "return", "if", "else", "for", "while", "const", "let", "var",
    "class", "import", "export", "from", "default", "async", "await", "new",
    "try", "catch", "finally", "throw", "typeof", "instanceof", "true", "false",
    "null", "undefined", "this", "super", "extends", "implements",
  ]);

  for (const p of pieces) {
    if (/^\s+$/.test(p)) {
      // 空白不计 token（实际上分词器可能合并，但估算简化）
      continue;
    }
    if (/^[\u4e00-\u9fa5]$/.test(p)) {
      // 中文字符：约 1.7 token
      tokens += 1.7;
    } else if (keywords.has(p)) {
      // 常见关键字：1 token
      tokens += 1;
    } else if (/^[a-zA-Z_]/.test(p)) {
      // 英文单词/标识符：常见词 1 token，长词按子词切分
      if (p.length <= 6) tokens += 1;
      else if (p.length <= 12) tokens += 1.5;
      else tokens += Math.ceil(p.length / 4);
    } else {
      // 符号：约 0.5-1 token
      tokens += 0.5;
    }
  }

  return Math.ceil(tokens);
}

// ---- 上下文窗口模拟器 ----
// 模拟把多段内容塞进有限窗口的行为：超出的部分被截断
class ContextWindow {
  constructor(modelName, maxTokens) {
    this.modelName = modelName;
    this.maxTokens = maxTokens;
    this.messages = []; // 每条消息：{ role, content, tokens }
  }

  // 添加一条消息
  add(role, content) {
    const tokens = estimateTokens(content);
    this.messages.push({ role, content, tokens });
    return this;
  }

  // 模拟模型“看到”的内容：保留系统提示词 + 末尾若干条，中间可能被截断
  // 真实模型截断策略各异，这里演示“保留 system + 最后 N 条”的简单策略
  getView() {
    let used = 0;
    const view = [];

    // 1. 先把所有 system 消息加进来（系统提示词优先级最高）
    for (const m of this.messages) {
      if (m.role === "system") {
        if (used + m.tokens > this.maxTokens) {
          // 系统提示词过长，截断尾部
          const remain = this.maxTokens - used;
          view.push({ ...m, content: m.content.slice(0, remain * 4) + "...[截断]", tokens: remain });
          used = this.maxTokens;
          break;
        }
        view.push(m);
        used += m.tokens;
      }
    }

    // 2. 从后往前加 user/assistant 消息，直到撑满
    const nonSystem = this.messages.filter((m) => m.role !== "system");
    for (let i = nonSystem.length - 1; i >= 0; i--) {
      const m = nonSystem[i];
      if (used + m.tokens > this.maxTokens) {
        // 撑满了，标记前面的消息“被截断”
        const truncated = nonSystem.slice(0, i);
        if (truncated.length > 0) {
          view.push({
            role: "system",
            content: \`...[\${truncated.length} 条历史消息因超出窗口被截断]...\`,
            tokens: estimateTokens(\`...[\${truncated.length} 条历史消息因超出窗口被截断]...\`),
          });
        }
        break;
      }
      view.push(m);
      used += m.tokens;
    }

    return { usedTokens: used, maxTokens: this.maxTokens, view };
  }
}

// ---- 测试：估算不同类型文本的 token ----
console.log("========================================");
console.log("  Token 估算器");
console.log("========================================\\n");

const samples = [
  { label: "英文短句", text: "Hello world, this is a token estimation test." },
  { label: "中文短句", text: "你好世界，这是一个 token 估算测试。" },
  { label: "JS 代码", text: "function add(a, b) {\\n  return a + b;\\n}" },
  { label: "长中文段落", text: "大语言模型通过预测下一个 token 来生成文本。理解 token 是使用 AI 的基础，因为它直接影响计费和上下文窗口。" },
];

samples.forEach((s) => {
  const t = estimateTokens(s.text);
  console.log(\`[\${s.label}]\`);
  console.log(\`  文本：\${s.text.replace(/\\n/g, " ")}\`);
  console.log(\`  字符数：\${s.text.length}\`);
  console.log(\`  估算 token：\${t}\`);
  console.log(\`  token/字符：\${(t / s.text.length).toFixed(2)}\\n\`);
});

// ---- 测试：模拟上下文窗口截断 ----
console.log("========================================");
console.log("  上下文窗口模拟器");
console.log("========================================\\n");

// 模拟一个 8K 窗口的小模型（早期 GPT-3.5 即如此）
const smallWindow = new ContextWindow("small-model-8k", 8000);
smallWindow.add("system", "你是一个 TypeScript 专家。");
// 模拟一个长对话
for (let i = 1; i <= 20; i++) {
  smallWindow.add("user", \`第 \${i} 轮：帮我写一个函数，需求是 \${"需求".repeat(50)}\`);
  smallWindow.add("assistant", \`好的，这是第 \${i} 个函数：function f\${i}() { /* \${"实现".repeat(80)} */ }\`);
}

const view1 = smallWindow.getView();
console.log(\`模型：\${smallWindow.modelName}（窗口 \${smallWindow.maxTokens} token）\`);
console.log(\`原始消息数：\${smallWindow.messages.length}\`);
console.log(\`实际可见消息数：\${view1.view.length}\`);
console.log(\`实际使用 token：\${view1.usedTokens} / \${view1.maxTokens}\`);
console.log(\`截断情况：\${view1.view.find((m) => m.content.includes("截断")) ? "有截断" : "无截断"}\\n\`);

// 模拟一个 200K 窗口的大模型（Claude 3.5 Sonnet）
const bigWindow = new ContextWindow("claude-3.5-sonnet-200k", 200000);
bigWindow.add("system", "你是一个 TypeScript 专家。");
for (let i = 1; i <= 20; i++) {
  bigWindow.add("user", \`第 \${i} 轮：帮我写一个函数，需求是 \${"需求".repeat(50)}\`);
  bigWindow.add("assistant", \`好的，这是第 \${i} 个函数：function f\${i}() { /* \${"实现".repeat(80)} */ }\`);
}

const view2 = bigWindow.getView();
console.log(\`模型：\${bigWindow.modelName}（窗口 \${bigWindow.maxTokens} token）\`);
console.log(\`原始消息数：\${bigWindow.messages.length}\`);
console.log(\`实际可见消息数：\${view2.view.length}\`);
console.log(\`实际使用 token：\${view2.usedTokens} / \${view2.maxTokens}\`);
console.log(\`截断情况：\${view2.view.find((m) => m.content.includes("截断")) ? "有截断" : "无截断"}\\n\`);

console.log("========================================");
console.log("  模型上下文窗口对比");
console.log("========================================");
const models = [
  { name: "GPT-4o", window: 128000 },
  { name: "Claude 3.5 Sonnet", window: 200000 },
  { name: "Claude 4 Sonnet 长上下文", window: 1000000 },
  { name: "Gemini 2.5 Pro", window: 2000000 },
  { name: "DeepSeek V3", window: 128000 },
];
models.forEach((m) => {
  console.log(\`  \${m.name.padEnd(28)} \${(m.window / 1000).toFixed(0).padStart(4)}K token\`);
});

console.log("\\n✅ 估算与模拟完成！");
console.log("实战中可以用类似的估算函数预判 prompt 是否会超窗口，避免 API 报错。");
`
  },
  {
    id: "aiapp-boundaries",
    icon: "⚠️",
    group: "AI编程入门",
    title: "AI 的能力边界与局限",
    content: `
# 第4章：AI 的能力边界与局限

## 4.1 为什么要谈“不能做什么”

前面几章讲了 AI 编程的强大能力，但任何工具的价值都不仅取决于“能做什么”，更取决于“不能做什么”。**只有清楚边界，才能安全地用**。把 AI 当成无所不能的“神”，会让你的项目埋下无数隐患；把 AI 当成有明确边界的“助手”，反而能最大化它的价值。

本章的目标不是泼冷水，而是给你一份“AI 能力地图”——哪里是安全区可以放手用，哪里是警戒区要审慎用，哪里是禁区绝不能让 AI 独立完成。

## 4.2 AI 擅长什么

理解 AI 擅长什么，本质上是理解“模式化任务”这个概念。AI 的能力来自训练数据中的统计规律，所以它在“有大量公开样例、规则清晰、可验证”的任务上表现最好。

**1. 模式化代码生成**

样板代码、CRUD 接口、数据校验、表单处理、状态管理 reducer……这些代码结构高度重复，AI 见过无数样例，生成质量极高。一段 50 行的 Express 路由，AI 几秒钟就能写出来，质量不输熟练工。

**2. 文档与注释**

JSDoc、README、API 文档、注释——这些“用自然语言描述代码”的任务是 AI 的强项。它能基于代码生成准确的文档，也能基于文档反推代码框架。

**3. 单元测试**

只要被测函数逻辑清晰，AI 能生成覆盖正常/边界/异常的测试用例。特别是配合 Vitest、Pytest 这类有明确模式的框架，AI 表现稳定。

**4. 代码解释与教学**

把陌生代码贴给 AI，让它解释每一段在做什么、整体流程是什么、为什么这么设计——AI 表现近乎完美。这是新手学习陌生代码库的最佳途径。

**5. 算法与数据结构题**

LeetCode 难度的题目，AI 大部分能解。这是因为它在训练时见过海量算法题。

**6. 跨语言翻译**

把 Python 翻成 Go、把 Class 组件翻成 Hooks，AI 的“翻译”能力很强，因为不同语言的等价模式在训练数据中重复出现。

**7. 正则与 SQL**

这两类声明式代码 AI 写得又快又准，因为模式固定、可验证。

**8. 通用最佳实践**

“Node.js 项目应该怎么组织目录”“React 性能优化的常见手段”“TypeScript 严格模式怎么配”——这类有标准答案的问题，AI 回答质量很高。

**9. 已知的 Bug 模式**

SQL 注入、XSS、未处理 Promise、内存泄漏——AI 能识别这些已知模式并给出修复建议。

**10. 重构小改动**

提取函数、重命名、消除魔法数字、改 if-else 为 switch——这些“局部小重构”AI 做得既快又稳。

## 4.3 AI 不擅长什么

AI 的弱项集中在“需要外部信息、需要业务判断、需要精确计算”的任务上。

**1. 最新 API 与框架变更**

模型的训练数据有截止日期（通常是几个月到一年前），新发布的 API、刚改破坏性更新的框架，AI 不知道。比如某库 v3 改了 API，AI 可能还按 v2 的写法生成，看起来对实则运行就报错。

**2. 复杂业务逻辑**

“这个订单的状态机要支持‘部分退款’‘商家拒绝后用户申诉’‘平台介入’等多种流转”——这种业务规则只存在于你的需求文档和团队共识里，AI 完全不知道。它生成的“看起来合理”的逻辑，往往不符合实际业务。

**3. 大型架构决策**

“这个系统该用微服务还是单体？”“该上 Kafka 还是 RabbitMQ？”“该用 SSR 还是 CSR？”这些决策需要结合团队规模、业务特点、运维能力、长期成本综合判断，AI 给的“通用建议”往往过于理论化，不落地。

**4. 精确数值计算**

LLM 本质是预测下一个 token，不是计算器。让它算“1234567 * 8910”很可能给出错误答案。涉及精确数学的任务（金融计算、加密、统计），绝不能让 AI 直接算，必须让它生成调用计算库的代码。

**5. 领域特定知识**

医疗、法律、金融等高度专业的领域，AI 会一本正经地编造看起来专业实则错误的“知识”。这些领域一旦出错代价巨大，必须人工复核。

**6. 隐式约束**

“这个字段不能给前端返回，因为有 PII”“这个接口要走单独的灰度网关”“这个表的写操作必须经过审批服务”——这些隐式约束只存在于团队规范里，AI 完全不知道，会“合规地”写出违规代码。

**7. 实时信息**

“现在线上 Redis 还连得上吗？”“今天的支付成功率是多少？”——AI 不能联网（除非接了工具），不能查实时数据。

**8. 长链推理**

“因为这个字段下游 A 服务要用来做 X，所以这里必须改成 Y，但 Y 又会影响 B 服务的 Z 逻辑，所以……”超过 3-4 步的长链推理，AI 经常在中间某步出错，导致结论完全相反。

**9. 创造性架构设计**

真正创新的架构（比如第一个发明 Redux 的人、第一个发明 React Hooks 的人），AI 做不到。它擅长组合已知模式，不擅长从 0 到 1 的范式创造。

**10. 心理与人性判断**

“这个 UX 改动用户会不会反感？”“这个文案会不会让用户觉得被冒犯？”——这些需要同理心和用户研究，AI 给的建议往往偏理论。

## 4.4 幻觉问题

**幻觉（Hallucination）** 是 LLM 最致命的缺陷：模型自信地生成事实上错误的内容。它不是“不知道”，而是“不知道自己不知道”，一本正经地编造。

**幻觉的典型表现**：

1. **编造不存在的 API**：\`array.last()\`（实际不存在）、\`Object.deepClone()\`（不存在）、\`fs.readFileAsync()\`（不存在）。
2. **引用不存在的库**：\`import { debounce } from 'lodash-debounce'\`（实际是 lodash 或 lodash.debounce）。
3. **过时的 API 用法**：还在用 React 16 的方式、Node 12 的 API。
4. **编造文档链接**：给一个看起来像官方文档的 URL，点进去 404。
5. **编造参数与返回值**：函数实际接受 2 个参数，AI 说接受 3 个。
6. **编造历史事件/人物**：在解释概念时穿插不存在的引用。

**幻觉的成因**：

1. **训练数据的噪声**：互联网上有大量错误代码，模型学到了错误模式。
2. **统计概率压过事实**：模型选“看起来最像”的 token，而非“事实正确”的 token。
3. **缺乏事实校验机制**：LLM 内部没有“这个 API 是否存在”的校验器。
4. **过强的角色扮演**：当被设定为“专家”时，模型更倾向于“给出答案”而非“承认不知道”。

**识别幻觉的方法**：

1. **看代码能不能跑**：最直接的验证，跑不通就是有问题。
2. **查官方文档**：对编造的 API/参数，去官方文档查证。
3. **看模型语气**：过于自信、缺乏“我不确定”的表述，要提高警惕。
4. **多模型交叉验证**：同一个 prompt 问 2-3 个模型，结果差异大时大概率有幻觉。
5. **看引用**：AI 给的论文/链接，去原始来源核对。

## 4.5 知识截止日期

每个模型都有训练数据的“截止日期”，截止日期之后发生的事情，模型不知道。这会导致几个典型问题：

1. **新框架/新版本不知道**：2025 年发布的库，模型如果训练截止是 2024 年，就完全不知道。
2. **API 已废弃但 AI 还在用**：很多库会废弃旧 API，AI 仍按废弃前的用法生成。
3. **安全漏洞已修复但 AI 仍写旧版本**：AI 可能生成有已知漏洞的代码。
4. **新语法不支持**：语言新特性（如装饰器、新 JSX 转换）AI 可能不熟悉。

**应对策略**：

1. **给 AI 喂最新文档**：在 prompt 里贴一段最新官方文档，让 AI 基于文档而非记忆生成。
2. **用 RAG**：把项目依赖的文档建索引，让 AI 检索后生成。
3. **接工具**：让 AI 联网搜索最新信息（如 Trae/Cursor 的 Web Search）。
4. **人工核对版本**：AI 给出的依赖版本号一定要去 npm/pypi 核对是否存在。

## 4.6 为什么不能完全信任 AI

“AI 给的代码看起来对”和“AI 给的代码真的对”之间，有一道容易忽略的鸿沟。下面几条是核心原因：

**1. AI 不理解你的业务**

AI 写的“登录函数”不知道你的登录要支持 SSO、要兼容 LDAP、要满足企业客户的合规要求。它生成的是“通用登录”，不是“你的登录”。

**2. AI 不理解你的代码库**

即使把整个项目喂给 AI，它也不真正理解项目演进史、为什么某段代码这么写、哪些是不能改的兼容逻辑。它看到的只是“当前状态的文本”。

**3. AI 不知道隐式约束**

PII 字段、灰度网关、审批流、内部 SLA——这些约束没有写在代码里，AI 不知道，会“合规地”写出违规代码。

**4. AI 的“正确”是概率正确**

AI 生成的代码可能“95% 的场景对”，但 5% 的边界场景会崩。生产环境里这 5% 就是事故。

**5. AI 不能承担责任**

代码上线出问题，AI 不会背锅，背锅的是提交代码的你。所以最终决策权必须在你手里。

**6. AI 会被 prompt 误导**

如果你问题描述有偏差，AI 会按偏差生成“看起来对”的代码。它没有“等等，你这个需求是不是搞反了”的判断力（少数情况会反问，但不可靠）。

## 4.7 常见失败模式案例

下面几个案例是真实项目中 AI 编程的典型失败模式，每个都附上“为什么会失败”和“如何避免”。

**案例 1：编造 API**

> 任务：用 JavaScript 获取数组最后一个元素。
> AI 输出：\`arr.last()\`

**为什么失败**：JavaScript 数组没有 \`last()\` 方法（直到 ES2025 才加入 \`Array.prototype.at()\`，部分环境有 \`last()\` 提案但未标准化）。AI 把 Python 的 \`list[-1]\` 或 lodash 的 \`_.last()\` 记成了原生方法。

**如何避免**：跑一遍代码立刻发现；或要求 AI“只用标准 ES2022 API”。

**案例 2：过时框架用法**

> 任务：用 React 写一个表单。
> AI 输出：用 class 组件 + \`this.setState\`。

**为什么失败**：现代 React 项目用函数组件 + Hooks，但 AI 训练数据里有大量 class 组件样例，可能默认输出旧写法。

**如何避免**：在系统提示词里写“只用函数组件 + Hooks，禁止 class 组件”。

**案例 3：编造依赖**

> 任务：实现一个深拷贝。
> AI 输出：\`import deepClone from 'js-deep-clone'\`

**为什么失败**：\`js-deep-clone\` 这个包不存在（或名字相近但不是 AI 说的那个）。AI 编造了一个看起来合理的包名。

**如何避免**：让 AI“只用 lodash 的 cloneDeep 或自己实现”；任何 import 的包都要去 npm 核对。

**案例 4：业务逻辑幻觉**

> 任务：实现订单退款。
> AI 输出：直接调 \`order.refund()\`，退款金额全额返回。

**为什么失败**：实际业务里退款要支持部分退款、要扣手续费、要走审批流、要更新财务系统。AI 不知道这些，生成了“看起来合理但完全不符合业务”的逻辑。

**如何避免**：复杂业务逻辑不要让 AI“直接写”，而是让它“先列业务规则清单”，人工确认规则后再写。

**案例 5：长链推理错误**

> 任务：因为 A 字段下游要算 B，所以这里改 C，但要保证 D 不受影响。
> AI 输出：改了 C，但漏掉了“保证 D 不受影响”的检查。

**为什么失败**：4 步以上的长链推理 AI 经常丢步。它“听懂”了前 3 步，但忘了第 4 步约束。

**如何避免**：长链任务拆成多个小任务，每步独立完成并验证；或让 AI“先复述完整需求”，确认后再写。

**案例 6：精确计算错误**

> 任务：计算 0.1 + 0.2。
> AI 输出：0.3（实际 JS 里是 0.30000000000000004）。

**为什么失败**：LLM 是预测 token，不是计算器，它给出“数学上正确”的答案，但忽略了浮点数精度问题。

**如何避免**：精确计算必须让 AI 写代码（\`Math.round((0.1 + 0.2) * 1e10) / 1e10\`），而不是让它直接算。

**案例 7：安全幻觉**

> 任务：写一个 SQL 查询。
> AI 输出：\`SELECT * FROM users WHERE name = '\${userName}'\`（字符串拼接）。

**为什么失败**：AI 知道 SQL 注入，但在“示例代码”场景下为了简洁用了拼接，埋下注入隐患。

**如何避免**：在系统提示词里写“所有 SQL 必须用参数化查询，禁止字符串拼接”。

## 4.8 如何验证 AI 输出

把 AI 输出当代码评审的对象，而不是“成品”。验证清单：

1. **能跑通吗**：本地或沙箱里跑一遍，最直接的验证。
2. **边界对吗**：空输入、超长输入、负数、null、undefined、并发场景。
3. **API 真实存在吗**：所有 import 的包、调用的 API，去官方文档核对。
4. **依赖版本对吗**：package.json 里的版本号去 npm 核对。
5. **安全吗**：SQL 注入、XSS、CSRF、未授权访问、敏感信息日志。
6. **性能可接受吗**：复杂度、N+1 查询、内存占用。
7. **符合业务约束吗**：业务规则、合规要求、SLA。
8. **测试覆盖了吗**：让 AI 补测试，再人工审查测试是否有效。
9. **可维护吗**：命名、结构、注释、是否符合项目规范。
10. **能回滚吗**：改动如果出问题，能快速回滚吗。

## 4.9 安全区、警戒区、禁区

把任务按风险分三档：

| 风险等级 | 任务类型 | 处理方式 |
| --- | --- | --- |
| 安全区 | 模式化代码、文档、测试、解释 | AI 生成 + 轻量审查 |
| 警戒区 | 业务逻辑、重构、依赖升级 | AI 生成 + 严格审查 + 测试 |
| 禁区 | 安全相关、合规相关、精确计算、生产数据 | AI 仅做参考，人工主导 |

## 4.10 本章小结

AI 的能力边界不是“缺陷”，而是“特性”。知道这些边界，你才能：

1. **在安全区大胆用**：把模式化工作交给 AI，自己专注高价值环节。
2. **在警戒区审慎用**：让 AI 起草，但人工严格审查。
3. **在禁区克制用**：AI 只做参考，绝不让它独立完成。

下一章我们将从“能力”转向“思维”——AI 时代的程序员该怎么思考、怎么工作、怎么成长，才能不被淘汰反而借势起飞。
`,
    code: `
// =============================================================
// 第4章示例代码：AI 输出风险评估器
// 输入一段 AI 生成的代码 + 任务描述，自动评估风险等级
// 并给出验证清单，演示“安全区/警戒区/禁区”的判定逻辑
// 可在 Node.js 沙箱中直接运行，无外部依赖
// =============================================================

// ---- 风险等级定义 ----
const RiskLevel = {
  SAFE: "安全区",      // 模式化任务，轻量审查即可
  WARNING: "警戒区",   // 业务/重构类，需严格审查
  DANGER: "禁区",      // 安全/合规/精确计算，人工主导
};

// ---- 任务特征 ----
// 一段 AI 输出包含：任务类型、是否涉及安全、是否涉及业务逻辑、是否涉及精确计算等
const sampleOutputs = [
  {
    name: "JSDoc 注释生成",
    taskType: "documentation",
    involvesSecurity: false,
    involvesBusiness: false,
    involvesPrecision: false,
    code: \`/**
 * 计算两个数的和
 * @param {number} a 第一个数
 * @param {number} b 第二个数
 * @returns {number} 两数之和
 */\`,
  },
  {
    name: "Express 登录路由",
    taskType: "feature",
    involvesSecurity: true,    // 涉及认证
    involvesBusiness: false,
    involvesPrecision: false,
    code: \`app.post('/login', async (req, res) => {
  const { username, password } = req.body;
  const user = await db.query('SELECT * FROM users WHERE name = ' + username);
  if (user.password === password) {
    res.json({ token: createToken(user) });
  }
});\`,
  },
  {
    name: "订单退款逻辑",
    taskType: "feature",
    involvesSecurity: false,
    involvesBusiness: true,     // 涉及业务规则
    involvesPrecision: false,
    code: \`function refund(order) {
  order.status = 'refunded';
  order.refundAmount = order.totalAmount;
  return saveOrder(order);
}\`,
  },
  {
    name: "金融利息计算",
    taskType: "calculation",
    involvesSecurity: false,
    involvesBusiness: true,
    involvesPrecision: true,    // 涉及精确计算
    code: \`function calculateInterest(principal, rate, days) {
  return principal * rate * days / 365;
}\`,
  },
  {
    name: "正则表达式",
    taskType: "pattern",
    involvesSecurity: false,
    involvesBusiness: false,
    involvesPrecision: false,
    code: \`const emailRegex = /^[^\\\\s@]+@[^\\\\s@]+\\\\.[^\\\\s@]+$/;\`,
  },
  {
    name: "权限校验中间件",
    taskType: "feature",
    involvesSecurity: true,    // 涉及权限
    involvesBusiness: true,
    involvesPrecision: false,
    code: \`function checkPermission(role) {
  return (req, res, next) => {
    if (req.user.role === role) next();
    else res.status(403).send('Forbidden');
  };
}\`,
  },
];

// ---- 风险评估函数 ----
function assessRisk(output) {
  // 禁区：涉及安全 + 精确计算，或涉及合规相关业务
  if (output.involvesSecurity && output.involvesPrecision) {
    return {
      level: RiskLevel.DANGER,
      reason: "同时涉及安全和精确计算，必须人工主导",
      checklist: dangerChecklist,
    };
  }
  if (output.involvesSecurity) {
    return {
      level: RiskLevel.DANGER,
      reason: "涉及安全（认证/权限/加密），AI 仅做参考",
      checklist: dangerChecklist,
    };
  }
  if (output.involvesPrecision) {
    return {
      level: RiskLevel.DANGER,
      reason: "涉及精确计算（金融/统计），必须用计算库而非 AI 直接算",
      checklist: dangerChecklist,
    };
  }

  // 警戒区：涉及业务逻辑、重构
  if (output.involvesBusiness) {
    return {
      level: RiskLevel.WARNING,
      reason: "涉及业务逻辑，需严格审查 + 测试覆盖",
      checklist: warningChecklist,
    };
  }
  if (output.taskType === "refactor") {
    return {
      level: RiskLevel.WARNING,
      reason: "重构类任务，需保证外部行为不变",
      checklist: warningChecklist,
    };
  }

  // 安全区：模式化任务
  if (["documentation", "pattern", "test", "explanation"].includes(output.taskType)) {
    return {
      level: RiskLevel.SAFE,
      reason: "模式化任务，轻量审查即可",
      checklist: safeChecklist,
    };
  }

  // 默认归到警戒区
  return {
    level: RiskLevel.WARNING,
    reason: "未明确分类，默认按警戒区处理",
    checklist: warningChecklist,
  };
}

// ---- 三档验证清单 ----
const safeChecklist = [
  "跑通基本流程",
  "检查命名是否符合项目规范",
  "检查注释是否准确",
];

const warningChecklist = [
  "跑通基本流程 + 边界场景",
  "核对 API 是否真实存在（去官方文档）",
  "核对依赖版本是否匹配",
  "补单元测试，覆盖正常/边界/异常",
  "审查业务规则是否符合需求",
  "Code Review 由另一位工程师复核",
];

const dangerChecklist = [
  "必须由资深工程师主导设计，AI 仅做参考",
  "所有安全相关代码用成熟库，不要 AI 自实现",
  "精确计算必须用 decimal.js / BigDecimal 等库",
  "安全审计：SQL 注入、XSS、CSRF、未授权访问",
  "合规审计：PII、GDPR、PCI-DSS 等是否满足",
  "红队测试：模拟攻击场景验证",
  "上线前必须由安全团队 review",
];

// ---- 执行评估 ----
console.log("========================================");
console.log("  AI 输出风险评估器");
console.log("========================================\\n");

sampleOutputs.forEach((output, idx) => {
  const risk = assessRisk(output);
  console.log(\`[\${idx + 1}] 输出：\${output.name}\`);
  console.log(\`    任务类型：\${output.taskType}\`);
  console.log(\`    涉及安全：\${output.involvesSecurity} | 涉及业务：\${output.involvesBusiness} | 涉及精确计算：\${output.involvesPrecision}\`);
  console.log(\`    风险等级：\${risk.level}\`);
  console.log(\`    判定理由：\${risk.reason}\`);
  console.log(\`    验证清单（\${risk.checklist.length} 项）：\`);
  risk.checklist.forEach((c, i) => console.log(\`      \${i + 1}. \${c}\`));
  console.log(\`    代码片段：\\n      \${output.code.replace(/\\n/g, "\\n      ")}\`);
  console.log("");
});

// ---- 风险分布统计 ----
console.log("========================================");
console.log("  风险等级分布");
console.log("========================================");
const counter = {};
sampleOutputs.forEach((o) => {
  const r = assessRisk(o).level;
  counter[r] = (counter[r] || 0) + 1;
});
Object.entries(counter).forEach(([k, v]) => {
  console.log(\`  \${k}：\${v} 个\`);
});

console.log("\\n✅ 风险评估完成！");
console.log("实战中可以基于这套思路，在 CI 流水线里加入“AI 生成代码风险标签”，强制高危代码人工 review。");
`
  },
  {
    id: "aiapp-mindset",
    icon: "🔄",
    group: "AI编程入门",
    title: "AI 时代的程序员思维转变",
    content: `
# 第5章：AI 时代的程序员思维转变

## 5.1 从“工具升级”到“思维升级”

前面四章讲了 AI 编程是什么、能做什么、概念是什么、边界在哪。但要真正用好 AI，最难的不是学工具，而是**换脑子**。工具是表层，思维是底层——同样的 Cursor、同样的 Claude，有人用得风生水起，有人用得鸡肋，差别就在思维。

本章是入门组的收尾，目标是帮你完成三个转变：

1. 从“写代码”到“指挥代码”。
2. 从“实现细节”到“架构与判断”。
3. 从“单兵作战”到“与 AI 协作的 5 个层级”。

完成这三个转变，你才算真正进入 AI 编程的大门。

## 5.2 转变一：从“写代码”到“指挥代码”

传统程序员的核心动作是“敲键盘写代码”——一行行、一个个字符地把想法变成可运行的程序。AI 时代，这个动作的地位在下降，取而代之的是“指挥 AI 写代码”。

“指挥”和“写”是两种完全不同的能力：

| 维度 | 写代码 | 指挥代码 |
| --- | --- | --- |
| 主要输出 | 字符 | 意图 + 验证 |
| 思考单位 | 语句 | 任务 |
| 衡量标准 | 代码行数 / 质量 | 任务完成度 / 代码质量 |
| 思考重心 | 怎么实现 | 实现什么 + 对不对 |
| 协作对象 | 编译器 | AI + 编译器 |
| 失败模式 | 语法错、逻辑错 | 意图表达不清、AI 误解、未审查 |

这个转变的难点在于：**老程序员一辈子练的是“写”，突然要改成“指挥”，肌肉记忆会抗拒**。很多人用 Cursor 还是忍不住自己一行行敲，AI 补全按 Tab 但不会主动提需求让 AI 生成——这就是没完成转变。

完成这个转变的标志：

1. **遇到任务先想“怎么描述给 AI 听”**，而不是“怎么从第一行开始写”。
2. **能区分哪些任务该指挥 AI、哪些该自己写**——简单的样板让 AI 写，复杂的算法自己想清楚再让 AI 实现。
3. **会验收 AI 的输出**——看代码、跑测试、判断是否符合需求，而不是无脑接受。
4. **能多轮指挥**——一次生成不满意，会调整 prompt 再来，而不是“一次不行就放弃 AI 自己写”。

\`\`\`text
传统流程：
  想需求 ──> 想实现 ──> 敲代码 ──> 调试 ──> 提交

AI 时代流程：
  想需求 ──> 描述给 AI ──> AI 生成 ──> 验收 ──> 调整 prompt / 自己改 ──> 提交
              ↑                                          │
              └──────────── 多轮对话 ────────────────────┘
\`\`\`

## 5.3 转变二：从“实现细节”到“架构与判断”

当 AI 接管了大部分“实现细节”，程序员的价值重心会向上移动——从“怎么实现”转向“要实现什么、为什么这么实现、值不值得实现”。

这个转变有几个具体体现：

**1. 需求理解的权重上升**

AI 不会问你“你真的想做这个吗”“这个需求是不是搞反了”。它只会按你说的做。所以你必须自己把需求想透——做这个功能的目的是什么、用户场景是什么、成功标准是什么。需求想错，AI 写得再好也是白费。

**2. 架构决策的权重上升**

“这个服务该拆成两个还是一个？”“这个数据该存在哪？”“这个流程该同步还是异步？”——这些决策 AI 给的建议是“通用最佳实践”，但具体到你的项目，要结合团队规模、运维能力、长期演进方向判断。AI 给的是参考，决策权在你。

**3. 风险判断的权重上升**

“这个改动会影响哪些下游？”“上线出问题能快速回滚吗？”“这个 API 一旦发布就改不回来，要不要预留兼容？”——这些风险判断需要全局视角，AI 看不到全貌。

**4. 价值判断的权重上升**

“这个功能值得做吗？”“做了之后对核心指标有帮助吗？”“有没有更高性价比的方案？”——AI 不懂业务价值，这些判断必须由人来做。

**5. 审查能力的权重上升**

AI 生成的代码越来越多，人审查的比重越来越高。能快速判断一段代码“对不对、安不安全、符不符合业务”，是新形势下核心能力。

\`\`\`text
传统价值分布：          AI 时代价值分布：
  实现细节 70%            实现细节 30%
  架构判断 20%            架构判断 40%
  需求理解 10%            需求理解 30%
\`\`\`

## 5.4 转变三：AI 协作的 5 个层级

不同人对 AI 的“使用深度”差异巨大。把这种深度分成 5 个层级，你能清楚知道自己在哪一层、下一层需要补什么能力。

**第 1 层：助手（Assistant）**

把 AI 当字典/搜索引擎用。问“这个 API 怎么用”“这个错误什么意思”。AI 给答案，你照着做。这层不需要任何 AI 编程工具，浏览器打开 ChatGPT 就行。

- 能力要求：会用自然语言提问。
- 适用人群：所有开发者，包括初学者。
- 风险：AI 给的答案可能错（幻觉），照抄会出 bug。
- 收益：解决零散问题的效率提升 2-3 倍。

**第 2 层：副驾（Copilot）**

把 AI 集成进 IDE，用 Inline 补全 + 偶尔的 Chat。AI 帮你补全样板代码、解释陌生代码、写小工具函数。你仍然是主力，AI 是辅助。

- 能力要求：会写清晰的注释和函数签名作为隐性 prompt，会判断补全是否正确。
- 适用人群：所有有项目经验的开发者。
- 风险：无脑接受补全引入 bug；过度依赖导致基础能力退化。
- 收益：日常编码效率提升 30-50%。

**第 3 层：同事（Peer）**

用 Chat 模式和 AI 来回讨论方案。AI 不只是补全，而是参与“这个功能怎么设计”“这个 bug 怎么排查”“这个重构怎么搞”的讨论。你和 AI 是“同事关系”，它出主意，你拍板。

- 能力要求：会写结构化 prompt（需求+约束+示例），会多轮引导 AI，会审查方案而不只是审查代码。
- 适用人群：有 1-2 年项目经验的开发者。
- 风险：被 AI 的“合理建议”带偏，缺乏独立判断。
- 收益：复杂任务效率提升 50-100%，方案质量提升。

**第 4 层：团队（Team）**

用 Agent 模式把整个任务委派给 AI。AI 自主读文件、改文件、跑测试、修 bug，循环多步直到完成。你是“项目经理”，AI 是“执行团队”。

- 能力要求：会拆解任务（把大需求拆成可委派的小任务），会写明确的验收标准，会在 AI 执行过程中做检查点 review。
- 适用人群：有扎实工程基础、能审查复杂改动的资深开发者。
- 风险：任务拆得不好导致 AI 跑偏；缺乏检查点导致错误累积；对 AI 输出失去控制感。
- 收益：单个开发者能完成原本 3-5 人的工作量。

**第 5 层：自主（Autonomous）**

AI 几乎完全自主地完成端到端任务——从需求理解到设计到实现到测试到部署。人只在关键决策点介入（“这个方向对吗”“这个能上线吗”）。

- 能力要求：极强的系统设计能力（能在 AI 设计后审查架构）、极强的风险判断能力（能预判 AI 自主执行的爆炸半径）、极强的流程设计能力（能给 AI 设计安全的执行边界）。
- 适用人群：资深架构师 + AI 工程专家。
- 风险：AI 自主决策出错导致生产事故；人失去对系统的理解导致无法干预；安全/合规风险。
- 收益：1 个人能运营原本 10+ 人的系统。但这层目前仍处于早期，不建议轻易尝试。

5 个层级的能力要求与风险对比：

| 层级 | 主要能力 | 关键风险 | 推荐人群 |
| --- | --- | --- | --- |
| 1 助手 | 提问 | 幻觉照抄 | 所有人 |
| 2 副驾 | 注释 + 补全判断 | 无脑接受 | 有经验者 |
| 3 同事 | 结构化 prompt + 方案审查 | 被带偏 | 1-2 年经验 |
| 4 团队 | 任务拆解 + 检查点 | 跑偏 + 失控 | 资深开发者 |
| 5 自主 | 系统设计 + 风险预判 | 爆炸半径大 | 架构师 + AI 专家 |

## 5.5 新人如何避免被 AI 带偏

新人（学生、刚入行 1-2 年的开发者）面临的特殊挑战是：AI 太好用，容易跳过基础训练。

**典型陷阱**：

1. **不学语法直接用 AI**：AI 帮你写代码，但你连基本语法都不熟，遇到 bug 完全看不懂。
2. **不练算法直接用 AI**：AI 帮你解 LeetCode，但面试官让你白板写就抓瞎。
3. **不理解框架原理**：AI 帮你用 React/Vue，但问“为什么 Hooks 不能放在 if 里”就答不上来。
4. **不会调试**：AI 帮你修 bug，但你自己看不懂报错，AI 一走你就不会了。
5. **缺乏判断力**：AI 给的“看起来合理”的方案你全盘接受，埋下隐患。

**新人的正确姿势**：

1. **基础一定要练**：语法、算法、数据结构、操作系统、网络——这些基础不能跳过。AI 是放大器，你基础越扎实，AI 放大效果越好；基础为零，AI 只能放大你的迷茫。
2. **AI 生成的代码必须读懂**：不能“能跑就行”。每一段 AI 生成的代码，都要逐行读懂，不懂就问 AI 解释，再不懂就查文档。
3. **关键概念用官方文档交叉验证**：AI 讲的有少量幻觉，重要概念一定要去官方文档核对。
4. **手写代码 + AI 代码结合**：纯手写会失去 AI 杠杆，纯 AI 会失去基础。建议“手写学习、AI 提效”——学习时手写，工作时用 AI。
5. **定期“断 AI”训练**：每周选 1-2 个任务，完全不用 AI 自己写，保持手感。
6. **把 AI 当老师而非答案机**：与其问“这个怎么做”，不如问“这个为什么这么做”“如果换种方式有什么区别”，让 AI 帮你建立理解而非依赖。

## 5.6 老程序员如何利用经验放大 AI

老程序员（5 年以上经验）的优势是经验，劣势是惯性。用经验放大 AI，关键在于克服惯性、重新定位自己的价值。

**老程序员的优势**：

1. **能看懂 AI 输出的对错**：新人看 AI 代码像看天书，老程序员一眼就能看出问题。
2. **能拆解复杂任务**：新人不知道一个大任务怎么拆，老程序员能把任务拆得细到 AI 能执行。
3. **能判断 AI 方案的优劣**：AI 给的“通用最佳实践”是否符合当前项目，老程序员能判断。
4. **能预判风险**：哪些改动会影响下游、哪些 API 不能动、哪些边界场景要测——这些经验 AI 没有。
5. **能审查架构**：AI 设计的架构能不能扛住流量、能不能演进、能不能维护，老程序员能审查。

**老程序员的惯性陷阱**：

1. **坚持纯手写**：觉得 AI 代码不可靠，坚持自己写，结果被采用 AI 的同行拉开效率。
2. **微观管理**：忍不住一行行改 AI 代码，把“指挥”变成了“重写”。
3. **拒绝新工具**：用惯了老 IDE，不愿学 Cursor/Trae，错过工具红利。
4. **凭经验否定 AI**：AI 给的方案跟自己经验不符就直接否定，错过了 AI 可能的创新视角。

**放大 AI 的正确姿势**：

1. **从“写”转向“审”**：把写代码的工作更多地交给 AI，自己专注审查、设计、决策。
2. **从“实现”转向“拆解”**：把复杂任务拆成 AI 能执行的子任务，这是老程序员的杀手锏。
3. **用 AI 加速学习新领域**：老程序员学新框架慢，AI 能让你几小时上手一个陌生技术栈。
4. **用 AI 跳出经验盲区**：主动问 AI“如果换种思路怎么做”，借助 AI 看到自己没想到的视角。
5. **把经验沉淀成 prompt**：你的“踩坑经验”“设计原则”可以写成项目级 rules 文件，让 AI 在所有任务里都遵循。
6. **做“AI 编排者”**：从“自己写代码”变成“编排多个 AI 任务完成大目标”，这是老程序员的高价值定位。

## 5.7 “AI 杠杆率”概念

**AI 杠杆率** = 借助 AI 后的产出 / 不借助 AI 时的产出。

不同人的 AI 杠杆率差异巨大：

- 新人 + AI：杠杆率 1.5-2x（AI 帮你跳过查文档，但你看不懂复杂输出）。
- 中级 + AI：杠杆率 2-3x（你能用 AI 完成更多任务，但复杂决策仍靠自己）。
- 资深 + AI：杠杆率 5-10x（你能拆解大任务给 AI、能审查复杂输出、能设计 Agent 工作流）。
- 架构师 + AI + Agent 编排：杠杆率 10x+（你能 1 个人运营原本 10 人的系统）。

**关键洞察**：AI 杠杆率随经验上升。这意味着 AI 不是“拉平”程序员，而是“拉开”程序员——基础越好、经验越足的人，从 AI 获得的杠杆越大。这跟传统工具相反（传统工具往往是新手受益更多）。

这个洞察有几个推论：

1. **新人不要急**：你的杠杆率会随经验上升，先打基础。
2. **中级要练拆解**：从“自己写”转向“指挥 AI 写”，是中等水平突破的关键。
3. **资深要练编排**：从“指挥单个 AI”转向“编排多个 AI 任务”，是高水平突破的关键。
4. **所有人都该练审查**：审查能力是 AI 时代的通用核心能力，不管哪个层级都要强。

\`\`\`text
AI 杠杆率
  ▲
  │                架构师 + Agent 编排 (10x+)
  │              ╱
  │            ╱  资深 + AI (5-10x)
  │          ╱
  │        ╱    中级 + AI (2-3x)
  │      ╱
  │    ╱      新人 + AI (1.5-2x)
  │  ╱
  └────────────────────────────────────▶ 经验/能力
\`\`\`

## 5.8 一份思维转变自检清单

完成本章学习后，用这份清单自检你的思维转变程度：

1. [ ] 遇到任务时，第一反应是“怎么描述给 AI 听”而非“怎么从第一行写起”。
2. [ ] 能区分哪些任务该让 AI 写、哪些该自己写。
3. [ ] 看 AI 生成的代码时会逐行审查，而非“能跑就行”。
4. [ ] 会写结构化 prompt（需求 + 约束 + 示例 + 验收标准）。
5. [ ] 能用 Agent 模式委派一个完整任务，并在过程中做检查点 review。
6. [ ] 能识别 AI 的幻觉，并用官方文档交叉验证。
7. [ ] 在做架构决策时，把 AI 当参考而非权威。
8. [ ] 定期“断 AI”训练，保持手写代码能力。
9. [ ] 把自己的经验沉淀成项目级 rules 文件，让 AI 遵循。
10. [ ] 能用 AI 跳出经验盲区，主动问“换种思路怎么做”。

每条做到打勾，能做到 7 条以上，说明思维转变基本完成；能做到 5-7 条，说明正在过渡；5 条以下，说明还在“传统思维”里，建议重读本章。

## 5.9 本章小结与入门组收尾

本章是入门组（第 1-5 章）的收尾。回顾这 5 章，我们一起走过了：

- 第 1 章：建立 AI 编程的全局认知——它是什么、从哪来、要往哪去。
- 第 2 章：盘点 AI 能做的事——八大场景全景，每场景配示例 prompt。
- 第 3 章：理解 AI 的内功——LLM、Token、上下文窗口、温度这些核心概念。
- 第 4 章：认清 AI 的边界——它擅长什么、不擅长什么、为什么不能完全信任。
- 第 5 章：完成思维转变——从“写代码”到“指挥代码”，从“实现”到“判断”，从“单兵”到“5 层协作”。

完成这 5 章的学习，你已经具备了“正确使用 AI 编程”的认知基础。后续批次将进入实战——主流工具的深度使用、Prompt 工程的方法论、Agent 工作流的设计、完整项目案例的拆解。

最后送你一句话：**AI 不会取代程序员，但会用 AI 的程序员会取代不会用 AI 的程序员**。这句话不新，但真正做到需要持续学习。希望你带着本章建立的思维框架，在后续章节里把每一个工具、每一个技巧都内化成自己的能力。
`,
    code: `
// =============================================================
// 第5章示例代码：AI 协作层级评估器与杠杆率计算器
// 输入一个开发者的能力描述，自动评估其所处的 AI 协作层级，
// 并估算 AI 杠杆率，给出进阶建议
// 可在 Node.js 沙箱中直接运行，无外部依赖
// =============================================================

// ---- AI 协作的 5 个层级定义 ----
const collaborationLevels = [
  {
    level: 1,
    name: "助手",
    skills: ["能用自然语言提问"],
    risk: "AI 幻觉照抄，引入 bug",
    leverageRange: [1.5, 2],
    nextStep: "学写清晰注释和函数签名，开始用 IDE 内补全",
  },
  {
    level: 2,
    name: "副驾",
    skills: ["会写清晰注释作为隐性 prompt", "能判断补全是否正确"],
    risk: "无脑接受补全引入 bug；过度依赖导致基础退化",
    leverageRange: [1.5, 2.5],
    nextStep: "学结构化 prompt，开始用 Chat 模式和 AI 来回讨论方案",
  },
  {
    level: 3,
    name: "同事",
    skills: ["会写结构化 prompt（需求+约束+示例）", "会多轮引导 AI", "会审查方案而非只审查代码"],
    risk: "被 AI 的合理建议带偏，缺乏独立判断",
    leverageRange: [2, 3],
    nextStep: "学任务拆解，开始用 Agent 模式委派完整任务",
  },
  {
    level: 4,
    name: "团队",
    skills: ["能拆解任务为可委派的小任务", "会写明确验收标准", "会在 AI 执行中做检查点 review"],
    risk: "任务拆不好导致 AI 跑偏；缺乏检查点导致错误累积",
    leverageRange: [5, 10],
    nextStep: "学系统设计和风险预判，向自主 Agent 编排进阶",
  },
  {
    level: 5,
    name: "自主",
    skills: ["极强的系统设计能力", "极强的风险判断能力", "极强的流程设计能力"],
    risk: "AI 自主决策出错导致生产事故；人失去对系统的理解",
    leverageRange: [10, 30],
    nextStep: "持续优化 Agent 工作流边界，建立安全护栏",
  },
];

// ---- 开发者能力画像 ----
// 通过一组能力维度的掌握程度（0-5），评估其所处层级
const sampleDevelopers = [
  {
    name: "应届生小李",
    skills: {
      basicSyntax: 3,        // 基础语法
      algorithm: 2,          // 算法数据结构
      framework: 2,          // 框架使用
      systemDesign: 1,       // 系统设计
      promptWriting: 2,      // 写 prompt
      codeReview: 2,         // 代码审查
      taskDecomposition: 1,  // 任务拆解
      riskJudgment: 1,       // 风险判断
      aiToolUsage: 2,        // AI 工具熟练度
    },
    yearsOfExperience: 0.5,
  },
  {
    name: "中级开发者小王",
    skills: {
      basicSyntax: 4,
      algorithm: 3,
      framework: 4,
      systemDesign: 2,
      promptWriting: 3,
      codeReview: 3,
      taskDecomposition: 2,
      riskJudgment: 2,
      aiToolUsage: 3,
    },
    yearsOfExperience: 3,
  },
  {
    name: "资深工程师老张",
    skills: {
      basicSyntax: 5,
      algorithm: 4,
      framework: 5,
      systemDesign: 4,
      promptWriting: 4,
      codeReview: 5,
      taskDecomposition: 4,
      riskJudgment: 4,
      aiToolUsage: 4,
    },
    yearsOfExperience: 8,
  },
  {
    name: "架构师赵工",
    skills: {
      basicSyntax: 5,
      algorithm: 5,
      framework: 5,
      systemDesign: 5,
      promptWriting: 5,
      codeReview: 5,
      taskDecomposition: 5,
      riskJudgment: 5,
      aiToolUsage: 5,
    },
    yearsOfExperience: 15,
  },
];

// ---- 层级评估函数 ----
// 根据能力画像，判断开发者最适合的 AI 协作层级
function assessLevel(dev) {
  const s = dev.skills;
  // 简化判定：综合看 promptWriting + taskDecomposition + systemDesign + riskJudgment
  // 这四项是区分层级的关键
  const keyAbility =
    s.promptWriting * 0.2 +
    s.codeReview * 0.2 +
    s.taskDecomposition * 0.25 +
    s.systemDesign * 0.2 +
    s.riskJudgment * 0.15;

  // 基础能力门槛：basicSyntax < 3 的新人，无论其他多强都从 L1 开始
  if (s.basicSyntax < 3) {
    return collaborationLevels[0];
  }

  if (keyAbility < 1.5) return collaborationLevels[0];
  if (keyAbility < 2.5) return collaborationLevels[1];
  if (keyAbility < 3.5) return collaborationLevels[2];
  if (keyAbility < 4.5) return collaborationLevels[3];
  return collaborationLevels[4];
}

// ---- 杠杆率计算 ----
// 基础杠杆率 = 经验系数 × 层级系数
function calculateLeverage(dev, level) {
  // 经验系数：年限越长，杠杆越高（0.5 年=0.8，3 年=1.0，8 年=1.3，15 年=1.5）
  const expCoef = Math.min(1.5, 0.7 + dev.yearsOfExperience * 0.05);
  // 层级系数：取层级杠杆范围的中位数
  const levelCoef = (level.leverageRange[0] + level.leverageRange[1]) / 2;
  // 综合杠杆率 = 经验系数 × 层级系数
  const leverage = expCoef * levelCoef;
  return { expCoef, levelCoef, leverage };
}

// ---- 执行评估 ----
console.log("========================================");
console.log("  AI 协作层级评估器");
console.log("========================================\\n");

sampleDevelopers.forEach((dev) => {
  const level = assessLevel(dev);
  const leverage = calculateLeverage(dev, level);

  console.log(\`开发者：\${dev.name}（\${dev.yearsOfExperience} 年经验）\`);
  console.log(\`  能力画像：\`);
  console.log(\`    基础语法    \${dev.skills.basicSyntax}/5\`);
  console.log(\`    算法        \${dev.skills.algorithm}/5\`);
  console.log(\`    框架        \${dev.skills.framework}/5\`);
  console.log(\`    系统设计    \${dev.skills.systemDesign}/5\`);
  console.log(\`    写 prompt   \${dev.skills.promptWriting}/5\`);
  console.log(\`    代码审查    \${dev.skills.codeReview}/5\`);
  console.log(\`    任务拆解    \${dev.skills.taskDecomposition}/5\`);
  console.log(\`    风险判断    \${dev.skills.riskJudgment}/5\`);
  console.log(\`    AI 工具     \${dev.skills.aiToolUsage}/5\`);
  console.log(\`  ─────────────────────────────────\`);
  console.log(\`  当前层级：L\${level.level} - \${level.name}\`);
  console.log(\`  关键能力：\${level.skills.join("、")}\`);
  console.log(\`  主要风险：\${level.risk}\`);
  console.log(\`  AI 杠杆率：\${leverage.leverage.toFixed(1)}x\`);
  console.log(\`    （经验系数 \${leverage.expCoef.toFixed(2)} × 层级系数 \${leverage.levelCoef.toFixed(1)}）\`);
  console.log(\`  进阶建议：\${level.nextStep}\\n\`);
});

// ---- 层级杠杆率分布 ----
console.log("========================================");
console.log("  5 个层级的 AI 杠杆率对比");
console.log("========================================");
collaborationLevels.forEach((l) => {
  const bar = "█".repeat(Math.floor(l.leverageRange[1]));
  console.log(\`  L\${l.level} \${l.name.padEnd(4)} \${l.leverageRange[0]}-\${l.leverageRange[1]}x  \${bar}\`);
});

console.log("\\n========================================");
console.log("  核心洞察");
console.log("========================================");
console.log("  AI 杠杆率随经验上升，AI 不是“拉平”程序员，而是“拉开”程序员。");
console.log("  基础越好、经验越足的人，从 AI 获得的杠杆越大。");
console.log("  推论：");
console.log("    - 新人不要急：先打基础，杠杆率会随经验上升");
console.log("    - 中级要练拆解：从“自己写”转向“指挥 AI 写”");
console.log("    - 资深要练编排：从“指挥单个 AI”转向“编排多个 AI 任务”");
console.log("    - 所有人都要练审查：审查能力是 AI 时代的通用核心能力");

console.log("\\n✅ 评估完成！");
console.log("入门组 5 章到此结束，下一批次将进入主流 AI 编程工具的深度使用。");
`
  },
];
