// =============================================================
// Python 执行代码原理（pyrun）—— 第一批章节
// -------------------------------------------------------------
// 用大白话讲清楚 Python 代码是怎么跑起来的。
// 第一批（1-5章）：代码是怎么变成指令的
//   pyrun-01：你写的代码，电脑其实看不懂
//   pyrun-02：词法分析，把代码切成零件
//   pyrun-03：语法分析，把零件组装成树
//   pyrun-04：编译，把树变成字节码
//   pyrun-05：Python 虚拟机，执行字节码
//
// 每个章节包含：
//   id      : 唯一标识
//   group   : 分组名
//   icon    : 展示用 emoji
//   title   : 章节标题
//   content : Markdown 格式的详细讲解（含表格、代码块、列表）
//   code    : 可运行、带详细中文注释的 Python 示例代码
//
// 代码运行环境约束：
//   - 通过 /api/run-py 用系统 python3 执行（推荐 3.7+）
//   - 仅使用 Python 标准库
//   - 所有 demo 单文件可独立运行
//   - 用 print 输出结果，每行代码都有中文注释
// =============================================================

export const chapters = [
  // =========================================================
  // 第一章：你写的代码，电脑其实看不懂
  // =========================================================
  {
    id: "pyrun-01",
    group: "代码到指令",
    icon: "🐍",
    title: "你写的代码，电脑其实看不懂",
    content: `## 一、从"写信"说起

想象一下：你写了一封中文信，想寄给一个**只懂英语**的朋友。
你能直接把中文信塞信封寄过去吗？当然不行——朋友看不懂。

你得先把中文信**翻译成英语**，朋友才能读懂，然后照着信里的要求去做事。

你写的 Python 代码，和电脑的关系，跟这封信一模一样：

- 你写的是**人能看懂的字符**（英文单词、符号、中文注释）
- 但 CPU 这个"朋友"只认识 **0 和 1**，根本看不懂你写啥
- 中间必须有人帮你"翻译"

这一章，我们就来聊聊：你敲下去的代码，到底是怎么一步步变成 CPU 能执行的东西的。

## 二、电脑其实只认识 0 和 1

不管你写的是 Python、Java、C 还是 JavaScript，落到 CPU 这一层，
全都是**电信号**：

- 高电平 = 1
- 低电平 = 0

- 你写 \`a = 1 + 2\`，CPU 看到的是一串 0 和 1
- 你写 \`print("hello")\`，CPU 看到的还是一串 0 和 1
- 你写一个几百行的类，CPU 看到的依然是一串 0 和 1

CPU 能**直接执行**的，叫**机器码（machine code）**，长这样（示意）：

\`\`\`
10100001 00000001 00000000
00000001 00000011 00000010
\`\`\`

人要直接写这个，得疯掉。所以我们发明了**高级语言**，先用接近人类的方式写，
再让程序帮我们翻译成 0 和 1。

## 三、高级语言是什么

高级语言 = 接近人类语言的编程语言。比如 Python：

\`\`\`python
total = 0
for i in range(10):
    total += i
print(total)
\`\`\`

你看，\`for\`、\`range\`、\`print\` 都是英文单词，缩进也很自然，
比一串 0 和 1 友好太多了。

但 CPU 还是看不懂。所以高级语言写出来后，**必须**经过一道"翻译"，
才能真的跑起来。这道翻译，就是这门课要讲的核心。

## 四、两种翻译方式：编译 vs 解释

翻译方式主要有两种（外加一种混合）：

| 翻译方式 | 比喻 | 谁这么做 | 特点 |
|---------|------|---------|------|
| **编译** (compile) | 把整本书翻译完再出版 | C、C++、Go、Rust | 一次翻译，多次运行；速度快 |
| **解释** (interpret) | 同声传译，说一句翻一句 | 传统 Shell、Ruby | 边读边执行；启动快 |
| **混合** | 先翻译成中间稿，再照着念 | Java、Python | 两步走，兼顾两者 |

### 4.1 编译型（像翻译一本书）

C 语言的流程：

\`\`\`
源代码 (.c) → [编译器] → 机器码 (.exe) → 直接运行
\`\`\`

- 翻译完生成一个**可执行文件**（.exe / ELF）
- 运行时不需要编译器在场
- 改一行代码要**重新编译**整个项目
- 运行起来飞快，因为已经是 CPU 直接能吃的机器码

### 4.2 解释型（像同声传译）

传统 Shell 的流程：

\`\`\`
源代码 → [解释器] 一行一行读 → 一行一行执行
\`\`\`

- 边读边跑，不生成可执行文件
- 改完代码立刻能跑，不用编译
- 一般比编译型慢，因为每次运行都要重新"理解"代码

### 4.3 混合型（先翻译成中间稿）

Java 和 Python 都是这种：先把源码翻译成一种**中间代码**，
再让一个"虚拟机"来执行中间代码。

\`\`\`
源码 → [编译器] → 中间码 → [虚拟机] → 执行
\`\`\`

- Java 的中间码叫**字节码**，跑在 JVM 上
- Python 的中间码也叫**字节码**，跑在 PVM 上

## 五、Python 属于哪种？

**Python 是"混合型"**，更准确地说：**先编译，后解释**。

很多人以为 Python 是纯解释型（一行一行读），其实**不准确**。
Python 执行代码分两步：

\`\`\`
第一步：源代码 (.py) → [编译器] → 字节码 (bytecode)
第二步：字节码 → [虚拟机 PVM] → 一条条执行
\`\`\`

只是这两步对你是**透明**的——你敲 \`python hello.py\`，它一口气全干了，
你感觉不到中间还有个"编译"步骤。但编译这一步真实存在，下一章开始你就会看到证据。

## 六、Python 代码的旅程

一整条完整路线：

\`\`\`
你写的源码 (.py)
      ↓  [词法分析] 切成 token
    token 流
      ↓  [语法分析] 组装成 AST
    抽象语法树 AST
      ↓  [编译器] 翻译
    字节码 bytecode
      ↓  [PVM 虚拟机] 一条条执行
    程序跑起来！
\`\`\`

后面几章我们会一步步拆开看，每一步到底干了啥：

- 第 2 章：词法分析（切 token）
- 第 3 章：语法分析（组装 AST）
- 第 4 章：编译（生成字节码）
- 第 5 章：虚拟机（执行字节码）

## 七、为什么要中间搞一层"字节码"？

你可能会问：既然最后都要变成机器码，Python 为啥不直接编译成机器码，
非得绕一道"字节码"？

好处有三个：

1. **跨平台**：字节码跟操作系统、CPU 无关。同一份字节码在 Windows、Mac、Linux
   都能跑（只要有对应的 PVM）。如果直接编译成机器码，就得每个平台各编一次。
2. **简化实现**：写一个虚拟机比写一个针对每种 CPU 的编译器容易多了。
   Python 只需要维护一份字节码规范，各平台共用。
3. **方便优化和缓存**：字节码是中间层，可以在这里做各种优化、还能缓存到 \`.pyc\`。

代价是**速度**：多了一层翻译，比 C 这种直接编译成机器码的慢一截。
这就是 Python "开发快、运行慢"的根本原因之一。

## 八、.pyc 文件是什么

你 import 一个模块时，常常会在 \`__pycache__\` 目录里看到 \`.pyc\` 文件。
\`.pyc\` 就是**缓存的字节码**。

- \`py\` = 源码（人写的）
- \`pyc\` = compiled 的字节码（编译后的）
- \`pyo\` = 优化过的字节码（老版本概念，新版已合并进 pyc）

**为什么要有 .pyc？** 省时间。

- 第一次 import 时编译，生成 .pyc
- 下次再 import，如果源码没改（看时间戳和大小），直接读 .pyc，跳过编译

注意两点：

1. **直接运行主脚本不会生成 .pyc**，只有被 import 的模块才会缓存
2. **.pyc 不是机器码**，里面还是字节码，没 PVM 还是跑不了

## 九、用大白话再走一遍

| 步骤 | 比喻 | Python 里 |
|------|------|-----------|
| 写代码 | 写中文信 | \`.py\` 源文件 |
| 词法分析 | 把信里的字一个个认出来 | 切成 token |
| 语法分析 | 理解句子结构 | 生成 AST |
| 编译 | 翻译成英语 | 生成字节码 |
| 执行 | 朋友读英语照做 | PVM 跑字节码 |

整条链路，就是"从人话到机器执行"的完整旅程。后面几章会把每一步拆开揉碎讲。

## 十、几个常见误区

1. **"Python 不编译"** —— 错！Python 会编译成字节码，只是不编译成机器码。
2. **".pyc 是机器码"** —— 错！.pyc 里是字节码，还得 PVM 解释。
3. **"换电脑 .pyc 不能用"** —— 同版本 Python 跨平台能用，版本不一致可能不行。
4. **"Python 慢纯粹是因为解释"** —— 部分对。慢是因为字节码还要被 PVM 解释，
   而且是动态类型、一切皆对象，开销大。
5. **"字节码 = 汇编"** —— 不一样。汇编接近机器码，字节码是虚拟机的指令。

## 十一、字节码长什么样

不用想象，下一章我们就动手看。这里先给个预览，\`a + b\` 这么简单的东西，
字节码大概是这样：

\`\`\`
LOAD_FAST   0 (a)    # 把局部变量 a 压到栈上
LOAD_FAST   1 (b)    # 把局部变量 b 压到栈上
BINARY_ADD           # 弹出 a 和 b，相加，结果压栈
RETURN_VALUE         # 返回栈顶
\`\`\`

是不是有点像"菜谱"——一步步告诉虚拟机该干啥？第 4、5 章会详细讲。

## 十二、动手感受：Python 确实会编译

想自己验证 Python 有编译步骤？可以在终端里这么玩：

\`\`\`python
import dis
dis.dis(lambda a, b: a + b)
\`\`\`

\`dis\` 模块能把一段代码的字节码**反汇编**出来给你看。如果你看到一堆
\`LOAD\`、\`BINARY_ADD\` 之类的指令，那就证明：源码确实被翻译成了字节码。

## 十三、本章 demo 说明

下面 demo 用 \`dis\` 模块（disassemble，反汇编）实际看看
一段简单 Python 代码到底被编译成了什么字节码。

你会看到：

1. 一个最简单的加法函数，字节码是什么样
2. 一个稍复杂的字符串函数，字节码是什么样
3. 代码对象（code object）里都存了啥信息
4. 直接反汇编一段表达式

跑完之后，你对"Python 会编译"这件事就会有直观感受了。
`,
    code: `# =============================================================
# 第一章 demo：查看 Python 代码的字节码
# 演示：
#   1. 用 dis 模块查看函数的字节码
#   2. 对比简单函数和复杂函数的字节码
#   3. 查看代码对象里存的信息
#   4. 直接反汇编一段表达式
# =============================================================

import dis                      # 导入反汇编模块，用来查看字节码
import sys                      # 导入系统模块，用来查看 Python 版本

print("=" * 50)                 # 打印 50 个等号作为分隔线
print("Python 执行原理 — 第一章 demo")  # 打印章节标题
print("Python 版本:", sys.version.split()[0])  # 打印当前 Python 版本号
print("=" * 50)                # 再打印一条分隔线
print()                        # 打印空行，让输出更清晰

# ===== 1. 定义一个最简单的函数 =====
def add(a, b):                 # 定义函数 add，接收两个参数 a 和 b
    return a + b               # 返回 a 加 b 的结果

# 查看这个函数的字节码
print("【1. 简单加法函数 add 的字节码】")  # 打印小节标题
print("源代码: def add(a, b): return a + b")  # 打印源代码说明
print("-" * 50)                # 打印一条短分隔线
dis.dis(add)                   # 用 dis.dis 打印 add 函数的字节码指令
print()                        # 打印空行

# ===== 2. 稍微复杂一点的函数 =====
def greet(name):               # 定义函数 greet，接收一个参数 name
    message = "你好, " + name  # 拼接字符串，把结果存到变量 message
    print(message)             # 打印 message 的内容
    return len(message)        # 返回 message 的长度

print("【2. 字符串处理函数 greet 的字节码】")  # 打印小节标题
print("源代码: def greet(name): ...")  # 打印源代码说明
print("-" * 50)                # 打印一条短分隔线
dis.dis(greet)                 # 用 dis.dis 打印 greet 函数的字节码
print()                        # 打印空行

# ===== 3. 查看代码对象里存的信息 =====
print("【3. 代码对象 (code object) 的信息】")  # 打印小节标题
code = add.__code__            # 获取 add 函数的代码对象
print("  函数名:", code.co_name)         # 打印代码对象里存的函数名
print("  参数个数:", code.co_argcount)   # 打印位置参数个数
print("  局部变量:", code.co_varnames)   # 打印局部变量名元组
print("  常量:", code.co_consts)         # 打印代码里用到的常量
print("  字节码长度:", len(code.co_code), "字节")  # 打印字节码的字节长度
print()                        # 打印空行

# ===== 4. 直接反汇编一段表达式 =====
print("【4. 直接反汇编表达式 1 + 2 * 3】")  # 打印小节标题
expr_code = compile("1 + 2 * 3", "<demo>", "eval")  # 把表达式编译成代码对象
dis.dis(expr_code)             # 反汇编这个表达式的字节码
print()                        # 打印空行

# ===== 5. 总结 =====
print("=" * 50)                # 打印分隔线
print("总结：")                 # 打印总结标题
print("• 你写的 Python 代码会先被编译成字节码")  # 打印要点 1
print("• 字节码存在代码对象的 co_code 字段里")   # 打印要点 2
print("• dis 模块可以把字节码反汇编成人能读的指令")  # 打印要点 3
print("• 每条指令都是给虚拟机执行的小步骤")     # 打印要点 4
print("=" * 50)                # 打印分隔线
`,
  },

  // =========================================================
  // 第二章：词法分析，把代码切成零件
  // =========================================================
  {
    id: "pyrun-02",
    group: "代码到指令",
    icon: "🔤",
    title: "词法分析：把代码切成零件",
    content: `## 一、从"读句子"说起

你读这句话的时候，大脑其实先干了一件事：**把一串字符切成一个一个词**。

"我 爱 编程" —— 你看到的是 3 个词，不是 6 个字。

Python 解释器读你的代码时，第一步也是干这个：把一整串字符，
切成一个一个**最小语法单元**，这个单元叫 **token**。

这一步就叫**词法分析（lexical analysis）**，也叫**扫描（scanning）**。

## 二、代码就是一串字符

你写的源码文件，本质上就是一长串字符：

\`\`\`
n  a  m  e     =     "  张  三  "  \\n  a  g  e     =     1  8
\`\`\`

Python 怎么知道：

- \`name\` 是一个变量名，不是 \`n\`、\`a\`、\`m\`、\`e\` 四个东西？
- \`=\` 是赋值符号，不是两个减号粘一起？
- \`"张三"\` 是一个字符串，不是 \`"\`、\`张\`、\`三\`、\`"\` 四个东西？

这就是词法分析的活：**按规则把字符黏合、切分成 token**。

## 三、token 是什么

**token = 最小的、有独立语法意义的单元**。

比如 \`age = 18 + 2\` 这行，能切成这些 token：

| token 文本 | 类型 | 含义 |
|-----------|------|------|
| \`age\` | NAME | 名字（变量名） |
| \`=\` | OP | 运算符（赋值） |
| \`18\` | NUMBER | 数字字面量 |
| \`+\` | OP | 运算符（加） |
| \`2\` | NUMBER | 数字字面量 |
| \`\\n\` | NEWLINE | 换行（语句结束） |

注意：\`18\` 是**一个** token，不会被切成 \`1\` 和 \`8\`。
\`age\` 也是一个 token，不会变成 \`a\`、\`g\`、\`e\`。

## 四、Python 里常见的 token 类型

Python 的 token 类型在标准库 \`token\` 模块里定义好了，常见的有：

| 类型名 | 含义 | 例子 |
|--------|------|------|
| NAME | 名字（变量、函数、关键字） | \`age\`、\`if\`、\`print\` |
| NUMBER | 数字 | \`18\`、\`3.14\`、\`0x1F\` |
| STRING | 字符串 | \`"hi"\`、\`'a'\` |
| OP | 运算符和标点 | \`+\`、\`=\`、\`(\`、\`,\` |
| NEWLINE | 换行 | 一行结束 |
| INDENT | 缩进增加 | 进入新的代码块 |
| DEDENT | 缩进减少 | 退出代码块 |
| COMMENT | 注释 | \`# 这是注释\` |
| ENDMARKER | 文件结束 | 源码读到头了 |

有个细节很有意思：**缩进也是 token**！这就是为什么 Python 靠缩进表示代码块——
词法分析阶段就把缩进信息记下来了，后面语法分析直接用。

## 五、用大白话：拆"主谓宾"

还记得小学语文吗？老师让你把句子拆成"主语、谓语、宾语"：

> "小明 吃 苹果" → 主语(小明) + 谓语(吃) + 宾语(苹果)

词法分析干的是类似的事，但更细：它不关心语法结构（那是下一步的事），
它只负责**把字符流切成一个个独立的词**，至于这些词之间啥关系，
它不管，留给下一章的语法分析去处理。

## 六、词法分析怎么切？规则长啥样

切 token 靠的是**规则**（正则 + 状态机），比如：

- **名字**：字母或下划线开头，后面跟字母、数字、下划线 → \`age\`、\`_tmp\`、\`x1\`
- **数字**：一串数字（可带小数点）→ \`18\`、\`3.14\`
- **字符串**：成对引号包起来，比如双引号 "hi" 或单引号 'a'
- **运算符**：\`+\`、\`-\`、\`*\`、\`/\`、\`==\`、\`!=\` 等
- **空白**：空格用来分隔 token，本身通常不作为 token（缩进除外）

有个经典的"歧义"例子：\`a+++b\` 会被切成 \`a\`、\`++\`、\`+\`、\`b\` 还是
\`a\`、\`+\`、\`++\`、\`b\`？Python 会按"最长匹配"原则，切成
\`a\`、\`+\`、\`+\`、\`+\`、\`b\`（因为 \`+++\` 不是合法运算符）。
不过这种写法很坑，别在真实代码里这么写。

## 七、关键字 vs 名字

有个容易混的点：\`if\`、\`for\`、\`while\` 这些**关键字**，在词法分析阶段
其实也是 \`NAME\` 类型的 token。词法分析不区分关键字和普通名字，
是**语法分析**阶段才把关键字挑出来当特殊语法结构处理的。

所以词法分析眼里：

- \`if\` 和 \`age\` 都是 NAME，没本质区别
- 区分它们是后面的事

## 八、注释去哪了

你写的 \`# 这是注释\`，词法分析会把它当成一个 \`COMMENT\` token。
但默认情况下，**注释在语法分析之前会被丢掉**（除非你显式要它）。

所以注释不影响程序运行，只给人看。这也是为什么注释里写错了代码不会报错——
它在很早的阶段就被"扔"出去了。

## 九、字符串里的特殊字符怎么办

比如 \`"a\\"b"\` 这种带转义的字符串，词法分析会**整个**当成一个 STRING token，
包括转义符。字符串内部的解析（\\"\` 变成引号）是更后面的事。

再比如 \`"第一行\\n第二行"\`，\`\\n\` 是字符串**内容**的一部分，
不会被当成 NEWLINE token。词法分析很聪明，知道引号包起来的东西要整体处理。

## 十、词法分析能发现什么错误

词法分析能抓的错误有限，主要是"切不动"的错误，比如：

- 引号没闭合：\`"abc\` → 报错 EOL while scanning string literal
- 缩进不一致：tab 和空格混用 → 报错

但语法错误（比如 \`if x\` 后面没冒号）它抓不到，要等语法分析。

## 十一、为什么这一步很重要

你可能会问：切 token 有啥了不起？其实它影响很大：

1. **决定代码怎么被理解**：\`==\` 是一个运算符还是两个 \`=\`？切法不同意思完全不同
2. **报错信息的基础**：SyntaxError 里的行号、列号，都来自 token 的位置信息
3. **工具的根基**：语法高亮、自动补全、格式化工具，底层都要做词法分析

## 十二、token 的位置信息

每个 token 不只是文本，还带着**位置信息**：起始行、起始列、结束行、结束列。
这就像每个词都贴了张小标签，写着"我在第几行第几列"。

位置信息有啥用？

- **报错定位**：SyntaxError 说"第 3 行第 5 列"，就是从 token 位置来的
- **调试工具**：IDE 高亮出错位置，靠的就是这个
- **代码格式化**：black 这类工具要保留原始位置，靠 token 位置对齐

\`\`\`python
import tokenize, io
for tok in tokenize.generate_tokens(io.StringIO("x = 1").readline):
    print(tok.start, tok.end, tok.string)
\`\`\`

输出里你能看到 (1, 0) 这样的坐标——第 1 行第 0 列。Python 的行列号都是从 0 开始数的。

## 十三、一个完整切分示例

拿 \`if age > 18:\` 这行，一步步看 Python 怎么切：

| 步骤 | 读到的字符 | 切出的 token | 类型 |
|------|-----------|-------------|------|
| 1 | \`if\` | \`if\` | NAME |
| 2 | 空格 | (跳过) | — |
| 3 | \`age\` | \`age\` | NAME |
| 4 | 空格 | (跳过) | — |
| 5 | \`>\` | \`>\` | OP |
| 6 | 空格 | (跳过) | — |
| 7 | \`18\` | \`18\` | NUMBER |
| 8 | \`:\` | \`:\` | OP |
| 9 | 换行 | 换行 | NEWLINE |

你看，9 步里切出 6 个有效 token，空格被跳过，换行单独成一个 token。
整个过程像流水线一样，字符一个个进来，token 一个个出去。

## 十四、tokenize 模块的两个常用函数

标准库 \`tokenize\` 给了两个主力函数：

| 函数 | 用途 | 输入 |
|------|------|------|
| \`tokenize.tokenize\` | 从真实文件读，带字节序处理 | 二进制文件对象 |
| \`tokenize.generate_tokens\` | 从字符串读，更轻量 | readline 函数 |

日常拿字符串做实验，\`generate_tokens\` 配 \`io.StringIO\` 最方便：

\`\`\`python
import tokenize, io
src = "x = 1"
for t in tokenize.generate_tokens(io.StringIO(src).readline):
    print(t)
\`\`\`

注意 \`generate_tokens\` 吃的是一个 **readline 函数**，不是字符串本身，
所以要用 \`io.StringIO(src).readline\` 包一层。每次调用这个 readline，
就返回源码的下一行，词法分析器靠它一行行喂字符。

## 十五、本章 demo 说明

Python 标准库自带了 \`tokenize\` 模块，能让你亲眼看到一行代码被切成了哪些 token。
下面 demo 演示：

1. 拿一段真实 Python 代码
2. 用 \`tokenize.generate_tokens\` 拆出所有 token
3. 打印每个 token 的类型、文本、位置
4. 统计各种 token 各有多少个

跑完你会发现：原来你写的代码，在 Python 眼里长这样。
`,
    code: `# =============================================================
# 第二章 demo：用 tokenize 拆解 Python 代码
# 演示：
#   1. 把一段源码切成 token
#   2. 打印每个 token 的类型、文本、位置
#   3. 按类型统计 token 数量
#   4. 单独看注释、字符串、缩进这些特殊 token
# =============================================================

import tokenize                  # 导入词法分析模块
import io                       # 导入 io 模块，用 StringIO 把字符串当文件
from token import tok_name      # 导入 token 编号到名字的映射表
from collections import Counter # 导入计数器，用来统计

# 准备一段示例 Python 源代码（带注释、字符串、缩进、运算符）
source = '''# 这是一个示例
name = "张三"
age = 18 + 2
if age > 18:
    print(f"{name} 已成年")
'''

print("=" * 50)                 # 打印分隔线
print("Python 执行原理 — 第二章 demo：词法分析")  # 打印标题
print("=" * 50)                 # 打印分隔线
print()                         # 打印空行

# ===== 1. 打印源代码 =====
print("【源代码】")              # 打印小节标题
print(source)                   # 打印源代码内容
print("-" * 50)                 # 打印短分隔线
print()                         # 打印空行

# ===== 2. 拆分 token 并逐个打印 =====
print("【所有 token】")          # 打印小节标题
reader = io.StringIO(source).readline  # 把源码字符串包装成可按行读的对象
for tok in tokenize.generate_tokens(reader):  # 逐个生成 token
    tname = tok_name[tok.type]  # 把数字类型编号转成可读的名字
    tstring = tok.string        # 取出 token 的文本内容
    start = tok.start           # 取出 token 的起始位置 (行, 列)
    end = tok.end               # 取出 token 的结束位置 (行, 列)
    line = tok.line             # 取出 token 所在的整行源码
    print(f"  {tname:12} text={tstring!r:22} pos={start}->{end}")  # 打印这个 token
print()                         # 打印空行
print("-" * 50)                 # 打印短分隔线
print()                         # 打印空行

# ===== 3. 按类型统计 token 数量 =====
print("【按类型统计】")          # 打印小节标题
reader2 = io.StringIO(source).readline  # 重新建一个 reader，因为上一个读完了
counter = Counter()             # 创建计数器
for tok in tokenize.generate_tokens(reader2):  # 再拆一遍 token
    counter[tok_name[tok.type]] += 1  # 给对应类型计数加 1
for name, count in counter.most_common():  # 按数量从多到少遍历
    print(f"  {name:12} {count} 个")  # 打印类型和数量
print()                         # 打印空行
print("-" * 50)                 # 打印短分隔线
print()                         # 打印空行

# ===== 4. 单独看几种特殊 token =====
print("【特殊 token 展示】")     # 打印小节标题
reader3 = io.StringIO(source).readline  # 再建一个 reader
print("  注释 token：")          # 打印小标签
for tok in tokenize.generate_tokens(reader3):  # 拆 token
    if tok_name[tok.type] == "COMMENT":  # 如果是注释
        print(f"    {tok.string!r}")  # 打印注释内容
reader4 = io.StringIO(source).readline  # 再建一个 reader
print("  字符串 token：")        # 打印小标签
for tok in tokenize.generate_tokens(reader4):  # 拆 token
    if tok_name[tok.type] == "STRING":  # 如果是字符串
        print(f"    {tok.string!r}")  # 打印字符串内容
reader5 = io.StringIO(source).readline  # 再建一个 reader
print("  缩进 token：")          # 打印小标签
for tok in tokenize.generate_tokens(reader5):  # 拆 token
    if tok_name[tok.type] in ("INDENT", "DEDENT"):  # 如果是缩进变化
        print(f"    {tok_name[tok.type]:8} (缩进变化)")  # 打印缩进类型
print()                         # 打印空行

# ===== 5. 总结 =====
print("=" * 50)                 # 打印分隔线
print("总结：")                  # 打印总结标题
print("• tokenize 把源码切成一个个最小单元 token")  # 打印要点 1
print("• 每个 token 带类型、文本、位置信息")  # 打印要点 2
print("• 关键字和变量名在词法阶段都是 NAME")  # 打印要点 3
print("• 缩进也是 token，Python 靠它表示代码块")  # 打印要点 4
print("• 这是编译的第一步：词法分析")  # 打印要点 5
print("=" * 50)                 # 打印分隔线
`,
  },

  // =========================================================
  // 第三章：语法分析，把零件组装成树
  // =========================================================
  {
    id: "pyrun-03",
    group: "代码到指令",
    icon: "🌳",
    title: "语法分析：把零件组装成树",
    content: `## 一、从"造句"说起

上一章我们把代码切成了一个一个 token（零件）。但光有零件没用——
一堆零件摊在地上，还是看不懂这句话想说啥。

就像给你一堆词："我"、"吃"、"苹果"，你得把它们**组装成句子**：
"我吃苹果"，才有意义。组装的方式（谁主语、谁谓语、谁宾语）就是**语法**。

Python 也一样：拿到 token 流之后，下一步要按语法规则，
把 token **组装成一棵树**，表示代码的结构。这一步叫**语法分析（parsing）**，
产物叫 **AST（抽象语法树）**。

## 二、AST 是什么

**AST = Abstract Syntax Tree，抽象语法树**。

它把你的代码组织成一棵**树形结构**，每个节点代表一个语法结构。

举个例子，\`1 + 2\` 的 AST 是这样：

\`\`\`
      BinOp (二元运算)
      /  |  \\
   1   Add   2
   (左) (运算符) (右)
\`\`\`

- 根节点是 \`BinOp\`（二元运算）
- 左子节点是数字 \`1\`
- 运算符是 \`Add\`（加）
- 右子节点是数字 \`2\`

这棵树清楚地表达了："1 和 2 做加法"。

## 三、为什么叫"抽象"

因为树里**丢掉了一些不影响语义的细节**，只保留和"意义"有关的信息。

比如 \`1 + 2\` 和 \`1 +2\` 和 \`1+2\`，空格不一样，但 AST 是同一棵——
空格在词法分析后就不再重要了。

再比如括号 \`(1 + 2) * 3\`，括号的作用体现在**树的层级**里，
AST 里不会专门有个"括号节点"，而是通过谁是谁的子节点来表达。

## 四、用大白话：组装"句法树"

还记得小学拆句子吗？老师让你把"小明吃苹果"画成句法树：

\`\`\`
        句子
       /    \\
   主语     谓语
   小明    吃苹果
           / \\
         谓  宾
         吃  苹果
\`\`\`

AST 干的就是这个事，只不过针对的是编程语言的语法。

\`x = 1 + 2\` 的 AST 大致是：

\`\`\`
        Assign (赋值语句)
       /        \\
   目标          值
    x         BinOp
             /  |  \\
            1  Add  2
\`\`\`

"把 x 赋值为 1+2 的结果"——结构一目了然。

## 五、AST 节点有哪些类型

Python 的 AST 节点类型很多，常见的有：

| 节点类型 | 表示什么 | 例子 |
|---------|---------|------|
| \`Module\` | 整个模块（根节点） | 一整个文件 |
| \`Assign\` | 赋值语句 | \`x = 1\` |
| \`BinOp\` | 二元运算 | \`a + b\` |
| \`Compare\` | 比较 | \`a > b\` |
| \`If\` | if 语句 | \`if x: ...\` |
| \`For\` | for 循环 | \`for i in ...: ...\` |
| \`While\` | while 循环 | \`while x: ...\` |
| \`FunctionDef\` | 函数定义 | \`def f(): ...\` |
| \`Call\` | 函数调用 | \`print(x)\` |
| \`Name\` | 名字引用 | \`x\` |
| \`Constant\` | 常量 | \`1\`、\`"hi"\` |
| \`Return\` | return 语句 | \`return x\` |

每种节点都有自己的字段，存子节点和附加信息。

## 六、为什么需要 AST

你可能会问：token 流不就够了吗，为啥还要费劲组装成树？

因为**树能表达嵌套和优先级**，这是 token 流做不到的。

\`\`\`
1 + 2 * 3
\`\`\`

token 流是扁平的：\`1\`、\`+\`、\`2\`、\`*\`、\`3\`，看不出谁先算。
但 AST 是：

\`\`\`
       BinOp (+)
      /        \\
     1       BinOp (*)
            /    \\
           2      3
\`\`\`

树的结构天然表达了"先算 2*3，再加 1"——\`*\` 在更深层，会先被算。

AST 的好处：

1. **表达结构**：嵌套、优先级、作用域，都能体现
2. **方便优化**：编译器可以在 AST 上做各种优化（常量折叠等）
3. **方便生成代码**：遍历树就能生成字节码
4. **工具的基石**：linter、类型检查、代码转换都基于 AST

## 七、用 ast 模块看 AST

Python 标准库的 \`ast\` 模块能让你亲眼看到 AST。

\`\`\`python
import ast
tree = ast.parse("x = 1 + 2")
print(ast.dump(tree))
\`\`\`

输出大概长这样：

\`\`\`
Module(body=[Assign(targets=[Name(id='x')], value=BinOp(left=Constant(value=1), op=Add(), right=Constant(value=2)))])
\`\`\`

虽然一行有点长，但仔细看就是一棵树的括号表示。

用 \`ast.dump(tree, indent=2)\` 还能输出带缩进的版本，更清楚。

## 八、语法分析怎么工作

简单说，语法分析器拿 token 流，按一套**语法规则**（文法）去匹配。

Python 的文法大致是这样（简化版）：

\`\`\`
语句 → 简单语句 | 复合语句
复合语句 → if语句 | for语句 | def语句 | ...
if语句 → 'if' 表达式 ':' 语句块
语句块 → 语句 语句块 | 语句
表达式 → 比较表达式 | 算术表达式 | ...
算术表达式 → 算术表达式 '+' 项 | 项
项 → 项 '*' 因子 | 因子
因子 → 数字 | 名字 | '(' 表达式 ')'
\`\`\`

这套规则叫**文法（grammar）**。语法分析器按规则把 token 一层层归约成节点，
最后组装成一棵完整的 AST。

Python 3.9+ 用的是 **PEG 解析器**（PEG = Parsing Expression Grammar），
比老的 LL(1) 更强大，能处理更多歧义。

## 九、语法错误是怎么来的

当 token 流**无法**按文法组装成合法 AST 时，就报 SyntaxError。

比如 \`if x\` 后面没冒号：

\`\`\`python
if x
    print(x)
\`\`\`

语法分析器读到 \`if x\` 后期待一个 \`:\`，但下一个 token 是换行，
对不上规则，于是报错：

\`\`\`
SyntaxError: expected ':'
\`\`\`

报错信息里的"expected ':'"就是文法规则里那个冒号。

## 十、AST 能干啥好玩的事

AST 不只是编译器内部用的，你也能玩：

- **代码检查**：遍历 AST，找出所有 \`eval\` 调用，警告"别用 eval"
- **代码转换**：自动把 \`print x\`（Python2）改成 \`print(x)\`（Python3）
- **自动生成文档**：从 AST 里提取所有函数定义，生成 API 文档
- **类型检查**：mypy 这类工具就是基于 AST 做类型分析
- **代码格式化**：black 把 AST 重新格式化成统一风格

下面演示一个超简单的小工具：**数一数代码里有多少个函数定义**。

## 十一、本章 demo 说明

下面 demo 用 \`ast\` 模块实际玩一玩 AST：

1. 把一段源码解析成 AST
2. 用 \`ast.dump\` 打印树的结构
3. 手动遍历所有节点
4. 写个小工具：从代码里找出所有函数定义，打印函数名和参数

跑完你会对"代码 = 一棵树"有直观感受。
`,
    code: `# =============================================================
# 第三章 demo：用 ast 模块查看抽象语法树
# 演示：
#   1. 把源码解析成 AST
#   2. 用 ast.dump 打印树结构
#   3. 手动遍历 AST 节点
#   4. 写个小工具：找出所有函数定义
# =============================================================

import ast                      # 导入抽象语法树模块

# 准备一段示例源代码
source = '''x = 1 + 2
if x > 2:
    print("大")
else:
    print("小")
'''

print("=" * 50)                 # 打印分隔线
print("Python 执行原理 — 第三章 demo：语法分析")  # 打印标题
print("=" * 50)                 # 打印分隔线
print()                         # 打印空行

# ===== 1. 打印源代码 =====
print("【1. 源代码】")           # 打印小节标题
print(source)                   # 打印源码内容
print("-" * 50)                 # 打印短分隔线
print()                         # 打印空行

# ===== 2. 解析成 AST =====
print("【2. 解析成 AST】")       # 打印小节标题
tree = ast.parse(source)        # 把源代码字符串解析成 AST
print("  AST 根节点类型:", type(tree).__name__)  # 打印根节点类型
print()                         # 打印空行

# ===== 3. 用 ast.dump 打印结构 =====
print("【3. AST 结构 (dump)】")  # 打印小节标题
print(ast.dump(tree, indent=2)) # 打印带缩进的 AST 结构
print()                         # 打印空行
print("-" * 50)                 # 打印短分隔线
print()                         # 打印空行

# ===== 4. 手动遍历所有节点 =====
print("【4. 手动遍历 AST 节点】")  # 打印小节标题
for node in ast.walk(tree):     # 遍历 AST 中的所有节点
    cls_name = type(node).__name__  # 取节点类名
    fields = []                 # 准备一个列表存字段信息
    for field_name, value in ast.iter_fields(node):  # 遍历节点字段
        fields.append(f"{field_name}={value!r}")  # 把字段拼成字符串
    print(f"  {cls_name}: {', '.join(fields)}")  # 打印节点信息
print()                         # 打印空行
print("-" * 50)                 # 打印短分隔线
print()                         # 打印空行

# ===== 5. 小工具：找出所有函数定义 =====
print("【5. 小工具：分析代码里的函数】")  # 打印小节标题
code2 = '''def greet(name, age=18):
    msg = "hi " + name
    return msg

def add(a, b):
    return a + b
'''
print("代码：")                  # 打印小标签
print(code2)                    # 打印第二段代码
tree2 = ast.parse(code2)        # 解析第二段代码
func_count = 0                  # 函数计数器清零
for node in ast.walk(tree2):    # 遍历所有节点
    if isinstance(node, ast.FunctionDef):  # 如果是函数定义节点
        func_count += 1         # 计数加 1
        name = node.name        # 取函数名
        args = [a.arg for a in node.args.args]  # 取参数名列表
        defaults = node.args.defaults  # 取默认值
        body_len = len(node.body)  # 取函数体语句数
        print(f"  函数 {func_count}: {name}{args}")  # 打印函数名和参数
        print(f"    默认值个数: {len(defaults)}")  # 打印默认值个数
        print(f"    函数体语句数: {body_len}")  # 打印函数体语句数
print()                         # 打印空行

# ===== 6. 总结 =====
print("=" * 50)                 # 打印分隔线
print("总结：")                  # 打印总结标题
print("• ast.parse 把源码变成抽象语法树 AST")  # 打印要点 1
print("• 每个节点是一个 AST 类的实例")  # 打印要点 2
print("• ast.walk 可以遍历所有节点")  # 打印要点 3
print("• 树结构能表达嵌套和优先级")  # 打印要点 4
print("• AST 是编译第二步的产物")  # 打印要点 5
print("=" * 50)                 # 打印分隔线
`,
  },

  // =========================================================
  // 第四章：编译，把树变成字节码
  // =========================================================
  {
    id: "pyrun-04",
    group: "代码到指令",
    icon: "⚙️",
    title: "编译：把树变成字节码",
    content: `## 一、从"菜谱"说起

上一章我们把代码变成了一棵 AST（语法树）。但 AST 只是"结构表示"，
还不能直接执行——它就像一棵组织好的知识树，告诉你"先做啥后做啥"，
但没告诉 CPU **具体怎么动手**。

就像你有一张菜谱的**结构图**：先备料 → 再下锅 → 最后装盘。
但具体每一步用什么动作（切、炒、加盐），还得翻译成**操作步骤**。

编译干的就是这个：**把 AST 翻译成一系列可执行的操作步骤**——字节码。

## 二、字节码是什么

**字节码（bytecode）= Python 虚拟机能理解的指令**。

它不是机器码（CPU 不认识），而是 PVM 专属的"中间指令"。
每条字节码指令做一件小事，比如：

- 把一个数压到栈上
- 弹出两个数做加法
- 跳到某条指令
- 调用一个函数

字面上，字节码就是一串**字节**（每个指令 1-2 字节），
存在代码对象的 \`co_code\` 字段里。

用 \`dis\` 模块反汇编后，能看到人能读的形式：

\`\`\`
LOAD_FAST   0 (a)    # 把局部变量 a 压栈
LOAD_FAST   1 (b)    # 把局部变量 b 压栈
BINARY_ADD          # 弹出 a,b，相加，结果压栈
RETURN_VALUE        # 返回栈顶
\`\`\`

## 三、AST → 字节码的过程

这一步由 Python 的**编译器**完成（CPython 里是 \`compile.c\` 那一块）。
过程大致是：

1. **遍历 AST**：从根节点开始，递归访问每个节点
2. **逐节点生成指令**：每种 AST 节点对应一段字节码生成规则
   - 看到 \`BinOp\`（加法）→ 生成"加载左、加载右、相加"
   - 看到 \`If\` → 生成"判断、条件跳转、then 块、else 块"
   - 看到 \`For\` → 生成"取迭代器、循环取值、跳回"
3. **组装成代码对象**：字节码 + 常量 + 变量名等打包成 \`code object\`

最终产物就是一个**代码对象**，可以被虚拟机执行。

## 四、字节码指令长啥样

每条字节码指令由两部分组成：

- **操作码（opcode）**：1 字节，表示"做什么"，比如 \`LOAD_CONST\`、\`BINARY_ADD\`
- **参数（arg）**：0-2 字节，表示"对谁做"，比如常量索引、变量索引

不是所有指令都有参数，没参数的叫"无参指令"。

用 \`dis\` 看时，每行大概是这样：

\`\`\`
  2           0 LOAD_CONST               1 (1)
              2 LOAD_CONST               2 (2)
              4 BINARY_ADD
              6 RETURN_VALUE
\`\`\`

- 第 1 列 \`2\`：源码行号
- 第 2 列 \`0\`、\`2\`、\`4\`：指令偏移量（字节地址）
- 第 3 列 \`LOAD_CONST\`：操作码名字
- 第 4 列 \`1\`：参数（常量池里的索引）
- 第 5 列 \`(1)\`：参数的可读解释（常量值是 1）

## 五、if/for/函数调用 的字节码

不同语法结构，字节码长得不一样：

### 5.1 if 语句

\`\`\`python
if x > 0:
    print("正")
else:
    print("非正")
\`\`\`

字节码思路：

\`\`\`
LOAD x, LOAD 0, COMPARE >     # 计算 x > 0
POP_JUMP_IF_FALSE -> else标签  # 为假就跳到 else
... then 块的指令 ...
JUMP_FORWARD -> end           # 跳过 else
else标签: ... else 块的指令 ...
end: ...
\`\`\`

**跳转指令**是控制流的关键。

### 5.2 for 循环

\`\`\`python
for i in range(3):
    print(i)
\`\`\`

字节码思路：

\`\`\`
GET_ITER                      # 取迭代器
循环头: FOR_ITER -> 结束标签   # 取下一个，没了就跳走
STORE i                       # 存到 i
... 循环体 ...
JUMP_BACKWARD -> 循环头       # 跳回循环头
结束标签: ...
\`\`\`

### 5.3 函数调用

\`\`\`python
print("hi", end="")
\`\`\`

字节码思路：

\`\`\`
LOAD print           # 把函数本身压栈
LOAD "hi"            # 压第一个参数
LOAD ""              # 压第二个参数
CALL 2               # 调用，参数个数 2
\`\`\`

调用指令在不同 Python 版本不一样：3.11+ 用 \`CALL\`，老版本用 \`CALL_FUNCTION\`。

## 六、.pyc 文件再详解

第 1 章提过 \`.pyc\`，现在结合编译讲清楚。

**.pyc 里存的是啥？** 编译后的**代码对象**，序列化后的二进制。

结构大概是：

\`\`\`
magic number (4字节)   # 标识 Python 版本，跨版本不兼容
时间戳 (4字节)         # 源码修改时间，用来判断是否过期
源码大小 (4字节)       # 源文件大小
序列化的代码对象       # 用 marshal 模块序列化
\`\`\`

**加载流程**：

1. import 模块时，先看 \`__pycache__/xxx.cpython-3xx.pyc\` 在不在
2. 在的话，比对里面的时间戳和源文件时间戳
3. 源文件没改 → 直接加载 pyc，跳过编译
4. 源文件改了 → 重新编译，覆盖 pyc

这就是为什么你改了源码，下次 import 自动生效——时间戳对不上了，重新编译。

## 七、用大白话：把句法树翻译成操作步骤

| 阶段 | 比喻 | 产物 |
|------|------|------|
| AST | 菜谱结构图（备料→下锅→装盘） | 知道做啥，不知道咋动手 |
| 编译 | 把每步细化成具体动作 | 字节码：切、炒、加盐、装盘 |
| 执行 | 厨师按步骤做 | PVM 跑字节码 |

AST 告诉你"要做加法"，字节码告诉你"先把 a 拿出来，再把 b 拿出来，加一下，放回去"。

## 八、编译期能做啥优化

Python 编译器比较"老实"，优化不多，但有几个：

1. **常量折叠**：\`1 + 2\` 在编译期就算成 \`3\`，不用运行时再算
2. **常量池去重**：同一个字符串字面量只存一份
3. **死代码消除**：\`if False:\` 块里的代码可能被丢掉

但 Python 不做激进优化（不像 C 那种内联、循环展开），因为它优先保证动态特性。

可以验证常量折叠：

\`\`\`python
import dis
dis.dis(compile("1 + 2 * 3", "<s>", "eval"))
\`\`\`

你会看到只有一个 \`LOAD_CONST 7\`，根本没加法乘法指令——编译期就算好了。

## 九、代码对象里都有啥

编译产物是**代码对象（code object）**，关键字段：

| 字段 | 含义 |
|------|------|
| \`co_code\` | 字节码（一串字节） |
| \`co_consts\` | 常量池（数字、字符串、None 等） |
| \`co_names\` | 全局/属性名字 |
| \`co_varnames\` | 局部变量名 |
| \`co_argcount\` | 位置参数个数 |
| \`co_filename\` | 源文件名 |
| \`co_firstlineno\` | 函数第一行行号 |
| \`co_stacksize\` | 执行需要的栈大小 |

字节码里的"参数"大多是这些池子里的**索引**。比如 \`LOAD_CONST 1\` 就是
取 \`co_consts[1]\`。这样指令本身不用存完整数据，省空间。

## 十、本章 demo 说明

下面 demo 用 \`dis\` 看真实代码的字节码：

1. 一个带 if/for 的函数，看控制流怎么用跳转实现
2. 查看代码对象里存的所有信息
3. 看一个函数调用的字节码
4. 感受常量折叠：编译期就算好的表达式

跑完你会明白：原来 \`if\`、\`for\` 在底层就是"比较 + 跳转"。
`,
    code: `# =============================================================
# 第四章 demo：查看 if/for/函数调用的字节码
# 演示：
#   1. 一个带 if/for 的函数的字节码
#   2. 代码对象里存的所有信息
#   3. 函数调用的字节码
#   4. 常量折叠：编译期就算好的表达式
# =============================================================

import dis                      # 导入反汇编模块
import sys                      # 导入系统模块

print("=" * 50)                 # 打印分隔线
print("Python 执行原理 — 第四章 demo：编译成字节码")  # 打印标题
print("Python 版本:", sys.version.split()[0])  # 打印 Python 版本
print("=" * 50)                 # 打印分隔线
print()                         # 打印空行

# ===== 1. 定义一个带 if/for 的函数 =====
def process(items):             # 定义函数 process，接收 items 参数
    total = 0                   # 初始化总数为 0
    for item in items:          # 遍历 items 里每个 item
        if item > 0:            # 如果 item 大于 0
            total += item       # 把 item 加到 total
        else:                   # 否则
            total -= 1          # total 减 1
    return total                # 返回 total

# ===== 2. 查看这个函数的字节码 =====
print("【1. process 函数的字节码】")  # 打印小节标题
print("源码：遍历 items，正数累加，非正数减 1")  # 打印源码说明
print("-" * 50)                 # 打印短分隔线
dis.dis(process)                # 反汇编 process 的字节码
print()                         # 打印空行
print("-" * 50)                 # 打印短分隔线
print()                         # 打印空行

# ===== 3. 查看代码对象里存的信息 =====
print("【2. 代码对象里的信息】")  # 打印小节标题
code = process.__code__         # 获取 process 的代码对象
print("  函数名:", code.co_name)          # 打印函数名
print("  参数个数:", code.co_argcount)    # 打印参数个数
print("  字节码字节数:", len(code.co_code))  # 打印字节码字节数
print("  常量池:", code.co_consts)        # 打印常量池
print("  局部变量:", code.co_varnames)    # 打印局部变量名
print("  自由变量:", code.co_freevars)    # 打印自由变量
print("  需要的栈大小:", code.co_stacksize)  # 打印栈大小
print()                         # 打印空行
print("-" * 50)                 # 打印短分隔线
print()                         # 打印空行

# ===== 4. 看一个函数调用的字节码 =====
print("【3. 函数调用的字节码】")  # 打印小节标题
def caller():                   # 定义函数 caller
    return process([1, -2, 3])  # 调用 process，传一个列表
print("源码：def caller(): return process([1, -2, 3])")  # 打印源码说明
print("-" * 50)                 # 打印短分隔线
dis.dis(caller)                 # 反汇编 caller 的字节码
print()                         # 打印空行
print("-" * 50)                 # 打印短分隔线
print()                         # 打印空行

# ===== 5. 常量折叠：编译期就算好的表达式 =====
print("【4. 常量折叠】")         # 打印小节标题
print("表达式：1 + 2 * 3")      # 打印表达式说明
print("（按理说运行时才算，但编译期就折叠了）")  # 打印说明
print("-" * 50)                 # 打印短分隔线
folded = compile("1 + 2 * 3", "<demo>", "eval")  # 编译表达式
dis.dis(folded)                 # 反汇编，看是否只有 LOAD_CONST 7
print()                         # 打印空行
print("（应该只看到一个 LOAD_CONST 7，没有加法乘法指令）")  # 打印说明
print()                         # 打印空行

# ===== 6. 实际执行一下验证 =====
print("【5. 执行结果验证】")     # 打印小节标题
result = process([1, -2, 3])    # 实际调用 process
print("  process([1, -2, 3]) =", result)  # 打印结果
print("  1 + 2 * 3 =", 1 + 2 * 3)  # 打印表达式结果
print()                         # 打印空行

# ===== 7. 总结 =====
print("=" * 50)                 # 打印分隔线
print("总结：")                  # 打印总结标题
print("• AST 被编译器翻译成字节码指令序列")  # 打印要点 1
print("• 每条指令有偏移量、操作码、参数")  # 打印要点 2
print("• if/for 通过跳转指令实现控制流")  # 打印要点 3
print("• 函数调用用 CALL 指令")  # 打印要点 4
print("• 常量折叠让 1+2*3 编译期就变 7")  # 打印要点 5
print("• 字节码缓存在 .pyc 里，省编译时间")  # 打印要点 6
print("=" * 50)                 # 打印分隔线
`,
  },

  // =========================================================
  // 第五章：Python 虚拟机，执行字节码
  // =========================================================
  {
    id: "pyrun-05",
    group: "代码到指令",
    icon: "🎭",
    title: "Python 虚拟机：执行字节码",
    content: `## 一、从"厨师做菜"说起

前几章我们走完了：源码 → token → AST → 字节码。
现在字节码摊在桌上，像一张菜谱，写着"取 a、取 b、相加、放回去"。

但菜谱自己不会做菜，得有个**厨师**照着菜谱一步步操作食材。

Python 虚拟机（PVM）就是这个厨师。它读字节码，一条条执行，
操作数据（食材），最终让程序跑起来。

## 二、PVM 是什么

**PVM = Python Virtual Machine，Python 虚拟机**。

它是 CPython 解释器里**最核心**的部分——一个用 C 写的、巨大的循环，
叫**求值循环（eval loop）**。

简化后的伪代码长这样：

\`\`\`
while True:
    取下一条字节码指令
    看是什么操作码
    执行对应动作（操作栈、变量、跳转）
    更新指令指针
\`\`\`

你写的所有 Python 代码，最终都在这个循环里被一条条执行。
这个循环有多重要？可以说它是 Python 的"心脏"。

## 三、栈式虚拟机原理

PVM 是一台**栈式虚拟机**（stack-based VM），核心是**操作数栈**。

什么是栈？一种"后进先出"的结构：

- 压栈（push）：放一个数上去
- 弹栈（pop）：拿最上面的数下来

PVM 执行字节码时，数据都放在**栈顶**，运算时从栈顶取数、把结果压回去。

\`\`\`
1 + 2 的执行过程：

指令 1: LOAD_CONST 1   → 栈: [1]
指令 2: LOAD_CONST 2   → 栈: [1, 2]
指令 3: BINARY_ADD     → 弹出 2 和 1，相加得 3，压回
                           栈: [3]
指令 4: RETURN_VALUE   → 弹出栈顶 3，返回
\`\`\`

所有的运算都靠这个栈。不管多复杂的表达式，最后都是"压、压、算、压"。

## 四、为什么不直接用寄存器？

你可能听过"寄存器式虚拟机"（比如 Lua、Dalvik），它用寄存器存操作数，
不像 PVM 这样用栈。

两种各有优劣：

| 维度 | 栈式（Python、JVM） | 寄存器式（Lua、Dalvik） |
|------|---------------------|------------------------|
| 指令长度 | 短（不用写寄存器号） | 长（要指定寄存器） |
| 指令条数 | 多（要频繁压弹） | 少（直接指定操作数） |
| 实现难度 | 简单 | 稍复杂 |
| 可移植性 | 好 | 一般 |

Python 选栈式，主要是**简单、好实现、好移植**。代价是同样的事要更多条指令，
但每条指令短，整体差距没那么大。

## 五、执行一条字节码指令的过程

拿 \`BINARY_ADD\`（加法）举例，PVM 收到这条指令后：

1. 从栈顶弹出右操作数 \`b\`
2. 再弹出左操作数 \`a\`
3. 调用 \`a + b\`（实际是 \`PyNumber_Add\`）
4. 把结果压回栈顶
5. 指令指针前进，取下一条

整个过程就是"动动栈，调个 C 函数，再动动栈"。

注意第 3 步：\`a + b\` 在 Python 里不是简单的 CPU 加法，而是要
走**运算符重载、类型分发**那一套——这就是 Python 比 C 慢的原因之一。
C 的 \`a + b\` 直接编译成一条 \`add\` 指令；Python 的 \`a + b\` 要查类型、
找 \`\_\_add\_\_\` 方法、可能创建新对象。

## 六、帧对象（frame）：函数调用的容器

每次**调用一个函数**，PVM 就会创建一个**帧（frame）**对象。

帧里存着：

| 字段 | 含义 |
|------|------|
| \`f_code\` | 这个函数的代码对象（字节码在这） |
| \`f_locals\` | 局部变量字典 |
| \`f_globals\` | 全局变量字典 |
| \`f_back\` | 调用方的帧（链起来形成调用栈） |
| \`f_lineno\` | 当前执行到第几行 |
| \`f_lasti\` | 当前执行到第几条字节码 |
| \`f_builtins\` | 内置名字空间 |

函数调用 = 创建新帧 + 压入调用栈 + 执行它的字节码 + 返回时弹出帧。

调用栈长这样（A 调用 B，B 调用 C）：

\`\`\`
栈顶 → 帧 C (正在执行)
        帧 B (等待 C 返回)
        帧 A (等待 B 返回)
栈底 → 帧 main
\`\`\`

递归太深会爆栈（\`RecursionError\`），就是因为帧太多，调用栈撑不住了。

## 七、用大白话：PVM 是个按菜谱操作的厨师

| 元素 | 比喻 | PVM 里 |
|------|------|--------|
| 字节码 | 菜谱 | 指令序列 |
| 操作数栈 | 厨师的操作台 | 存中间数据 |
| 帧 | 每道菜一个工作区 | 每个函数调用一个帧 |
| 调用栈 | 一摞工作区 | 函数调用链 |
| 指令指针 | 厨师看到第几步 | \`f_lasti\` |

厨师（PVM）看菜谱（字节码），在工作台（栈）上操作食材（数据），
每道菜（函数）有独立工作区（帧），多道菜摞起来（调用栈）。

## 八、执行循环的细节

CPython 的主循环（简化版）大致：

\`\`\`
取指令 = code[f_lasti]
switch (取指令) {
    case LOAD_CONST:
        把常量压栈; f_lasti += 2; break;
    case LOAD_FAST:
        把局部变量压栈; f_lasti += 2; break;
    case BINARY_ADD:
        弹两个, 加, 压回; f_lasti += 2; break;
    case RETURN_VALUE:
        弹栈顶, 返回给调用帧; break;
    case JUMP_FORWARD:
        f_lasti = 跳转目标; break;
    ...
}
\`\`\`

每条指令执行后，要么前进，要么跳转。整个程序就是不停转这个循环，
直到所有指令跑完。

## 九、GIL 和虚拟机的关系

顺便提一句：CPython 有个 **GIL（全局解释器锁）**，同一时刻只有一个线程
能执行字节码。也就是说，多线程在 PVM 这一层其实是**串行**的——
轮流抢 GIL，谁抢到谁跑一段循环。

这是为什么 Python 多线程不能真正利用多核跑 CPU 密集任务。
（I/O 密集没事，因为等 I/O 时会释放 GIL。）

GIL 不是字节码本身的限制，而是 CPython 实现的限制。
PyPy、Jython 的实现策略不同。第 5 章先了解这个概念，后面专门讲。

## 十、用 sys._getframe 窥探帧

Python 提供了一个"后门"：\`sys._getframe()\`，能拿到**当前正在执行的帧**。

\`\`\`python
import sys

def f():
    g()

def g():
    frame = sys._getframe()       # 当前帧（g 的帧）
    print(frame.f_code.co_name)   # 'g'
    caller = frame.f_back         # 调用方帧（f 的帧）
    print(caller.f_code.co_name)  # 'f'
\`\`\`

通过 \`f_back\` 一路往上，就能遍历整个调用栈。错误 traceback 里那一串
"File xxx, line yyy, in func" 就是这么来的——遍历帧链，打印每个帧的
文件、行号、函数名。

注意：\`sys._getframe\` 是 CPython 特有的，不建议在生产代码里乱用，
但用来理解原理非常合适。

## 十一、把前 4 章串起来

到这里，整条链路就完整了：

\`\`\`
源码 (.py)
  ↓ 词法分析 (tokenize)
token 流
  ↓ 语法分析 (ast.parse)
AST
  ↓ 编译 (compile)
字节码 (code object)
  ↓ PVM 执行 (eval loop)
程序运行！
\`\`\`

你终于知道：每次敲下 \`python xxx.py\`，背后发生了这一长串事。

## 十二、本章 demo 说明

下面 demo 带你：

1. 看 \`1 + 2\` 的字节码，并手动模拟栈式执行过程
2. 用 \`sys._getframe\` 查看当前帧、调用方帧
3. 看帧里的局部变量
4. 看一段循环的字节码

跑完你会真切感受到：PVM 就是个"按菜谱操作栈"的厨师。
`,
    code: `# =============================================================
# 第五章 demo：Python 虚拟机与帧
# 演示：
#   1. 1 + 2 的字节码和栈式执行过程
#   2. 用 sys._getframe 查看当前帧和调用链
#   3. 帧里的局部变量
#   4. 一段循环的字节码
# =============================================================

import sys                       # 导入系统模块
import dis                       # 导入反汇编模块

print("=" * 50)                  # 打印分隔线
print("Python 执行原理 — 第五章 demo：PVM 执行字节码")  # 打印标题
print("=" * 50)                  # 打印分隔线
print()                          # 打印空行

# ===== 1. 查看 1 + 2 的字节码并执行 =====
print("【1. 1 + 2 的字节码】")   # 打印小节标题
code = compile("1 + 2", "<demo>", "eval")  # 把表达式编译成代码对象
dis.dis(code)                    # 反汇编这段表达式的字节码
result = eval(code)              # 让 PVM 执行这段字节码
print("  执行结果:", result)     # 打印执行结果
print()                          # 打印空行
print("-" * 50)                  # 打印短分隔线
print()                          # 打印空行

# ===== 2. 手动模拟栈式执行过程 =====
print("【2. 栈式执行 1 + 2 的过程】")  # 打印小节标题
print("  指令 LOAD_CONST 1  → 栈: [1]")          # 打印第 1 步
print("  指令 LOAD_CONST 2  → 栈: [1, 2]")       # 打印第 2 步
print("  指令 BINARY_ADD    → 弹出 1,2 压入 3 → 栈: [3]")  # 打印第 3 步
print("  指令 RETURN_VALUE  → 返回栈顶 3")       # 打印第 4 步
print()                          # 打印空行
print("-" * 50)                  # 打印短分隔线
print()                          # 打印空行

# ===== 3. 用 sys._getframe 查看调用链 =====
print("【3. 用 sys._getframe 查看调用链】")  # 打印小节标题

def outer():                     # 定义外层函数 outer
    middle()                     # outer 里调用 middle

def middle():                    # 定义中间函数 middle
    inner()                      # middle 里调用 inner

def inner():                     # 定义内层函数 inner
    frame = sys._getframe()      # 获取当前帧（inner 的帧）
    print("  当前函数:", frame.f_code.co_name)  # 打印当前函数名
    frame2 = frame.f_back        # 获取调用方帧（middle 的帧）
    print("  调用者:  ", frame2.f_code.co_name)  # 打印调用者函数名
    frame3 = frame2.f_back       # 再往上一层（outer 的帧）
    print("  上上层:  ", frame3.f_code.co_name)  # 打印上上层函数名
    frame4 = frame3.f_back       # 再往上一层（模块顶层的帧）
    print("  顶层:    ", frame4.f_code.co_name)  # 打印顶层函数名
    print("  文件名:  ", frame.f_code.co_filename)  # 打印文件名
    print("  当前行号:", frame.f_lineno)  # 打印当前行号

outer()                          # 调用 outer，启动整条调用链
print()                          # 打印空行
print("-" * 50)                  # 打印短分隔线
print()                          # 打印空行

# ===== 4. 帧的局部变量 =====
print("【4. 帧的局部变量】")     # 打印小节标题
def show_locals(a, b):           # 定义函数，带两个参数
    c = a + b                    # 计算 c = a + b
    d = c * 2                    # 计算 d = c * 2
    frame = sys._getframe()      # 获取当前帧
    print("  局部变量:", frame.f_locals)  # 打印帧的局部变量
    print("  函数名:  ", frame.f_code.co_name)  # 打印函数名
    print("  参数个数:", frame.f_code.co_argcount)  # 打印参数个数

show_locals(10, 20)              # 调用函数
print()                          # 打印空行
print("-" * 50)                  # 打印短分隔线
print()                          # 打印空行

# ===== 5. 一段循环的字节码 =====
print("【5. 循环的字节码】")     # 打印小节标题
def loop_demo():                 # 定义循环示例函数
    s = 0                        # 初始化累加和为 0
    for i in range(3):           # 循环 3 次
        s += i                   # 把 i 累加到 s
    return s                     # 返回累加和

print("源码：s=0; for i in range(3): s+=i; return s")  # 打印源码说明
print("-" * 50)                  # 打印短分隔线
dis.dis(loop_demo)               # 反汇编循环函数的字节码
print()                          # 打印空行
print("  执行结果:", loop_demo())  # 打印实际执行结果
print()                          # 打印空行

# ===== 6. 总结 =====
print("=" * 50)                  # 打印分隔线
print("总结：")                   # 打印总结标题
print("• PVM 是栈式虚拟机，靠操作数栈运算")  # 打印要点 1
print("• 每条字节码指令操作栈顶")  # 打印要点 2
print("• 每次函数调用创建一个帧 frame")  # 打印要点 3
print("• 帧保存局部变量、字节码、调用链")  # 打印要点 4
print("• sys._getframe 可以窥探运行时帧")  # 打印要点 5
print("• 整条链路：源码→token→AST→字节码→PVM 执行")  # 打印要点 6
print("=" * 50)                  # 打印分隔线
`,
  },
];
