// =============================================================
// Batch 1：硬件基础（4 章）
// =============================================================
// 「计算机原理实战教程」第 1 批章节
//   · 定位：面向开发者的计算机原理教程，生活化比喻讲解运转本质
//   · 每章配可运行的 C / Shell demo（本地沙箱有 clang 和 bash）
//   · 4 章：CPU 架构 / 内存缓存 / 总线 I/O / 并行并发
// =============================================================

export const chapters = [
  // ============================================================
  // 第 1 章：CPU 架构与指令周期
  // ============================================================
  {
    id: "comp-cpu-architecture",
    group: "硬件基础",
    icon: "🔧",
    title: "CPU 架构与指令周期",
    content: `
## 概述
CPU 是计算机的「大脑」，但打开外壳你会发现它内部其实是一个高度协作的流水线车间。本章从 ALU、寄存器、控制单元、缓存四大部件讲起，拆解一条指令从内存到执行的完整生命周期（取指-译码-执行-写回），再延伸到时钟频率、IPC、流水线与分支预测这些决定 CPU 真实性能的关键概念。理解了这些，你才会明白为什么「主频高不一定快」、为什么「分支预测失败会拖慢程序」。

把 CPU 想象成一个厨房主厨：ALU 是他的炒锅（真正干活的地方），寄存器是案板上伸手就够到的调料盒（最快但容量极小），L1/L2/L3 缓存是不同距离的食材架（越近越快越贵），内存则是楼下仓库（容量大但来回跑很慢）。主厨做一道菜，需要不断从不同距离取食材——这正对应 CPU 取数据的过程：能就近绝不跑远。

## 核心要点
- **ALU（算术逻辑单元）**: CPU 里真正干活的运算核心 - 负责加减、逻辑运算（与/或/非）、移位。所有算术最终都汇集到这里。
- **寄存器**: CPU 内部最快的存储 - 通用寄存器（rax/rbx 等）、指令指针（rip）、标志寄存器（rflags）。x86_64 只有 16 个通用寄存器，数量极少但速度是 L1 的好几倍。
- **控制单元（CU）**: CPU 的「指挥」 - 负责取指、译码、发出控制信号，协调 ALU/寄存器/总线协同工作。
- **缓存层级**: L1（每核独占，约 1ns，几十 KB）→ L2（每核独占，约 3-4ns，几百 KB）→ L3（全核共享，约 10-20ns，几 MB）→ 内存（约 100ns）。
- **指令周期**: 取指（Fetch，从缓存/内存读到指令）→ 译码（Decode，解析指令含义）→ 执行（Execute，ALU 运算）→ 写回（Writeback，结果写回寄存器）。
- **时钟频率**: 主频 = 每秒时钟周期数。3GHz 表示每秒 30 亿个周期。但「主频高 ≠ 性能强」——还要看每个周期能干多少活。
- **IPC（Instructions Per Cycle）**: 每周期执行的指令数。现代 CPU 通过流水线和超标量设计让 IPC > 1，理想可达 4-6。
- **流水线**: 把指令执行拆成多级（如 14-20 级），不同指令的不同阶段并行推进 - 像工厂流水线，吞吐量翻倍但单条延迟不变。
- **分支预测**: 遇到 if/else 时 CPU 提前猜测走哪条路并预取 - 猜对零开销，猜错要清空流水线（约 10-20 周期惩罚）。
- **超标量与乱序执行**: 现代 CPU 每周期可发射多条指令，且不严格按代码顺序执行（只要无依赖），充分利用 ALU 资源。

## 原理与机制
- **指令周期的本质**: 一条机器指令看似「一步完成」，其实硬件内部走了多步。取指要从缓存/内存搬字节，译码要查指令表发出控制信号，执行要驱动 ALU，写回要更新寄存器堆。这四步对应流水线的不同级。
- **为什么主频高不一定快**: 性能 = 主频 × IPC。同架构下主频提升确实加快；但跨架构比较，A 架构 2GHz 可能比 B 架构 4GHz 更快，因为 A 每个 IPC 更高、流水线更深。
- **流水线的吞吐 vs 延迟**: 14 级流水线意味着单条指令要 14 个周期才走完（延迟），但每周期都能完成一条指令（吞吐）。就像灌满水管——水到达终点要等管子充满，但充满后每秒出水量恒定。
- **分支预测的代价**: CPU 遇到条件跳转时不知道走哪条路，但停等会浪费流水线。于是它根据历史模式猜测（准确率 >95%）。猜错就要「冲刷」流水线里已预取的错误指令，约 15-20 周期打水漂。这就是为什么规律分支（如 \`a>0?1:0\`）快，而完全随机的分支慢。
- **缓存的物理距离**: 光速限制了信号传输速度。L1 离核心几毫米，1ns 内能往返；内存条在主板另一端，往返要 100ns。物理距离就是性能差距的根因之一。
- **功耗与频率的权衡**: 频率提升带来电压平方级的功耗增长（动态功耗 P ∝ C·V²·f）- 所以 CPU 频率提升遇到「功耗墙」，转而靠多核、缓存、预测提升性能。

## 易错点与陷阱
- **陷阱**: 以为「主频越高 CPU 越快」 - 跨代际、跨架构比较时主频毫无意义，要看 IPC 和实际基准测试。
- **陷阱**: 把 IPC 当成固定值 - IPC 强依赖代码特征（有多少分支、多少数据依赖），同一段代码在不同数据上 IPC 可能差一倍。
- **陷阱**: 用 \`clock()\` 测量「真实耗时」 - clock() 测的是 CPU 时间（占用的周期），多线程下会累加多核时间，测墙钟时间要用 \`clock_gettime(CLOCK_MONOTONIC)\`。
- **陷阱**: 编译器把空循环优化掉 - 用空循环测性能时若结果未被使用，\`-O2\` 会直接删掉整个循环。加 \`volatile\` 或累加并输出可避免。
- **陷阱**: rdtsc 在不同 CPU 核间可能不一致 - 现代系统用 \`RDTSCP\` 或绑定核来减少漂移，精确测量最好固定到一个核。

## 实战建议
- **建议**: 性能优化先看 IPC 和缓存命中率（用 \`perf stat\`），再谈算法 - 一个缓存不友好的 O(n) 可能比缓存友好的 O(n log n) 还慢。
- **建议**: 热点循环里尽量减少分支，尤其随机分支；可预测的分支（如越界检查）放外面，不可预测的改用位运算无分支写法。
- **建议**: 测量短耗时用 rdtsc/clock_gettime，别用 time()（秒级粒度太粗）；测量长耗时用墙钟时间，注意排除系统调度干扰。
- **建议**: 写基准测试时务必开 \`-O2\`，但也要警惕优化把待测代码删掉 - 用 volatile 汇聚结果再输出。
`,
    code: `// ===== CPU 架构与指令周期 演示 =====
// 用 rdtsc（时间戳计数器）测量一段空循环的 CPU 周期数
// 展示单条指令的执行速度量级，体现流水线 + 分支预测的高效

#include <stdio.h>
#include <time.h>

// 读取 CPU 时间戳计数器（x86 专有指令 rdtsc）
// 在非 x86 平台（如 ARM / Apple Silicon）回退到 clock()，保证可移植
#if defined(__x86_64__) || defined(__i386__)
#include <x86intrin.h>
static unsigned long long read_tsc(void) {
    return __rdtsc();              // 读取 64 位 TSC 计数器
}
#define TSC_NAME "rdtsc (CPU 周期)"
#else
static unsigned long long read_tsc(void) {
    return (unsigned long long)clock();   // 回退：进程时钟周期
}
#define TSC_NAME "clock() (非 x86 回退)"
#endif

int main(void) {
    const long N = 100000000L;     // 一亿次空循环
    volatile long sink = 0;        // volatile 防止编译器把循环优化掉

    printf("测量 %ld 次空循环的耗时（用 %s）\\n", N, TSC_NAME);

    unsigned long long t0 = read_tsc();
    for (long i = 0; i < N; i++) {
        sink = i;                  // 一次写操作，几乎不干活
    }
    unsigned long long t1 = read_tsc();

    unsigned long long total = t1 - t0;
    double per_iter = (double)total / N;

    printf("总周期数   : %llu\\n", total);
    printf("每次循环   : %.4f 个周期\\n", per_iter);
    printf("说明：单次循环远小于 1 个周期，\\n");
    printf("      这得益于流水线、分支预测与寄存器命中。\\n");

    return 0;
}
`,
  },

  // ============================================================
  // 第 2 章：内存层次与缓存
  // ============================================================
  {
    id: "comp-memory-cache",
    group: "硬件基础",
    icon: "💾",
    title: "内存层次与缓存",
    content: `
## 概述
程序员常把内存当成一个「均匀的大数组」，但真实硬件里数据访问的速度天差地别——访问一个寄存器只要 0.3ns，而从机械硬盘读数据要 10ms，相差一亿倍。CPU 之所以能高速运转，靠的是在寄存器和内存之间架起多级缓存，把「常用的数据」放到「最近的地方」。本章讲清楚内存层级、缓存行、命中与缺失、空间/时间局部性，并解释为什么「遍历数组比遍历链表快」这种实战问题。

生活化比喻：寄存器是你手中正在用的笔（最快，但只有一支）；L1 缓存是桌面的笔筒（伸手就到，容量小）；L2 是抽屉（要弯腰，稍慢）；L3 是办公室角落的文件柜（共享，要走几步）；内存是楼下的仓库（要下楼，很慢但容量大）；SSD 是隔壁楼的档案室（要出楼，更慢）；HDD 是城另一头的冷库（要开车，慢到离谱）。CPU 取数据就是这样一个「能就近绝不跑远」的过程。

## 核心要点
- **内存层级（由快到慢）**: 寄存器（<1ns）→ L1（~1ns，几十 KB）→ L2（~3ns，几百 KB）→ L3（~10-20ns，几 MB）→ 内存（~100ns，GB 级）→ SSD（~100μs）→ HDD（~10ms）。
- **每级价格递减、容量递增**: 越快的越贵越小，越慢的越便宜越大 - 整个层级是用少量快的存储「模拟」一个又快又大的理想存储。
- **缓存行（cache line）**: 缓存与内存之间以「行」为单位搬运，主流 CPU 一行 = 64 字节。即使你只读 1 个字节，也会把相邻 64 字节一起搬进缓存。
- **缓存命中（hit）与缺失（miss）**: 要的数据已在缓存 = 命中（快）；不在 = 缺失，要从下一级搬 - 命中率决定程序真实速度。
- **空间局部性**: 访问了某个地址，很可能马上访问其邻居 - 所以连续内存（数组）访问友好，缓存行预取命中率高。
- **时间局部性**: 访问过的数据很可能不久再被访问 - 所以热点变量放一起、循环里反复用的值会被缓存留住。
- **缓存友好 vs 不友好**: 按行遍历二维数组（连续访问）远快于按列遍历（跨行跳跃），差距常达 5-10 倍。
- **数组 vs 链表**: 数组内存连续，遍历时缓存行预取高效；链表节点分散在堆各处，每次跳转几乎都 cache miss - 这就是为什么 vector 常比 list 快。
- **TLB 缓存**: 还有专门缓存「虚拟地址→物理地址」映射的 TLB，大页（huge page）能减少 TLB miss。

## 原理与机制
- **缓存行搬运的代价**: 一次 cache miss 不只是「读一个数据慢」，而是要等整行 64B 从下级搬上来。如果之后这 64B 里的数据都用上了（空间局部性好），这次搬运就赚了；如果只用了 1 字节，就亏了。
- **为什么数组遍历快**: 访问 arr[0] 触发搬入 64B（含 arr[0..15]），接下来 arr[1]..arr[15] 全部命中。16 次访问只付出 1 次 miss 代价。链表每个 node 分散，每次 next 跳转都是新行 → 几乎 100% miss。
- **写缓存与一致性**: 写数据时 CPU 要保证多核间缓存一致（MESI 协议）。一个核写某行，其他核中该行失效 - 多线程频繁写同一缓存行的不同变量（false sharing）会引发反复失效，性能暴跌。
- **预取（prefetch）**: 现代 CPU 会根据访问模式预测性地把后续缓存行提前搬入 - 顺序访问模式下预取器工作最好，随机访问它就废了。
- **层级是「概率加速器」**: 缓存并不改变程序逻辑，只是把高频数据放近 - 命中率 99% 和 90% 看似都「很高」，但前者平均 2ns、后者平均 11ns，差 5 倍。
- **替换策略**: 缓存满了要淘汰旧行，常用 LRU（最近最少使用）- 所以「刚用的还会再用」（时间局部性）的数据更可能留在缓存。

## 易错点与陷阱
- **陷阱**: 以为「算法复杂度低就一定快」 - O(n) 的链表遍历可能比 O(n log n) 的数组操作还慢，因为缓存缺失把常数项放大了几十倍。
- **陷阱**: false sharing - 两个线程各写一个相邻变量（同一缓存行），表面无共享却因缓存一致性协议反复失效，多核比单核还慢。
- **陷阱**: 数据结构布局无视缓存 - struct 里把热字段和冷字段混排，导致一个缓存行里大部分是不用的数据，命中率低。应把热字段聚拢、冷字段分离。
- **陷阱**: 用 \`sizeof\` 估算「能放进缓存」就够 - 还要考虑对齐、多核竞争、预取窗口，实际可用容量远小于标称。
- **陷阱**: 随机访问大数组看似 O(1)，实则每次 miss - 在大数据下「随机访问」比「顺序访问」慢一个数量级。

## 实战建议
- **建议**: 性能敏感场景优先用连续内存容器（数组/vector）而非链表 - 即使理论复杂度相同，实际快几倍。
- **建议**: 遍历多维数组时，保证最内层循环访问连续维度（C 语言行优先，所以按行遍历） - 简单一行改动可能提速 5-10 倍。
- **建议**: 多线程写共享数据时，按缓存行对齐（如 \`alignas(64)\`）隔离各线程的计数器 - 消除 false sharing。
- **建议**: 把高频访问的「热数据」与低频的「冷数据」分到不同结构体 - 提高缓存行利用率。
- **建议**: 大内存服务考虑大页（huge page）减少 TLB miss，尤其数据库、JVM 类应用。
`,
    code: `// ===== 内存层次与缓存 演示 =====
// 对比按行遍历 vs 按列遍历二维数组的耗时
// 体现缓存行（cache line）与空间局部性对性能的巨大影响

#include <stdio.h>
#include <time.h>

#define N 2048
static int arr[N][N];     // 2048*2048*4 = 16MB，远超 L3 缓存

// 读取墙钟时间（毫秒），CLOCK_MONOTONIC 不受系统时间调整影响
static double now_ms(void) {
    struct timespec ts;
    clock_gettime(CLOCK_MONOTONIC, &ts);
    return ts.tv_sec * 1000.0 + ts.tv_nsec / 1.0e6;
}

int main(void) {
    // 先填充数据（按行，避免初始缺失干扰对比）
    for (int i = 0; i < N; i++)
        for (int j = 0; j < N; j++)
            arr[i][j] = (i + j) & 0xff;

    // 1. 按行遍历：arr[i][j]，内层 j 连续 —— 缓存友好
    double t0 = now_ms();
    long sum1 = 0;
    for (int i = 0; i < N; i++)
        for (int j = 0; j < N; j++)
            sum1 += arr[i][j];       // 连续地址，命中缓存行
    double row_ms = now_ms() - t0;

    // 2. 按列遍历：arr[j][i]，内层 j 跨越整行 —— 缓存不友好
    t0 = now_ms();
    long sum2 = 0;
    for (int i = 0; i < N; i++)
        for (int j = 0; j < N; j++)
            sum2 += arr[j][i];       // 每次跳 8KB，几乎都 cache miss
    double col_ms = now_ms() - t0;

    printf("数组大小 : %d x %d (%d MB)\\n", N, N, (int)(N * N * 4 / (1024 * 1024)));
    printf("按行遍历 : %.2f ms (sum=%ld)\\n", row_ms, sum1);
    printf("按列遍历 : %.2f ms (sum=%ld)\\n", col_ms, sum2);
    printf("差异倍数 : %.2fx\\n", col_ms / row_ms);
    printf("原因：按行访问连续内存，命中 64B 缓存行；\\n");
    printf("      按列跨越整行宽度，每次几乎都 cache miss。\\n");

    return 0;
}
`,
  },

  // ============================================================
  // 第 3 章：总线与 I/O
  // ============================================================
  {
    id: "comp-bus-io",
    group: "硬件基础",
    icon: "🔌",
    title: "总线与 I/O",
    content: `
## 概述
CPU 不能独自完成所有工作——它要读硬盘、收网卡数据、响应键盘。这些外设如何与 CPU 通信？靠的是「总线」与「I/O 机制」。本章讲三类总线、I/O 端口与 MMIO、DMA 如何解放 CPU、中断驱动与轮询的区别。理解这些，你才能明白为什么「网卡收包时 CPU 不用一直盯着」、为什么「DMA 是高性能 I/O 的基石」。

生活化比喻：总线是公司里的走廊。数据总线是走廊的宽度——走廊越宽，一次能搬的货越多（32 位 vs 64 位）；地址总线是门牌号的位数——位数越多，能寻址的房间越多（32 位最多 4GB，64 位天文数字）；控制总线是走廊里的红绿灯——决定谁在什么时候可以走、读还是写。DMA 则是请了搬运工，让老板（CPU）不用亲自搬货，搬完搬运工按一下门铃（中断）通知老板即可。

## 核心要点
- **总线（Bus）**: 连接 CPU、内存、外设的公共数据通道 - 分数据总线、地址总线、控制总线三类。
- **数据总线**: 决定一次能传输多少位 - 64 位 CPU 一次搬 8 字节。总线越宽，吞吐越高。
- **地址总线**: 决定能寻址多大空间 - 32 位地址总线最多寻址 4GB 内存，64 位理论上能寻址 16EB。
- **控制总线**: 传输读/写、时钟、中断等控制信号 - 协调各部件「谁说话、谁闭嘴」。
- **I/O 端口（Port I/O）**: 通过专用 \`in\`/\`out\` 指令访问外设寄存器 - x86 特有，独立于内存地址空间。
- **MMIO（内存映射 I/O）**: 把外设寄存器映射到一段内存地址 - 读写这段地址就是读写外设，现代外设（PCIe 设备）几乎都用 MMIO。
- **DMA（直接内存访问）**: 让外设与内存之间直接搬运数据，不经过 CPU - 网卡收包、磁盘读写大量数据时 DMA 是性能关键。
- **中断（Interrupt）**: 外设完成后发信号通知 CPU - CPU 不用轮询等待，可以去做别的事。
- **中断驱动 vs 轮询**: 中断驱动适合稀疏事件（键盘）；轮询适合高吞吐场景（万兆网卡收包，中断太频繁反而慢）。
- **中断向量与 IRQ**: 每个中断源有编号（IRQ），CPU 据此找到对应处理函数（中断服务程序 ISR）。

## 原理与机制
- **总线的分时共享**: 一根总线同一时刻只能一对设备通信，所以总线是分时复用的 - 这就是为什么总线带宽是稀缺资源，多设备争用会成为瓶颈。
- **MMIO 的妙处**: 把外设寄存器映射到内存地址后，CPU 访问外设就和访问内存用同一套指令（mov 等），无需专门的 I/O 指令 - 简化指令集，也方便操作系统统一管理。
- **DMA 为什么解放 CPU**: 没有 DMA 时，磁盘读 4KB 要 CPU 逐字节搬运（浪费几千个周期）；有 DMA 后，DMA 控制器自己搬，CPU 在这期间能跑别的任务，搬完发个中断通知 CPU 即可。
- **中断的代价**: 一次中断要保存现场、查中断向量、执行 ISR、恢复现场，约几千到上万周期 - 所以高吞吐场景「中断风暴」会拖垮 CPU，改用 NAPI（轮询+中断混合）或 DPDK（纯轮询）。
- **中断亲和性（IRQ affinity）**: 多核系统可以把不同设备的中断绑定到不同核 - 避免单核被打爆、提升缓存局部性。
- **PCIe 与传统总线**: 现代外设走 PCIe（点对点串行，不再共享总线），但软件模型仍保留 MMIO + DMA + 中断三件套。

## 易错点与陷阱
- **陷阱**: 以为「CPU 越快 I/O 就越快」 - 磁盘/网络延迟由外设和总线决定，CPU 再快也得等 - 这就是为什么「CPU 占用低但响应慢」。
- **陷阱**: 高并发网络服务用纯中断模式 - 包风暴下中断处理开销吃满 CPU，应改用 epoll（事件驱动批量）或 NAPI。
- **陷阱**: 把 I/O 端口和 MMIO 混淆 - x86 两者地址空间独立，\`in\`/\`out\` 访问端口，\`mov\` 访问 MMIO；ARM 只有 MMIO。
- **陷阱**: 以为 DMA 不需要 CPU 参与「就不耗资源」 - DMA 仍占用内存带宽，与 CPU 抢带宽时可能拖慢 CPU。
- **陷阱**: 多队列网卡不绑核 - 所有队列中断都打到 CPU0，单核打爆而其他核空闲 - 必须配 IRQ 亲和性。

## 实战建议
- **建议**: 高吞吐网络服务启用多队列网卡 + IRQ 亲和性，把收发中断分散到多核 - 配合 RPS/RFS 进一步均衡。
- **建议**: 大数据量磁盘读写用异步 I/O（io_uring/AIO）+ DMA，让 CPU 在 I/O 期间做别的 - 别用阻塞 read 一字节一字节搬。
- **建议**: 排查「响应慢但 CPU 闲」时优先看 I/O 等待（\`iostat\`/\`mpstat\` 的 \`%iowait\`） - 多半是磁盘或网络瓶颈。
- **建议**: 极低延迟场景（高频交易）考虑 DPDK 绕过内核中断，纯轮询 + CPU 绑核 - 用 CPU 占用换延迟。
- **建议**: 用 \`/proc/interrupts\`（Linux）观察中断分布是否均衡，单核中断过高就调亲和性。
`,
    code: `# ===== 总线与 I/O 演示 =====
# 查看硬件中断分布，理解中断驱动 I/O 与多核中断亲和性
# macOS 无 /proc/interrupts，自动回退到典型示例输出

echo "=== 1. /proc/interrupts：硬件中断统计（Linux） ==="
if [ -f /proc/interrupts ]; then
    head -n 15 /proc/interrupts
else
    echo "（当前系统无 /proc/interrupts，典型 Linux 输出示例：）"
    echo "            CPU0       CPU1       CPU2       CPU3"
    echo "  0:          45          0          0          0  IO-APIC  2-edge  timer"
    echo "  1:           8          0          0          0  IO-APIC  1-edge  i8042"
    echo "NMI:           0          0          0          0  Non-maskable interrupts"
    echo "LOC:     1023456    1021100    1022200    1020050  Local timer interrupts"
    echo "RES:        120        135        128        140  Rescheduling interrupts"
    echo "TLB:         45         50         48         52  TLB shootdowns"
fi

echo ""
echo "=== 2. 中断类型含义 ==="
echo "LOC  : 时钟中断，内核调度的时间基准（频率最高）"
echo "RES  : IPI 核间中断，唤醒其他核重新调度"
echo "TLB  : 多核同步页表缓存（TLB shootdown）"
echo "IO-APIC xx : 外设硬件中断（网卡/键盘/磁盘/USB）"

echo ""
echo "=== 3. 外设中断（网卡/磁盘，Linux） ==="
if [ -f /proc/interrupts ]; then
    grep -iE "eth|ens|nvme|ata|virtio|sd" /proc/interrupts | head -n 8
else
    echo "（macOS 无 /proc/interrupts）Linux 示例："
    echo "  31:    12345    9876    4521    3012   PCI-MSI  nvme0q0"
    echo "  32:     8000     120     115      98   PCI-MSI  eth0-TxRx-0"
fi

echo ""
echo "=== 4. 中断驱动 vs 轮询 ==="
echo "中断驱动：外设完成后发中断通知 CPU（适合稀疏事件，如键盘）"
echo "轮询模式：CPU 主动查外设状态（适合高吞吐，如万兆网卡，避免中断风暴）"
echo "现代混合：NAPI / epoll = 中断唤醒 + 批量轮询，兼顾延迟与吞吐"
`,
  },

  // ============================================================
  // 第 4 章：并行与并发基础
  // ============================================================
  {
    id: "comp-parallel-concurrency",
    group: "硬件基础",
    icon: "⚡",
    title: "并行与并发基础",
    content: `
## 概述
「并行」和「并发」常被混用，但它们是两回事：并发是「同时处理多件事」（可能交替），并行是「真正同时执行多件事」（多核）。本章讲进程与线程的区别、时间片轮转、上下文切换的开销、SMP 与超线程，并解释为什么「多线程不一定快」——锁竞争、缓存失效、上下文切换可能让多线程比单线程还慢。

生活化比喻：进程是不同的工厂——各自独立厂房、独立仓库，互不干涉；线程是同一个工厂里的工人——共享同一个车间（地址空间、内存），协作方便但要抢公共工具（锁）。上下文切换就是工人从任务 A 换到任务 B 时，要收起 A 的工具、拿出 B 的工具、回忆 B 干到哪了——这个「切换」本身不产出任何东西，切换太频繁反而拖慢整体效率。

## 核心要点
- **进程（Process）**: 操作系统资源分配的基本单位 - 拥有独立地址空间、文件描述符表。进程间隔离，通信要走 IPC（管道/共享内存/socket）。
- **线程（Thread）**: CPU 调度的基本单位 - 同进程内线程共享地址空间和资源，通信方便但要同步。
- **并发 vs 并行**: 并发是「同一时段处理多任务」（单核靠时间片切换也能并发）；并行是「同一时刻真正同时执行」（必须多核）。
- **时间片轮转**: OS 给每个就绪线程分一个时间片（几 ms～几十 ms），用完就强制切换到下一个 - 让所有线程「看起来同时在跑」。
- **上下文切换**: 线程换下 CPU 时要保存寄存器/PC/栈指针，换上新的 - 单次约 1-10μs，频繁切换是性能杀手。
- **SMP（对称多处理）**: 多个核平等共享内存，OS 调度线程到任意核 - 主流多核 CPU 都是 SMP。
- **超线程（SMT）**: 一个物理核模拟两个逻辑核，共享 ALU 但有独立寄存器 - 利用 ALU 空闲槽，约提升 15-30% 吞吐，但不是真双核。
- **锁竞争**: 多线程抢同一把锁，未抢到的线程阻塞或自旋 - 临界区越大、竞争越激烈，并行收益越小。
- **锁的粒度**: 粗锁简单但竞争大；细锁并发高但易死锁、开销大 - 要按场景权衡。
- **阿姆达尔定律**: 并行加速比受「串行部分」限制 - 若 10% 代码必须串行，4 核最多加速 3.07x，无穷核也最多 10x。

## 原理与机制
- **为什么进程比线程重**: 创建进程要复制页表（即便有 COW 也要）、文件表等；线程只建栈和寄存器，共享其他 - 所以线程创建/切换成本低一个数量级。
- **上下文切换的真实开销**: 不只是保存寄存器——切换后新线程的缓存大概率失效（它的数据不在当前核的 L1/L2），要重新预热缓存 - 这部分「缓存缺失」开销往往比保存寄存器大得多。
- **超线程的本质**: 一个核执行遇到访存 stall（等内存）时，ALU 空闲。超线程让另一个线程用这段空闲 ALU - 所以对访存密集型收益大，对纯计算型收益小甚至无。
- **多线程不一定快的根因**: 1) 锁竞争让线程排队，退化成串行；2) 上下文切换和缓存失效吃掉并行收益；3) false sharing 让缓存行反复失效；4) 阿姆达尔定律的串行部分封顶。
- **fork 与线程的区别**: fork 产生独立地址空间的子进程（COW），隔离性强但通信要走 IPC；线程共享地址空间，通信直接读写但易出错。多进程容错性好（一崩不连累），多线程性能好但要小心同步。
- **调度优先级与实时性**: OS 调度器按优先级和时间片分配 CPU - 普通线程可能被抢占，实时任务要靠 SCHED_FIFO 等策略保证及时性。

## 易错点与陷阱
- **陷阱**: 以为「线程越多越快」 - 超过物理核数后，线程争抢 CPU + 切换开销，反而变慢 - 线程数应与任务类型和核数匹配。
- **陷阱**: 锁粒度过粗 - 一把大锁保护整个数据结构，多线程退化成串行；应拆成细锁或用无锁结构。
- **陷阱**: 误用共享变量不加锁 - 看似「偶尔」结果不对，实为数据竞争（data race）- 即使读写在某些架构上不是原子的。
- **陷阱**: false sharing - 多线程各写相邻变量，缓存一致性协议让缓存行反复失效，多核比单核慢 - 按缓存行对齐。
- **陷阱**: 高估并行收益 - 忽略阿姆达尔定律，串行部分占 20% 时 8 核最多加速 3.5x，不是 8x。
- **陷阱**: 频繁 fork/join 短任务 - 进程创建开销超过计算本身，应改用线程池或长驻进程。

## 实战建议
- **建议**: CPU 密集型任务线程数 ≈ 物理核数；I/O 密集型可多于核数（线程等待 I/O 时让出 CPU）- 用线程池复用，避免频繁创建销毁。
- **建议**: 先用单线程把逻辑跑通、用 profile 找热点，再针对性并行 - 不要盲目多线程化。
- **建议**: 多线程共享数据优先用无锁结构（原子操作、CAS）或读写锁 - 减少临界区竞争。
- **建议**: 对容错性要求高的服务用多进程（如 Nginx worker 崩溃不影响 master），对性能要求高用多线程或事件循环。
- **建议**: 测量并行加速比要固定输入、多次取中位数，排除调度抖动 - 别被一次「异常快」误导。
`,
    code: `// ===== 并行与并发基础 演示 =====
// 对比串行 vs fork 多进程并行计算一段 CPU 密集任务
// 展示并行加速比，子进程经 pipe 把结果汇聚回父进程

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <time.h>
#include <sys/wait.h>

// CPU 密集任务：统计 [start, end) 内的素数个数
static long count_primes(long start, long end) {
    long cnt = 0;
    for (long n = start; n < end; n++) {
        if (n < 2) continue;
        int is_prime = 1;
        for (long d = 2; d * d <= n; d++) {
            if (n % d == 0) { is_prime = 0; break; }
        }
        if (is_prime) cnt++;
    }
    return cnt;
}

// 读取墙钟时间（毫秒）
static double now_ms(void) {
    struct timespec ts;
    clock_gettime(CLOCK_MONOTONIC, &ts);
    return ts.tv_sec * 1000.0 + ts.tv_nsec / 1.0e6;
}

int main(void) {
    const long RANGE = 5000000L;     // 统计 [2, 5_000_000) 内素数
    const int WORKERS = 4;           // 4 个子进程并行

    // 1. 串行计算（基准）
    double t0 = now_ms();
    long serial = count_primes(2, RANGE);
    double t_serial = now_ms() - t0;

    // 2. 并行计算：fork 4 个子进程，各算一段，结果经 pipe 回传
    double p0 = now_ms();
    int fd[2];
    pipe(fd);                        // 管道：子进程写，父进程读
    long chunk = (RANGE - 2) / WORKERS;
    for (int k = 0; k < WORKERS; k++) {
        pid_t pid = fork();
        if (pid == 0) {              // 子进程
            long s = 2 + (long)k * chunk;
            long e = (k == WORKERS - 1) ? RANGE : s + chunk;
            long part = count_primes(s, e);
            close(fd[0]);            // 子进程只写
            write(fd[1], &part, sizeof(part));
            close(fd[1]);
            _exit(0);
        }
    }
    close(fd[1]);                    // 父进程只读，关闭写端
    long parallel = 0, got;
    // 读到 EOF 表示所有子进程都写完并关闭了写端
    while (read(fd[0], &got, sizeof(got)) == sizeof(got)) {
        parallel += got;             // 累加各子进程的素数计数
    }
    close(fd[0]);
    for (int k = 0; k < WORKERS; k++) wait(NULL);  // 回收子进程

    double t_parallel = now_ms() - p0;

    printf("统计 [2, %ld) 内的素数\\n", RANGE);
    printf("串行结果 : %ld  耗时 %.2f ms\\n", serial, t_serial);
    printf("并行结果 : %ld  耗时 %.2f ms (%d 进程)\\n", parallel, t_parallel, WORKERS);
    printf("加速比   : %.2fx\\n", t_serial / t_parallel);
    printf("说明：fork 后各子进程独立地址空间，无锁竞争；\\n");
    printf("      加速比受核数、调度、pipe 汇聚开销影响。\\n");

    return 0;
}
`,
  },
];