// =============================================================
// LeetCode 面试算法 200 题 - 第十九批章节（位运算与数学，共 10 题）
// 章节 lc-181 ~ lc-190：异或/位计数/汉明距离/质数/阶乘零/Pow/Sqrt 等
// =============================================================

export const chapters = [
  // =============================================================
  // lc-181 #136 只出现一次的数字
  // =============================================================
  {
    id: "lc-181",
    group: "位运算与数学",
    icon: "🔢",
    title: "#136 只出现一次的数字（简单）",
    content: `## 题目

**LeetCode #136 只出现一次的数字** | 难度：简单

给定一个非空整数数组 \`nums\`，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现一次的元素。要求时间复杂度 O(n)，空间复杂度 O(1)。

示例：

\`\`\`
输入：nums = [4,1,2,1,2]
输出：4
\`\`\`

## 思路

本题是**异或（XOR）**的经典应用。异或运算符 \`^\` 的核心性质：

1. **恒等律**：\`a ^ 0 = a\`——任何数异或 0 都不变。
2. **归零律**：\`a ^ a = 0\`——任何数异或自身都为 0。
3. **交换律与结合律**：\`a ^ b ^ c = c ^ b ^ a\`，顺序不影响结果。

由于其他元素都出现两次，把所有元素依次异或起来：成对的元素两两异或得 0，最后只剩下出现一次的那个元素。例如 \`4 ^ 1 ^ 2 ^ 1 ^ 2 = 4 ^ (1^1) ^ (2^2) = 4 ^ 0 ^ 0 = 4\`。

每个位上的运算原理：异或在二进制逐位上做「不进位加法」，相同位异或为 0，不同位异或为 1。所以出现偶数次的相同值必然抵消为 0。

## Python 实现

\`\`\`python
class Solution:
    def singleNumber(self, nums):
        # 利用异或：a ^ a = 0, a ^ 0 = a
        result = 0
        for num in nums:
            result ^= num  # 逐个异或，成对的会抵消
        return result
\`\`\`

## JavaScript 实现

\`\`\`javascript
var singleNumber = function(nums) {
    // 异或所有元素，成对的抵消为 0
    let result = 0;
    for (const num of nums) {
        result ^= num;
    }
    return result;
};
\`\`\`

## 复杂度

- 时间复杂度：O(n)，遍历一次数组
- 空间复杂度：O(1)，只用一个变量

## 拓展

- #137 只出现一次的数字 II：其余元素出现三次，用位计数统计每个二进制位 mod 3。
- #260 只出现一次的数字 III：有两个出现一次的数，先全体异或再按某位分组。`
  },

  // =============================================================
  // lc-182 #191 位 1 的个数
  // =============================================================
  {
    id: "lc-182",
    group: "位运算与数学",
    icon: "🔢",
    title: "#191 位 1 的个数（简单）",
    content: `## 题目

**LeetCode #191 位 1 的个数** | 难度：简单

编写一个函数，输入是一个无符号整数（以二进制串形式），返回其二进制表达式中数字位数为 \`1\` 的个数（也称为汉明权重）。

示例：

\`\`\`
输入：n = 00000000000000000000000000001011
输出：3
\`\`\`

## 思路

统计二进制中 1 的个数有几种经典做法：

1. **循环检查每一位**：每次用 \`n & 1\` 取最低位是否为 1，然后把 \`n\` 右移一位（Python 用 \`>>\`）。运算符 \`&\`（按位与）：两位都为 1 才为 1，所以 \`n & 1\` 只保留最低位。

2. **n & (n-1) 技巧（推荐）**：\`n - 1\` 会把 n 最低位的 1 变成 0，并把其后所有 0 变成 1。例如 \`10100 - 1 = 10011\`。于是 \`n & (n-1)\` 会把 n 最低位的 1 抹掉（\`10100 & 10011 = 10000\`）。每抹掉一个 1 计数加一，直到 n 为 0。这样循环次数等于 1 的个数，比逐位检查更快。

Python 中需要处理输入可能是 32 位无符号数（题目以 \`000...1011\` 形式给出时实际是普通整数），JavaScript 位运算会自动转 32 位有符号整数，需用 \`>>> 0\` 处理。

## Python 实现

\`\`\`python
class Solution:
    def hammingWeight(self, n):
        # n & (n-1) 抹掉最低位的 1
        count = 0
        while n:
            n &= n - 1   # 消去最低位的 1
            count += 1
        return count
\`\`\`

## JavaScript 实现

\`\`\`javascript
var hammingWeight = function(n) {
    let count = 0;
    while (n !== 0) {
        n &= (n - 1);   // 消去最低位的 1
        count++;
    }
    return count;
};
\`\`\`

## 复杂度

- 时间复杂度：O(k)，k 为 1 的个数，最坏 O(32)
- 空间复杂度：O(1)

## 拓展

- \`n & (n-1)\` 是判断 2 的幂的核心：2 的幂只有一个 1，抹掉后为 0（见 lc-186）。
- Python 也可用 \`bin(n).count('1')\`，但面试更看重位运算写法。`
  },

  // =============================================================
  // lc-183 #338 比特位计数
  // =============================================================
  {
    id: "lc-183",
    group: "位运算与数学",
    icon: "🔢",
    title: "#338 比特位计数（简单）",
    content: `## 题目

**LeetCode #338 比特位计数** | 难度：简单

给你一个整数 \`n\`，对于 \`0 <= i <= n\` 中的每个 \`i\`，计算其二进制表示中 1 的个数，返回一个长度为 \`n + 1\` 的数组。

示例：

\`\`\`
输入：n = 5
输出：[0,1,1,2,1,2]
解释：0->0, 1->1, 2->10, 3->11, 4->100, 5->101
\`\`\`

## 思路

逐个用 \`n & (n-1)\` 计算也可以，但要求线性时间且不用额外空间，要用**动态规划 + 位运算**。

关键观察：\`i\` 的 1 的个数与它去掉最低位后的数有关。设 \`i\` 右移一位为 \`i >> 1\`（即 \`i // 2\`），则：

- **转移一（最高有效位）**：\`bits[i] = bits[i >> 1] + (i & 1)\`。因为 \`i >> 1\` 去掉了最低位，1 的个数就是它的高位部分；再加上最低位 \`i & 1\`（\`&\` 取最低位：1&1=1，0&1=0）。
- **转移二（最低设置位）**：\`bits[i] = bits[i & (i-1)] + 1\`。\`i & (i-1)\` 抹掉 i 最低位的 1，剩下部分的 1 的个数已经算过，再加 1。

两种转移都利用了「比 i 小的值已经算过」这一性质，实现 O(n) 一次遍历。

## Python 实现

\`\`\`python
class Solution:
    def countBits(self, n):
        # bits[i] = bits[i >> 1] + (i & 1)
        bits = [0] * (n + 1)
        for i in range(1, n + 1):
            # i >> 1 去掉最低位，i & 1 取最低位
            bits[i] = bits[i >> 1] + (i & 1)
        return bits
\`\`\`

## JavaScript 实现

\`\`\`javascript
var countBits = function(n) {
    const bits = new Array(n + 1).fill(0);
    for (let i = 1; i <= n; i++) {
        // i >> 1 去掉最低位，i & 1 取最低位
        bits[i] = bits[i >> 1] + (i & 1);
    }
    return bits;
};
\`\`\`

## 复杂度

- 时间复杂度：O(n)，一次遍历
- 空间复杂度：O(1)，输出数组不计入额外空间

## 拓展

- 也可用 \`bits[i] = bits[i & (i-1)] + 1\`，思路不同但同样 O(n)。
- 这类「利用低位与高位关系」的 DP 是位运算题常见套路。`
  },

  // =============================================================
  // lc-184 #461 汉明距离
  // =============================================================
  {
    id: "lc-184",
    group: "位运算与数学",
    icon: "🔢",
    title: "#461 汉明距离（简单）",
    content: `## 题目

**LeetCode #461 汉明距离** | 难度：简单

两个整数之间的**汉明距离**指的是这两个数字对应二进制位不同的位置的数目。给你两个整数 \`x\` 和 \`y\`，计算它们之间的汉明距离。

示例：

\`\`\`
输入：x = 1, y = 4
输出：2
解释：1 (0 0 0 1)，4 (0 1 0 0)，不同位数为 2
\`\`\`

## 思路

汉明距离 = 两个数二进制中不同位的个数。判断「哪些位不同」用**异或**：\`xor = x ^ y\`。

异或 \`^\` 的原理：逐位比较，相同为 0，不同为 1。所以 \`x ^ y\` 中 1 的个数，正好就是 x 和 y 不同的位数，即汉明距离。

接下来统计 \`xor\` 中 1 的个数，复用 #191 的 \`n & (n-1)\` 技巧：每抹掉一个 1 计数一次。\`n & (n-1)\` 中 \`&\`（按位与）把 n 最低位的 1 置 0。也可以直接调用语言内置的 popcount（如 Python \`bin(xor).count('1')\`），但位运算是面试考察重点。

## Python 实现

\`\`\`python
class Solution:
    def hammingDistance(self, x, y):
        # 异或后统计 1 的个数
        xor = x ^ y
        count = 0
        while xor:
            xor &= xor - 1   # 抹掉最低位的 1
            count += 1
        return count
\`\`\`

## JavaScript 实现

\`\`\`javascript
var hammingDistance = function(x, y) {
    let xor = x ^ y;       // 异或：不同位为 1
    let count = 0;
    while (xor !== 0) {
        xor &= (xor - 1);   // 抹掉最低位的 1
        count++;
    }
    return count;
};
\`\`\`

## 复杂度

- 时间复杂度：O(k)，k 为不同位数，最坏 O(32)
- 空间复杂度：O(1)

## 拓展

- #191 是统计单个数的汉明权重，本题是统计两数异或后的汉明权重。
- #477 汉明距离总和：求数组中所有两两汉明距离之和，按位统计贡献。`
  },

  // =============================================================
  // lc-185 #268 丢失的数字
  // =============================================================
  {
    id: "lc-185",
    group: "位运算与数学",
    icon: "🔢",
    title: "#268 丢失的数字（简单）",
    content: `## 题目

**LeetCode #268 丢失的数字** | 难度：简单

给定一个包含 \`[0, n]\` 中 \`n\` 个数的数组 \`nums\`，找出 \`[0, n]\` 这个范围内没有出现在数组中的那个数字。

示例：

\`\`\`
输入：nums = [3,0,1]
输出：2
解释：n = 3，因为有 3 个数字，所以范围是 [0,3]，缺失的是 2
\`\`\`

## 思路

有三种主流解法：

1. **数学求和法**：\`[0, n]\` 完整和为 \`n*(n+1)/2\`，减去数组实际和，差值就是丢失的数。最简单直观。

2. **异或法（推荐）**：利用异或的归零律。把数组所有元素与 \`0..n\` 全部异或：出现两次的数抵消为 0，只剩出现一次的那个丢失数。原理：\`0 ^ 1 ^ 2 ^ ... ^ n ^ nums[0] ^ nums[1] ^ ... ^ nums[n-1]\`，其中出现的数各出现两次（一次在 0..n，一次在 nums），抵消为 0，丢失的数只在 0..n 中出现一次，保留下来。

3. **哈希集合**：把 nums 存入集合，遍历 0..n 找不在集合中的。空间 O(n)。

异或法空间最优且体现了位运算思想，下面给出异或法和数学法两种实现。

## Python 实现

\`\`\`python
class Solution:
    # 写法一：异或
    def missingNumber(self, nums):
        # 异或 [0,n] 全部下标与所有元素
        n = len(nums)
        xor = 0
        for i in range(n + 1):
            xor ^= i
        for num in nums:
            xor ^= num
        return xor

    # 写法二：数学求和
    def missingNumber_math(self, nums):
        n = len(nums)
        total = n * (n + 1) // 2   # 0..n 的和
        return total - sum(nums)
\`\`\`

## JavaScript 实现

\`\`\`javascript
var missingNumber = function(nums) {
    const n = nums.length;
    let xor = 0;
    // 异或 0..n 的所有下标
    for (let i = 0; i <= n; i++) xor ^= i;
    // 异或所有元素
    for (const num of nums) xor ^= num;
    return xor;
};

var missingNumberMath = function(nums) {
    const n = nums.length;
    const total = n * (n + 1) / 2;   // 0..n 的和
    return total - nums.reduce((a, b) => a + b, 0);
};
\`\`\`

## 复杂度

- 时间复杂度：O(n)
- 空间复杂度：O(1)（异或法、数学法）；O(n)（哈希法）

## 拓展

- 本题是 #136 异或思想的应用，思路完全一致：让出现两次的抵消。
- 注意数学法可能溢出（JS 大数无影响，但其他语言需注意），异或法则无溢出问题。`
  },

  // =============================================================
  // lc-186 #231 2 的幂
  // =============================================================
  {
    id: "lc-186",
    group: "位运算与数学",
    icon: "🔢",
    title: "#231 2 的幂（简单）",
    content: `## 题目

**LeetCode #231 2 的幂** | 难度：简单

给你一个整数 \`n\`，请你判断该整数是否是 2 的幂次方。如果是，返回 \`true\`；否则返回 \`false\`。整数 \`n\` 是 2 的幂次方需满足：存在整数 \`x\` 使得 \`n == 2^x\`。

示例：

\`\`\`
输入：n = 16
输出：true
解释：2^4 = 16

输入：n = 3
输出：false
\`\`\`

## 思路

2 的幂在二进制中只有一个 1，其余位全是 0，例如 \`1 = 1\`、\`2 = 10\`、\`4 = 100\`、\`8 = 1000\`。利用这个性质有两种位运算判断：

1. **n & (n-1) == 0**：\`n - 1\` 会把 n 最低位的 1 变 0，其后 0 变 1。对于 2 的幂，只有一个 1，抹掉后全为 0，所以 \`n & (n-1) = 0\`。但要先排除 \`n <= 0\`（负数和 0 不是 2 的幂）。运算符 \`&\`（按位与）：两者都为 1 才为 1。

2. **n & (-n) == n**：\`-n\` 是 \`n\` 的补码（取反加一），它只保留 n 最低位的 1。\`n & (-n)\` 取出 n 最低位的 1，若它等于 n 本身，说明 n 只有一个 1，是 2 的幂。例如 \`n = 8 (1000)\`，\`-n\` 在补码下是 \`...1000\`，\`8 & -8 = 8\`。

两种判断都要先保证 n 为正数。

## Python 实现

\`\`\`python
class Solution:
    def isPowerOfTwo(self, n):
        # 正数且只有一个 1：n & (n-1) 抹掉唯一 1 后为 0
        return n > 0 and (n & (n - 1)) == 0

    def isPowerOfTwo2(self, n):
        # n & (-n) 取出最低位的 1，等于 n 则只有一个 1
        return n > 0 and (n & -n) == n
\`\`\`

## JavaScript 实现

\`\`\`javascript
var isPowerOfTwo = function(n) {
    // 正数且 n & (n-1) 为 0
    return n > 0 && (n & (n - 1)) === 0;
};

var isPowerOfTwo2 = function(n) {
    // n & (-n) 取最低位的 1
    return n > 0 && (n & -n) === n;
};
\`\`\`

## 复杂度

- 时间复杂度：O(1)，单次位运算
- 空间复杂度：O(1)

## 拓展

- #326 3 的幂、#342 4 的幂：非 2 的幂需用循环或换底公式判断。
- \`n & (-n)\` 是位运算中「取最低位 1」的经典技巧，也用于树状数组 lowbit 操作。`
  },

  // =============================================================
  // lc-187 #204 计数质数
  // =============================================================
  {
    id: "lc-187",
    group: "位运算与数学",
    icon: "🔢",
    title: "#204 计数质数（中等）",
    content: `## 题目

**LeetCode #204 计数质数** | 难度：中等

给定整数 \`n\`，返回所有小于非负整数 \`n\` 的质数的数量。

示例：

\`\`\`
输入：n = 10
输出：4
解释：小于 10 的质数有 2, 3, 5, 7，共 4 个
\`\`\`

## 思路

逐个判断质数会超时（O(n√n)），用**埃拉托斯特尼筛法（埃氏筛）** 可做到接近 O(n log log n)。

原理：如果一个数 \`i\` 是质数，那么它的所有倍数 \`2i, 3i, 4i ...\` 都不是质数（它们能被 i 整除）。从 2 开始遍历，遇到质数就把它的倍数标记为合数。

优化点：
1. 外层只需遍历到 \`√n\`：因为任何大于 √n 的合数 \`k\` 必然有一个不超过 √n 的质因子，已被更小的质数筛掉。
2. 内层从 \`i*i\` 开始标记：\`i*2, i*3 ... i*(i-1)\` 这些倍数在处理更小的质因子时已经被筛过，所以从 \`i*i\` 开始即可。
3. 用布尔数组 \`is_prime\` 标记，初始全 True，被筛掉的置 False。

## Python 实现

\`\`\`python
class Solution:
    def countPrimes(self, n):
        if n < 3:
            return 0
        # is_prime[i] 表示 i 是否为质数，初始假设都是
        is_prime = [True] * n
        is_prime[0] = is_prime[1] = False
        # 只需筛到 sqrt(n)
        i = 2
        while i * i < n:
            if is_prime[i]:
                # 从 i*i 开始标记合数
                for j in range(i * i, n, i):
                    is_prime[j] = False
            i += 1
        return sum(is_prime)
\`\`\`

## JavaScript 实现

\`\`\`javascript
var countPrimes = function(n) {
    if (n < 3) return 0;
    const isPrime = new Array(n).fill(true);
    isPrime[0] = isPrime[1] = false;
    // 只需筛到 sqrt(n)
    for (let i = 2; i * i < n; i++) {
        if (isPrime[i]) {
            // 从 i*i 开始标记合数
            for (let j = i * i; j < n; j += i) {
                isPrime[j] = false;
            }
        }
    }
    return isPrime.filter(Boolean).length;
};
\`\`\`

## 复杂度

- 时间复杂度：O(n log log n)，埃氏筛
- 空间复杂度：O(n)，标记数组

## 拓展

- **线性筛（欧拉筛）** 可做到严格 O(n)：每个合数只被它的最小质因子筛一次，需要额外维护质数列表。
- 本题是数论入门题，筛法是求区间内所有质数的标准模板。`
  },

  // =============================================================
  // lc-188 #172 阶乘后的零
  // =============================================================
  {
    id: "lc-188",
    group: "位运算与数学",
    icon: "🔢",
    title: "#172 阶乘后的零（中等）",
    content: `## 题目

**LeetCode #172 阶乘后的零** | 难度：中等

给定一个整数 \`n\`，返回 \`n!\` 结果中尾随零的数量。注意：\`0 <= n <= 10^4\`。

示例：

\`\`\`
输入：n = 5
输出：1
解释：5! = 120，有一个尾随零

输入：n = 3
输出：0
解释：3! = 6，没有尾随零
\`\`\`

## 思路

尾随零由因子 10 产生，而 \`10 = 2 * 5\`。在阶乘 \`n!\` 中，因子 2 的数量远多于因子 5（每两个数就有一个偶数贡献因子 2），所以**尾随零的数量等于因子 5 的数量**。

如何统计 1..n 中因子 5 的总数？不能只数 5 的倍数——像 25 = 5*5 贡献两个 5，125 = 5*5*5 贡献三个 5。

公式：\`count = n/5 + n/25 + n/125 + ...\`，即不断除以 5 累加商，直到商为 0。每次除以 5 统计的是「至少含 k 个因子 5 的数的个数」。

例如 n = 25：\`25/5 = 5\`（5,10,15,20,25 各贡献至少一个 5），\`25/25 = 1\`（25 额外贡献第二个 5），共 6 个零。这样既不重复也不遗漏。

## Python 实现

\`\`\`python
class Solution:
    def trailingZeroes(self, n):
        # 统计因子 5 的个数
        count = 0
        while n > 0:
            n //= 5        # 每次除以 5
            count += n      # 累加含因子 5 的数的个数
        return count
\`\`\`

## JavaScript 实现

\`\`\`javascript
var trailingZeroes = function(n) {
    let count = 0;
    while (n > 0) {
        n = Math.floor(n / 5);   // 每次除以 5
        count += n;              // 累加因子 5 的个数
    }
    return count;
};
\`\`\`

## 复杂度

- 时间复杂度：O(log n)，每次 n 除以 5
- 空间复杂度：O(1)

## 拓展

- 关键洞察：阶乘中因子 2 远多于因子 5，所以零的数量由因子 5 决定。
- 类似思想：统计某质数 p 在 n! 中的幂次，用 \`n/p + n/p^2 + ...\`。`
  },

  // =============================================================
  // lc-189 #50 Pow(x, n)
  // =============================================================
  {
    id: "lc-189",
    group: "位运算与数学",
    icon: "🔢",
    title: "#50 Pow(x, n)（中等）",
    content: `## 题目

**LeetCode #50 Pow(x, n)** | 难度：中等

实现 \`pow(x, n)\`，即计算 \`x\` 的整数 \`n\` 次幂函数（即 \`x^n\`）。

示例：

\`\`\`
输入：x = 2.00000, n = 10
输出：1024.00000

输入：x = 2.00000, n = -2
输出：0.25000
解释：2^-2 = 1/2^2 = 0.25
\`\`\`

## 思路

朴素连乘 O(n) 会超时（n 可达 2^31）。用**快速幂**将复杂度降到 O(log n)。

核心思想：\`x^n = (x^2)^(n/2)\`，每次把指数减半、底数平方。

- 当 n 为偶数：\`x^n = (x * x)^(n/2)\`
- 当 n 为奇数：\`x^n = x * (x * x)^((n-1)/2)\`，先拿出一个 x 再处理剩余。

用二进制视角理解：把 n 写成二进制，逐位处理。若该位为 1，就把当前底数乘进结果；每次循环底数自平方、n 右移一位。运算符 \`>>\`（右移）相当于除以 2，\`& 1\` 取最低位判断奇偶。

处理负指数：\`x^(-n) = 1 / x^n\`，先把 n 取正，最后取倒数。注意 \`n = -2^31\` 取负会溢出（32 位最小负数），需先转成正并用 64 位/JS Number 处理。

## Python 实现

\`\`\`python
class Solution:
    def myPow(self, x, n):
        # 快速幂迭代
        if n < 0:
            x = 1 / x
            n = -n
        result = 1
        while n:
            # n 最低位为 1，乘入当前底数
            if n & 1:
                result *= x
            x *= x          # 底数平方
            n >>= 1         # 指数右移（除 2）
        return result
\`\`\`

## JavaScript 实现

\`\`\`javascript
var myPow = function(x, n) {
    if (n < 0) {
        x = 1 / x;
        n = -n;            // JS Number 是 64 位，不会溢出
    }
    let result = 1;
    while (n > 0) {
        if (n & 1) {        // 当前位为 1，乘入底数
            result *= x;
        }
        x *= x;             // 底数平方
        n = Math.floor(n / 2);  // 指数减半（JS 无 >>> 大数安全，用 floor）
    }
    return result;
};
\`\`\`

## 复杂度

- 时间复杂度：O(log n)，每次指数减半
- 空间复杂度：O(1)（迭代写法）；递归写法为 O(log n) 栈空间

## 拓展

- 递归写法更易理解：\`pow(x, n) = pow(x, n//2)^2 * (n 奇 ? x : 1)\`。
- 矩阵快速幂是同一思想的推广，用于线性递推（如斐波那契）加速到 O(log n)。
- 注意 JS 用 \`n >>= 1\` 处理大于 2^31 的数会出问题，故用 \`Math.floor(n / 2)\`。`
  },

  // =============================================================
  // lc-190 #69 x 的平方根
  // =============================================================
  {
    id: "lc-190",
    group: "位运算与数学",
    icon: "🔢",
    title: "#69 x 的平方根（简单）",
    content: `## 题目

**LeetCode #69 x 的平方根** | 难度：简单

给你一个非负整数 \`x\`，计算并返回 \`x\` 的**算术平方根**的整数部分。由于返回类型是整数，小数部分将被舍去。注意：不允许使用任何内置指数函数和算符，例如 \`pow(x, 0.5)\` 或者 \`x ** 0.5\`。

示例：

\`\`\`
输入：x = 8
输出：2
解释：8 的算术平方根是 2.828...，整数部分为 2

输入：x = 4
输出：2
\`\`\`

## 思路

求平方根的整数部分，即找最大的 \`k\` 使得 \`k * k <= x\`。两种主流方法：

1. **二分查找（推荐）**：在 \`[0, x]\` 上二分。每次取 mid，比较 \`mid * mid\` 与 \`x\`：若 \`mid*mid <= x\`，说明 mid 可能为答案，往右找更大的；否则往左找更小的。最终 left 会停在第一个 \`k*k > x\` 的位置，答案是 \`left - 1\`。

   注意 \`mid * mid\` 可能溢出，可用 \`mid <= x / mid\` 代替 \`mid*mid <= x\`（等价但避免大数相乘），或用大整数。

2. **牛顿迭代法**：求 \`f(t) = t^2 - x = 0\` 的根。迭代式 \`t = (t + x / t) / 2\`，收敛极快（二次收敛），通常几次迭代就达到精度。

二分法直观且不易出错，下面给出二分法实现，并附牛顿法变体。

## Python 实现

\`\`\`python
class Solution:
    # 写法一：二分查找
    def mySqrt(self, x):
        left, right = 0, x
        ans = 0
        while left <= right:
            mid = (left + right) // 2
            if mid * mid <= x:
                ans = mid        # mid 可能是答案，继续往右找更大的
                left = mid + 1
            else:
                right = mid - 1   # mid 太大，往左找
        return ans

    # 写法二：牛顿迭代
    def mySqrt_newton(self, x):
        if x == 0:
            return 0
        t = x
        while t * t > x:
            t = (t + x // t) // 2   # 迭代逼近
        return t
\`\`\`

## JavaScript 实现

\`\`\`javascript
var mySqrt = function(x) {
    let left = 0, right = x, ans = 0;
    while (left <= right) {
        const mid = Math.floor((left + right) / 2);
        if (mid * mid <= x) {
            ans = mid;           // mid 可能是答案，往右找更大的
            left = mid + 1;
        } else {
            right = mid - 1;     // mid 太大，往左找
        }
    }
    return ans;
};

var mySqrtNewton = function(x) {
    if (x === 0) return 0;
    let t = x;
    while (t * t > x) {
        t = Math.floor((t + Math.floor(x / t)) / 2);  // 迭代逼近
    }
    return t;
};
\`\`\`

## 复杂度

- 时间复杂度：O(log x)（二分）；O(log log x)（牛顿法，更快）
- 空间复杂度：O(1)

## 拓展

- 牛顿法是数值计算经典方法，对求各种方程根都适用，收敛速度极快。
- 若要求浮点平方根精度，牛顿法只需设置一个精度阈值（如 \`|t*t - x| < 1e-7\`）即可。`
  }
];
