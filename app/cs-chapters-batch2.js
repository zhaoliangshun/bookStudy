// =============================================================
// 计算机原理入门教程 - batch2
// 章节 9-16：数据怎么表示 + 硬件组成
// -------------------------------------------------------------
// 风格：简单入门、生活例子、干货、对编程有帮助。
// 演示代码用 Python（仅标准库），通过 /api/run-py 调用系统 python3 执行。
// 转义规则：反引号写作 \`，\${ 写作 \$\{。
// =============================================================

export const chapters = [
  // ============================================================
  // 第 9 章：文字怎么存：ASCII 到 Unicode
  // ============================================================
  {
    id: "cs-09",
    group: "数据怎么表示",
    icon: "🔤",
    title: "文字怎么存：ASCII 到 Unicode",
    content: `## 从摩斯电码说起

你看过战争片里发电报吗？滴滴答答的声音，长短组合起来就能代表一个字母。这就是摩斯电码——人类最早用"约定好的符号"传递信息的系统之一。A 是"·-"，B 是"-···"，双方都拿着同一张对照表，才能把滴答声翻译成文字。

还有旗语：水手用两面旗子的不同位置代表不同字母，远处的船看旗子的角度就知道在说什么。这些都是在做同一件事——**用一套约定好的规则，把信息变成符号，再从符号还原信息**。

计算机存文字也是一模一样的道理。只不过它用的"符号"是 0 和 1，"对照表"就是编码表。本章讲清楚计算机怎么用 0/1 存下全世界的文字，以及让你抓狂的"乱码"到底是怎么来的。

## ASCII：英文世界的 128 个符号

1960 年代，美国人定了一套标准叫 **ASCII**（美国信息交换标准代码）。它用 **7 位二进制**（2⁷=128）给 128 个字符各分配一个编号：

- 0~31：控制字符（换行、回车、响铃等不可见符号）
- 32~126：可打印字符（空格、标点、数字 0~9、大写 A~Z、小写 a~z）
- 65 是 \`A\`，97 是 \`a\`，48 是 \`0\`

ASCII 对英文世界够用了，每个字符刚好 1 字节（8 位，最高位留作校验）。但你发现没有——它根本没有中文、日文、阿拉伯文的位置。这就是后来"中文编码"问题的根源。

## 中文怎么存：GB2312 与 GBK

ASCII 只有 128 个符号，中文几万个汉字根本塞不进去。于是中国人自己定了一套 **GB2312**：用**两个字节**表示一个汉字（第一个字节叫"高字节"，第二个叫"低字节"），能表示 6763 个常用汉字。

后来发现 6763 个不够用（生僻字、繁体字缺），又扩展成 **GBK**，还是两字节，能表示 2 万多个汉字。Windows 中文版早期默认就是 GBK。

问题来了：每个国家都搞自己的一套——中国的 GBK、日本的 Shift-JIS、韩国的 EUC-KR。**同一个二进制字节串，用不同编码解释出来完全不同**。于是文件传到国外就乱码，这就是"乱码时代"。

## Unicode：统一全世界的文字

为了终结这种混乱，1990 年代国际组织搞了 **Unicode**（万国码）。它的目标很宏大：**给全世界所有文字、符号甚至 emoji 都分配一个唯一编号**。

比如：

- \`A\` 的 Unicode 编号是 U+0041
- \`中\` 的 Unicode 编号是 U+4E2D
- \`😀\` 的 Unicode 编号是 U+1F600

Unicode 只是一个"编号表"，它规定每个字符是几号，但**没规定这个编号怎么存成字节**。这就引出了 UTF-8、UTF-16 等具体的"存储方案"。

## UTF-8 vs UTF-16：怎么把编号存成字节

**UTF-8** 是最流行的存储方案，它用**变长编码**：

| 字符范围 | 字节数 | 例子 |
| --- | --- | --- |
| ASCII（0~127） | 1 字节 | \`A\` → 1 字节 |
| 拉丁文、希腊文等 | 2 字节 | \`é\` → 2 字节 |
| 常用中文（含大部分汉字） | 3 字节 | \`中\` → 3 字节 |
| emoji、生僻字 | 4 字节 | \`😀\` → 4 字节 |

UTF-8 的聪明之处：**ASCII 字符还是 1 字节**，和老的 ASCII 系统完全兼容，所以英文文本用 UTF-8 没有多余开销。这就是为什么今天互联网上 UTF-8 一统天下。

**UTF-16** 则用 2 或 4 字节，对中文相对友好（汉字大多 2 字节），但对英文反而变大了（英文也要 2 字节）。Windows 内部、Java 字符串用的就是 UTF-16。

## 编码 vs 解码：一对反向操作

- **编码（encode）**：把字符串变成字节串（用某套规则）。比如 \`"中".encode("utf-8")\` → \`b'\\xe4\\xb8\\xad'\`。
- **解码（decode）**：把字节串变回字符串（用同一套规则）。比如 \`b'\\xe4\\xb8\\xad'.decode("utf-8")\` → \`"中"\`。

**铁律：编码和解码必须用同一套规则**。用 UTF-8 编码的字节，必须用 UTF-8 解码；如果错用 GBK 解码，就变成乱码。

## 编程启发：乱码的根源

新手最常遇到的乱码场景：

1. **文件乱码**：一个文件用 UTF-8 存的，你用 GBK 打开就乱码。解决：用正确的编码重新打开。
2. **网络乱码**：网页声明是 GBK 但实际是 UTF-8，浏览器按声明解析就乱码。
3. **数据库乱码**：数据库连接编码和表编码不一致，存进去就乱码。

记住一句话：**乱码 = 编码和解码用了不同规则**。遇到乱码别慌，先查"这个字节是用什么编码存的"，再用同样的编码解。

## 表格对比：常见编码

| 编码 | 字符集 | 中文占字节 | 兼容 ASCII | 用途 |
| --- | --- | --- | --- | --- |
| ASCII | 128 个英文符号 | 不支持 | — | 老系统、英文环境 |
| GBK | 中文+英文 | 2 字节 | 部分兼容 | Windows 中文版 |
| UTF-8 | 全世界文字 | 3 字节 | 完全兼容 | 互联网主流 |
| UTF-16 | 全世界文字 | 2 字节 | 不兼容 | Windows/Java 内部 |

## ⚠️ 常见坑

- **坑 1：以为 \`len("中文")\` 是字节数**。Python 里 \`len\` 数的是字符数（2），要看字节数得用 \`len("中文".encode("utf-8"))\`（6）。
- **坑 2：打开文件不指定编码**。Python 默认用系统编码（Windows 是 GBK，Mac/Linux 是 UTF-8），跨平台会乱码。永远写 \`open(f, encoding="utf-8")\`。
- **坑 3：以为 UTF-8 和 Unicode 是一回事**。Unicode 是编号表，UTF-8 是存储方案，别混说。

## 小结

- ASCII 用 7 位存 128 个英文符号，每字符 1 字节。
- 中文用 GBK（2 字节），但各国标准不同导致乱码。
- Unicode 给全世界文字统一编号，UTF-8 是它的变长存储方案（英文 1 字节、中文 3 字节）。
- 乱码根源：编码和解码用了不同规则。解决：用同一套规则。

## 常见疑问 Q&A

**Q1：为什么中文在 UTF-8 里要 3 字节，比 GBK 还多？**
UTF-8 要兼容全球，设计上对 ASCII 友好（英文 1 字节），代价是中文要 3 字节。GBK 专为中文设计，中文 2 字节但英文也是 1 字节。存储中文文本 GBK 更省，但牺牲了国际通用性。

**Q2：emoji 是怎么存的？**
emoji 编号超过 65535，在 UTF-8 里要 4 字节，在 UTF-16 里要用"代理对"（两个 2 字节）。这就是为什么有些老系统显示 emoji 会变成两个方框——它不认代理对。

**Q3：Python 2 和 Python 3 字符串有什么区别？**
Python 2 的字符串是字节串，中文要加 \`u\` 前缀；Python 3 的字符串默认就是 Unicode，\`str\` 是字符、\`bytes\` 是字节，区分清楚。这是 Py3 最重要的改进之一。`,
    code: `# ===== 第 9 章：文字怎么存——ASCII 到 Unicode =====
# 演示字符编码、解码，以及不同编码占多少字节

print("=== 1. 字符与编号：ord / chr ===")
print("'A' 的编号 =", ord('A'))      # 65
print("'a' 的编号 =", ord('a'))      # 97
print("'0' 的编号 =", ord('0'))      # 48
print("编号 66 对应字符:", chr(66))   # B
print("编号 20013 对应字符:", chr(20013))  # 中

print()
print("=== 2. ASCII：前 128 个字符 ===")
print("ASCII 范围 0~127，英文符号各占 1 字节")
for ch in ['A', 'Z', 'a', 'z', '0', '9', ' ']:
    print("  '%s' = %d" % (ch, ord(ch)))

print()
print("=== 3. encode / decode：字符串与字节 ===")
s = "hello"
b = s.encode("ascii")
print("字符串:", s, "→ 字节:", b, "长度:", len(b))
print("字节解码回:", b.decode("ascii"))

print()
print("=== 4. 中文在不同编码下占多少字节 ===")
zh = "你好"
print("中文:", zh)
print("  GBK    编码:", zh.encode("gbk"), "长度", len(zh.encode("gbk")), "字节")
print("  UTF-8  编码:", zh.encode("utf-8"), "长度", len(zh.encode("utf-8")), "字节")
print("  UTF-16 编码:", zh.encode("utf-16"), "长度", len(zh.encode("utf-16")), "字节")

print()
print("=== 5. 乱码是怎么来的：编码解码规则不一致 ===")
text = "计算机"
encoded = text.encode("utf-8")
wrong = encoded.decode("gbk", errors="replace")
print("原文:", text)
print("UTF-8 编码后用 GBK 解码:", wrong, "(乱码!)")

print()
print("=== 6. UTF-8 的变长特性 ===")
for ch in ['A', '中', 'é', '😀']:
    b = ch.encode("utf-8")
    print("  '%s' (U+%04X) → %d 字节 %s" % (ch, ord(ch), len(b), b.hex()))

print()
print("=== 7. 字符数 vs 字节数 ===")
s = "hello你好"
print("字符串:", s)
print("字符数 len(s)        =", len(s))
print("字节数 len(encode)   =", len(s.encode("utf-8")))
print("说明：英文1字节，中文3字节，5+6=11")
`,
  },

  // ============================================================
  // 第 10 章：图片怎么存：像素与颜色
  // ============================================================
  {
    id: "cs-10",
    group: "数据怎么表示",
    icon: "🖼️",
    title: "图片怎么存：像素与颜色",
    content: `## 从十字绣说起

你见过十字绣吗？一块布上密密麻麻绣满小方格，每个方格一种颜色。离近看是一个个色块，离远了看就是一幅完整的画——蒙娜丽莎、风景、人像都能绣出来。

马赛克拼图也是一样：用成千上万块小瓷砖，每块一个颜色，拼起来就是大图。**像素就是数字世界的"小瓷砖"**——一张数码图片，本质就是几百万个带颜色的小方格排成的阵列。

## 像素：图片的最小单位

**像素（pixel）** 是图片里最小的、不可再分的色块。一张照片有多少像素，就有多少个色点。把手机拍的照片放到最大放大，你会看到画面变成一个个方块——那就是像素。

像素的数量用"分辨率"描述。比如手机拍的照片分辨率是 \`4032×3024\`，意思是横向 4032 个像素、纵向 3024 个像素，总共约 1220 万个像素点（俗称"1200 万像素摄像头"）。

## RGB：红绿蓝三色拼出所有颜色

每个像素的颜色怎么存？答案是 **RGB 模型**：用红（Red）、绿（Green）、蓝（Blue）三种光按不同比例混合。这跟你小学美术课学的"红黄蓝"三原色不同——那是颜料混合（越混越黑），RGB 是光混合（越混越白），因为屏幕是发光的。

生活类比：调色盘。每个通道给 0~255 共 256 个档位：

- 纯红 = (255, 0, 0)
- 纯绿 = (0, 255, 0)
- 纯蓝 = (0, 0, 255)
- 白色 = (255, 255, 255)（三色全开）
- 黑色 = (0, 0, 0)（三色全关）
- 黄色 = (255, 255, 0)（红+绿）

每个通道 256 档 = 8 位 = 1 字节，三个通道共 3 字节，能组合出 256³ = **1670 万种颜色**（俗称"真彩色"）。人眼大约能分辨 1000 万种颜色，所以 3 字节够用了。

## 分辨率：1920×1080 是什么意思

分辨率写成"宽×高"，描述图片有多少像素：

| 名称 | 分辨率 | 像素数 | 常见用途 |
| --- | --- | --- | --- |
| VGA | 640×480 | 30 万 | 早期屏幕 |
| 高清 720p | 1280×720 | 92 万 | 早期视频 |
| 全高清 1080p | 1920×1080 | 207 万 | 主流屏幕、视频 |
| 2K | 2560×1440 | 369 万 | 手机、显示器 |
| 4K | 3840×2160 | 829 万 | 高清电视、电影 |

分辨率越高，画面越细腻，但文件也越大。

## 一张图多大：宽×高×3 字节

未压缩的图片大小很容易算：

\`\`\`
大小（字节）= 宽 × 高 × 3
\`\`\`

举例：一张 1920×1080 的未压缩图 = 1920 × 1080 × 3 = 6,220,800 字节 ≈ **5.93 MB**。

一张 4K 图 = 3840 × 2160 × 3 ≈ **23.7 MB**。

这就是为什么"原始图片"特别占空间——每像素 3 字节，几百万像素就是几十 MB。实际中我们用 JPG/PNG 压缩后才几百 KB 到几 MB（下一章讲压缩）。

## 怎么看：颜色用十六进制表示

前端写颜色 \`#FF5733\` 就是十六进制的 RGB：

- \`FF\` = 红色通道 255
- \`57\` = 绿色通道 87
- \`33\` = 蓝色通道 51

2 位十六进制正好 1 字节（0~255），所以 \`#RRGGBB\` 共 6 位 = 3 字节，刚好对应一个像素的颜色。

## 编程启发：图片处理的基础

1. **理解文件大小**：一张未压缩 1080p 图约 6MB，所以处理图片要考虑内存——加载 1000 张图就是 6GB，可能爆内存。
2. **图像处理本质**：所有 PS 滤镜、美颜都是"对每个像素的 RGB 值做数学运算"。比如变暗就是把每个值乘 0.8。
3. **为什么照片占内存**：手机拍 4000×3000 的照片未压缩约 36MB，压缩成 JPG 才 3~5MB。这就是压缩的威力。
4. **缩放的代价**：把图放大 2 倍，像素数变 4 倍（宽高都翻倍），内存占用也 4 倍。

## 表格对比：常见图片格式

| 格式 | 压缩方式 | 支持透明 | 适合 | 大小 |
| --- | --- | --- | --- | --- |
| BMP | 不压缩 | 否 | 老格式、理论大小 | 最大 |
| PNG | 无损压缩 | 是 | 图标、截图、需要透明 | 中等 |
| JPG | 有损压缩 | 否 | 照片 | 小 |
| GIF | 无损（256色） | 是 | 动图、简单图形 | 小 |

## ⚠️ 常见坑

- **坑 1：以为图片大小就是文件大小**。文件是压缩后的，处理时解码到内存是"宽×高×3"甚至×4（带透明通道）。
- **坑 2：以为分辨率越高越清晰**。分辨率只是像素数，清晰度还取决于镜头、对焦、光线。渣镜头拍 4K 也是糊的。
- **坑 3：处理图片时不注意通道顺序**。有的库是 RGB，有的是 BGR（如 OpenCV），搞反了颜色就全错。

## 小结

- 图片由像素组成，像素是最小色块，分辨率=宽×高。
- 每像素用 RGB 三通道存颜色，每通道 1 字节，共 3 字节/像素。
- 未压缩图片大小 = 宽 × 高 × 3 字节。
- 实际文件用 PNG/JPG 压缩后才变小。
- 颜色 \`#FF5733\` 就是十六进制的 RGB，6 位 = 3 字节。

## 常见疑问 Q&A

**Q1：为什么屏幕用 RGB 而不是颜料的三原色？**
屏幕发光，光是"加色混合"（越加越白），所以用红绿蓝；颜料是"减色混合"（越混越黑），用青品黄。两者物理原理相反。

**Q2：透明度怎么存？**
加一个 Alpha 通道（0 全透明，255 不透明），变成 RGBA 共 4 字节/像素。PNG 支持 alpha 所以能存透明背景的图标。

**Q3：为什么老照片传到网上会变色？**
早期 sRGB 色彩空间和现在 Display P3 等不同，加上压缩，颜色会偏。专业设计要管"色彩管理"，保证不同设备显示一致。`,
    code: `# ===== 第 10 章：图片怎么存——像素与颜色 =====
# 演示图片大小计算，并用文字模拟一张像素图

print("=== 1. 分辨率：1920x1080 是什么意思 ===")
width = 1920
height = 1080
pixels = width * height
print("1920x1080 共有 %d 个像素" % pixels)
print("约 %d 万像素" % (pixels // 10000))

print()
print("=== 2. RGB：每个像素 3 字节 ===")
bytes_per_pixel = 3
total_bytes = pixels * bytes_per_pixel
print("每像素 %d 字节 x %d 像素 = %d 字节" % (bytes_per_pixel, pixels, total_bytes))
print("约 %.2f MB（未压缩）" % (total_bytes / 1024 / 1024))

print()
print("=== 3. 颜色用 RGB 表示 ===")
def to_rgb(hex_color):
    """把 #FF5733 这种十六进制颜色拆成 RGB"""
    r = int(hex_color[1:3], 16)
    g = int(hex_color[3:5], 16)
    b = int(hex_color[5:7], 16)
    return r, g, b

for c in ["#FF0000", "#00FF00", "#0000FF", "#FFFFFF", "#FF5733"]:
    r, g, b = to_rgb(c)
    print("  %s -> R=%d G=%d B=%d" % (c, r, g, b))

print()
print("=== 4. 用文字模拟一张像素图（8x8 心形）===")
# 1=黑色块, 0=空白
heart = [
    "00110011",
    "01111111",
    "01111111",
    "01111111",
    "00111110",
    "00011100",
    "00001000",
    "00000000",
]
for row in heart:
    line = row.replace("0", "  ").replace("1", "██")
    print(line)

print()
print("=== 5. 不同分辨率图片大小对比 ===")
for name, w, h in [("VGA", 640, 480), ("720p", 1280, 720), ("1080p", 1920, 1080), ("4K", 3840, 2160)]:
    size_mb = w * h * 3 / 1024 / 1024
    print("  %-5s %dx%d -> %.2f MB（未压缩）" % (name, w, h, size_mb))

print()
print("=== 6. RGB 能组合多少种颜色 ===")
channels = 256
total = channels ** 3
print("每通道 %d 档，三通道共 %d 种颜色" % (channels, total))
print("即约 %d 万色（真彩色）" % (total // 10000))
`,
  },

  // ============================================================
  // 第 11 章：声音怎么存：采样与编码
  // ============================================================
  {
    id: "cs-11",
    group: "数据怎么表示",
    icon: "🎵",
    title: "声音怎么存：采样与编码",
    content: `## 从拍视频说起

视频是什么？其实就是一连串快速播放的照片——每秒拍 24 张（或 30、60 张），连起来放就是动的画面。这是对"视觉"的切片记录。

声音也是一样的思路。声音本质是空气的连续振动——一个平滑的波形。但计算机存不了"无限连续"的东西，只能存离散的数字。怎么办？**像拍视频一样，对声波"切片"**：每隔极短时间测一次声波的振幅，记下一个数字。测得越频繁、每次记得越精确，还原出来的声音就越接近原声。

这就是数字音频的核心思想——**采样**和**量化**。

## 声音是连续的波形

声音在物理上是一条连续的曲线：横轴是时间，纵轴是空气压力的强弱（振幅）。说话、音乐、噪音都是这种波形，只是形状不同。

- 音调高 = 波形抖得快（频率高）
- 声音大 = 波形上下幅度大（振幅大）

但这条曲线是"无限连续"的，计算机的内存是有限的，没法存"无限个点"。所以必须把它"离散化"——隔一段取一个点，用数字记下来。

## 采样：每秒测多少次

**采样率（sample rate）** 就是每秒测多少次声波，单位是赫兹（Hz）。采样率越高，记录的细节越多。

- 电话音质：8000 Hz（每秒 8000 次）——够听清说话，但声音闷
- CD 音质：44100 Hz——人耳听觉上限约 20000 Hz，按"奈奎斯特定理"采样率要≥2 倍频率，所以 44100 Hz 能完美还原人耳能听到的所有声音
- 高清音频：96000 Hz、192000 Hz——专业录音用

生活类比：采样率就像"拍照的帧率"。每秒拍 8 张 vs 每秒拍 44 张，后者能捕捉更快的动作。声音里，高频率的尖叫声需要高采样率才能还原。

## 量化：每次测的值用多少位存

每次采样测到一个振幅值（比如 0.314），但这个值也要用二进制存。**位深（bit depth）** 决定能存多精确：

- 8 位：256 个档位——音质糙，像老游戏音效
- 16 位：65536 个档位——CD 音质，足够听
- 24 位：1600 万档位——专业录音

位深就像"调色盘的档位数"——档位越多，渐变越平滑。位深太小，安静处会出现"沙沙"的量化噪声。

## 一首歌多大：采样率×位深×声道×时长

未压缩音频的大小公式：

\`\`\`
大小 = 采样率 × (位深/8) × 声道数 × 时长（秒）
\`\`\`

举例：CD 音质（44100 Hz、16 bit、立体声）1 分钟：

\`\`\`
44100 × 2 × 2 × 60 = 10,584,000 字节 ≈ 10.1 MB
\`\`\`

一首 4 分钟的歌约 40 MB。这就是为什么早期 MP3 播放器只能存几十首歌——原始 WAV 太大了。MP3 压缩后能缩到 4 MB 左右（压缩到 1/10）。

## 声道：单声道 vs 立体声

- 单声道（mono）：1 个声道，所有声音混一路
- 立体声（stereo）：2 个声道（左右），有空间感
- 环绕声：5.1（6 声道）、7.1（8 声道）——影院用

声道数直接乘进大小公式。立体声是单声道的 2 倍数据量。早期 MP3 播放器存歌少，除了原始 WAV 大，也和立体声双倍数据有关——所以有的语音录音用单声道省一半空间，听感差别不大。

## 编程启发：音频处理的基础

1. **理解音频文件大小**：未压缩 CD 音质每分钟约 10MB，所以实时音频传输要压缩（语音通话用 8000 Hz 节省带宽）。
2. **为什么 CD 音质好**：44100 Hz 采样+16 bit 量化刚好覆盖人耳极限，是"听感无损"的工程最优解。
3. **语音识别为什么降采样**：语音主要信息在 8000 Hz 以下，所以 ASR 系统常把音频降到 16000 Hz 处理，省算力。
4. **延迟与缓冲**：实时音频要低延迟，但太小缓冲会卡顿，这是音视频开发的经典权衡。

## 表格对比：常见音质参数

| 场景 | 采样率 | 位深 | 声道 | 每分钟大小 |
| --- | --- | --- | --- | --- |
| 电话 | 8000 Hz | 8 bit | 1 | 0.46 MB |
| 语音通话 | 16000 Hz | 16 bit | 1 | 1.83 MB |
| CD | 44100 Hz | 16 bit | 2 | 10.1 MB |
| 录音棚 | 96000 Hz | 24 bit | 2 | 33 MB |

## ⚠️ 常见坑

- **坑 1：以为采样率越高越好**。超过人耳极限的采样率（如 192 kHz）人听不出差别，反而文件巨大，专业用途才需要。
- **坑 2：混淆位深和音量**。位深是精度（档位数），音量是振幅大小，不是一回事。
- **坑 3：以为 MP3 是无损的**。MP3 是有损压缩，丢掉了人耳不敏感的频率。听感接近但数据已损。

## 小结

- 声音是连续波形，计算机用"采样+量化"把它变成离散数字。
- 采样率 = 每秒测多少次（CD 是 44100 Hz）；位深 = 每次用多少位存（CD 是 16 bit）。
- 音频大小 = 采样率 × 位深/8 × 声道 × 时长。
- CD 音质每分钟约 10MB，压缩成 MP3 约 1MB。
- 奈奎斯特定理：采样率要≥2 倍最高频率才能完美还原。

## 常见疑问 Q&A

**Q1：为什么 CD 是 44100 Hz 而不是 40000？**
人耳上限约 20000 Hz，按定理采样率≥40000 即可。选 44100 是当年配合视频信号（每秒 44100/30/1.47... 的工程凑数）定的，沿用至今。

**Q2：蓝牙耳机音质为什么不如有线？**
蓝牙带宽有限，要把音频高压缩率传输，常用有损编码（SBC/AAC）。所以即使用高级耳机，蓝牙传输这一步就丢了信息。

**Q3：录音时底噪是怎么来的？**
采样量化误差和环境电磁干扰都会产生底噪。提高位深（24 bit）能降低量化噪声，但不能完全消除物理噪声。`,
    code: `# ===== 第 11 章：声音怎么存——采样与编码 =====
# 演示音频文件大小计算，并模拟波形采样

import math

print("=== 1. CD 音质的参数 ===")
sample_rate = 44100
bit_depth = 16
channels = 2
duration = 60
print("采样率: %d Hz" % sample_rate)
print("位深: %d bit" % bit_depth)
print("声道数: %d" % channels)
print("时长: %d 秒" % duration)

print()
print("=== 2. 一首歌多大 ===")
size_bytes = sample_rate * (bit_depth // 8) * channels * duration
print("每秒数据: %d x %d x %d = %d 字节" % (sample_rate, bit_depth // 8, channels, sample_rate * (bit_depth // 8) * channels))
print("1 分钟约 %.2f MB" % (size_bytes / 1024 / 1024))
print("1 首歌(4分钟)约 %.2f MB" % (size_bytes * 4 / 1024 / 1024))

print()
print("=== 3. 模拟声波采样：把连续波形切成离散点 ===")
def sine_wave(t, freq=440):
    """模拟一个 440Hz 的正弦波（A 音）"""
    return math.sin(2 * math.pi * freq * t)

print("对 440Hz 声音采样 0.01 秒：")
for rate in [1000, 200, 50]:
    points = []
    for i in range(rate // 100):
        t = i / rate
        points.append(round(sine_wave(t), 3))
    print("  %d Hz 采样 -> %d 个点: %s" % (rate, len(points), points[:6]))

print()
print("=== 4. 量化误差：连续值截成离散值 ===")
levels = 2 ** 16
print("16 位量化 -> %d 个电平档位" % levels)
val = 0.3333
quantized = round(val * levels) / levels
print("模拟值 %.4f 截成 16 位 -> %.6f（误差 %.6f）" % (val, quantized, abs(val - quantized)))

print()
print("=== 5. 不同音质大小对比 ===")
for name, sr, bd, ch in [("电话", 8000, 8, 1), ("FM广播", 22050, 16, 2), ("CD", 44100, 16, 2), ("高清", 96000, 24, 2)]:
    per_sec = sr * (bd // 8) * ch
    print("  %-7s %dHz/%dbit/%d声道 -> 每秒 %d 字节, 每分 %.2f MB" % (name, sr, bd, ch, per_sec, per_sec * 60 / 1024 / 1024))

print()
print("=== 6. 奈奎斯特定理：采样率要 >= 2 倍最高频率 ===")
print("人耳听觉上限约 20000 Hz")
print("CD 采样率 44100 Hz >= 2 x 20000 = 40000，刚好够")
print("电话只传语音(约 3400 Hz)，8000 Hz 采样足够")
`,
  },

  // ============================================================
  // 第 12 章：压缩原理：让文件变小
  // ============================================================
  {
    id: "cs-12",
    group: "数据怎么表示",
    icon: "📦",
    title: "压缩原理：让文件变小",
    content: `## 从抽真空压缩袋说起

冬天收羽绒服，你拿出一个抽真空压缩袋，把衣服塞进去，吸尘器一抽——蓬松的一大袋瞬间变成薄薄一片，体积缩到 1/3。衣服一件没少，但占的空间大幅缩小。这就是压缩的直观形象。

还有种压缩更像"缩写"：你和朋友聊天，"哈哈哈哈哈哈哈"打成"哈哈x7"，意思一样但字数少了。或者用"GG"代替"good game"，用"yyds"代替"永远的神"——用更短的符号代表重复或常见的内容。

计算机压缩文件就是这两种思路的结合：**发现数据里的冗余（重复、规律），用更短的方式重新表示**。本章讲清楚压缩为什么能奏效，以及无损和有损的区别。

## 为什么能压缩：数据有冗余

一段数据里如果完全没有规律，那是压不动的（比如已经压缩过的文件、随机噪声）。但真实数据几乎都有冗余：

- 文本里大量重复的空格、常用字母（e、t、a 出现频率高）
- 图片里大片相同颜色的区域（蓝天、白墙）
- 视频里相邻帧几乎一样（背景不动，只有人物动）

**冗余 = 可压缩的空间**。压缩算法就是找到这些规律并重新编码。

## 无损压缩：一个比特都不能丢

**无损压缩** 压缩后再解压，得到的数据和原始数据**一模一样**，一个字节都不差。适用于"不能出错"的场景：文本、代码、程序、数据表。

常见格式：zip、gz、png、flac。

原理举例：

**行程编码（RLE）**：把连续重复的字符记成"字符+次数"。\`AAAAABBBB\` → \`A5B4\`，5 个字符变 4 个字符。对有大片纯色的图片（图标、漫画）特别有效。

**哈夫曼编码**：统计每个字符出现频率，**高频字符用短码、低频字符用长码**。就像摩斯电码里常用的 \`E\` 是一个点"·"，少见的 \`Q\` 是"--·-"。这样总体平均码长变短。

## 有损压缩：丢掉不重要的细节

**有损压缩** 解压后不能完全还原原始数据，但"看起来/听起来差不多"。适用于"人感知不敏感"的场景：照片、音乐、视频。

常见格式：jpg、mp3、mp4、aac。

原理：利用人眼/人耳的弱点，丢掉"感知不到"的信息。

- **JPG**：人眼对颜色细节不如对亮度敏感，所以颜色压缩得狠、亮度保留多。一片蓝天细微的色阶变化人眼看不出，删掉就删掉。
- **MP3**：人耳对某些频率同时响时听不到弱的那部分（听觉掩蔽），直接删掉。
- **视频**：相邻帧背景一样，只存"这一帧比上一帧多了什么"（帧间预测）。

有损压缩可以压到原文件的 1/10 甚至 1/100，但**每压缩一次质量就降一点**，反复压缩会越来越糊（"代际损失"）。

## 压缩率：能压多少

| 数据类型 | 无损压缩率 | 有损压缩率 |
| --- | --- | --- |
| 纯文本 | 约 50~70% | 不适用 |
| 代码 | 约 40~60% | 不适用 |
| 未压缩图片 BMP→PNG | 约 30~70% | — |
| 未压缩图片 BMP→JPG | — | 约 5~15% |
| 未压缩音频 WAV→MP3 | — | 约 10% |
| 未压缩视频→MP4 | — | 约 2~5% |

注意：已经压缩过的文件（jpg、mp3、zip）再压缩几乎没效果，因为冗余已经被榨干了。这也是为什么加密后的文件很难压缩——好加密算法的输出接近随机噪声，没有任何规律可循。日常工作中，把一堆已压缩的图片打包成 zip，体积几乎不会变小，只是方便传输而已。

## 怎么看：选对格式能省大量空间

1. **照片用 JPG**：能压到 1/10，肉眼几乎看不出差别。
2. **图标、截图、有透明背景的用 PNG**：无损、支持透明，但照片用 PNG 比 JPG 大很多。
3. **文字扫描件用 PNG 不用 JPG**：JPG 会让文字边缘出现毛刺（块效应），PNG 清晰。
4. **传输代码必须用 zip/gz 无损**：代码差一个字符就跑不起来，绝不能用有损。
5. **存档备份用无损**：重要的数据不能有损。

## 表格对比：无损 vs 有损

| 维度 | 无损压缩 | 有损压缩 |
| --- | --- | --- |
| 还原性 | 100% 还原 | 近似还原 |
| 压缩率 | 较低（2~3 倍） | 很高（10~100 倍） |
| 适用 | 文本、代码、数据 | 照片、音乐、视频 |
| 反复压缩 | 无损 | 质量递减 |
| 典型格式 | zip/png/flac | jpg/mp3/mp4 |

## ⚠️ 常见坑

- **坑 1：把 JPG 反复编辑保存**。每存一次质量降一档，存 10 次就糊了。需要编辑的照片应转成 PNG 或无损格式处理，最后再导出 JPG。
- **坑 2：以为压缩过还能再压**。zip 里再 zip 几乎不变小，因为冗余已没了。
- **坑 3：用有损格式存重要数据**。比如用 JPG 存含文字的截图，文字边缘会糊。截图要用 PNG。

## 小结

- 压缩的本质是发现并利用数据里的冗余。
- 无损压缩（zip/png）：完全还原，适用于文本、代码、数据。
- 有损压缩（jpg/mp3）：丢感知不到的细节，适用于照片、音视频。
- 行程编码（RLE）、哈夫曼编码是无损压缩的经典原理。
- 选对格式能省大量空间：照片 JPG、图标截图 PNG、代码 zip。

## 常见疑问 Q&A

**Q1：为什么压缩文件不能无限压缩？**
信息论（香农定理）证明每类数据有"信息熵"下限，低于这个极限就压不动了。已压缩文件的熵已经接近上限，再压没效果。

**Q2：RAR 和 ZIP 有什么区别？**
都是无损压缩格式。RAR 压缩率略高但闭源，ZIP 开放通用。日常用 ZIP 兼容性最好。

**Q3：为什么视频压缩率这么高？**
视频里相邻帧 90% 以上是一样的（背景不动），只存差异就行。再加上帧内 JPG 压缩，所以能压到原数据的几十分之一。`,
    code: `# ===== 第 12 章：压缩原理——让文件变小 =====
# 演示行程编码(RLE)、字符频率统计（哈夫曼思想）

print("=== 1. 行程编码(RLE)：连续重复就记次数 ===")
def rle_encode(s):
    """把 AAAABBBCC 压缩成 A4B3C2"""
    if not s:
        return ""
    result = []
    prev = s[0]
    count = 1
    for ch in s[1:]:
        if ch == prev:
            count += 1
        else:
            result.append(prev + str(count))
            prev, count = ch, 1
    result.append(prev + str(count))
    return "".join(result)

original = "AAAAABBBBBBBCCDDDDDDDDDD"
compressed = rle_encode(original)
print("原文: %s (%d字符) -> 压缩: %s (%d字符)" % (original, len(original), compressed, len(compressed)))
print("压缩率: %.0f%%" % (len(compressed) / len(original) * 100))

print()
print("=== 2. RLE 对不同数据效果不同 ===")
for name, s in [("重复多", "AAAAAAAAAA"), ("重复少", "ABCDEFGHIJ"), ("无重复", "ABCABCABCA")]:
    enc = rle_encode(s)
    print("  %-5s '%s' -> '%s' (%d->%d)" % (name, s, enc, len(s), len(enc)))

print()
print("=== 3. 字符频率统计（哈夫曼编码基础）===")
text = "this is an example for huffman encoding"
freq = {}
for ch in text:
    freq[ch] = freq.get(ch, 0) + 1
sorted_freq = sorted(freq.items(), key=lambda x: -x[1])
print("文本:", text)
print("字符频率(前6):", [(ch, n) for ch, n in sorted_freq[:6]])

print()
print("=== 4. 哈夫曼思想：高频字符用短码 ===")
codes = {}
for i, (ch, _) in enumerate(sorted_freq):
    codes[ch] = "1" * i + "0"
print("高频->短码（示意）:", {ch: codes[ch] for ch, _ in sorted_freq[:5]})

print()
print("=== 5. 无损 vs 有损 + 再压缩没效果 ===")
print("无损 zip/png/flac: 完全还原；有损 jpg/mp3/mp4: 丢感知不到的细节")
print("1080p BMP 5.9MB -> PNG 3-4MB -> JPG 0.3-0.8MB（约1/10）")
print("已压缩文件再压缩:", rle_encode("A4B3C2D10"), "（反而变长，冗余已榨干）")
`,
  },

  // ============================================================
  // 第 13 章：CPU：计算机的大脑
  // ============================================================
  {
    id: "cs-13",
    group: "硬件组成",
    icon: "🧠",
    title: "CPU：计算机的大脑",
    content: `## 从厨师做菜说起

想象一个厨房：厨师站在灶台前，助手递过来一张菜谱"番茄炒蛋"，上面写着：

1. 拿两个鸡蛋
2. 打散
3. 热油
4. 倒蛋液翻炒
5. 加番茄
6. 出锅

厨师不会一次看完整张菜谱然后"想"怎么做，而是**看一条做一条**：看第 1 步、执行、看第 2 步、执行……直到做完。厨师就是 CPU，菜谱就是程序，食材就是数据，灶台就是内存。

CPU 干活的方式和厨师一模一样：**它不会"思考"，只会机械地一条一条执行指令**。但这机械的速度极快，每秒能做几十亿次，快到看起来像在"思考"。

## CPU 干什么：取指-解码-执行-写回

CPU 内部反复执行一个固定流程，叫"指令周期"：

1. **取指（Fetch）**：从内存取出当前要执行的指令。
2. **解码（Decode）**：翻译这条指令是什么意思（加法？读取？跳转？）。
3. **执行（Execute）**：让算术逻辑单元（ALU）真正算，或控制其他部件。
4. **写回（Write-back）**：把结果写回寄存器或内存。

这四步不断循环，每秒循环几十亿次。你写的每一行代码，最终都变成这样的指令序列被 CPU 一条条执行。

## 主频：每秒执行多少次

**主频（时钟频率）** 是 CPU 每秒的时钟周期数，单位是赫兹。3 GHz = 每秒 30 亿次周期。

- 主频越高，每秒执行的指令越多，单核越快。
- 但主频不是唯一指标——还要看"每个周期能做多少事"（IPC）。
- 早期 CPU 主频从几 MHz 涨到几 GHz，但**约 2005 年后基本停在 3~5 GHz 不再大涨**。

为什么停了？因为**发热和功耗**。功耗大致与主频的平方成正比——主频翻倍，功耗变 4 倍，散热跟不上了。所以厂商不再拼主频，改拼"多核"。

## 核心数：多核 = 多个厨师同时做

**多核 CPU** 是把多个独立运算单元（核心）塞进一颗芯片：

- 单核：1 个厨师，一次做一道菜
- 4 核：4 个厨师同时做 4 道菜
- 8 核、16 核：更多厨师并行

多核能并行处理多个任务，但有个前提：**任务得能拆开**。如果任务必须顺序执行（一道菜必须先炒蛋再放番茄），再多厨师也没用。

这就是为什么"多核不一定让程序变快"——如果你的程序是单线程顺序的，8 核也只能用 1 个核，其他 7 个闲着。要用满多核，必须写**并发/并行**代码（多线程、多进程）。这也是为什么近年面试越来越看重并发编程能力——硬件不再靠主频免费送性能，性能红利要靠程序员自己把任务拆开榨取。

## 为什么主频有上限：发热与功耗

CPU 是几十亿个晶体管集成的电路，每次开关都耗电发热：

- 主频↑ → 单位时间开关次数↑ → 功耗指数级↑
- 功耗↑ → 发热↑ → 温度↑
- 温度过高 → CPU 烧毁或自动降频保护

这就是"功耗墙"。现在 CPU 散热器越做越大，笔记本还要风扇狂转，就是因为这个限制。早年 Intel 奔腾 4 想把主频推到 4 GHz 以上，结果功耗和发热失控，最终放弃改走多核路线——这就是著名的"主频之争"终结。厂商的策略：

1. 不再大幅提主频，改增加核心数。
2. 引入"睿频"：负载高时短时超频，温度上来再降速。
3. 移动端用 ARM 架构（精简指令集，功耗低）。

## 怎么看：编程中 CPU 相关的概念

1. **多线程的意义**：把可拆分的任务分到多个核并行执行，充分利用多核。但线程太多反而因切换开销变慢。
2. **为什么有些程序快不了**：如果算法是 O(n) 顺序依赖的，加核没用；要改算法或用并行计算（GPU）。
3. **计算密集 vs IO 密集**：CPU 算个不停的叫计算密集（加密、渲染）；大部分时间等硬盘/网络的叫 IO 密集。优化方向不同。
4. **基准测试**：用 \`time\` 测程序耗时，但要注意 CPU 时间 vs 真实时间（多核并行时 CPU 时间可能 > 真实时间）。

## 表格对比：单核 vs 多核

| 维度 | 单核 | 多核 |
| --- | --- | --- |
| 同时执行任务 | 1 个 | 多个并行 |
| 适合的任务 | 顺序依赖的 | 可拆分的 |
| 程序改造 | 不用改 | 要写并发代码 |
| 主频提升空间 | 受功耗墙限制 | 靠加核提升总吞吐 |
| 典型场景 | 简单脚本 | 视频转码、3D 渲染、服务器 |

## ⚠️ 常见坑

- **坑 1：以为核越多程序越快**。单线程程序多核也只用 1 核。要先看任务能否并行。
- **坑 2：以为主频高一定快**。不同架构 IPC 不同，3 GHz 的 ARM 和 3 GHz 的 x86 性能差很多。同架构下主频才有可比性。
- **坑 3：开太多线程反而变慢**。线程切换有开销，线程数远超核心数时争抢资源，性能下降。

## 小结

- CPU 像厨师，按指令周期"取指-解码-执行-写回"机械循环，每秒几十亿次。
- 主频 = 每秒周期数，3 GHz = 30 亿次/秒；受功耗墙限制，已停在 3~5 GHz。
- 多核 = 多个厨师并行，但任务必须能拆分才能加速。
- 单线程程序用不满多核，要用并发代码。
- 计算密集看 CPU，IO 密集看硬盘网络。

## 常见疑问 Q&A

**Q1：为什么手机 CPU 主频比电脑低？**
手机靠电池供电，要省电，所以主频低（2 GHz 左右）、核心多、用 ARM 架构。电脑插电可以堆功耗，主频高、用 x86。

**Q2：超线程是什么？**
一个物理核心模拟成两个逻辑核心，能更好利用空闲的执行单元，提升约 15~30% 吞吐。但不是真双核，两个线程还是共享一个核心的资源。

**Q3：GPU 和 CPU 有什么区别？**
CPU 核心少但每个核心强，适合复杂逻辑；GPU 核心成百上千但每个简单，适合大量并行计算（图像、AI 训练）。所以深度学习用 GPU。`,
    code: `# ===== 第 13 章：CPU——计算机的大脑 =====
# 演示计算量、计时、模拟多核加速效果

import time

print("=== 1. CPU 执行流程：取指-解码-执行-写回 ===")
for step, desc in [("取指","从内存读指令"), ("解码","翻译指令含义"), ("执行","ALU运算"), ("写回","结果存回")]:
    print("  %s: %s" % (step, desc))
print("-> CPU 每秒重复这个流程几十亿次")

print()
print("=== 2. 主频：3GHz = 每秒 30 亿次 ===")
freq_ghz = 3.0
cycles_per_sec = freq_ghz * 10**9
print("3 GHz CPU 每秒约 %.0f 次时钟周期" % cycles_per_sec)
print("1 个周期约 %.2f 纳秒" % (1 / cycles_per_sec * 10**9))

print()
print("=== 3. 测速：CPU 干一千万次加法要多久 ===")
n = 10_000_000
start = time.time()
total = 0
for i in range(n):
    total += i
elapsed = time.time() - start
print("%d 次加法用时 %.3f 秒，每次约 %.1f 纳秒" % (n, elapsed, elapsed / n * 10**9))

print()
print("=== 4. 模拟多核加速 ===")
def work(n):
    s = 0
    for i in range(n):
        s += i
    return s

task_total = 20_000_000
t0 = time.time(); work(task_total); single = time.time() - t0
t0 = time.time()
for _ in range(4):
    work(task_total // 4)
quarter = time.time() - t0
print("单核跑 %d 次: %.3f 秒" % (task_total, single))
print("4 核各跑 1/4(理论): %.3f 秒, 加速比 %.1f 倍" % (quarter, single / quarter if quarter > 0 else 0))

print()
print("=== 5. 为什么主频不能无限高 ===")
print("主频↑ -> 发热和功耗指数级↑（功耗≈主频²）")
for freq in [2, 3, 4, 5]:
    print("  %d GHz -> 功耗指数 %d" % (freq, freq ** 2))
print("-> 所以厂商改加核心，不再死磕主频")

print()
print("=== 6. 计算密集 vs IO 密集 ===")
print("计算密集: 加密、压缩、3D渲染 -> 优化算法、用多核/GPU")
print("IO 密集:  读写文件、网络请求 -> 用异步、减少 IO 次数")
`,
  },

  // ============================================================
  // 第 14 章：内存：临时记忆
  // ============================================================
  {
    id: "cs-14",
    group: "硬件组成",
    icon: "📋",
    title: "内存：临时记忆",
    content: `## 从桌面和抽屉说起

想象你在办公桌前工作。桌面是你伸手就能拿到的地方，抽屉要弯腰去翻，书架要起身走过去。聪明的做法是：**正在用的文件摊在桌面上**，随时能拿；不常用的收进抽屉；很久不碰的存到书架。

电脑里：

- **桌面 = 内存（RAM）**：CPU 直接读写，超快，但面积有限（几个 G）
- **抽屉/书架 = 硬盘**：容量大（几百 G~几 T），但要"走过去拿"，慢
- **你本人 = CPU**：只在桌面上工作

这就是为什么"程序要先从硬盘加载到内存才能运行"——CPU 不会自己跑去抽屉翻东西，得先把文件搬到桌面上。桌面越大（内存越大），同时摊开的东西越多，能同时开的程序就越多。

## 内存干什么：临时存正在用的数据

内存（RAM，Random Access Memory）是 CPU 的"工作台"：

- 存正在运行的程序代码
- 存程序里的变量、对象、数据结构
- 存操作系统本身

你打开浏览器、微信、编辑器，每个程序都把自己的一部分搬进内存。关掉程序，它占的内存就释放了。**内存是"临时"的**——断电即清空，这就是为什么没保存的文档断电就没了。

## 内存地址：每个格子有个编号

内存像一个超长的抽屉柜，每个抽屉能存 1 个字节，每个抽屉有个唯一编号——**内存地址**。

- 8GB 内存 ≈ 86 亿个抽屉，编号从 0 到 86 亿
- CPU 读写内存时，就是"按地址找抽屉，往里放/往外取数据"
- 地址用十六进制表示，如 \`0x7ffeeb40\`

编程里的"指针"就是"内存地址"。C 语言里你能直接操作地址（强大但危险），Python/Java 把地址藏起来（安全但少了灵活）。即使如此，理解"变量其实在内存某个地址"很重要。这也是为什么数组按下标访问特别快——下标就是地址偏移量，CPU 直接算出地址就能取到数据，不用一个一个找。

## 读写速度：内存比硬盘快 100 倍

| 操作 | 速度 | 类比 |
| --- | --- | --- |
| CPU 寄存器 | 纳秒级 | 手里拿着 |
| CPU 缓存 L1/L2 | 1~10 纳秒 | 桌面手边 |
| 内存（RAM） | ~100 纳秒 | 桌面 |
| SSD 硬盘 | ~100 微秒 | 抽屉（快 1000 倍慢） |
| HDD 机械硬盘 | ~10 毫秒 | 书架（慢 10 万倍） |

这就是为什么"频繁读写硬盘的程序慢"。把数据放内存里反复用，比每次都从硬盘读快成百上千倍。

## 断电就忘：为什么叫"临时"

内存用的是**动态随机存储器（DRAM）**，靠电容存电荷，必须持续供电才能保持数据，断电电荷流失数据就没了。这叫**易失性存储**。

而硬盘是**非易失性**——用磁性（HDD）或闪存（SSD），断电数据保留。

所以重要数据必须"保存到硬盘"（写文件/数据库）。变量只在内存里，程序一退出或断电就消失。新手常踩的坑：以为"赋值给变量就是保存了"，其实只是临时记住，要永久保存还得写文件。

## 怎么看：编程中内存相关概念

1. **变量在内存里**：\`x = 5\` 就是把 5 存到内存某地址，\`x\` 是这个地址的标签。
2. **id() 看地址**：Python 的 \`id()\` 返回对象的唯一标识（本质是地址）。
3. **赋值 vs 拷贝**：\`b = a\` 让两个变量指向同一对象，改一个另一个也变；要独立副本得用 \`copy\`。
4. **为什么大数据要分块**：一次加载 10GB 数据到 8GB 内存的电脑会崩溃，得分块读处理。
5. **内存泄漏**：程序不断占内存不释放，最终耗光。Python 有垃圾回收自动释放，但引用环、全局变量仍可能泄漏。

## 表格对比：内存 vs 硬盘

| 维度 | 内存（RAM） | 硬盘（SSD/HDD） |
| --- | --- | --- |
| 速度 | ~100 纳秒 | SSD ~100 微秒，HDD ~10 毫秒 |
| 容量 | 几 GB ~ 几十 GB | 几百 GB ~ 几 TB |
| 断电 | 数据丢失 | 数据保留 |
| 价格 | 贵 | 便宜 |
| 存什么 | 正在运行的程序和变量 | 所有文件、安装包 |
| 编程对应 | 变量、对象、列表 | 文件、数据库 |

## ⚠️ 常见坑

- **坑 1：以为变量赋值就是永久保存**。变量在内存，程序退出即没。要持久化必须写文件/数据库。
- **坑 2：把"内存"和"硬盘"混为一谈**。"我电脑 512G 内存"——那是硬盘。内存通常 8~32G。
- **坑 3：以为 Python 列表赋值是拷贝**。\`b = a\` 是引用同一个列表，改 \`a\` 也改了 \`b\`。要拷贝用 \`a.copy()\` 或 \`a[:]\`。

## 小结

- 内存是 CPU 的"工作台"，临时存正在用的程序和数据，断电即丢。
- 内存按地址访问，每个字节一个编号，CPU 按地址读写。
- 内存比硬盘快 100~10000 倍，所以频繁用的数据要放内存。
- 变量在内存里，id() 可看地址；赋值是引用不是拷贝。
- 重要数据要存硬盘（文件/数据库），不能只放变量。

## 常见疑问 Q&A

**Q1：内存越大电脑越快吗？**
不一定。内存够用就行，多了用不上。如果你开很多程序内存爆满（用硬盘当虚拟内存），那加内存会明显变快；如果内存本来就够用，加更多没感觉。

**Q2：虚拟内存是什么？**
内存不够时，操作系统拿一部分硬盘当"虚拟内存"用。但硬盘比内存慢百倍，所以频繁用虚拟内存会卡。这就是为什么"内存满了电脑变卡"。

**Q3：Python 的变量和 C 的变量在内存里有什么不同？**
C 的 \`int x\` 直接在栈上开 4 字节存值；Python 的 \`x = 5\` 是 \`x\` 指向一个整数对象（对象含类型、引用计数等），开销大得多。所以 Python 更灵活但更耗内存。`,
    code: `# ===== 第 14 章：内存——临时记忆 =====
# 演示用 id() 看地址、估算对象内存占用

import sys

print("=== 1. 变量在内存里：用 id() 看地址 ===")
a = 100
b = 100
print("a = %d, id(a) = %d" % (a, id(a)))
print("b = %d, id(b) = %d" % (b, id(b)))
print("a is b?", a is b, "(小整数被缓存，地址相同)")

c = 100000
d = 100000
print("c = %d, id(c) = %d" % (c, id(c)))
print("d = %d, id(d) = %d" % (d, id(d)))
print("c is d?", c is d, "(大整数不缓存，地址不同)")

print()
print("=== 2. 修改对象 vs 重新赋值 ===")
lst1 = [1, 2, 3]
lst2 = lst1
print("赋值后 lst1 is lst2?", lst1 is lst2)
lst1.append(4)
print("lst1 追加后 lst2 =", lst2, "(lst2 也变了!)")
lst3 = lst1.copy()
lst3.append(5)
print("拷贝后 lst1 =", lst1, "lst3 =", lst3, "(互不影响)")

print()
print("=== 3. 估算对象的内存占用 ===")
print("整数 0 占 %d 字节" % sys.getsizeof(0))
print("整数 100 占 %d 字节" % sys.getsizeof(100))
print("整数 10**100 占 %d 字节" % sys.getsizeof(10**100))
print("空列表 [] 占 %d 字节" % sys.getsizeof([]))
print("[1,2,3] 占 %d 字节" % sys.getsizeof([1, 2, 3]))
print("空字符串 '' 占 %d 字节" % sys.getsizeof(""))
print("'hello' 占 %d 字节" % sys.getsizeof("hello"))

print()
print("=== 4. 大数据占多少内存 ===")
big_list = list(range(1000000))
print("100 万个整数的列表占 %.2f MB" % (sys.getsizeof(big_list) / 1024 / 1024))
print("注意：这只是列表指针数组，整数对象还要单独算")

print()
print("=== 5. 内存 vs 硬盘速度对比 ===")
print("CPU 寄存器 : ~1 纳秒")
print("L1/L2 缓存 : 1~10 纳秒")
print("内存 RAM   : ~100 纳秒")
print("SSD 硬盘   : ~100 微秒 (慢 1000 倍)")
print("HDD 硬盘   : ~10 毫秒 (慢 10 万倍)")
print("-> 所以要把常用数据放内存，少读写硬盘")
`,
  },

  // ============================================================
  // 第 15 章：硬盘：长期记忆
  // ============================================================
  {
    id: "cs-15",
    group: "硬件组成",
    icon: "💾",
    title: "硬盘：长期记忆",
    content: `## 从黑胶唱片和闪存卡说起

老式机械硬盘（HDD）其实像一台"黑胶唱片机"：里面有圆盘在转，一个磁头悬在上面读写数据。你要听第 5 首歌，唱针得移动到对应位置等盘子转过来——这个机械动作需要时间，所以慢。

固态硬盘（SSD）则像"超大号 U 盘/闪存卡"：里面没有机械部件，全是电子电路，要读哪里直接通电就读，不用等任何东西"转过来"。所以 SSD 比 HDD 快得多。

这是两种硬盘的本质区别——**有没有机械动作**。这一差别决定了它们的速度、价格、寿命。

## 机械硬盘 HDD：磁盘+磁头

HDD 内部：

- **盘片**：金属/玻璃圆盘，表面镀磁性材料，数据靠磁化方向（N/S）存 0/1
- **磁头**：悬在盘片上方几纳米，读写磁信号
- **主轴电机**：带动盘片高速旋转（5400/7200 转/分）

读写时磁头要先"寻道"（移到对应磁道），再等盘片转过来（旋转延迟）。这两个机械动作各需几毫秒，加起来一次随机访问约 5~10 毫秒。听起来很快，但 CPU 一毫秒能做几百万次运算，相对 CPU 这慢得不可忍。

HDD 优点：**便宜、容量大**（几 TB 起步）。缺点：**慢、怕震动**（磁头可能划伤盘片）、有噪音。

## 固态硬盘 SSD：闪存

SSD 用**闪存芯片**存数据，靠电荷存 0/1，没有机械部件：

- 随机访问约 100 微秒（比 HDD 快 100 倍）
- 没有寻道和旋转延迟，任何位置一样快
- 抗震动、无噪音、低功耗

SSD 缺点：**贵、有写入寿命**（闪存单元写入次数有限，约几千到几万次）。但现代 SSD 用"磨损均衡"算法把写入分散开，日常用 5~10 年没问题。

SSD 已是主流：系统盘、开发机必须用 SSD，否则开机/编译慢到崩溃。

## 顺序读写 vs 随机读写

这是理解硬盘性能的关键：

- **顺序读写**：连续读写一大片数据（拷贝大文件）。HDD 和 SSD 都不错，因为磁头不用频繁移动。
- **随机读写**：东读一点西读一点，位置不连续（数据库查询、启动系统）。HDD 灾难性慢（磁头疯狂寻道），SSD 影响小。

| 操作 | HDD | SSD |
| --- | --- | --- |
| 顺序读 | 100~200 MB/s | 500~7000 MB/s |
| 随机读 | 0.5~2 MB/s（慢） | 30~100 MB/s（快几十倍） |
| 随机写 | 更慢 | 较快 |

**这就是为什么"把系统装 SSD 后开机快 10 倍"**——开机要随机读几百个小文件，HDD 寻道累死，SSD 秒读。

## 为什么 SSD 快：没有机械动作

HDD 随机访问慢的根源是**机械动作要时间**：

- 寻道（磁头移动）：约 5 毫秒
- 旋转延迟（等盘片转）：约 4 毫秒
- 加起来约 10 毫秒/次

SSD 没有任何机械动作，全靠电信号，访问任何位置都是约 100 微秒/次。差距约 100 倍。

而且 HDD 的随机访问"每次都要等"，访问 1 万个小文件要 100 秒；SSD 同样操作只要 1 秒。

## 怎么看：编程中硬盘相关概念

1. **数据库为什么放 SSD**：数据库查询是大量随机读写，HDD 上慢到不可用，SSD 上飞快。
2. **日志文件顺序写**：日志追加写是顺序 IO，HDD 也能扛，所以日志系统常用 HDD 省钱。
3. **批量 vs 逐条**：写 1000 条数据，逐条写（1000 次随机 IO）比批量写（1 次顺序 IO）慢得多。这就是"批量插入数据库比循环插入快"的底层原因。
4. **缓存文件到内存**：反复读的文件读到内存里缓存，避免每次碰硬盘。
5. **小文件多的大目录**：HDD 上几万个小文件读取极慢，SSD 好很多但仍不如内存。

## 表格对比：HDD vs SSD

| 维度 | 机械硬盘 HDD | 固态硬盘 SSD |
| --- | --- | --- |
| 原理 | 磁盘+磁头（机械） | 闪存芯片（电子） |
| 顺序速度 | 100~200 MB/s | 500~7000 MB/s |
| 随机速度 | 极慢（~10ms/次） | 快（~0.1ms/次） |
| 容量 | 大（几 TB 起便宜） | 较小（贵） |
| 抗震 | 怕震动 | 抗震 |
| 噪音 | 有 | 无 |
| 寿命 | 长（机械磨损慢） | 闪存写入次数有限 |
| 适用 | 大容量存储、备份 | 系统盘、开发、数据库 |

## ⚠️ 常见坑

- **坑 1：把数据库放 HDD**。数据库是随机 IO 重灾区，HDD 上慢到用户超时。生产环境数据库必须 SSD。
- **坑 2：循环里频繁写小文件**。每写一次是一次 IO，循环 1 万次慢死。改成批量写或攒内存一次写。
- **坑 3：以为 SSD 无限快**。SSD 写入寿命有限，疯狂写日志会磨损。重要数据仍要备份。

## 小结

- HDD 像黑胶唱片（磁盘+磁头），有机械动作，慢但便宜大容量。
- SSD 像闪存卡（电子电路），无机械动作，快但贵有写入寿命。
- 顺序读写：连续大块；随机读写：东读西读。HDD 随机灾难性慢，SSD 影响小。
- 系统盘、数据库必须 SSD；冷数据备份可用 HDD。
- 编程要减少随机 IO，多用批量操作和内存缓存。

## 常见疑问 Q&A

**Q1：SSD 用久了会变慢吗？**
会。SSD 写入前要先擦除，用满后擦除累积会变慢（需 TRIM 优化）。留 10~20% 空闲能保持性能。

**Q2：为什么 U 盘比 SSD 慢？**
U 盘用的闪存芯片质量/接口不如 SSD，主控也简单。同样原理但用料和设计差很多，速度差几倍到几十倍。

**Q3：云服务器的"云硬盘"是什么？**
云厂商提供的网络存储，分 SSD 云盘、高效云盘、普通云盘。性能和价格递减，按需选。数据库用 SSD 云盘，冷备份用普通云盘。`,
    code: `# ===== 第 15 章：硬盘——长期记忆 =====
# 演示文件读写、顺序 vs 随机读写的差异

import os
import time

tmpfile = "/tmp/cs_disk_demo.txt"

print("=== 1. 写文件到硬盘（持久化）===")
with open(tmpfile, "w") as f:
    f.write("这是要永久保存的内容\\n第二行数据\\n")
print("文件大小:", os.path.getsize(tmpfile), "字节")
with open(tmpfile) as f:
    print("读回:", repr(f.read()))

print()
print("=== 2. 顺序写 vs 随机写 ===")
t0 = time.time()
with open(tmpfile, "w") as f:
    for i in range(50000):
        f.write("line %d\\n" % i)
seq_write = time.time() - t0

t0 = time.time()
for i in range(500):  # 随机写只做 500 次（频繁开关文件）
    with open(tmpfile, "a") as f:
        f.write("line %d\\n" % i)
rand_write = time.time() - t0
print("顺序写 50000 行: %.3f 秒" % seq_write)
print("随机写 500 行:   %.3f 秒（每行开关文件，开销大）" % rand_write)

print()
print("=== 3. 顺序读 vs 随机读 ===")
t0 = time.time()
with open(tmpfile) as f:
    for line in f:
        pass
seq_read = time.time() - t0

t0 = time.time()
with open(tmpfile) as f:
    for pos in range(0, 50000, 100):
        f.seek(pos)
        f.readline()
rand_read = time.time() - t0
print("顺序读 50000 行: %.4f 秒" % seq_read)
print("随机读 500 次:   %.4f 秒（跳跃访问未命中多）" % rand_read)

print()
print("=== 4. SSD vs HDD 速度对比 ===")
print("         顺序读         随机读")
print("HDD     100-200 MB/s    ~2 MB/s(慢)")
print("SSD     500-7000 MB/s   30-100 MB/s(快几十倍)")
print("-> 数据库是随机读写重灾区，必须用 SSD")

os.remove(tmpfile)
print("\\n演示完毕，已清理临时文件")
`,
  },

  // ============================================================
  // 第 16 章：缓存：为什么有 L1/L2/L3
  // ============================================================
  {
    id: "cs-16",
    group: "硬件组成",
    icon: "⚡",
    title: "缓存：为什么有 L1/L2/L3",
    content: `## 从书桌手边说起

想象你在写论文。手边（桌上右手位置）放着最近在翻的 3 本书，伸手就拿到；抽屉里放着这学期常用的十几本，要弯腰翻；书架上放着几百本，要起身走过去；图书馆里有几万本，要下楼走 5 分钟。

你不会把所有书都摊在桌上（放不下），也不会每查一个字都去图书馆（太慢）。聪明的做法：**把最常用的放手边，次常用的放抽屉，其他的放书架**。这就是缓存的思想。

电脑里：

- **L1 缓存 = 桌面手边**：超快但极小（几十 KB）
- **L2 缓存 = 抽屉**：较快较小（几百 KB）
- **L3 缓存 = 书架**：稍慢稍大（几 MB~几十 MB）
- **内存 = 图书馆**：慢但大（几 GB）
- **硬盘 = 隔壁城市的仓库**：极慢但巨大

CPU 找数据时，先查 L1，找不到查 L2，再找不到查 L3，最后才去内存。越靠前越快越小。

## 为什么有缓存：CPU 太快内存跟不上

CPU 主频 3 GHz，一个周期约 0.3 纳秒。但内存访问约 100 纳秒——**CPU 等一次内存要白白等 300 多个周期**！这就像厨师每做一道菜都要走 5 分钟去仓库取食材，效率全浪费在路上。

解决方案：在 CPU 和内存之间加几级"小而快"的缓存，把常用数据预先搬过来。CPU 先查缓存，命中就直接用（快），没命中再去内存（慢）。

缓存用 **SRAM**（静态随机存储器）做的，比内存的 DRAM 快但贵得多，所以做不大——这就是为什么 L1 只有几十 KB。

## L1/L2/L3：越近越小越快

| 缓存级 | 容量 | 速度 | 位置 | 共享 |
| --- | --- | --- | --- | --- |
| L1 | 32~64 KB | ~1 纳秒 | CPU 核心内 | 每核独享 |
| L2 | 256 KB~1 MB | ~3 纳秒 | CPU 核心内 | 每核独享 |
| L3 | 几 MB~几十 MB | ~10 纳秒 | CPU 内 | 多核共享 |
| 内存 | 几 GB | ~100 纳秒 | 主板 | 全 CPU 共享 |

层级设计是"速度和容量的折中"：L1 极快但太小装不下多少，L3 大些但慢些。CPU 找数据从 L1 一路查下去，命中就停。

## 缓存命中 vs 缓存未命中

- **缓存命中（hit）**：要的数据正好在缓存里 → 直接用，快
- **缓存未命中（miss）**：不在缓存 → 去下一级取，慢

**命中率** = 命中次数 / 总访问次数。L1 命中率通常 90% 以上，整体命中率 95%+。命中率差几个百分点，性能差几倍——这就是缓存的关键性。

## 局部性原理：缓存为什么有效

缓存能有 90%+ 命中率，靠的是程序的**局部性原理**：

**时间局部性**：刚用过的数据，很快还会再用。比如循环里反复读同一个变量 \`i\`，第一次从内存取来后放进缓存，后面几十次都命中。

**空间局部性**：用了某个地址，附近的地址也会被用到。比如遍历数组，读了 \`a[0]\` 后 \`a[1]\`、\`a[2]\` 大概率马上读。所以 CPU 从内存取数据时**不是只取 1 字节，而是取一整块（缓存行，通常 64 字节）**，把附近的数据一起搬进缓存，下次读邻居就命中了。

这就是缓存的魔力——利用局部性，让 90%+ 的访问都命中超快的缓存，整体性能接近缓存速度。

## 编程启发：缓存友好的代码

1. **数组比链表快**：数组内存连续，遍历时下一个元素已在缓存行里（空间局部性好）；链表节点散落各处，每次跳着读都未命中。同样遍历 N 个元素，数组常快几倍。
2. **按行遍历二维数组**：C/Java 里二维数组按行存储，按行遍历连续访问命中率高；按列遍历跳跃访问未命中多。Python 列表也类似。
3. **数据紧凑**：把常用字段放一起（结构体拆分），减少缓存行浪费。
4. **循环顺序**：嵌套循环把大循环放外层小循环放内层不一定对，要看内存访问顺序——让内层访问连续内存。
5. **别频繁 new 大对象**：每次分配新对象数据都不在缓存，尽量复用对象。

## 表格对比：缓存层级

| 层级 | 容量 | 延迟 | 命中率 |
| --- | --- | --- | --- |
| L1 | 几十 KB | ~1ns | ~50% |
| L2 | 几百 KB | ~3ns | ~30% |
| L3 | 几 MB | ~10ns | ~15% |
| 内存 | 几 GB | ~100ns | 未命中部分 |
| 总命中 | — | — | 95%+ |

## ⚠️ 常见坑

- **坑 1：以为算法复杂度低就一定快**。小数据量下，缓存友好的 O(n) 可能比缓存不友好的 O(n log n) 还快，因为常数因子差很大。
- **坑 2：随机访问大数组**。跳跃访问破坏空间局部性，缓存狂 miss，看似 O(n) 实际慢很多。
- **坑 3：过度优化**。缓存优化要基于实测，别凭感觉改。先 profile 找瓶颈再优化。

## 小结

- 缓存是因为 CPU 太快内存跟不上，在中间加小而快的 SRAM 缓存。
- L1（几十 KB，~1ns）→ L2（几百 KB，~3ns）→ L3（几 MB，~10ns）→ 内存（几 GB，~100ns）。
- 命中就快，未命中就慢；命中率 95%+ 靠局部性原理。
- 时间局部性：刚用的还会用；空间局部性：附近的会被用（缓存行预取）。
- 缓存友好代码：用数组不用链表、按行遍历、数据紧凑。

## 常见疑问 Q&A

**Q1：为什么不能把 L1 做大点？**
L1 用 SRAM，6 个晶体管存 1 位，又贵又占地方。做大了延迟也会增加（信号传得远）。所以 L1 只能小而快，靠 L2/L3 补容量。

**Q2：Python 程序员需要关心缓存吗？**
Python 解释器本身的开销远大于缓存效应，普通脚本看不出差别。但写高性能计算（NumPy、Cython）时，缓存友好性影响很大。NumPy 快的原因之一就是连续内存布局缓存友好。

**Q3：CPU 缓存和浏览器缓存、CDN 缓存是一回事吗？**
原理一样（用小快存储暂存常用数据），但层次不同。CPU 缓存是硬件级（纳秒），浏览器/CDN 缓存是应用级（毫秒~秒）。思想都是"利用局部性，用快存储扛常用请求"。`,
    code: `# ===== 第 16 章：缓存——为什么有 L1/L2/L3 =====
# 演示缓存层级、缓存友好 vs 不友好的访问速度差异

import time

print("=== 1. 缓存层级：越近越小越快 ===")
for lv, size, speed, loc in [("L1","几十KB","~1纳秒","CPU核心内,每核独享"), ("L2","几百KB","~3纳秒","CPU核心内,每核独享"), ("L3","几MB","~10纳秒","CPU内,多核共享"), ("内存","几GB","~100纳秒","主板")]:
    print("  %-4s %-8s %-10s %s" % (lv, size, speed, loc))

print()
print("=== 2. 缓存友好 vs 不友好：按行 vs 按列遍历 ===")
N = 1500
matrix = [[0] * N for _ in range(N)]

t0 = time.time(); total = 0
for i in range(N):
    for j in range(N):
        total += matrix[i][j]
row_time = time.time() - t0

t0 = time.time(); total = 0
for j in range(N):
    for i in range(N):
        total += matrix[i][j]
col_time = time.time() - t0
print("按行遍历 %dx%d: %.3f 秒（缓存友好）" % (N, N, row_time))
print("按列遍历 %dx%d: %.3f 秒（缓存不友好）" % (N, N, col_time))
print("按列/按行 = %.1f 倍（列遍历未命中多）" % (col_time / row_time if row_time > 0 else 0))

print()
print("=== 3. 局部性原理 ===")
print("时间局部性: 刚用过的数据很快还会再用（如循环变量）")
print("空间局部性: 用了某地址附近的也会被用到（缓存行预取）")

print()
print("=== 4. 模拟缓存命中效果 ===")
cache = {}
def access(addr, src):
    if addr in cache:
        return True
    cache[addr] = src[addr]
    return False

data_source = {i: "data%d" % i for i in range(100)}
hit = miss = 0
for i in range(1000):
    if access(i % 50, data_source):  # 只访问前 50 个，反复命中
        hit += 1
    else:
        miss += 1
print("访问 1000 次: 命中 %d, 未命中 %d, 命中率 %.1f%%" % (hit, miss, hit / 1000 * 100))

print()
print("=== 5. 为什么数组比链表快 ===")
print("数组: 内存连续 -> 空间局部性好 -> 缓存命中率高")
print("链表: 节点散落 -> 跳跃访问 -> 缓存未命中多 -> 同样遍历慢几倍")
`,
  },
];
