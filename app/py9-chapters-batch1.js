// =============================================================
// Python 逐层深入教程 - batch1
// 章节 1-10：起步认识 Python + 数据基础（数字/字符串/布尔/列表）
// =============================================================

export const chapters = [
  // -----------------------------------------------------------
  // 第 1 章：编程与 Python
  // -----------------------------------------------------------
  {
    id: "py9-01",
    group: "起步：认识 Python",
    icon: "🐍",
    title: "编程与 Python：计算机到底在听什么？",
    content: `## 先回答一个最根本的问题

你每天都在用手机、浏览网页、玩游戏。这些软件本质上是**一连串指令**，告诉计算机"该做什么"。写这些指令，就是**编程**。

问题来了：计算机本身只听得懂 0 和 1（高低电平）。让人直接写 0 和 1 太反人类了，于是人们发明了**编程语言**——一种人类能读懂、又能被翻译成 0 和 1 的"中间语言"。

\`\`\`
你写的 Python 代码  →  翻译器（解释器）  →  计算机能执行的 0/1
\`\`\`

## Python 是什么

Python 是一门**高级、解释型**编程语言。"高级"不是夸它厉害，而是说它离人类语言近、离机器细节远——你不用管内存地址、寄存器这些。"解释型"意思是它**逐行翻译逐行执行**，不像 C 那样先整体编译成可执行文件。

### 为什么是 Python

- **语法接近英语**：\`print("hi")\` 就是"打印 hi"，几乎不用解释
- **能做的事多**：网站、数据分析、AI、自动化脚本、爬虫……
- **生态丰富**：别人写好的轮子（库）你直接拿来用
- **入门友好**：不用先学一堆底层概念就能写出有用的程序

## 这门课怎么学

最好的学法是**边读边跑代码**。每一章你都点"运行代码"，看看输出，再改几个数字、改几行，观察变化。**改错了不会坏电脑**，放心折腾。

下面是我们这门课的第一段代码。它做的事很简单：让计算机在屏幕上输出一句话。但别小看它——你已经让计算机听话了。

### 一个小约定

Python 用 \`#\` 号写注释，注释是给人看的，计算机会忽略。后面你会看到大量注释，那是"这行代码在干嘛"的解释。`,
    code: `# ============================================
# 第 1 章：第一段代码
# 目标：让计算机在屏幕上输出文字
# ============================================

# print() 是一个"函数"：你给它内容，它把内容显示到屏幕上
# 引号里的内容叫"字符串"，就是一串文字
print("你好，Python！")

# 可以一次打印多行，用三个引号
print("""
这是第二行
这是第三行
看出来了吗？三引号可以包住多行文字
""")

# 数字也能打印，不用引号
print(42)
print(3.14)

# 还能做计算，print 会先算出结果再显示
print(1 + 2)        # 加法
print(10 * 10)      # 乘法

# 改一改：把上面的文字换成你想说的话，再运行试试
print("我正在学 Python，感觉还不错")`
  },

  // -----------------------------------------------------------
  // 第 2 章：运行方式
  // -----------------------------------------------------------
  {
    id: "py9-02",
    group: "起步：认识 Python",
    icon: "▶️",
    title: "Python 的三种运行方式",
    content: `## 代码写在哪、怎么跑

学 Python 你会遇到三种"跑代码"的方式，先认识它们，免得后面糊涂。

### 方式一：交互式（>>> 提示符）

在终端输入 \`python3\`，会进入一个 \`>>>\` 提示符的环境。你输一行，它立刻执行一行，回车就出结果。适合"试试这行代码对不对"。

\`\`\`
>>> 1 + 1
2
>>> print("hi")
hi
\`\`\`

退出输入 \`exit()\` 或按 Ctrl+D。

### 方式二：脚本文件（.py）

把代码写进一个文件，比如 \`hello.py\`，然后命令行运行 \`python3 hello.py\`。这是**正式开发**用的方式，代码能保存、能复用、能发给别人。

### 方式三：在线 / IDE 运行（你现在用的）

像这个教程页面，你在浏览器里写代码、点按钮运行。它背后其实也是调用了系统的 \`python3\`，只是帮你包装好了界面。**Trae、VS Code、Jupyter** 都属于这类。

## 解释器到底在干嘛

你写 \`print("hi")\`，解释器做三件事：
1. **读**：把这一行读进来
2. **理解**：识别出 \`print\` 是个函数，\`"hi"\` 是个字符串
3. **执行**：调用 print，把 "hi" 送出去

如果某行有它不认识的写法，就会报 **SyntaxError（语法错误）**，并指出大致在第几行。这是后面你最常遇到的错误之一，别怕，看报错信息修就行。

## 本章 demo

这个 demo 演示"代码是按从上到下的顺序执行的"，并模拟一段简单交互。注意：在线环境没有键盘输入，所以我们不用 \`input()\`，而是用变量模拟用户输入（变量下一章细讲，这里先看效果）。`,
    code: `# ============================================
# 第 2 章：代码的执行顺序
# ============================================

# Python 代码默认从上到下，一行一行执行
print("第一步：我开始执行了")
print("第二步：这一行在第一行下面，所以后执行")
print("第三步：以此类推")

# 空行不影响执行，只是给人看的
print("第四步：空行上面那行不影响我")

# 用分隔线让输出更清楚
print("-" * 30)

# 模拟一个"计算器"流程
# 假装用户输入了两个数（真实场景会用 input()，这里用固定值模拟）
a = 15
b = 4

print("我们假装用户输入了：a =", a, "，b =", b)
print("a + b =", a + b)
print("a - b =", a - b)
print("a * b =", a * b)
print("a / b =", a / b)

# 故意写一个会出错的例子（注释掉了）
# print("语法错误演示"   # 少了右括号，取消注释会报 SyntaxError
# 报错时解释器会告诉你大概在哪一行，照着改就行

print("-" * 30)
print("程序结束")`
  },

  // -----------------------------------------------------------
  // 第 3 章：print 详解
  // -----------------------------------------------------------
  {
    id: "py9-03",
    group: "起步：认识 Python",
    icon: "📤",
    title: "print —— 让计算机开口说话",
    content: `## print 比你想的能干

\`print\` 是你接触的第一个函数。它把"东西"变成文字显示出来。但它的细节值得单独讲一章，因为调试程序时你 90% 时间在和 print 打交道。

### 基本用法

\`\`\`python
print("hello")        # 打印字符串
print(123)            # 打印数字
print(1 + 2)          # 打印表达式的结果
\`\`\`

### 打印多个东西：用逗号

\`\`\`python
print("我今年", 18, "岁")
# 输出：我今年 18 岁
\`\`\`

逗号之间默认用**空格**分隔。这个分隔符可以改，用 \`sep\` 参数：

\`\`\`python
print("2025", "07", "01", sep="-")   # 2025-07-01
\`\`\`

### 结尾换行：end 参数

print 默认在结尾加一个**换行符** \`\\n\`，所以两次 print 各占一行。用 \`end\` 参数可以改：

\`\`\`python
print("加载中", end="")
print("...完成")     # 会在同一行
\`\`\`

### 转义字符

字符串里有些字符不能直接写，比如"换行"。Python 用反斜杠 \`\\\`\` 开头的转义字符表示：

| 写法 | 含义 |
|------|------|
| \`\\n\` | 换行 |
| \`\\t\` | 制表符（对齐用） |
| \\\\ | 反斜杠本身 |
| \\\' | 单引号（在单引号字符串里用） |
| \\" | 双引号（在双引号字符串里用） |

### f-string（强烈推荐）

想在字符串里嵌入变量的值，用 f-string 最方便——字符串前加 \`f\`，用 \`{}\` 包变量：

\`\`\`python
name = "小明"
age = 18
print(f"我叫{name}，今年{age}岁")
\`\`\`

这比老式的 \`% \` 或 \`.format()\` 直观得多，本教程统一用 f-string。

### 想看变量的"类型"

\`type()\` 告诉你一个东西是什么类型，调试很有用：

\`\`\`python
print(type(42))        # <class 'int'>
print(type("hi"))      # <class 'str'>
\`\`\`

## 本章 demo

下面 demo 把 print 的各种用法一次演示，对比着看效果最清楚。`,
    code: `# ============================================
# 第 3 章：print 的各种用法
# ============================================

# --- 1. 基本打印 ---
print("=== 1. 基本打印 ===")
print("你好")
print(42)
print(3.14)
print(2 * 8)              # 先算再打印

# --- 2. 多个参数，默认空格分隔 ---
print("\\n=== 2. 多个参数 ===")
print("语文", 90, "数学", 85, "英语", 95)
# 用 sep 改分隔符
print("2025", "07", "01", sep="-")
print("a", "b", "c", sep=" | ")

# --- 3. end 参数控制结尾 ---
print("\\n=== 3. end 参数 ===")
print("加载中", end="")
print("...", end="")
print("完成")              # 三句会在同一行

# --- 4. 转义字符 ---
print("\\n=== 4. 转义字符 ===")
print("第一行\\n第二行")   # \\n 换行
print("姓名\\t年龄\\t分数")  # \\t 对齐
print("文件路径：C:\\\\Users\\\\test")  # \\\\ 表示一个反斜杠
print('it\\'s ok')         # 单引号字符串里的单引号要转义

# --- 5. f-string 嵌入变量 ---
print("\\n=== 5. f-string ===")
name = "小明"
age = 18
height = 1.75
print(f"我叫{name}，今年{age}岁，身高{height}米")
# f-string 里还能放表达式
print(f"明年我{age + 1}岁")
print(f"{name}的体重是{70}公斤，BMI={70 / height**2:.2f}")  # :.2f 保留两位小数

# --- 6. type() 看类型 ---
print("\\n=== 6. type() 看类型 ===")
print("42 的类型是", type(42))
print("3.14 的类型是", type(3.14))
print("\\"hi\\" 的类型是", type("hi"))
print("True 的类型是", type(True))`
  },

  // -----------------------------------------------------------
  // 第 4 章：注释、缩进、变量
  // -----------------------------------------------------------
  {
    id: "py9-04",
    group: "起步：认识 Python",
    icon: "📝",
    title: "注释、缩进与变量：给数据起名字",
    content: `## 注释：写给未来的自己

注释是代码里"给人看、机器忽略"的部分。Python 用 \`#\`：

\`\`\`python
# 这是整行注释
age = 18   # 这是行尾注释
\`\`\`

什么时候写注释？**解释"为什么这么做"，而不是"这行在干嘛"**。如果代码本身能看懂在干嘛，就不用注释；如果是绕了个弯、有特殊原因，才写注释说明。

## 缩进：Python 的灵魂

大多数语言用 \`{}\` 表示代码块，Python 用**缩进**（行首的空格）。**4 个空格**是约定俗成的标准。

\`\`\`python
if True:
    print("这行属于 if")     # 缩进了 4 空格
    print("这行也属于 if")
print("这行不属于 if")        # 没缩进，是外面的
\`\`\`

缩进错了，Python 会报 \`IndentationError\`。**同一块代码的缩进必须一致**，不能一行 4 空格、一行 2 空格混着来。

> 在线编辑器里按 Tab 会自动输入 4 个空格，放心用。

## 变量：给数据贴标签

变量是**名字**，你用它记住一个值，以后能用名字引用它。

\`\`\`python
age = 18
name = "小明"
\`\`\`

### 关键理解：变量是"标签"不是"盒子"

很多人把变量想象成"装东西的盒子"。更准确的比喻是**贴标签**：\`age = 18\` 是把一个写着 \`age\` 的标签贴到了数字 18 上。后面 \`age = 20\` 是把标签撕下来贴到 20 上。

这个理解在后面学"可变对象"（列表、字典）时很重要，先记住。

### 赋值的几种花样

\`\`\`python
# 同时给多个变量赋值
a, b, c = 1, 2, 3

# 交换两个变量（Python 特色，不用临时变量）
a, b = b, a

# 多个变量同一个值
x = y = z = 0
\`\`\`

### 变量命名规则

- 只能用**字母、数字、下划线**，且**不能数字开头**
- 区分大小写：\`age\` 和 \`Age\` 是两个变量
- 不能用关键字（\`if\`、\`for\`、\`def\` 等被 Python 占用了）
- 推荐用**小写+下划线**：\`user_name\`、\`total_score\`，别用拼音或中文

## 本章 demo

下面 demo 演示注释、缩进、变量的各种用法，并展示一个"变量是标签"的小证据。`,
    code: `# ============================================
# 第 4 章：注释、缩进、变量
# ============================================

# --- 1. 变量基本用法 ---
# 把值"赋给"变量，用等号 =
name = "小明"          # 字符串
age = 18               # 整数
height = 1.75          # 浮点数
is_student = True      # 布尔值

print(f"姓名: {name}")
print(f"年龄: {age}")
print(f"身高: {height} 米")

# --- 2. 变量可以重新赋值 ---
print("\\n--- 重新赋值 ---")
score = 80
print(f"第一次: {score}")
score = 95             # 标签撕下来，贴到 95
print(f"第二次: {score}")
score = score + 5      # 先算右边 score+5=100，再赋给 score
print(f"第三次: {score}")

# --- 3. 同时给多个变量赋值 ---
print("\\n--- 多变量赋值 ---")
a, b, c = 1, 2, 3
print(f"a={a}, b={b}, c={c}")

# 交换两个变量（不用临时变量！）
a, b = b, a
print(f"交换后: a={a}, b={b}")

# 多个变量同一个值
x = y = z = 0
print(f"x={x}, y={y}, z={z}")

# --- 4. 缩进演示（用 if 暂时展示，下章细讲）---
print("\\n--- 缩进演示 ---")
if age >= 18:
    print(f"{name} 已成年")      # 这两行缩进了 4 空格，属于 if
    print(f"可以办身份证")
print("这行没缩进，无论如何都会执行")  # 在 if 外面

# --- 5. 变量是"标签"的小证据 ---
print("\\n--- 变量是标签 ---")
# 把 list 赋给 a，再把 a 赋给 b
# 后面讲列表会细说，这里先看效果：改 a，b 也变了
a_list = [1, 2, 3]
b_list = a_list           # b_list 和 a_list 贴的是同一个东西
a_list.append(4)          # 往 a_list 里加个 4
print(f"a_list = {a_list}")
print(f"b_list = {b_list}")  # b_list 也变了！因为它们是同一份
print("这说明变量是'标签'，a_list 和 b_list 贴在同一份数据上")`
  },

  // -----------------------------------------------------------
  // 第 5 章：输入与类型转换
  // -----------------------------------------------------------
  {
    id: "py9-05",
    group: "起步：认识 Python",
    icon: "⌨️",
    title: "输入与类型转换",
    content: `## 程序要和外界打交道

前面我们的数据都是写死在代码里的。真实程序经常需要**从用户那里拿数据**——比如让用户输入名字、年龄。

### input()：读用户输入

\`input("提示文字")\` 会显示提示，等用户敲一行字按回车，把那行字作为字符串返回：

\`\`\`python
name = input("你叫什么？")
print("你好，" + name)
\`\`\`

⚠️ **重要**：\`input()\` 拿到的**永远是字符串**，哪怕用户输入的是 \`18\`。这点坑了无数新手。

\`\`\`python
age = input("年龄？")   # 用户输入 18
print(age + 1)          # 报错！age 是 "18" 字符串，不能加数字
\`\`\`

### 类型转换

要把"数字样子的字符串"变成真正的数字，用转换函数：

| 函数 | 作用 | 例子 |
|------|------|------|
| \`int()\` | 转整数 | \`int("18")\` → 18 |
| \`float()\` | 转浮点 | \`float("3.14")\` → 3.14 |
| \`str()\` | 转字符串 | \`str(18)\` → "18" |

\`\`\`python
age = int(input("年龄？"))   # 输入后立刻转成 int
print(age + 1)               # 现在能算了
\`\`\`

### 转换可能失败

如果用户乱输 \`int("abc")\` 会报 \`ValueError\`。真实程序要用 try/except 兜底（异常那章讲），现在先假设用户规规矩矩。

## 在线环境的小限制

在线运行没有键盘交互，\`input()\` 会卡住等输入。所以本教程的 demo **不用 input()**，而是模拟"已经拿到的输入"。但你要知道真实程序里怎么用。

## 本章 demo

demo 模拟一个"注册流程"：拿到字符串形式的输入，做类型转换，再处理。`,
    code: `# ============================================
# 第 5 章：类型转换
# 在线环境无键盘输入，用变量模拟 input() 的返回值
# ============================================

# 模拟用户在 input() 里输入的字符串
# 真实代码是：name = input("请输入姓名：")
name_input = "小明"
age_input = "18"        # 注意：这是字符串"18"，不是数字18
height_input = "1.75"

print(f"模拟输入：姓名={name_input}, 年龄={age_input}, 身高={height_input}")
print(f"它们的类型：{type(name_input)}, {type(age_input)}, {type(height_input)}")
print("注意：input() 拿到的全是字符串，哪怕长得像数字\\n")

# --- 类型转换 ---
age = int(age_input)          # "18" → 18（整数）
height = float(height_input)  # "1.75" → 1.75（浮点）
print(f"转换后：age={age} 类型={type(age)}")
print(f"转换后：height={height} 类型={type(height)}")

# 现在能做数字运算了
print(f"\\n明年 {age + 1} 岁")
print(f"BMI 示例：{70 / height**2:.2f}")

# --- 数字转字符串：拼接时常用 ---
print("\\n--- 字符串拼接 ---")
score = 95
# 错误写法（注释掉）：print("成绩" + score)  # 字符串不能直接加数字
print("成绩" + str(score) + "分")   # 用 str() 转一下
print(f"成绩{score}分")             # f-string 自动处理，更方便

# --- 转换失败的情况 ---
print("\\n--- 转换失败演示 ---")
bad = "abc"
try:
    int(bad)
except ValueError as e:
    print(f"int(\\"{bad}\\") 失败：{e}")
    print("真实程序要处理这种'用户乱输'的情况（异常那章讲）")

# --- 综合小练习：模拟注册 ---
print("\\n--- 模拟注册 ---")
username = "xiaoming"
password_input = "abc12345"               # 假装用户输入的密码字符串
password_len = len(password_input)        # 用 len() 数出长度
print(f"用户名: {username}")
print(f"密码长度: {password_len} 位")
if password_len >= 6:
    print("密码长度合格 ✓")
else:
    print("密码太短，至少6位")`
  },

  // -----------------------------------------------------------
  // 第 6 章：数字与运算符
  // -----------------------------------------------------------
  {
    id: "py9-06",
    group: "数据：Python 里的东西",
    icon: "🔢",
    title: "数字与运算符：从算术说起",
    content: `## Python 里的数字有两种

- **整数（int）**：\`1\`、\`-5\`、\`0\`、\`999999\`。Python 的整数**没有大小上限**，能表示天文数字。
- **浮点数（float）**：带小数点的，\`3.14\`、\`-0.5\`、\`1.0\`。

还有复数 \`complex\`（\`3+4j\`），日常开发几乎用不到，知道有就行。

## 算术运算符

| 运算符 | 含义 | 例子 | 结果 |
|--------|------|------|------|
| \`+\` | 加 | \`5 + 3\` | 8 |
| \`-\` | 减 | \`5 - 3\` | 2 |
| \`*\` | 乘 | \`5 * 3\` | 15 |
| \`/\` | 除 | \`7 / 2\` | 3.5（总是浮点） |
| \`//\` | 整除 | \`7 // 2\` | 3（向下取整） |
| \`%\` | 取余 | \`7 % 2\` | 1 |
| \`**\` | 幂 | \`2 ** 3\` | 8 |

### 几个容易混的

**\`/\` 永远返回浮点**：\`6 / 2\` 是 \`3.0\` 不是 \`3\`。

**\`//\` 是向下取整**，对负数要注意：\`-7 // 2\` 是 \`-4\`（不是 -3，因为 -4 比 -3.5 更小）。

**\`%\` 的符号跟除数一致**：\`-7 % 2\` 是 \`1\`（Python 特性，和 C 不同）。

## 运算优先级

和数学一样：括号 > 幂 > 乘除 > 加减。不确定就**加括号**，永远没错。

## 复合赋值运算符

\`\`\`python
x = 10
x += 5    # 等价 x = x + 5
x -= 3    # x = x - 3
x *= 2    # x = x * 2
x //= 4   # x = x // 4
\`\`\`

## 浮点数精度问题

\`0.1 + 0.2\` 在 Python 里**不等于 0.3**，而是 \`0.30000000000000004\`。这不是 bug，是计算机用二进制存小数的固有限制。涉及钱要用 \`decimal\` 模块。

## 本章 demo

demo 把上面所有运算都跑一遍，特别演示"坑点"：整除负数、取余符号、浮点精度。`,
    code: `# ============================================
# 第 6 章：数字与运算符
# ============================================

# --- 1. 基本算术 ---
print("=== 1. 基本算术 ===")
print(f"5 + 3 = {5 + 3}")
print(f"5 - 3 = {5 - 3}")
print(f"5 * 3 = {5 * 3}")
print(f"7 / 2 = {7 / 2}     ← / 总是返回浮点")
print(f"6 / 2 = {6 / 2}     ← 即使整除也是浮点")
print(f"7 // 2 = {7 // 2}    ← // 整除，去掉小数")
print(f"7 % 2 = {7 % 2}      ← % 取余数")
print(f"2 ** 10 = {2 ** 10}  ← ** 幂运算，2的10次方")

# --- 2. 整除和取余的坑（负数）---
print("\\n=== 2. 负数的整除和取余 ===")
print(f"7 // 2 = {7 // 2}      （正常）")
print(f"-7 // 2 = {-7 // 2}    （向下取整，-4 而不是 -3！）")
print(f"7 % 2 = {7 % 2}")
print(f"-7 % 2 = {-7 % 2}      （Python 取余符号跟除数，结果是 1）")
print("记住：// 是'向下取整'不是'去掉小数'，负数时差一个")

# --- 3. 运算优先级 ---
print("\\n=== 3. 优先级 ===")
print(f"2 + 3 * 4 = {2 + 3 * 4}        ← 先乘除后加减，=14")
print(f"(2 + 3) * 4 = {(2 + 3) * 4}      ← 括号优先，=20")
print(f"2 + 3 ** 2 = {2 + 3 ** 2}      ← 幂高于加减，=11")
print(f"2 ** 3 ** 2 = {2 ** 3 ** 2}    ← 幂从右往左，=2的9次方=512")

# --- 4. 复合赋值 ---
print("\\n=== 4. 复合赋值 ===")
x = 100
x += 10;  print(f"x += 10 → {x}")
x -= 20;  print(f"x -= 20 → {x}")
x *= 3;   print(f"x *= 3 → {x}")
x //= 5;  print(f"x //= 5 → {x}")
x %= 7;   print(f"x %= 7 → {x}")
x **= 2;  print(f"x **= 2 → {x}")

# --- 5. 浮点精度问题 ---
print("\\n=== 5. 浮点精度 ===")
print(f"0.1 + 0.2 = {0.1 + 0.2}")
print(f"0.1 + 0.2 == 0.3? {0.1 + 0.2 == 0.3}    ← 竟然不等！")
# 解决办法：用 round 或 decimal
print(f"round(0.1+0.2, 2) = {round(0.1 + 0.2, 2)}    ← 用 round 处理")

# --- 6. 实用技巧 ---
print("\\n=== 6. 实用技巧 ===")
# 判断奇偶
n = 17
print(f"{n} 是{'偶数' if n % 2 == 0 else '奇数'}")
# 位数分离：取百位十位个位
num = 456
print(f"{num} 的百位={num // 100}, 十位={num // 10 % 10}, 个位={num % 10}")
# 秒转时分秒
total = 3666
print(f"{total}秒 = {total // 3600}时 {(total % 3600) // 60}分 {total % 60}秒")
# 大整数也没问题
big = 10 ** 50
print(f"10的50次方 = {big}")`
  },

  // -----------------------------------------------------------
  // 第 7 章：字符串基础
  // -----------------------------------------------------------
  {
    id: "py9-07",
    group: "数据：Python 里的东西",
    icon: "🔤",
    title: "字符串（一）：创建、拼接、索引、切片",
    content: `## 字符串是什么

字符串就是**一串文字**，用引号包起来。Python 单引号 \`'\` 和双引号 \`"\` 完全等价，习惯用哪个都行。三引号 \`"""\` 能包多行。

\`\`\`python
s1 = 'hello'
s2 = "hello"
s3 = """这是
多行"""
\`\`\`

## 字符串不可变

这是重要概念：**字符串一旦创建，里面的字符不能改**。

\`\`\`python
s = "hello"
s[0] = "H"   # 报错！TypeError
\`\`\`

想"改"只能造一个新的：\`s = "H" + s[1:]\`。

## 拼接和重复

\`\`\`python
"ab" + "cd"      # "abcd" 拼接
"ha" * 3         # "hahaha" 重复
\`\`\`

## 索引：取一个字符

字符串里每个字符有个**编号**，从 0 开始。**支持负数**，-1 是最后一个。

\`\`\`python
s = "Python"
s[0]    # 'P'   第1个
s[1]    # 'y'
s[-1]   # 'n'   最后1个
s[-2]   # 'o'   倒数第2
\`\`\`

记忆：\`s[0]\` 是开头，\`s[-1]\` 是结尾，负号表示"从后往前数"。

## 切片：取一段

\`s[起:止]\` 取从"起"到"止-1"的子串（**包含起，不包含止**）：

\`\`\`python
s = "Python"
s[0:3]    # 'Pyt'    第0,1,2个
s[2:5]    # 'tho'
s[:3]     # 'Pyt'    省略起=从头
s[3:]     # 'hon'    省略止=到尾
s[:]      # 'Python' 全部（复制一份）
s[-3:]    # 'hon'    最后3个
\`\`\`

切片还有第三个参数**步长**：\`s[起:止:步长]\`

\`\`\`python
s[::2]    # 'Pto'    隔一个取一个
s[::-1]   # 'nohtyP' 反转字符串！
\`\`\`

## len() 看长度

\`\`\`python
len("hello")    # 5
\`\`\`

## 本章 demo

demo 把索引、切片、拼接、反转都跑一遍，重点是切片的各种写法。`,
    code: `# ============================================
# 第 7 章：字符串基础
# ============================================

s = "Python"

# --- 1. 创建 ---
print("=== 1. 创建 ===")
a = '单引号'
b = "双引号"
c = """三引号
可以多行"""
print(a, b, c, sep=" | ")

# --- 2. 拼接和重复 ---
print("\\n=== 2. 拼接和重复 ===")
print("hello" + " " + "world")     # 拼接
print("ha" * 3)                     # 重复
print("-" * 30)                     # 画分隔线常用

# --- 3. 索引 ---
print("\\n=== 3. 索引 ===")
print(f"s = '{s}'")
print(f"s[0]  = '{s[0]}'    ← 第1个")
print(f"s[1]  = '{s[1]}'")
print(f"s[-1] = '{s[-1]}'   ← 最后1个")
print(f"s[-2] = '{s[-2]}'   ← 倒数第2")

# --- 4. 切片（重点！）---
print("\\n=== 4. 切片 ===")
print(f"s[0:3]  = '{s[0:3]}'    ← 取0,1,2，不含3")
print(f"s[2:5]  = '{s[2:5]}'")
print(f"s[:3]   = '{s[:3]}'    ← 省起=从头")
print(f"s[3:]   = '{s[3:]}'    ← 省止=到尾")
print(f"s[-3:]  = '{s[-3:]}'   ← 最后3个字符")

# --- 5. 步长 ---
print("\\n=== 5. 步长 ===")
print(f"s[::2]   = '{s[::2]}'    ← 隔一个取一个")
print(f"s[::-1]  = '{s[::-1]}'   ← 反转！常用技巧")
print(f"s[1::2]  = '{s[1::2]}'   ← 从第1个开始，隔一取")

# --- 6. len 和遍历 ---
print("\\n=== 6. 长度和遍历 ===")
print(f"len(s) = {len(s)}")
for i, ch in enumerate(s):       # enumerate 同时给下标和字符
    print(f"  第{i}个: {ch}")

# --- 7. 字符串不可变 ---
print("\\n=== 7. 不可变 ===")
# s[0] = "H"  # 这行会报错
# 想"改"要造新的
new_s = "H" + s[1:]
print(f"'改'首字母: '{s}' → '{new_s}'")

# --- 8. 实用例子 ---
print("\\n=== 8. 实用例子 ===")
phone = "13800138000"
print(f"手机号脱敏: {phone[:3]}****{phone[-4:]}")
filename = "report_2025_07.csv"
print(f"取后缀: {filename[-4:]}")
print(f"反转: {''.join(reversed(s))}")`
  },

  // -----------------------------------------------------------
  // 第 8 章：字符串方法与格式化
  // -----------------------------------------------------------
  {
    id: "py9-08",
    group: "数据：Python 里的东西",
    icon: "🛠️",
    title: "字符串（二）：方法与格式化",
    content: `## 什么是"方法"

字符串自带很多"动作"，叫**方法**，用 \`.\` 调用：

\`\`\`python
"hello".upper()    # 'HELLO'  把字符串转大写
\`\`\`

方法**不会改原字符串**（字符串不可变），而是返回一个新的。

## 常用方法

### 大小写
\`\`\`python
s.upper()      # 全大写
s.lower()      # 全小写
s.title()      # 每个单词首字母大写
s.capitalize() # 首字母大写
s.swapcase()   # 大小写互换
\`\`\`

### 查找
\`\`\`python
s.find("xx")   # 找子串，返回下标，找不到返回 -1
s.index("xx")  # 同上，找不到抛异常
s.count("xx")  # 出现几次
"xx" in s      # 是否包含，返回布尔
\`\`\`

### 替换和分割
\`\`\`python
s.replace("a", "b")   # 把 a 替换成 b
s.split(",")          # 按逗号切成列表
s.split()             # 按空白切（默认）
",".join(list)        # 用逗号把列表拼回字符串
\`\`\`

### 去空白
\`\`\`python
s.strip()    # 去首尾空白
s.lstrip()   # 去左
s.rstrip()   # 去右
\`\`\`

### 判断
\`\`\`python
s.isdigit()   # 是不是全数字
s.isalpha()   # 是不是全字母
s.startswith("xx")  # 以xx开头
s.endswith(".py")   # 以.py结尾
\`\`\`

## 格式化：三种方式

### 1. f-string（推荐）
\`\`\`python
name = "小明"; score = 95.678
print(f"{name}考了{score:.1f}分")   # 小明考了95.7分
\`\`\`

格式控制：\`{值:格式}\`
- \`:.2f\` 保留2位小数
- \`:>10\` 右对齐宽10
- \`:<10\` 左对齐
- \`:0>5\` 用0填充到宽5
- \`,.2f\` 千分位+2位小数

### 2. format 方法（老式）
\`\`\`python
"{}考了{}分".format(name, score)
\`\`\`

### 3. % 占位符（更老）
\`\`\`python
"%s考了%.1f分" % (name, score)
\`\`\`

新代码统一用 f-string，遇到老代码能看懂后两种即可。

## 本章 demo

demo 覆盖上面所有方法，每个都打印对比。`,
    code: `# ============================================
# 第 8 章：字符串方法与格式化
# ============================================

s = "  Hello, World  "

# --- 1. 大小写 ---
print("=== 1. 大小写 ===")
print(f"原: '{s}'")
print(f"upper:       '{s.upper()}'")
print(f"lower:       '{s.lower()}'")
print(f"title:       '{s.title()}'")
print(f"capitalize:  '{s.capitalize()}'")

# --- 2. 去空白 ---
print("\\n=== 2. 去空白 ===")
print(f"strip:  '{s.strip()}'    ← 首尾空白都去掉")
print(f"lstrip: '{s.lstrip()}'")
print(f"rstrip: '{s.rstrip()}'")

# --- 3. 查找 ---
print("\\n=== 3. 查找 ===")
t = s.strip()
print(f"'{t}'.find('World') = {t.find('World')}    ← 找到返回下标")
print(f"'{t}'.find('xxx')   = {t.find('xxx')}      ← 找不到返回-1")
print(f"'{t}'.count('l')    = {t.count('l')}        ← l出现几次")
print(f"'World' in '{t}'    = {'World' in t}")

# --- 4. 替换 ---
print("\\n=== 4. 替换 ===")
print(f"'{t}'.replace('World', 'Python') = '{t.replace('World', 'Python')}'")
# 替换还能指定次数
print(f"'aaa'.replace('a', 'b', 2) = '{'aaa'.replace('a', 'b', 2)}'")

# --- 5. 分割和拼接 ---
print("\\n=== 5. 分割和拼接 ===")
csv = "张三,25,北京,程序员"
parts = csv.split(",")
print(f"'{csv}'.split(',') = {parts}")
# join 把列表拼回字符串
print(f"' | '.join(parts) = {' | '.join(parts)}")

# 默认 split 按任意空白切，多个空格也算一个
print(f"'a  b   c'.split() = {'a  b   c'.split()}")

# --- 6. 判断 ---
print("\\n=== 6. 判断 ===")
print(f"'123'.isdigit()       = {'123'.isdigit()}")
print(f"'abc'.isalpha()       = {'abc'.isalpha()}")
print(f"'abc123'.isalnum()    = {'abc123'.isalnum()}")
print(f"'test.py'.endswith('.py') = {'test.py'.endswith('.py')}")
print(f"'Hello'.startswith('He')  = {'Hello'.startswith('He')}")

# --- 7. 格式化 ---
print("\\n=== 7. 格式化 ===")
name = "小明"
score = 95.678
price = 1234567.891

# f-string（推荐）
print(f"{name}考了{score:.1f}分")          # 保留1位小数
print(f"{name}考了{score:08.2f}分")        # 总宽8，不足补0
print(f"{'姓名':<8}{'分数':>8}")            # 对齐
print(f"价格: {price:,.2f} 元")            # 千分位

# 对比三种方式
print("\\n--- 三种格式化对比 ---")
print(f"{name}成绩{score:.1f}")                    # f-string
print("{}成绩{:.1f}".format(name, score))          # format
print("%s成绩%.1f" % (name, score))                # % 占位符

# --- 8. 实用：解析一行数据 ---
print("\\n=== 8. 实用例子 ===")
line = "  张三 , 25 , 北京  "
clean = [x.strip() for x in line.split(",")]
print(f"原始: '{line}'")
print(f"清洗: {clean}")`
  },

  // -----------------------------------------------------------
  // 第 9 章：布尔与比较
  // -----------------------------------------------------------
  {
    id: "py9-09",
    group: "数据：Python 里的东西",
    icon: "🚦",
    title: "布尔值、比较与逻辑运算",
    content: `## 布尔值：真与假

布尔类型只有两个值：\`True\`（真）和 \`False\`（假）。**首字母大写**，不是 \`true\`/\`false\`。

布尔是程序"做决定"的基础——下一章的 if 判断，本质就是在判断一个布尔值。

## 比较运算符：产生布尔值

| 运算符 | 含义 | 例子 |
|--------|------|------|
| \`==\` | 等于 | \`5 == 5\` → True |
| \`!=\` | 不等 | \`5 != 3\` → True |
| \`>\` \`<\` | 大于/小于 | \`5 > 3\` → True |
| \`>=\` \`<=\` | 大于等于/小于等于 | |

⚠️ \`==\` 是"判断相等"，\`=\` 是"赋值"，**别混**。新手最常犯的错。

### 链式比较（Python 特色）

\`\`\`python
0 < x < 10    # 等价 0 < x and x < 10
\`\`\`

数学里的写法直接能用，很舒服。

## 逻辑运算符

| 运算符 | 含义 |
|--------|------|
| \`and\` | 且：两边都真才真 |
| \`or\` | 或：一边真就真 |
| \`not\` | 非：取反 |

\`\`\`python
age = 20
age >= 18 and age <= 60    # 成年且未老年
age < 18 or age > 60       # 未成年或老年
not True                   # False
\`\`\`

### 短路求值

\`and\` 如果左边是假，右边就不算了（结果已定）。\`or\` 如果左边是真，右边也不算了。这叫**短路**。

\`\`\`python
False and (1/0)    # 不会报错，右边没算
True or (1/0)      # 不会报错
\`\`\`

## 真值与假值

不是只有 \`True\`/\`False\` 能判断，Python 把这些当作"假"：

- \`False\`、\`0\`、\`0.0\`
- 空字符串 \`""\`、空列表 \`[]\`、空字典 \`{}\`、\`None\`
- 其他都是"真"

\`\`\`python
if "":        # 假
if "hello":   # 真
if []:        # 假
if [0]:       # 真（列表非空，即使元素是0）
\`\`\`

这个特性很常用：\`if my_list:\` 判断"列表非空"。

## 本章 demo

demo 演示比较、逻辑、短路、真值假值。`,
    code: `# ============================================
# 第 9 章：布尔、比较、逻辑
# ============================================

# --- 1. 布尔值 ---
print("=== 1. 布尔值 ===")
t = True
f = False
print(f"t = {t}, 类型 {type(t)}")
print(f"f = {f}, 类型 {type(f)}")
print(f"注意首字母大写，不是 true/false")

# --- 2. 比较 ---
print("\\n=== 2. 比较 ===")
print(f"5 == 5 : {5 == 5}")
print(f"5 != 3 : {5 != 3}")
print(f"5 > 3  : {5 > 3}")
print(f"5 <= 5 : {5 <= 5}")
print(f"'a' < 'b' : {'a' < 'b'}    ← 字符串也能比（按字典序）")
print(f"'apple' < 'banana' : {'apple' < 'banana'}")

# 链式比较（Python 特色）
x = 5
print(f"\\nx = {x}")
print(f"0 < x < 10 : {0 < x < 10}    ← 链式比较，等价 0<x and x<10")

# --- 3. 逻辑运算 ---
print("\\n=== 3. 逻辑运算 ===")
age = 25
print(f"age = {age}")
print(f"age >= 18 and age <= 60 : {age >= 18 and age <= 60}")
print(f"age < 18 or age > 60    : {age < 18 or age > 60}")
print(f"not (age > 30)          : {not (age > 30)}")

# --- 4. 短路求值 ---
print("\\n=== 4. 短路求值 ===")
# and 左边假，右边不算（所以 1/0 不会执行，不报错）
result = False and (1 / 0)
print(f"False and (1/0) = {result}    ← 右边没算，不报错")
result = True or (1 / 0)
print(f"True or (1/0) = {result}      ← 右边没算，不报错")

# 利用短路做默认值
name = ""
display = name or "匿名"     # name 为空（假），取后面的
print(f"name or '匿名' = '{display}'")

# --- 5. 真值假值 ---
print("\\n=== 5. 真值假值 ===")
print("以下都被当作'假':")
for v in [False, 0, 0.0, "", [], {}, None]:
    print(f"  bool({v!r}) = {bool(v)}")
print("以下都被当作'真':")
for v in [True, 1, -1, "a", [0], [None], {"k": 0}]:
    print(f"  bool({v!r}) = {bool(v)}")

# --- 6. 实用：判断空 ---
print("\\n=== 6. 判断空（推荐写法）===")
items = []
# 推荐：直接判断真假
if items:
    print("列表非空")
else:
    print("列表空 ← items 是 []，被当作假")

name = ""
if not name:    # 等价 name == ""，但更 Pythonic
    print("名字是空的")`
  },

  // -----------------------------------------------------------
  // 第 10 章：列表基础
  // -----------------------------------------------------------
  {
    id: "py9-10",
    group: "数据：Python 里的东西",
    icon: "📋",
    title: "列表（一）：有序容器的诞生",
    content: `## 为什么需要列表

前面变量只能存一个值。但现实里数据常常是"一堆"：一个班 30 个成绩、购物车 5 件商品。一个个变量 \`score1\`、\`score2\`… 太蠢了。

**列表**就是装一堆值的容器，**有序、可变**：

\`\`\`python
scores = [90, 85, 78, 92, 88]
names = ["张三", "李四", "王五"]
mixed = [1, "hello", True, 3.14]   # 可以混装不同类型
\`\`\`

- **有序**：元素有先后顺序，有下标
- **可变**：能增删改里面的元素

## 索引和切片（和字符串一样）

\`\`\`python
scores[0]      # 90，第1个
scores[-1]     # 88，最后1个
scores[1:3]    # [85, 78]，切片得到新列表
\`\`\`

字符串的索引切片规则在列表上完全适用，复习一下：包含起、不包含止。

## 修改元素

列表可变，能直接改：

\`\`\`python
scores[0] = 95    # 把第1个改成95
\`\`\`

## 遍历

\`\`\`python
for s in scores:        # 直接遍历元素
    print(s)
for i, s in enumerate(scores):   # 同时要下标和元素
    print(i, s)
\`\`\`

## 常用操作

| 操作 | 例子 | 含义 |
|------|------|------|
| 长度 | \`len(scores)\` | 元素个数 |
| 是否包含 | \`90 in scores\` | 90 在不在列表里 |
| 拼接 | \`[1,2] + [3,4]\` | [1,2,3,4] |
| 重复 | \`[0] * 3\` | [0,0,0] |
| 最大最小 | \`max(scores)\` / \`min(scores)\` | |
| 求和 | \`sum(scores)\` | |

## 本章 demo

demo 演示创建、索引、修改、遍历、常用操作。下一章讲列表的方法（增删改查）。`,
    code: `# ============================================
# 第 10 章：列表基础
# ============================================

# --- 1. 创建 ---
print("=== 1. 创建列表 ===")
scores = [90, 85, 78, 92, 88]
names = ["张三", "李四", "王五"]
mixed = [1, "hello", True, 3.14]
empty = []                       # 空列表
print(f"成绩: {scores}")
print(f"姓名: {names}")
print(f"混合: {mixed}")
print(f"空列表: {empty}, 长度 {len(empty)}")

# --- 2. 索引 ---
print("\\n=== 2. 索引 ===")
print(f"scores = {scores}")
print(f"scores[0]  = {scores[0]}    ← 第1个")
print(f"scores[-1] = {scores[-1]}   ← 最后1个")
print(f"scores[2]  = {scores[2]}")

# --- 3. 切片 ---
print("\\n=== 3. 切片 ===")
print(f"scores[1:3] = {scores[1:3]}   ← 取下标1,2")
print(f"scores[:2]  = {scores[:2]}    ← 前2个")
print(f"scores[-2:] = {scores[-2:]}   ← 最后2个")
print(f"scores[::-1] = {scores[::-1]} ← 反转")

# --- 4. 修改（列表可变！）---
print("\\n=== 4. 修改 ===")
print(f"修改前: {scores}")
scores[0] = 95                    # 改第1个
print(f"scores[0]=95 后: {scores}")
scores[1:3] = [80, 70]            # 切片赋值，一次改一段
print(f"scores[1:3]=[80,70] 后: {scores}")

# --- 5. 遍历 ---
print("\\n=== 5. 遍历 ===")
print("方式1：直接遍历元素")
for s in scores:
    print(f"  {s}", end=" ")
print()
print("方式2：用 enumerate 拿下标+元素")
for i, s in enumerate(scores):
    print(f"  第{i}个: {s}")

# --- 6. 常用操作 ---
print("\\n=== 6. 常用操作 ===")
nums = [90, 85, 78, 92, 88]
print(f"nums = {nums}")
print(f"len(nums)   = {len(nums)}")
print(f"sum(nums)   = {sum(nums)}")
print(f"max(nums)   = {max(nums)}")
print(f"min(nums)   = {min(nums)}")
print(f"avg         = {sum(nums) / len(nums):.1f}")
print(f"90 in nums  = {90 in nums}")
print(f"100 in nums = {100 in nums}")
print(f"[1,2] + [3,4] = {[1,2] + [3,4]}")
print(f"[0] * 5 = {[0] * 5}")

# --- 7. 列表里装列表（嵌套）---
print("\\n=== 7. 嵌套列表 ===")
matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9],
]
print(f"矩阵: {matrix}")
print(f"matrix[0]    = {matrix[0]}     ← 第1行")
print(f"matrix[1][2] = {matrix[1][2]}  ← 第2行第3列")
# 遍历二维
print("遍历矩阵:")
for row in matrix:
    for v in row:
        print(v, end=" ")
    print()`
  },
];
