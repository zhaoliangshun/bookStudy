// =============================================================
// Python 逐层深入教程 - batch1
// 章节 1-10：起步认识 Python + 数据基础（数字/字符串/布尔/列表）
// =============================================================

export const chapters = [
  // -----------------------------------------------------------
  // 第 1 章：编程与 Python
  // -----------------------------------------------------------
  {
    id: "py9-01",
    group: "起步：认识 Python",
    icon: "🐍",
    title: "编程与 Python：计算机到底在听什么？",
    content: `## 先回答一个最根本的问题

你每天都在用手机、浏览网页、玩游戏、刷视频。这些软件本质上是**一连串指令**，告诉计算机"该做什么"。写这些指令的过程，就是**编程**。

但这里有个根本矛盾：计算机本身只听得懂 0 和 1（高低电平）。让人直接写 0 和 1 太反人类了——想象一下，要输出一个"你好"，可能要写 \`01101000 01101001\` 这样一长串，没人受得了。于是人们发明了**编程语言**——一种人类能读懂、又能被翻译成 0 和 1 的"中间语言"。

\`\`\`
你写的 Python 代码  →  翻译器（解释器）  →  计算机能执行的 0/1
\`\`\`

打个比方：你和一个只会说英语的外国人交流，你不会说英语，但有个翻译在场。你用中文说，翻译帮你翻成英语。编程语言就是你说的"中文"，翻译器就是那个"翻译"，计算机是那个"外国人"。你不需要懂英语（机器语言），只要会用中文（Python）+ 有翻译（解释器），就能让计算机干活。

## Python 是什么

Python 是一门**高级、解释型**编程语言。这两个词值得拆开讲清楚：

- **"高级"不是夸它厉害**，而是说它离人类语言近、离机器细节远。你不用管内存地址、寄存器、CPU 指令这些底层玩意儿。相比之下，C 语言就是"低级"语言，写 C 要操心内存怎么分配、变量存在哪个地址。Python 帮你把这些脏活累活都包了。

- **"解释型"**意思是它**逐行翻译逐行执行**，不像 C 那样先把整个程序编译成一个可执行文件再运行。这有好处也有坏处：好处是开发快、调试方便（错一行改一行立刻看效果）；坏处是运行速度比编译型语言慢（因为每次运行都要现翻译）。但对新手和大多数应用来说，这个速度差异可以忽略——你写脚本慢个 0.01 秒根本感觉不到。

### 为什么是 Python

你可能听过很多编程语言：Java、C++、JavaScript、Go……为什么这门课选 Python？看下面这张对比表：

| 特点 | Python | 其他语言对比 |
|------|--------|--------------|
| **语法接近英语** | \`print("hi")\` 几乎不用解释 | Java 要 \`System.out.println("hi")\` |
| **能做的事多** | 网站、数据分析、AI、爬虫、自动化 | 各有专长 |
| **生态丰富** | 别人写好的库直接拿来用 | C++ 库相对少 |
| **入门友好** | 不用先学底层概念 | C 一上来就讲指针内存 |
| **社区活跃** | 遇到问题搜一下基本有答案 | 小众语言资料少 |

一个真实数据：Python 是 AI、数据分析领域的事实标准。你以后想做机器学习、爬数据、写自动化脚本，Python 几乎是首选。换句话说，学 Python 是"性价比"最高的选择——投入少、能干的事多。

## 这门课怎么学

最好的学法是**边读边跑代码**。每一章你都点"运行代码"，看看输出，再改几个数字、改几行，观察变化。**改错了不会坏电脑**，放心折腾——这是学编程最重要的心态。

你要建立一个认知：读懂代码和"自己写出来"之间差着十万八千里。光看不动手是学不会的。建议每章学完，自己改一改 demo 里的代码：把数字换大、把字符串换内容、把运算符换一个，看输出怎么变。这种"瞎折腾"恰恰是最好的学习方式。错了？改回来再试。编程就是不断试错的过程，连资深程序员每天也在 Google、在试错。

下面是我们这门课的第一段代码。它做的事很简单：让计算机在屏幕上输出一句话。但别小看它——你已经让计算机听话了。

### 一个小约定

Python 用 \`#\` 号写注释，注释是给人看的，计算机会忽略。后面你会看到大量注释，那是"这行代码在干嘛"的解释。注释也是写给你自己未来的——三个月后的你回来看代码，会感谢现在写注释的自己。

## 逐行解读第一段代码

\`\`\`python
print("你好，Python！")  # 调用 print 函数，把字符串显示到屏幕上
\`\`\`

这一行做了三件事：
1. 调用 \`print\` 这个**函数**（函数后面会细讲，现在先理解为"一个动作"）
2. 把 \`"你好，Python！"\` 这个**字符串**（用引号包起来的文字）作为参数传给它
3. \`print\` 把这个字符串显示到屏幕上

\`\`\`python
print("""           # 三个双引号开头，表示多行字符串的开始
这是第二行
这是第三行
看出来了吗？三引号可以包住多行文字
""")              # 三引号结尾，print 把整段多行文字输出到屏幕
\`\`\`

这里用**三个引号**包了一大段文字。注意它和单引号的区别：单引号只能包一行，三引号可以包多行，里面的换行会保留。这在写文档、写长文本时很有用。

\`\`\`python
print(42)       # 打印整数，不用引号
print(3.14)     # 打印浮点数（带小数点的数）
\`\`\`

数字也能打印，**不用引号**。带引号的是"文字"（字符串），不带引号的是"数字"。这俩在 Python 里是不同类型，后面会讲。你可能会问：为什么文字要加引号？因为不加引号 Python 会把它当成"变量名"或"命令"，加了引号才是"字面文字"。

\`\`\`python
print(1 + 2)     # print 先算表达式 1+2=3，再打印结果
print(10 * 10)   # 先算 10*10=100，再打印
\`\`\`

\`print\` 里能放**表达式**，它会先算出结果再显示。\`1 + 2\` 会被算成 \`3\` 再打印，而不是打印 "1 + 2" 这几个字符。这说明 \`print\` 很聪明——它打印的是"值"，不是"代码本身"。

## ⚠️ 初学者常见坑

1. **引号必须成对**：\`print("hello)\` 少了右引号会报 SyntaxError。中文引号和英文引号长得像但不一样，**一定要用英文引号**。
2. **括号必须成对**：\`print("hi"\` 少了右括号同样报错。
3. **标点符号要英文**：逗号、括号、引号都要用英文半角，中文标点会报错。

## 小结

- 编程就是写指令让计算机执行；编程语言是人类和机器之间的"中间语言"
- Python 是高级、解释型语言，语法接近英语，入门友好
- \`print()\` 是第一个函数，把内容显示到屏幕
- 字符串用引号包，数字不用引号，二者是不同类型
- \`#\` 是注释，给代码加说明，机器忽略

## 常见疑问 Q&A

**Q：Python 和 Java、C++ 哪个好？**
A：没有"最好"的语言，只有"适合"的语言。Python 适合数据分析、AI、脚本；Java 适合大型后端；C++ 适合游戏引擎、底层。新手从 Python 入门最轻松。

**Q：学 Python 能找到工作吗？**
A：能，但光会 Python 不够。Python 是工具，关键是你要有"用它解决问题"的能力——比如做数据分析、写爬虫、做 Web 后端。本教程是入门第一步。

**Q：注释写中文会不会出错？**
A：不会。Python 3 默认支持 UTF-8，中文注释、中文字符串都没问题。本教程的注释就是中文的。

**Q：为什么是 \`print\` 不是 \`println\` 或 \`echo\`？**
A：这是 Python 的设计选择。不同语言叫法不同，JavaScript 用 \`console.log\`，PHP 用 \`echo\`，本质都是"输出到屏幕"。名字不重要，理解它干什么才重要。`,
    code: `# ============================================
# 第 1 章：第一段代码
# 目标：让计算机在屏幕上输出文字
# ============================================

# print() 是一个"函数"：你给它内容，它把内容显示到屏幕上
# 引号里的内容叫"字符串"，就是一串文字
print("你好，Python！")

# 可以一次打印多行，用三个引号
print("""
这是第二行
这是第三行
看出来了吗？三引号可以包住多行文字
""")

# 数字也能打印，不用引号
print(42)
print(3.14)

# 还能做计算，print 会先算出结果再显示
print(1 + 2)        # 加法
print(10 * 10)      # 乘法

# 改一改：把上面的文字换成你想说的话，再运行试试
print("我正在学 Python，感觉还不错")`
  },

  // -----------------------------------------------------------
  // 第 2 章：运行方式
  // -----------------------------------------------------------
  {
    id: "py9-02",
    group: "起步：认识 Python",
    icon: "▶️",
    title: "Python 的三种运行方式",
    content: `## 代码写在哪、怎么跑

学 Python 你会遇到三种"跑代码"的方式，先认识它们，免得后面糊涂。这三种方式各有用途，搞清楚区别对你后面学习很有帮助。

### 方式一：交互式（>>> 提示符）

在终端输入 \`python3\`，会进入一个 \`>>>\` 提示符的环境。你输一行，它立刻执行一行，回车就出结果。适合"试试这行代码对不对"，就像用计算器一样。

\`\`\`
>>> 1 + 1       # 在 >>> 提示符后输入算式，回车立刻得到结果
2
>>> print("hi")  # 输入 print 调用，回车后输出 hi
hi
\`\`\`

这种方式的优点是**即时反馈**——你输完立刻看到结果，不用保存文件、不用运行命令。缺点是**代码不保存**，关掉窗口就没了。所以它适合"临时试一试"，不适合写正式程序。

退出输入 \`exit()\` 或按 Ctrl+D（Mac/Linux）。

### 方式二：脚本文件（.py）

把代码写进一个文件，比如 \`hello.py\`，然后命令行运行 \`python3 hello.py\`。这是**正式开发**用的方式，代码能保存、能复用、能发给别人。

打个比方：交互式像"口头说一句话"，说完就没了；脚本文件像"写日记本上"，能反复看、能给别人看。实际工作中 99% 的代码都是写在文件里的。

### 方式三：在线 / IDE 运行（你现在用的）

像这个教程页面，你在浏览器里写代码、点按钮运行。它背后其实也是调用了系统的 \`python3\`，只是帮你包装好了界面。**Trae、VS Code、Jupyter** 都属于这类。

| 方式 | 适合场景 | 代码保存 | 即时反馈 |
|------|----------|----------|----------|
| 交互式 \`>>>\` | 试一行代码、查个函数 | ❌ 不保存 | ✅ 立刻出结果 |
| 脚本文件 \`.py\` | 正式开发、长程序 | ✅ 保存 | ❌ 要运行命令 |
| IDE/在线 | 学习、调试、写项目 | ✅ 保存 | ✅ 点按钮即跑 |

你现在用的就是方式三，最方便学习。

## 解释器到底在干嘛

你写 \`print("hi")\`，解释器做三件事：

1. **读（Read）**：把这一行读进来，分析有哪些"词"——\`print\`、\`(\`、\`"hi"\`、\`)\`
2. **理解（Parse）**：识别出 \`print\` 是个函数名，\`"hi"\` 是个字符串参数，整体是一个函数调用
3. **执行（Execute）**：调用 print 函数，把 "hi" 送出去显示

这个过程叫"解析→执行"，每行都这么来一遍。这就是为什么叫"解释型"——它逐行解释你的代码什么意思，然后执行。

如果某行有它不认识的写法（比如少了个括号、拼错了关键字），就会报 **SyntaxError（语法错误）**，并指出大致在第几行。这是后面你最常遇到的错误之一，别怕，看报错信息修就行。报错不是"你笨"，是解释器在帮你——它告诉你"这里我看不懂，你检查一下"。

## 代码是按顺序执行的

这是本章的核心知识点：**Python 代码默认从上到下，一行一行执行**。第一行执行完才执行第二行，第二行执行完才执行第三行。这看起来理所当然，但理解它对后面学 if、for、函数调用很重要。

你可能会问：有没有不按顺序的情况？有——函数调用、循环、条件判断会"跳着执行"，但那是后面的章节。现在先记住"从上到下"这个基本规则。

⚠️ **注意**：空行不影响执行，注释也不影响执行，它们只是给人看的。所以你可以在代码里加空行让结构更清晰，加注释解释在干嘛，机器完全忽略。

### 顺序执行的一个细节

你可能会好奇：如果某一行出错了，后面的代码还会执行吗？答案是**不会**——Python 遇到错误会立刻停下，报出错误信息，后面的代码都不跑了。这叫"异常中断"。

\`\`\`python
print("第一行")  # 打印输出到屏幕
print(1 / 0)       # 除以 0 会抛出 ZeroDivisionError，程序在此中断
print("第三行")     # 上一行报错，这行不会执行！
\`\`\`

所以调试时如果发现"后面的代码没跑"，先看看前面有没有报错。学会看报错信息是编程的基本功——错误信息会告诉你错误类型（SyntaxError、NameError、TypeError 等）和大致位置（第几行），照着改就行。

## 逐行解读本章 demo

demo 第一部分演示"从上到下执行"：

\`\`\`python
print("第一步：我开始执行了")              # 最先执行
print("第二步：这一行在第一行下面，所以后执行")  # 第一行执行完才轮到它
print("第三步：以此类推")                  # 顺序往下执行
\`\`\`

三行 print 按顺序输出。如果把第二行和第三行换个位置，输出顺序也会变——这就是"顺序执行"。

\`\`\`python
print("-" * 30)   # 字符串重复：把 "-" 重复 30 次，得到一条分隔线
\`\`\`

这行用了字符串的"重复"功能：\`"-" * 30\` 表示把 \`"-"\` 重复 30 次，得到一行 30 个减号。这是画分隔线的常用技巧。

\`\`\`python
a = 15            # 把 15 赋给变量 a（变量像贴标签）
b = 4             # 把 4 赋给变量 b
print("a + b =", a + b)  # 逗号分隔多个参数，一起打印，中间默认加空格
\`\`\`

这里 \`a\` 和 \`b\` 是"变量"（下一章细讲），可以理解为"装了值的盒子"。\`print\` 里用逗号隔开多个东西，会一起打印，中间默认加空格。所以输出是 \`a + b = 19\`。

## ⚠️ 初学者常见坑

1. **语法错误最常见**：少括号、少引号、多括号都会报 SyntaxError。看到报错别慌，看它说第几行，去那行检查。
2. **中文标点**：在中文输入法下打的逗号、括号、引号，Python 不认识。写代码时**切到英文输入法**。
3. **缩进错误**：Python 对缩进很敏感，后面会专门讲。现在只要知道"不要随便在行首加空格"。

## 小结

- Python 有三种运行方式：交互式（试代码）、脚本文件（正式开发）、IDE/在线（学习开发两用）
- 解释器逐行"读→理解→执行"，遇到不认识的写法报 SyntaxError
- 代码默认从上到下顺序执行，空行和注释不影响
- 报错不可怕，它是解释器在帮你定位问题

## 常见疑问 Q&A

**Q：交互式和脚本文件，平时该用哪个？**
A：学习时两个都用——临时试一行用交互式，写完整程序用脚本文件。工作中主要用脚本文件。

**Q：报错信息看不懂怎么办？**
A：先看错误类型（SyntaxError 是语法错、NameError 是名字没找到、TypeError 是类型不对），再看它指出的行号，去那行检查。还看不懂就把报错信息复制去搜。

**Q：为什么我的代码在交互式里能跑，写成文件就报错？**
A：最常见原因是缩进——交互式里有些缩进自动处理了，文件里要严格写对。另外检查有没有混用 Tab 和空格。

**Q：\`python\` 和 \`python3\` 有什么区别？**
A：有些系统 \`python\` 指向 Python 2（已淘汰），\`python3\` 明确指 Python 3。建议统一用 \`python3\`。`,
    code: `# ============================================
# 第 2 章：代码的执行顺序
# ============================================

# Python 代码默认从上到下，一行一行执行
print("第一步：我开始执行了")
print("第二步：这一行在第一行下面，所以后执行")
print("第三步：以此类推")

# 空行不影响执行，只是给人看的
print("第四步：空行上面那行不影响我")

# 用分隔线让输出更清楚
print("-" * 30)

# 模拟一个"计算器"流程
# 假装用户输入了两个数（真实场景会用 input()，这里用固定值模拟）
a = 15
b = 4

print("我们假装用户输入了：a =", a, "，b =", b)
print("a + b =", a + b)
print("a - b =", a - b)
print("a * b =", a * b)
print("a / b =", a / b)

# 故意写一个会出错的例子（注释掉了）
# print("语法错误演示"   # 少了右括号，取消注释会报 SyntaxError
# 报错时解释器会告诉你大概在哪一行，照着改就行

print("-" * 30)
print("程序结束")`
  },

  // -----------------------------------------------------------
  // 第 3 章：print 详解
  // -----------------------------------------------------------
  {
    id: "py9-03",
    group: "起步：认识 Python",
    icon: "📤",
    title: "print —— 让计算机开口说话",
    content: `## print 比你想的能干

\`print\` 是你接触的第一个函数。它把"东西"变成文字显示出来。但它的细节值得单独讲一章，因为调试程序时你 90% 时间在和 print 打交道——不知道变量是多少？print 一下；不知道程序走到哪了？print 一下；不知道结果对不对？print 一下。print 是程序员的"眼睛"。

先把 print 当成一个"黑盒"：你给它东西，它把东西显示出来。至于它内部怎么实现的，先不用管。我们关心的是"怎么用它"。

### 基本用法

\`\`\`python
print("hello")        # 打印字符串：引号包起来的是文字
print(123)            # 打印数字：不用引号
print(1 + 2)          # 打印表达式的结果：先算 1+2=3 再打印
\`\`\`

注意第三行：\`print\` 打印的是"值"，所以 \`1 + 2\` 会先算成 \`3\` 再打印，而不是打印 "1 + 2" 这串字符。这说明 \`print\` 括号里可以放任何"有值的东西"。

### 打印多个东西：用逗号

\`\`\`python
print("我今年", 18, "岁")   # 逗号分隔多个参数，一起打印，中间默认加空格
# 输出：我今年 18 岁
\`\`\`

逗号之间默认用**空格**分隔。这个分隔符可以改，用 \`sep\` 参数：

\`\`\`python
print("2025", "07", "01", sep="-")   # sep 设置分隔符为 "-"，输出 2025-07-01
\`\`\`

你可能会问：为什么默认是空格？这是 Python 设计者的选择，方便大多数情况。需要别的分隔符就用 \`sep\` 改。比如拼日期用 \`-\`，拼路径用 \`/\`，拼 CSV 用逗号。

### 结尾换行：end 参数

print 默认在结尾加一个**换行符** \`\\n\`，所以两次 print 各占一行。用 \`end\` 参数可以改：

\`\`\`python
print("加载中", end="")   # end="" 表示结尾不换行，下一个 print 接着同一行输出
print("...完成")     # 因为上一行没换行，这句会和"加载中"在同一行
\`\`\`

这里 \`end=""\` 表示结尾什么都不加，所以下一个 print 接着同一行输出。这个技巧在做进度条、做"逐步显示"效果时很有用。

### 转义字符

字符串里有些字符不能直接写，比如"换行"——你总不能在字符串中间真的按个回车吧？Python 用反斜杠 \`\\\`\` 开头的转义字符表示：

| 写法 | 含义 | 用途 |
|------|------|------|
| \`\\n\` | 换行 | 输出多行 |
| \`\\t\` | 制表符（对齐用） | 做表格对齐 |
| \\\\ | 反斜杠本身 | 写文件路径 |
| \\\' | 单引号 | 在单引号字符串里用单引号 |
| \\" | 双引号 | 在双引号字符串里用双引号 |

为什么需要转义？因为引号、反斜杠这些字符在字符串里有"特殊含义"——引号表示字符串开始结束，反斜杠表示转义。你想用这些字符"本身"，就得转义告诉 Python"这是普通字符，不是特殊符号"。

### f-string（强烈推荐）

想在字符串里嵌入变量的值，用 f-string 最方便——字符串前加 \`f\`，用 \`{}\` 包变量：

\`\`\`python
name = "小明"                          # 字符串变量
age = 18                               # 整数变量
print(f"我叫{name}，今年{age}岁")       # f-string：{} 里放变量，自动替换成值
\`\`\`

这比老式的 \`% \` 或 \`.format()\` 直观得多，本教程统一用 f-string。f-string 的 \`{}\` 里能放任何表达式，包括运算、函数调用：

\`\`\`python
print(f"明年我{age + 1}岁")              # {} 里能放算术表达式
print(f"名字长度{len(name)}")             # {} 里能放函数调用
print(f"BMI={70 / 1.75**2:.2f}")          # :.2f 表示保留 2 位小数
\`\`\`

\`:.2f\` 表示"保留 2 位小数"，这是格式化语法，后面会专门讲。

### 想看变量的"类型"

\`type()\` 告诉你一个东西是什么类型，调试很有用：

\`\`\`python
print(type(42))        # 42 是 int（整数）类型
print(type("hi"))      # "hi" 是 str（字符串）类型
\`\`\`

为什么需要看类型？因为有时候你以为变量是数字，其实是字符串（比如从 input 拿来的），算术就会出错。\`type()\` 帮你确认"这东西到底是什么"。

## 逐行解读本章 demo

demo 分 6 部分，每部分用分隔线标出：

**第 1 部分 基本打印**：演示 print 能打印字符串、整数、浮点数、表达式结果。注意 \`print(2 * 8)\` 打印的是 16，不是 "2 * 8"。

**第 2 部分 多参数**：演示用逗号打印多个东西，以及用 \`sep\` 改分隔符。\`sep="-"\` 把默认空格换成减号，所以 \`2025 07 01\` 变成 \`2025-07-01\`。

**第 3 部分 end 参数**：三个 print 用 \`end=""\` 让它们输出在同一行。如果把 \`end=""\` 去掉，每行各占一行。

**第 4 部分 转义字符**：演示 \`\\n\` 换行、\`\\t\` 对齐、\`\\\\\` 表示一个反斜杠、\`\\'\` 在单引号字符串里用单引号。注意文件路径 \`C:\\\\Users\\\\test\` 要写双反斜杠，因为单个 \`\\\` 是转义符。

**第 5 部分 f-string**：演示变量嵌入、表达式嵌入、格式化。\`{70 / height**2:.2f}\` 先算 BMI 再保留 2 位小数。

**第 6 部分 type()**：打印各种值的类型。你会发现 \`42\` 是 \`int\`，\`3.14\` 是 \`float\`，\`"hi"\` 是 \`str\`，\`True\` 是 \`bool\`。

## ⚠️ 初学者常见坑

1. **字符串和数字不能直接相加**：\`"年龄" + 18\` 会报 TypeError。要用 \`str(18)\` 转成字符串，或用 f-string。
2. **引号嵌套**：字符串里想用引号，要么转义，要么用不同的引号。\`"it's ok"\` 用双引号包就不用转义。
3. **f-string 忘了加 f**：\`"我叫{name}"\` 没加 f 会原样输出，不会替换变量。
4. **sep 和 end 容易混**：\`sep\` 是多个参数之间的分隔，\`end\` 是结尾加什么。

## 小结

- \`print\` 是调试的"眼睛"，把值显示到屏幕
- 多个参数用逗号隔开，\`sep\` 控制分隔符，\`end\` 控制结尾
- 转义字符用 \`\\\` 开头，常用 \`\\n\`（换行）、\`\\t\`（制表）、\`\\\\\`（反斜杠）
- f-string 是嵌入变量的最佳方式：\`f"...{变量}..."\`
- \`type()\` 查看变量的类型，调试必备

## 常见疑问 Q&A

**Q：\`print\` 和 \`return\` 有什么区别？**
A：\`print\` 是"显示到屏幕"，给人看；\`return\` 是"把值返回给调用者"，给程序用。两者完全不同，学函数时会深刻理解。

**Q：f-string 是 Python 哪个版本加的？**
A：Python 3.6 加的。现在主流版本都支持，放心用。老代码可能用 \`.format()\` 或 \`%\`，能看懂就行。

**Q：怎么打印不换行？**
A：用 \`end=""\`。比如 \`print("hi", end="")\` 就不换行，下一个 print 接着同一行。

**Q：\`print()\` 不传参数会怎样？**
A：打印一个空行。这常用来"空一行让输出更清晰"。`,
    code: `# ============================================
# 第 3 章：print 的各种用法
# ============================================

# --- 1. 基本打印 ---
print("=== 1. 基本打印 ===")
print("你好")
print(42)
print(3.14)
print(2 * 8)              # 先算再打印

# --- 2. 多个参数，默认空格分隔 ---
print("\\n=== 2. 多个参数 ===")
print("语文", 90, "数学", 85, "英语", 95)
# 用 sep 改分隔符
print("2025", "07", "01", sep="-")
print("a", "b", "c", sep=" | ")

# --- 3. end 参数控制结尾 ---
print("\\n=== 3. end 参数 ===")
print("加载中", end="")
print("...", end="")
print("完成")              # 三句会在同一行

# --- 4. 转义字符 ---
print("\\n=== 4. 转义字符 ===")
print("第一行\\n第二行")   # \\n 换行
print("姓名\\t年龄\\t分数")  # \\t 对齐
print("文件路径：C:\\\\Users\\\\test")  # \\\\ 表示一个反斜杠
print('it\\'s ok')         # 单引号字符串里的单引号要转义

# --- 5. f-string 嵌入变量 ---
print("\\n=== 5. f-string ===")
name = "小明"
age = 18
height = 1.75
print(f"我叫{name}，今年{age}岁，身高{height}米")
# f-string 里还能放表达式
print(f"明年我{age + 1}岁")
print(f"{name}的体重是{70}公斤，BMI={70 / height**2:.2f}")  # :.2f 保留两位小数

# --- 6. type() 看类型 ---
print("\\n=== 6. type() 看类型 ===")
print("42 的类型是", type(42))
print("3.14 的类型是", type(3.14))
print("\\"hi\\" 的类型是", type("hi"))
print("True 的类型是", type(True))`
  },

  // -----------------------------------------------------------
  // 第 4 章：注释、缩进、变量
  // -----------------------------------------------------------
  {
    id: "py9-04",
    group: "起步：认识 Python",
    icon: "📝",
    title: "注释、缩进与变量：给数据起名字",
    content: `## 注释：写给未来的自己

注释是代码里"给人看、机器忽略"的部分。Python 用 \`#\`：

\`\`\`python
# 这是整行注释
age = 18   # 这是行尾注释
\`\`\`

什么时候写注释？这是个好问题。原则是：**解释"为什么这么做"，而不是"这行在干嘛"**。

如果代码本身能看懂在干嘛，就不用注释——\`age = 18\` 不用注释"把 18 赋给 age"，废话。但如果是绕了个弯、有特殊原因，才写注释说明：

\`\`\`python
# 这里 +1 是因为后端返回的月份从 0 开始
month = response_month + 1  # 赋值变量 month
\`\`\`

这种注释就有价值——它解释了"为什么 +1"，而不是"这行在加 1"。好的注释回答"为什么"，坏的注释重复"是什么"。

⚠️ **注意**：注释虽好，但别过度。如果每行都注释 \`x = 1  # 把1赋给x\`，反而干扰阅读。注释要写在"需要解释"的地方。

## 缩进：Python 的灵魂

大多数语言用 \`{}\` 表示代码块（哪些语句属于一起），Python 用**缩进**（行首的空格）。**4 个空格**是约定俗成的标准。

\`\`\`python
if True:                          # if 条件为 True，执行缩进的代码块
    print("这行属于 if")     # 缩进 4 空格，属于 if
    print("这行也属于 if")       # 同样缩进，也属于 if
print("这行不属于 if")        # 没缩进，在 if 外面，无论如何都执行
\`\`\`

为什么 Python 用缩进而不用 \`{}\`？这是设计者 Guido 的选择，他认为缩进本身就是"代码结构"的视觉表达——人眼看缩进就能知道层次，再用 \`{}\` 是多余的。好处是代码强制整洁，坏处是缩进错了会出错。

缩进错了，Python 会报 \`IndentationError\`。**同一块代码的缩进必须一致**，不能一行 4 空格、一行 2 空格混着来。这是 Python 和其他语言最大的区别之一，新手最容易栽在这。

| 语言 | 代码块表示 | 缩进重要吗 |
|------|-----------|-----------|
| Python | 缩进（4 空格） | ✅ 必须正确，否则报错 |
| Java/C++ | \`{}\` | ❌ 只是风格，不影响运行 |
| JavaScript | \`{}\` | ❌ 只是风格 |

> 在线编辑器里按 Tab 会自动输入 4 个空格，放心用。

## 变量：给数据贴标签

变量是**名字**，你用它记住一个值，以后能用名字引用它。

\`\`\`python
age = 18        # 把整数 18 赋给变量 age
name = "小明"   # 把字符串赋给变量 name
\`\`\`

这里 \`age\` 和 \`name\` 就是变量。变量让你不用记"那个数字是 18"，而是记"age 是年龄"，代码可读性大大提高。

### 关键理解：变量是"标签"不是"盒子"

很多人把变量想象成"装东西的盒子"——\`age = 18\` 是把 18 放进 age 这个盒子。这个比喻**不够准确**。更准确的比喻是**贴标签**：\`age = 18\` 是把一个写着 \`age\` 的标签贴到了数字 18 上。后面 \`age = 20\` 是把标签撕下来贴到 20 上。

为什么要强调这个区别？看这个例子：

\`\`\`python
a = [1, 2, 3]    # a 贴在 [1,2,3] 上
b = a            # b 也贴在同一个 [1,2,3] 上
a.append(4)      # 往那个列表里加 4
print(b)         # b 也是 [1,2,3,4]！
\`\`\`

如果变量是"盒子"，\`b = a\` 应该是"把 a 盒子的东西复制一份到 b 盒子"，改 a 不影响 b。但实际是 \`b\` 也变了——因为 a 和 b 贴在**同一份**列表上。这个理解在后面学"可变对象"（列表、字典）时极其重要，先记住。

### 赋值的几种花样

\`\`\`python
# 同时给多个变量赋值
a, b, c = 1, 2, 3      # 右边元组解包给左边的三个变量

# 交换两个变量（Python 特色，不用临时变量）
a, b = b, a            # 右边先组成 (b,a)，再解包赋给左边的 a,b

# 多个变量同一个值
x = y = z = 0          # 三个变量都指向同一个 0
\`\`\`

\`a, b = b, a\` 这个交换写法是 Python 的"特色"——其他语言要写三行（用临时变量），Python 一行搞定。原理是"先算右边，再赋给左边"，右边的 \`b, a\` 先组成一个元组，再拆开赋给左边的 \`a, b\`。

### 变量命名规则

- 只能用**字母、数字、下划线**，且**不能数字开头**（\`1name\` 不行，\`name1\` 行）
- 区分大小写：\`age\` 和 \`Age\` 是两个变量
- 不能用关键字（\`if\`、\`for\`、\`def\` 等被 Python 占用了）
- 推荐用**小写+下划线**：\`user_name\`、\`total_score\`，别用拼音或中文

| 命名风格 | 例子 | 用在哪 |
|----------|------|--------|
| 小写+下划线（snake_case） | \`user_name\` | Python 变量、函数（推荐） |
| 驼峰（camelCase） | \`userName\` | Java、JS 常用 |
| 全大写 | \`MAX_SIZE\` | Python 常量 |

⚠️ **注意**：变量名要"见名知意"。\`a = 18\` 不如 \`age = 18\` 清楚，\`s = "小明"\` 不如 \`name = "小明"\` 清楚。代码是给人读的，好名字胜过注释。

## 逐行解读本章 demo

**第 1 部分变量基本用法**：演示字符串、整数、浮点数、布尔四种类型赋值，再用 f-string 打印。

**第 2 部分重新赋值**：演示变量可以改值，以及 \`score = score + 5\` 这种"自增"写法——先算右边 \`score + 5\`，再赋给左边的 \`score\`。

**第 3 部分多变量赋值**：演示 \`a, b, c = 1, 2, 3\`、\`a, b = b, a\` 交换、\`x = y = z = 0\` 三种写法。

**第 4 部分缩进演示**：用 if 展示缩进——缩进的属于 if，不缩进的不属于。下章细讲 if。

**第 5 部分变量是标签**：演示 \`b_list = a_list\` 后改 \`a_list\`，\`b_list\` 也变了——证明它们贴在同一份数据上。这是"标签"理解的关键证据。

## ⚠️ 初学者常见坑

1. **缩进混用 Tab 和空格**：会报 \`TabError\`。统一用空格（编辑器会帮你）。
2. **变量名用关键字**：\`if = 1\` 会报错，因为 \`if\` 是关键字。
3. **变量没定义就用**：\`print(x)\` 但没给 \`x\` 赋值，会报 \`NameError\`。
4. **以为 \`=\` 是判断相等**：\`=\` 是赋值，\`==\` 才是判断相等（下章讲）。

## 小结

- 注释解释"为什么"，不是重复"是什么"
- Python 用缩进表示代码块，4 空格是标准，缩进错会报错
- 变量是"贴标签"不是"装盒子"，可变对象共享要小心
- 多变量赋值、交换变量是 Python 特色写法
- 变量名要见名知意，用小写+下划线

## 常见疑问 Q&A

**Q：为什么 Python 用缩进不用 \`{}\`？**
A：设计者认为缩进本身就能表达结构，\`{}\` 是多余的。好处是强制代码整洁，坏处是新手容易缩进错。

**Q：变量名能用中文吗？**
A：能（Python 3 支持），但不推荐。中文变量名在某些环境可能有问题，而且不够通用。

**Q：\`a, b = b, a\` 是怎么实现交换的？**
A：Python 先把右边的 \`b, a\` 组成一个元组，再拆开赋给左边的 \`a, b\`。所以是"同时"交换，不需要临时变量。

**Q：怎么知道哪些是关键字不能用？**
A：在 Python 里输入 \`import keyword; print(keyword.kwlist)\` 会列出所有关键字。常见的有 \`if\`、\`for\`、\`while\`、\`def\`、\`class\`、\`return\` 等。`,
    code: `# ============================================
# 第 4 章：注释、缩进、变量
# ============================================

# --- 1. 变量基本用法 ---
# 把值"赋给"变量，用等号 =
name = "小明"          # 字符串
age = 18               # 整数
height = 1.75          # 浮点数
is_student = True      # 布尔值

print(f"姓名: {name}")
print(f"年龄: {age}")
print(f"身高: {height} 米")

# --- 2. 变量可以重新赋值 ---
print("\\n--- 重新赋值 ---")
score = 80
print(f"第一次: {score}")
score = 95             # 标签撕下来，贴到 95
print(f"第二次: {score}")
score = score + 5      # 先算右边 score+5=100，再赋给 score
print(f"第三次: {score}")

# --- 3. 同时给多个变量赋值 ---
print("\\n--- 多变量赋值 ---")
a, b, c = 1, 2, 3
print(f"a={a}, b={b}, c={c}")

# 交换两个变量（不用临时变量！）
a, b = b, a
print(f"交换后: a={a}, b={b}")

# 多个变量同一个值
x = y = z = 0
print(f"x={x}, y={y}, z={z}")

# --- 4. 缩进演示（用 if 暂时展示，下章细讲）---
print("\\n--- 缩进演示 ---")
if age >= 18:
    print(f"{name} 已成年")      # 这两行缩进了 4 空格，属于 if
    print(f"可以办身份证")
print("这行没缩进，无论如何都会执行")  # 在 if 外面

# --- 5. 变量是"标签"的小证据 ---
print("\\n--- 变量是标签 ---")
# 把 list 赋给 a，再把 a 赋给 b
# 后面讲列表会细说，这里先看效果：改 a，b 也变了
a_list = [1, 2, 3]
b_list = a_list           # b_list 和 a_list 贴的是同一个东西
a_list.append(4)          # 往 a_list 里加个 4
print(f"a_list = {a_list}")
print(f"b_list = {b_list}")  # b_list 也变了！因为它们是同一份
print("这说明变量是'标签'，a_list 和 b_list 贴在同一份数据上")`
  },

  // -----------------------------------------------------------
  // 第 5 章：输入与类型转换
  // -----------------------------------------------------------
  {
    id: "py9-05",
    group: "起步：认识 Python",
    icon: "⌨️",
    title: "输入与类型转换",
    content: `## 程序要和外界打交道

前面我们的数据都是写死在代码里的——\`age = 18\` 这个 18 是你写代码时定的。但真实程序经常需要**从用户那里拿数据**——比如让用户输入名字、年龄，然后程序根据输入做处理。

想象一个注册流程：用户输入用户名、密码、邮箱，程序验证后保存。这里"输入"就是程序和外界的交互。

### input()：读用户输入

\`input("提示文字")\` 会显示提示，等用户敲一行字按回车，把那行字作为字符串返回：

\`\`\`python
name = input("你叫什么？")   # input 显示提示并等待用户输入，返回字符串
print("你好，" + name)       # 用 + 拼接字符串后打印
\`\`\`

运行时屏幕会显示 \`你叫什么？\` 然后等用户输入，用户敲"小明"按回车，\`name\` 就得到 \`"小明"\`。这就是最简单的"人机交互"。

⚠️ **重要**：\`input()\` 拿到的**永远是字符串**，哪怕用户输入的是 \`18\`。这点坑了无数新手。为什么？因为 \`input\` 不知道用户会输什么——可能输数字、可能输字母、可能输乱码，所以它统一当成"文字"返回，最安全。

\`\`\`python
age = input("年龄？")   # input 拿到的永远是字符串，即使输入 18 也是 "18"
print(age + 1)          # 报错！字符串 "18" 不能和数字 1 相加
\`\`\`

这里 \`age\` 是字符串 \`"18"\`，不是数字 18。字符串 + 数字会报 TypeError。你要先把它"转成数字"。

### 类型转换

要把"数字样子的字符串"变成真正的数字，用转换函数：

| 函数 | 作用 | 例子 | 结果 |
|------|------|------|------|
| \`int()\` | 转整数 | \`int("18")\` | 18 |
| \`float()\` | 转浮点 | \`float("3.14")\` | 3.14 |
| \`str()\` | 转字符串 | \`str(18)\` | "18" |
| \`bool()\` | 转布尔 | \`bool(0)\` | False |

\`\`\`python
age = int(input("年龄？"))   # 先 input 拿字符串，再用 int 转成整数
print(age + 1)               # 转成整数后就能做算术了
\`\`\`

这里 \`int(input(...))\` 是"嵌套调用"——先执行 \`input\` 拿到字符串，再执行 \`int\` 把它转成整数。理解这个执行顺序：**从内到外**，先算括号里的。

### 转换可能失败

如果用户乱输 \`int("abc")\` 会报 \`ValueError\`——因为 "abc" 不是数字样子的字符串，转不成整数。真实程序要用 try/except 兜底（异常那章讲），现在先假设用户规规矩矩。

\`\`\`python
try:  # 尝试执行可能出错的代码
    n = int("abc")            # 尝试把 "abc" 转成整数（会失败）
except ValueError:            # 转换失败会抛 ValueError，这里捕获
    print("输入的不是数字")   # 捕获后做兜底处理，不让程序崩溃
\`\`\`

为什么 Python 不自动处理这种错误？因为"输入不是数字"是业务逻辑问题，Python 不知道你遇到这种情况想怎么办——是给默认值？是重新让用户输？是直接退出？这得你决定。所以 Python 选择"抛异常让你处理"，而不是自作主张。

## 字符串和数字的拼接

这是新手高频遇到的坑：想把数字拼到字符串里。

\`\`\`python
score = 95  # 定义数值 score
print("成绩" + score + "分")   # 报错！字符串不能直接加数字
\`\`\`

为什么报错？因为 \`+\` 对字符串是"拼接"，对数字是"加法"，Python 不知道你想要哪个。两种解法：

\`\`\`python
# 方法1：用 str() 把数字转成字符串
print("成绩" + str(score) + "分")   # str(95) 转成 "95"，再用 + 拼接

# 方法2：用 f-string（推荐，自动处理）
print(f"成绩{score}分")             # f-string 自动把 {} 里的变量转成字符串
\`\`\`

f-string 会自动把 \`{}\` 里的值转成字符串，不用你手动 \`str()\`，所以更方便。本教程统一用 f-string。

## 在线环境的小限制

在线运行没有键盘交互，\`input()\` 会卡住等输入，而浏览器里没法输入。所以本教程的 demo **不用 input()**，而是模拟"已经拿到的输入"——用变量假装是 input 的返回值。但你要知道真实程序里怎么用 \`input()\`。

这个"模拟"思路本身也很重要：写代码时经常需要"假装"某个数据已经有了，先写后面的逻辑，等逻辑写完再接真实数据源。这叫"先假设、后对接"。

## 逐行解读本章 demo

**开头模拟输入**：用 \`name_input = "小明"\` 等变量假装是 input 的返回值。注意 \`age_input = "18"\` 是字符串，不是数字 18——这正是 input 的真实行为。

**类型转换部分**：用 \`int(age_input)\` 把字符串 "18" 转成整数 18，用 \`float(height_input)\` 把 "1.75" 转成浮点 1.75。转换后才能做算术。

**字符串拼接部分**：对比 \`str()\` 转换和 f-string 两种写法，f-string 更简洁。

**转换失败演示**：用 try/except 捕获 \`int("abc")\` 的 ValueError，演示真实程序怎么处理"用户乱输"。

**模拟注册**：用 \`len()\` 数密码长度，判断是否 >= 6 位。这是真实注册逻辑的简化版。

## ⚠️ 初学者常见坑

1. **忘记 input 返回字符串**：\`age = input()\` 后直接 \`age + 1\` 必报错。养成习惯：input 后要算术就 \`int()\` 或 \`float()\`。
2. **int 和 float 搞混**：\`int("3.14")\` 会报错（"3.14" 不是纯整数），要先 \`float("3.14")\` 再 \`int()\`。
3. **字符串拼接数字**：\`"成绩" + 95\` 报错。用 f-string 或 \`str()\`。
4. **不处理异常**：用户乱输就崩。真实程序要 try/except。

## 小结

- \`input()\` 读用户输入，**永远返回字符串**，这是大坑
- 用 \`int()\`、\`float()\`、\`str()\` 做类型转换
- 转换可能失败（\`int("abc")\` 报 ValueError），真实程序要处理
- 字符串和数字不能直接 \`+\`，用 f-string 或 \`str()\` 拼接
- 在线环境用变量模拟 input 的返回值

## 常见疑问 Q&A

**Q：为什么 input 不直接返回数字？**
A：因为 input 不知道用户会输什么——可能输数字、可能输字母。统一返回字符串最安全，需要数字你自己转。

**Q：\`int("3.14")\` 为什么报错？**
A：因为 "3.14" 里有小数点，不是"纯整数字符串"。要 \`int(float("3.14"))\` 先转浮点再转整数。

**Q：怎么让用户多次输入直到合法？**
A：用 while 循环 + try/except，输错了就让用户重新输。学完循环和异常就会写。

**Q：\`input()\` 和 \`raw_input()\` 有什么区别？**
A：Python 3 只有 \`input()\`（返回字符串）。Python 2 有 \`input()\` 和 \`raw_input()\`，但 Python 2 已淘汰，不用管。`,
    code: `# ============================================
# 第 5 章：类型转换
# 在线环境无键盘输入，用变量模拟 input() 的返回值
# ============================================

# 模拟用户在 input() 里输入的字符串
# 真实代码是：name = input("请输入姓名：")
name_input = "小明"
age_input = "18"        # 注意：这是字符串"18"，不是数字18
height_input = "1.75"

print(f"模拟输入：姓名={name_input}, 年龄={age_input}, 身高={height_input}")
print(f"它们的类型：{type(name_input)}, {type(age_input)}, {type(height_input)}")
print("注意：input() 拿到的全是字符串，哪怕长得像数字\\n")

# --- 类型转换 ---
age = int(age_input)          # "18" → 18（整数）
height = float(height_input)  # "1.75" → 1.75（浮点）
print(f"转换后：age={age} 类型={type(age)}")
print(f"转换后：height={height} 类型={type(height)}")

# 现在能做数字运算了
print(f"\\n明年 {age + 1} 岁")
print(f"BMI 示例：{70 / height**2:.2f}")

# --- 数字转字符串：拼接时常用 ---
print("\\n--- 字符串拼接 ---")
score = 95
# 错误写法（注释掉）：print("成绩" + score)  # 字符串不能直接加数字
print("成绩" + str(score) + "分")   # 用 str() 转一下
print(f"成绩{score}分")             # f-string 自动处理，更方便

# --- 转换失败的情况 ---
print("\\n--- 转换失败演示 ---")
bad = "abc"
try:
    int(bad)
except ValueError as e:
    print(f"int(\\"{bad}\\") 失败：{e}")
    print("真实程序要处理这种'用户乱输'的情况（异常那章讲）")

# --- 综合小练习：模拟注册 ---
print("\\n--- 模拟注册 ---")
username = "xiaoming"
password_input = "abc12345"               # 假装用户输入的密码字符串
password_len = len(password_input)        # 用 len() 数出长度
print(f"用户名: {username}")
print(f"密码长度: {password_len} 位")
if password_len >= 6:
    print("密码长度合格 ✓")
else:
    print("密码太短，至少6位")`
  },

  // -----------------------------------------------------------
  // 第 6 章：数字与运算符
  // -----------------------------------------------------------
  {
    id: "py9-06",
    group: "数据：Python 里的东西",
    icon: "🔢",
    title: "数字与运算符：从算术说起",
    content: `## Python 里的数字有两种

数字是编程里最基础的数据类型。Python 里的数字主要分两种：

- **整数（int）**：\`1\`、\`-5\`、\`0\`、\`999999\`。Python 的整数**没有大小上限**，能表示天文数字。这是 Python 的优势——C 语言里 int 有 32 位限制，超过 21 亿就溢出，Python 不会。
- **浮点数（float）**：带小数点的，\`3.14\`、\`-0.5\`、\`1.0\`。注意 \`1.0\` 虽然值是 1，但因为有 小数点，它是 float 不是 int。

还有复数 \`complex\`（\`3+4j\`），日常开发几乎用不到，知道有就行。

为什么区分整数和浮点数？因为计算机存这两种数的方式不同——整数存得精确，浮点数存得"近似"（后面讲精度问题）。所以涉及钱的计算用整数（分），不用浮点数（元），避免精度误差。

## 算术运算符

Python 的算术运算符和数学符号很像，但有几个不同：

| 运算符 | 含义 | 例子 | 结果 | 备注 |
|--------|------|------|------|------|
| \`+\` | 加 | \`5 + 3\` | 8 | |
| \`-\` | 减 | \`5 - 3\` | 2 | |
| \`*\` | 乘 | \`5 * 3\` | 15 | 数学里是 ×，这里用 * |
| \`/\` | 除 | \`7 / 2\` | 3.5 | **总是返回浮点** |
| \`//\` | 整除 | \`7 // 2\` | 3 | 向下取整 |
| \`%\` | 取余 | \`7 % 2\` | 1 | 余数 |
| \`**\` | 幂 | \`2 ** 3\` | 8 | 2 的 3 次方 |

注意 \`*\` 表示乘法（不是 ×），\`**\` 表示幂（不是 ^）。新手容易把 \`^\` 当幂运算，但 \`^\` 在 Python 里是"按位异或"，完全不同的东西。

### 几个容易混的

**\`/\` 永远返回浮点**：\`6 / 2\` 是 \`3.0\` 不是 \`3\`。为什么这样设计？因为除法结果可能是小数（\`7/2=3.5\`），统一返回浮点避免"有时整数有时小数"的混乱。

**\`//\` 是向下取整**，对负数要注意：\`-7 // 2\` 是 \`-4\`（不是 -3）。因为 -3.5 向下取整是 -4（-4 比 -3.5 更小）。新手常以为 \`//\` 是"去掉小数"，对正数是这样，对负数就差一个。

**\`%\` 的符号跟除数一致**：\`-7 % 2\` 是 \`1\`（Python 特性，和 C 不同，C 里是 -1）。这个特性在做"周期性"计算时很有用，比如星期几、月份循环。

### 整除和取余的关系

\`//\` 和 \`%\` 是一对：\`a = (a // b) * b + (a % b)\`。比如 \`7 = (7//2)*2 + (7%2) = 3*2 + 1 = 7\`。理解这个关系，就能灵活运用。

实际用途：
- \`%\` 判断奇偶：\`n % 2 == 0\` 是偶数
- \`%\` 取个位：\`456 % 10 = 6\`
- \`//\` 取位数：\`456 // 100 = 4\`（百位）
- \`//\` 和 \`%\` 配合做"秒转时分秒"

## 运算优先级

和数学一样：括号 > 幂 > 乘除 > 加减。不确定就**加括号**，永远没错。

\`\`\`python
2 + 3 * 4       # 14，先乘后加
(2 + 3) * 4     # 20，括号优先
2 ** 3 ** 2     # 512，幂从右往左（2的9次方）
\`\`\`

⚠️ 幂运算 \`**\` 是**从右往左**结合的，所以 \`2 ** 3 ** 2\` 是 \`2 ** (3**2)\` = \`2 ** 9\` = 512，不是 \`(2**3) ** 2\` = 64。这和数学里的一致（幂运算本来就从右往左），但容易记混。

## 复合赋值运算符

\`\`\`python
x = 10  # 定义数值 x
x += 5    # 等价 x = x + 5
x -= 3    # x = x - 3
x *= 2    # x = x * 2
x //= 4   # x = x // 4
\`\`\`

复合赋值是"运算+赋值"的简写。\`x += 5\` 就是"把 x 加 5 再赋回 x"。所有运算符都有对应的复合版本：\`+=\`、\`-=\`、\`*=\`、\`/=\`、\`//=\`、\`%=\`、\`**=\`。

为什么用复合赋值？一是简洁，二是某些情况下性能更好（不用重复求值左边的变量）。对新手来说，主要是简洁。

## 浮点数精度问题

这是本章最重要的"坑"：\`0.1 + 0.2\` 在 Python 里**不等于 0.3**，而是 \`0.30000000000000004\`。

\`\`\`python
print(0.1 + 0.2 == 0.3)   # False！
\`\`\`

这不是 bug，是计算机用二进制存小数的固有限制。就像十进制没法精确表示 1/3（只能写 0.333...），二进制也没法精确表示 0.1（它是无限循环小数）。所以浮点数有微小误差。

**怎么解决？**
- 用 \`round()\` 四舍五入：\`round(0.1 + 0.2, 2)\` = 0.3
- 涉及钱用 \`decimal\` 模块（精确十进制）
- 比较浮点数用 \`abs(a - b) < 1e-9\`（差值足够小就认为相等）

⚠️ 永远不要用 \`==\` 直接比两个浮点数，会出问题。

## 逐行解读本章 demo

**第 1 部分基本算术**：演示 7 个运算符。注意 \`7 / 2 = 3.5\`（浮点），\`7 // 2 = 3\`（整数），\`7 % 2 = 1\`（余数）。

**第 2 部分负数的坑**：演示 \`-7 // 2 = -4\`（不是 -3）和 \`-7 % 2 = 1\`（符号跟除数）。这是新手最容易算错的地方。

**第 3 部分优先级**：演示 \`2 + 3 * 4 = 14\`、\`(2+3)*4 = 20\`、\`2 ** 3 ** 2 = 512\`（从右往左）。

**第 4 部分复合赋值**：演示 \`x += 10\` 等，每步打印 x 的变化。

**第 5 部分浮点精度**：演示 \`0.1 + 0.2 != 0.3\`，用 \`round()\` 解决。

**第 6 部分实用技巧**：判断奇偶、分离位数、秒转时分秒、大整数。这些是面试常考的"基本功"。

## ⚠️ 初学者常见坑

1. **\`/\` 返回浮点**：\`6 / 2\` 是 \`3.0\` 不是 \`3\`。要整数用 \`//\`。
2. **负数整除**：\`-7 // 2 = -4\` 不是 \`-3\`。向下取整！
3. **\`^\` 不是幂**：\`2 ^ 3\` 是按位异或，不是 8。幂用 \`**\`。
4. **浮点数不能 ==**：\`0.1 + 0.2 == 0.3\` 是 False。用 \`round()\` 或差值比较。
5. **整数除以 0 报错**：\`5 / 0\` 会 ZeroDivisionError。要先判断除数是否为 0。

## 小结

- Python 整数无大小限制，浮点数有小数点
- \`/\` 总返回浮点，\`//\` 整除，\`%\` 取余，\`**\` 幂
- \`//\` 和 \`%\` 对负数有特殊规则，要小心
- 复合赋值 \`+=\`、\`-=\` 等是简写
- 浮点数有精度问题，不能直接 \`==\`，用 \`round()\` 或差值比较

## 常见疑问 Q&A

**Q：为什么 \`0.1 + 0.2 != 0.3\`？**
A：二进制无法精确表示 0.1（就像十进制无法精确表示 1/3）。所以浮点数有微小误差。涉及精确计算用 \`decimal\` 模块。

**Q：\`//\` 和 \`%\` 有什么用？**
A：取余判断奇偶、取个位、做周期循环；整除取位数、做时间转换。配合使用能解决很多"拆数"问题。

**Q：怎么判断一个数是不是整数？**
A：\`isinstance(x, int)\` 或 \`x == int(x)\`（后者对 3.0 也返回 True，因为 3.0 == 3）。

**Q：Python 的大整数怎么实现的？**
A：Python 的 int 自动扩展位数，底层用数组存。你不用担心溢出，想算 10 的 100 次方都行。`,
    code: `# ============================================
# 第 6 章：数字与运算符
# ============================================

# --- 1. 基本算术 ---
print("=== 1. 基本算术 ===")
print(f"5 + 3 = {5 + 3}")
print(f"5 - 3 = {5 - 3}")
print(f"5 * 3 = {5 * 3}")
print(f"7 / 2 = {7 / 2}     ← / 总是返回浮点")
print(f"6 / 2 = {6 / 2}     ← 即使整除也是浮点")
print(f"7 // 2 = {7 // 2}    ← // 整除，去掉小数")
print(f"7 % 2 = {7 % 2}      ← % 取余数")
print(f"2 ** 10 = {2 ** 10}  ← ** 幂运算，2的10次方")

# --- 2. 整除和取余的坑（负数）---
print("\\n=== 2. 负数的整除和取余 ===")
print(f"7 // 2 = {7 // 2}      （正常）")
print(f"-7 // 2 = {-7 // 2}    （向下取整，-4 而不是-3！）")
print(f"7 % 2 = {7 % 2}")
print(f"-7 % 2 = {-7 % 2}      （Python 取余符号跟除数，结果是 1）")
print("记住：// 是'向下取整'不是'去掉小数'，负数时差一个")

# --- 3. 运算优先级 ---
print("\\n=== 3. 优先级 ===")
print(f"2 + 3 * 4 = {2 + 3 * 4}        ← 先乘除后加减，=14")
print(f"(2 + 3) * 4 = {(2 + 3) * 4}      ← 括号优先，=20")
print(f"2 + 3 ** 2 = {2 + 3 ** 2}      ← 幂高于加减，=11")
print(f"2 ** 3 ** 2 = {2 ** 3 ** 2}    ← 幂从右往左，=2的9次方=512")

# --- 4. 复合赋值 ---
print("\\n=== 4. 复合赋值 ===")
x = 100
x += 10;  print(f"x += 10 → {x}")
x -= 20;  print(f"x -= 20 → {x}")
x *= 3;   print(f"x *= 3 → {x}")
x //= 5;  print(f"x //= 5 → {x}")
x %= 7;   print(f"x %= 7 → {x}")
x **= 2;  print(f"x **= 2 → {x}")

# --- 5. 浮点精度问题 ---
print("\\n=== 5. 浮点精度 ===")
print(f"0.1 + 0.2 = {0.1 + 0.2}")
print(f"0.1 + 0.2 == 0.3? {0.1 + 0.2 == 0.3}    ← 竟然不等！")
# 解决办法：用 round 或 decimal
print(f"round(0.1+0.2, 2) = {round(0.1 + 0.2, 2)}    ← 用 round 处理")

# --- 6. 实用技巧 ---
print("\\n=== 6. 实用技巧 ===")
# 判断奇偶
n = 17
print(f"{n} 是{'偶数' if n % 2 == 0 else '奇数'}")
# 位数分离：取百位十位个位
num = 456
print(f"{num} 的百位={num // 100}, 十位={num // 10 % 10}, 个位={num % 10}")
# 秒转时分秒
total = 3666
print(f"{total}秒 = {total // 3600}时 {(total % 3600) // 60}分 {total % 60}秒")
# 大整数也没问题
big = 10 ** 50
print(f"10的50次方 = {big}")`
  },

  // -----------------------------------------------------------
  // 第 7 章：字符串基础
  // -----------------------------------------------------------
  {
    id: "py9-07",
    group: "数据：Python 里的东西",
    icon: "🔤",
    title: "字符串（一）：创建、拼接、索引、切片",
    content: `## 字符串是什么

字符串就是**一串文字**，用引号包起来。这是编程里处理文本的核心类型——你看到的每句话、每个文件名、每条 URL 都是字符串。

Python 单引号 \`'\` 和双引号 \`"\` 完全等价，习惯用哪个都行。三引号 \`"""\` 能包多行。

\`\`\`python
s1 = 'hello'           # 单引号字符串
s2 = "hello"           # 双引号字符串（和单引号完全等价）
s3 = """这是            # 三引号字符串，可以跨多行
多行"""
\`\`\`

为什么有两种引号？为了"嵌套"方便——如果字符串里有单引号，就用双引号包：\`"it's ok"\`；如果有双引号，就用单引号包：\`'他说"你好"\`。这样不用转义。

三引号字符串的特殊之处是**保留换行**——你写在里面的回车会真的变成换行。这适合写长文本、文档、多行提示信息。

## 字符串不可变

这是**极其重要**的概念：**字符串一旦创建，里面的字符不能改**。

\`\`\`python
s = "hello"  # 定义字符串 s
s[0] = "H"   # 报错！TypeError: 'str' object does not support item assignment
\`\`\`

为什么字符串不可变？这是 Python（和很多语言）的设计选择。好处是：安全（不会被意外修改）、可哈希（能当字典的 key）、性能优化（相同字符串可共享）。坏处是"修改"要造新字符串，但 Python 优化得很好，不用担心性能。

想"改"只能造一个新的：\`s = "H" + s[1:]\`。注意这是"造新字符串再贴标签"，不是"改原字符串"。

\`\`\`python
s = "hello"  # 定义字符串 s
s = "H" + s[1:]   # s 现在指向新的 "Hello"，原来的 "hello" 还在内存里（会被回收）
\`\`\`

## 拼接和重复

\`\`\`python
"ab" + "cd"      # "abcd" 拼接
"ha" * 3         # "hahaha" 重复
\`\`\`

\`+\` 对字符串是"拼接"，\`*\` 对字符串是"重复"。注意 \`*\` 的右边必须是整数。\`"ha" * 3\` 和 \`3 * "ha"\` 等价。这俩运算和数字的 \`+\`、\`*\` 是同一个符号，但含义不同——Python 根据操作数类型决定行为，这叫"运算符重载"。

## 索引：取一个字符

字符串里每个字符有个**编号**，从 0 开始。**支持负数**，-1 是最后一个。

\`\`\`python
s = "Python"  # 定义字符串 s
s[0]    # 'P'   第1个
s[1]    # 'y'
s[-1]   # 'n'   最后1个
s[-2]   # 'o'   倒数第2
\`\`\`

为什么从 0 开始？这是编程界的传统（C 语言带的头）。好处是"偏移量"概念统一——\`s[0]\` 表示"偏移 0 个位置"，\`s[1]\` 表示"偏移 1 个位置"。理解成"距离开头的距离"就好。

记忆口诀：\`s[0]\` 是开头，\`s[-1]\` 是结尾，负号表示"从后往前数"。

⚠️ **注意**：索引越界会报错。\`s = "Python"\` 长度 6，\`s[6]\` 或 \`s[7]\` 会 IndexError。负数也别超：\`s[-7]\` 也报错。

## 切片：取一段

\`s[起:止]\` 取从"起"到"止-1"的子串（**包含起，不包含止**）：

\`\`\`python
s = "Python"  # 定义字符串 s
s[0:3]    # 'Pyt'    第0,1,2个
s[2:5]    # 'tho'
s[:3]     # 'Pyt'    省略起=从头
s[3:]     # 'hon'    省略止=到尾
s[:]      # 'Python' 全部（复制一份）
s[-3:]    # 'hon'    最后3个
\`\`\`

为什么"包含起不包含止"？这叫"左闭右开"区间。好处是：\`s[0:3]\` 的长度正好是 \`3-0=3\`，\`s[3:6]\` 接着 \`s[0:3]\` 不重叠不遗漏。这个设计让切片拼接很自然。

切片还有第三个参数**步长**：\`s[起:止:步长]\`

\`\`\`python
s[::2]    # 'Pto'    隔一个取一个
s[::-1]   # 'nohtyP' 反转字符串！
\`\`\`

\`s[::-1]\` 是"反转字符串"的经典写法——步长 -1 表示"从后往前每步取 1 个"，结果就是反转。这个技巧面试常考，记住。

切片的妙处：**越界不报错**。\`s[0:100]\` 不会报错，只会取到末尾。这比索引安全，因为切片对越界"宽容"。

## len() 看长度

\`\`\`python
len("hello")    # 5
\`\`\`

\`len()\` 返回字符串长度（字符个数）。注意中文字符在 Python 3 里算 1 个长度（\`len("你好")\` = 2），因为 Python 3 字符串是 Unicode。

## 逐行解读本章 demo

**第 1 部分创建**：演示单引号、双引号、三引号三种创建方式。三引号能跨行。

**第 2 部分拼接重复**：\`"hello" + " " + "world"\` 拼接，\`"ha" * 3\` 重复，\`"-" * 30\` 画分隔线。

**第 3 部分索引**：演示 \`s[0]\`、\`s[1]\`、\`s[-1]\`、\`s[-2]\`。正数从前往后，负数从后往前。

**第 4 部分切片**：演示 \`s[0:3]\`、\`s[2:5]\`、\`s[:3]\`、\`s[3:]\`、\`s[-3:]\`。重点理解"包含起不包含止"。

**第 5 部分步长**：\`s[::2]\` 隔一取一，\`s[::-1]\` 反转，\`s[1::2]\` 从第 1 个开始隔一取。

**第 6 部分长度遍历**：\`len()\` 看长度，\`enumerate()\` 同时拿下标和字符。enumerate 是遍历时"既想要位置又想要值"的利器。

**第 7 部分不可变**：演示想改 \`s[0]\` 会报错，正确做法是 \`"H" + s[1:]\` 造新字符串。

**第 8 部分实用例子**：手机号脱敏（前 3 后 4，中间星号）、取文件后缀、反转字符串。这些是工作中真实用到的。

## ⚠️ 初学者常见坑

1. **索引越界**：\`s = "abc"\`，\`s[3]\` 报错。记住长度 3 的字符串索引是 0、1、2。
2. **混淆索引和切片**：\`s[0]\` 是字符（长度 1），\`s[0:1]\` 是字符串（长度 1）。类型不同。
3. **以为字符串能改**：\`s[0] = "H"\` 报错。字符串不可变，要造新的。
4. **切片方向搞反**：\`s[3:0]\` 是空字符串（止比起小，正步长取不到）。要倒着取用 \`s[3:0:-1]\`。

## 小结

- 字符串用引号包，单双引号等价，三引号可跨行
- 字符串**不可变**，"改"要造新字符串
- \`+\` 拼接，\`*\` 重复，\`len()\` 看长度
- 索引从 0 开始，支持负数（-1 是末尾）
- 切片 \`s[起:止:步长]\`，左闭右开，\`s[::-1]\` 反转

## 常见疑问 Q&A

**Q：为什么索引从 0 开始？**
A：编程传统（C 语言带的）。理解成"偏移量"——\`s[0]\` 是偏移 0 个位置，即第一个。

**Q：\`s[0:3]\` 为什么不包含第 3 个？**
A：左闭右开区间设计。好处是长度 = 止 - 起，拼接不重叠。习惯就好。

**Q：字符串不可变有什么好处？**
A：安全（不会被意外改）、可哈希（能当字典 key）、Python 能优化内存（相同字符串共享）。

**Q：怎么反转字符串？**
A：\`s[::-1]\` 是最 Pythonic 的写法。原理是步长 -1 从后往前取。也可以用 \`''.join(reversed(s))\`，但 \`s[::-1]\` 更简洁。`,
    code: `# ============================================
# 第 7 章：字符串基础
# ============================================

s = "Python"

# --- 1. 创建 ---
print("=== 1. 创建 ===")
a = '单引号'
b = "双引号"
c = """三引号
可以多行"""
print(a, b, c, sep=" | ")

# --- 2. 拼接和重复 ---
print("\\n=== 2. 拼接和重复 ===")
print("hello" + " " + "world")     # 拼接
print("ha" * 3)                     # 重复
print("-" * 30)                     # 画分隔线常用

# --- 3. 索引 ---
print("\\n=== 3. 索引 ===")
print(f"s = '{s}'")
print(f"s[0]  = '{s[0]}'    ← 第1个")
print(f"s[1]  = '{s[1]}'")
print(f"s[-1] = '{s[-1]}'   ← 最后1个")
print(f"s[-2] = '{s[-2]}'   ← 倒数第2")

# --- 4. 切片（重点！）---
print("\\n=== 4. 切片 ===")
print(f"s[0:3]  = '{s[0:3]}'    ← 取0,1,2，不含3")
print(f"s[2:5]  = '{s[2:5]}'")
print(f"s[:3]   = '{s[:3]}'    ← 省起=从头")
print(f"s[3:]   = '{s[3:]}'    ← 省止=到尾")
print(f"s[-3:]  = '{s[-3:]}'   ← 最后3个字符")

# --- 5. 步长 ---
print("\\n=== 5. 步长 ===")
print(f"s[::2]   = '{s[::2]}'    ← 隔一个取一个")
print(f"s[::-1]  = '{s[::-1]}'   ← 反转！常用技巧")
print(f"s[1::2]  = '{s[1::2]}'   ← 从第1个开始，隔一取")

# --- 6. len 和遍历 ---
print("\\n=== 6. 长度和遍历 ===")
print(f"len(s) = {len(s)}")
for i, ch in enumerate(s):       # enumerate 同时给下标和字符
    print(f"  第{i}个: {ch}")

# --- 7. 字符串不可变 ---
print("\\n=== 7. 不可变 ===")
# s[0] = "H"  # 这行会报错
# 想"改"要造新的
new_s = "H" + s[1:]
print(f"'改'首字母: '{s}' → '{new_s}'")

# --- 8. 实用例子 ---
print("\\n=== 8. 实用例子 ===")
phone = "13800138000"
print(f"手机号脱敏: {phone[:3]}****{phone[-4:]}")
filename = "report_2025_07.csv"
print(f"取后缀: {filename[-4:]}")
print(f"反转: {''.join(reversed(s))}")`
  },

  // -----------------------------------------------------------
  // 第 8 章：字符串方法与格式化
  // -----------------------------------------------------------
  {
    id: "py9-08",
    group: "数据：Python 里的东西",
    icon: "🛠️",
    title: "字符串（二）：方法与格式化",
    content: `## 什么是"方法"

字符串自带很多"动作"，叫**方法**，用 \`.\` 调用：

\`\`\`python
"hello".upper()    # 'HELLO'  把字符串转大写
\`\`\`

方法可以理解为"对象自带的函数"。\`"hello"\` 是字符串对象，\`.upper()\` 是它的方法。这就像"对象.动作"——\`狗.叫()\`、\`车.开()\`。你不用关心怎么实现的，调用就行。

方法**不会改原字符串**（字符串不可变），而是返回一个新的。这是新手常犯的错：

\`\`\`python
s = "hello"  # 定义字符串 s
s.upper()        # 返回 "HELLO"，但 s 还是 "hello"！
print(s)         # "hello"
s = s.upper()    # 这样才行，把返回的新字符串再赋给 s
print(s)         # "HELLO"
\`\`\`

记住：**字符串方法都返回新字符串，原字符串不变**。要"改"就得接住返回值。

## 常用方法

字符串方法很多，但常用的就这几类。不用全背，用的时候查文档或让 AI 帮你找。

### 大小写
\`\`\`python
s.upper()      # 全大写
s.lower()      # 全小写
s.title()      # 每个单词首字母大写
s.capitalize() # 首字母大写
s.swapcase()   # 大小写互换
\`\`\`

这些在做"大小写不敏感"比较时很有用：\`if name.lower() == "admin"\`，不管用户输 "Admin" 还是 "ADMIN" 都能匹配。

### 查找
\`\`\`python
s.find("xx")   # 找子串，返回下标，找不到返回 -1
s.index("xx")  # 同上，找不到抛异常
s.count("xx")  # 出现几次
"xx" in s      # 是否包含，返回布尔
\`\`\`

\`find\` 和 \`index\` 的区别：\`find\` 找不到返回 -1（不报错），\`index\` 找不到抛 ValueError。一般用 \`find\` 更安全，或者直接用 \`in\` 判断"有没有"。

### 替换和分割
\`\`\`python
s.replace("a", "b")   # 把 a 替换成 b
s.split(",")          # 按逗号切成列表
s.split()             # 按空白切（默认）
",".join(list)        # 用逗号把列表拼回字符串
\`\`\`

\`split\` 和 \`join\` 是一对：\`split\` 把字符串切成列表，\`join\` 把列表拼回字符串。处理 CSV、解析数据时常用。注意 \`join\` 的写法有点反直觉——是 \`"分隔符".join(列表)\`，不是 \`列表.join("分隔符")\`。

### 去空白
\`\`\`python
s.strip()    # 去首尾空白
s.lstrip()   # 去左
s.rstrip()   # 去右
\`\`\`

处理用户输入时必用——用户可能在名字前后多打了空格，\`strip()\` 帮你清掉。注意只去首尾，中间的空格不动。

### 判断
\`\`\`python
s.isdigit()   # 是不是全数字
s.isalpha()   # 是不是全字母
s.startswith("xx")  # 以xx开头
s.endswith(".py")   # 以.py结尾
\`\`\`

这些方法返回布尔值，适合用在 if 判断里。比如判断文件是不是 Python 文件：\`if filename.endswith(".py")\`。

## 格式化：三种方式

"格式化"就是把变量的值填进字符串里。Python 有三种方式，推荐用 f-string。

### 1. f-string（推荐）
\`\`\`python
name = "小明"; score = 95.678  # 定义字符串 name
print(f"{name}考了{score:.1f}分")   # 小明考了95.7分
\`\`\`

f-string 是 Python 3.6+ 的语法，最直观最高效。格式控制语法 \`{值:格式}\`：

| 格式 | 含义 | 例子 | 结果 |
|------|------|------|------|
| \`:.2f\` | 保留2位小数 | \`{3.14159:.2f}\` | 3.14 |
| \`:>10\` | 右对齐宽10 | \`{"hi":>10}\` | "        hi" |
| \`:<10\` | 左对齐 | \`{"hi":<10}\` | "hi        " |
| \`:^10\` | 居中 | \`{"hi":^10}\` | "    hi    " |
| \`:0>5\` | 用0填充到宽5 | \`{42:0>5}\` | "00042" |
| \`,.2f\` | 千分位+2位小数 | \`{1234567.89:,.2f}\` | 1,234,567.89 |

这些格式化在做报表、对齐输出时极其有用。比如做表格用 \`:<8\` 左对齐宽 8，做编号用 \`:0>5\` 补零。

### 2. format 方法（老式）
\`\`\`python
"{}考了{}分".format(name, score)   # format 方法：{} 按顺序填入参数
\`\`\`

这是 f-string 出现前的主流写法。现在新代码用 f-string，遇到老代码能看懂就行。

### 3. % 占位符（更老）
\`\`\`python
"%s考了%.1f分" % (name, score)   # % 占位符：%s 字符串，%.1f 保留1位小数
\`\`\`

这是从 C 语言继承的写法，\`%s\` 是字符串，\`%d\` 是整数，\`%.1f\` 是 1 位小数浮点。现在基本不用，知道有这回事即可。

## 逐行解读本章 demo

**第 1 部分大小写**：演示 upper、lower、title、capitalize。注意原字符串 \`s\` 有首尾空格，所以输出带着空格。

**第 2 部分去空白**：\`strip()\` 去首尾空格，\`lstrip()\` 只去左，\`rstrip()\` 只去右。中间空格不动。

**第 3 部分查找**：\`find\` 找到返回下标，找不到返回 -1；\`count\` 数出现次数；\`in\` 判断是否包含。

**第 4 部分替换**：\`replace\` 替换子串，还能指定替换次数（第 3 个参数）。

**第 5 部分分割拼接**：\`split(",")\` 按逗号切，\`" | ".join(parts)\` 用" | "拼回。注意 \`split()\` 不传参默认按任意空白切。

**第 6 部分判断**：\`isdigit\`、\`isalpha\`、\`isalnum\`、\`endswith\`、\`startswith\` 返回布尔值。

**第 7 部分格式化**：演示 f-string 的 \`:>8\`、\`:<8\`、\`,.2f\` 等，并对比三种格式化方式。

**第 8 部分实用例子**：解析 \`"  张三 , 25 , 北京  "\` 这种脏数据——先 \`split(",")\` 切开，再对每段 \`strip()\` 清洗。

## ⚠️ 初学者常见坑

1. **方法返回新字符串，原字符串不变**：\`s.upper()\` 后 \`s\` 还是原样，要 \`s = s.upper()\`。
2. **\`join\` 写法反直觉**：是 \`"分隔符".join(列表)\`，不是 \`列表.join("分隔符")\`。
3. **\`strip\` 只去首尾**：中间的空格不去。要去所有空格用 \`s.replace(" ", "")\`。
4. **f-string 忘加 f**：\`"{name}"\` 不加 f 会原样输出 \`{name}\`，不会替换。
5. **\`find\` 和 \`index\` 混用**：\`find\` 找不到返回 -1，\`index\` 找不到报错。用 \`find\` 更安全。

## 小结

- 方法是"对象.动作"，字符串方法返回新字符串，原字符串不变
- 常用方法：大小写、查找、替换、分割拼接、去空白、判断
- \`split\` 切字符串成列表，\`join\` 把列表拼回字符串
- 格式化推荐 f-string：\`f"{值:格式}"\`
- \`:.2f\` 保留小数，\`:>10\` 右对齐，\`,.2f\` 千分位

## 常见疑问 Q&A

**Q：方法记不住怎么办？**
A：不用全背。记住常用几个（upper、lower、strip、split、join、replace、find），其他用的时候查文档或问 AI。用多了自然就记住了。

**Q：\`find\` 和 \`index\` 用哪个？**
A：一般用 \`find\`，因为它找不到返回 -1 不报错，更安全。\`index\` 适合"确定能找到"的场景，找不到抛异常提醒你。

**Q：f-string 和 \`.format()\` 哪个快？**
A：f-string 更快（Python 3.6+ 优化过），而且写法更直观。新代码统一用 f-string。

**Q：怎么把字符串里的所有空格去掉？**
A：\`s.replace(" ", "")\` 替换所有空格。要去所有空白（包括制表符、换行）用 \`"".join(s.split())\`。`,
    code: `# ============================================
# 第 8 章：字符串方法与格式化
# ============================================

s = "  Hello, World  "

# --- 1. 大小写 ---
print("=== 1. 大小写 ===")
print(f"原: '{s}'")
print(f"upper:       '{s.upper()}'")
print(f"lower:       '{s.lower()}'")
print(f"title:       '{s.title()}'")
print(f"capitalize:  '{s.capitalize()}'")

# --- 2. 去空白 ---
print("\\n=== 2. 去空白 ===")
print(f"strip:  '{s.strip()}'    ← 首尾空白都去掉")
print(f"lstrip: '{s.lstrip()}'")
print(f"rstrip: '{s.rstrip()}'")

# --- 3. 查找 ---
print("\\n=== 3. 查找 ===")
t = s.strip()
print(f"'{t}'.find('World') = {t.find('World')}    ← 找到返回下标")
print(f"'{t}'.find('xxx')   = {t.find('xxx')}      ← 找不到返回-1")
print(f"'{t}'.count('l')    = {t.count('l')}        ← l出现几次")
print(f"'World' in '{t}'    = {'World' in t}")

# --- 4. 替换 ---
print("\\n=== 4. 替换 ===")
print(f"'{t}'.replace('World', 'Python') = '{t.replace('World', 'Python')}'")
# 替换还能指定次数
print(f"'aaa'.replace('a', 'b', 2) = '{'aaa'.replace('a', 'b', 2)}'")

# --- 5. 分割和拼接 ---
print("\\n=== 5. 分割和拼接 ===")
csv = "张三,25,北京,程序员"
parts = csv.split(",")
print(f"'{csv}'.split(',') = {parts}")
# join 把列表拼回字符串
print(f"' | '.join(parts) = {' | '.join(parts)}")

# 默认 split 按任意空白切，多个空格也算一个
print(f"'a  b   c'.split() = {'a  b   c'.split()}")

# --- 6. 判断 ---
print("\\n=== 6. 判断 ===")
print(f"'123'.isdigit()       = {'123'.isdigit()}")
print(f"'abc'.isalpha()       = {'abc'.isalpha()}")
print(f"'abc123'.isalnum()    = {'abc123'.isalnum()}")
print(f"'test.py'.endswith('.py') = {'test.py'.endswith('.py')}")
print(f"'Hello'.startswith('He')  = {'Hello'.startswith('He')}")

# --- 7. 格式化 ---
print("\\n=== 7. 格式化 ===")
name = "小明"
score = 95.678
price = 1234567.891

# f-string（推荐）
print(f"{name}考了{score:.1f}分")          # 保留1位小数
print(f"{name}考了{score:08.2f}分")        # 总宽8，不足补0
print(f"{'姓名':<8}{'分数':>8}")            # 对齐
print(f"价格: {price:,.2f} 元")            # 千分位

# 对比三种方式
print("\\n--- 三种格式化对比 ---")
print(f"{name}成绩{score:.1f}")                    # f-string
print("{}成绩{:.1f}".format(name, score))          # format
print("%s成绩%.1f" % (name, score))                # % 占位符

# --- 8. 实用：解析一行数据 ---
print("\\n=== 8. 实用例子 ===")
line = "  张三 , 25 , 北京  "
clean = [x.strip() for x in line.split(",")]
print(f"原始: '{line}'")
print(f"清洗: {clean}")`
  },

  // -----------------------------------------------------------
  // 第 9 章：布尔与比较
  // -----------------------------------------------------------
  {
    id: "py9-09",
    group: "数据：Python 里的东西",
    icon: "🚦",
    title: "布尔值、比较与逻辑运算",
    content: `## 布尔值：真与假

布尔类型只有两个值：\`True\`（真）和 \`False\`（假）。**首字母大写**，不是 \`true\`/\`false\`（那是 JavaScript 的写法）。

\`\`\`python
t = True            # 布尔值 True（真），首字母必须大写
f = False           # 布尔值 False（假）
print(type(t))   # <class 'bool'>  查看类型
\`\`\`

布尔是程序"做决定"的基础——下一章的 if 判断，本质就是在判断一个布尔值。你可以把布尔理解为"开关"：True 是开，False 是关，程序根据开关状态决定走哪条路。

为什么需要布尔？因为现实世界里到处是"是/否"判断：用户成年了吗？密码对吗？文件存在吗？这些问题的答案就是 True 或 False，程序据此做不同处理。

## 比较运算符：产生布尔值

比较运算符比较两个值，返回 True 或 False：

| 运算符 | 含义 | 例子 | 结果 |
|--------|------|------|------|
| \`==\` | 等于 | \`5 == 5\` | True |
| \`!=\` | 不等 | \`5 != 3\` | True |
| \`>\` | 大于 | \`5 > 3\` | True |
| \`<\` | 小于 | \`5 < 3\` | False |
| \`>=\` | 大于等于 | \`5 >= 5\` | True |
| \`<=\` | 小于等于 | \`5 <= 3\` | False |

⚠️ \`==\` 是"判断相等"，\`=\` 是"赋值"，**别混**。这是新手最常犯的错：

\`\`\`python
if age = 18:     # 错！这是赋值，不是判断
if age == 18:    # 对！这是判断相等
\`\`\`

为什么 Python 用 \`==\` 表示判断？因为 \`=\` 已经被"赋值"占用了，判断相等只能用别的符号。这是从 C 语言继承的传统，几乎所有语言都这样。

### 字符串也能比较

\`\`\`python
'a' < 'b'          # True，按字典序
'apple' < 'banana' # True，a 开头比 b 开头小
'Apple' < 'apple'  # True，大写字母比小写小（ASCII 码）
\`\`\`

字符串比较按"字典序"——逐个字符比 ASCII 码。注意大写字母比小写字母小（\`'A'\` 是 65，\`'a'\` 是 97），所以排序时大小写会影响结果。要不区分大小写先 \`lower()\`。

### 链式比较（Python 特色）

\`\`\`python
0 < x < 10    # 等价 0 < x and x < 10
\`\`\`

数学里的写法直接能用，很舒服。这是 Python 的特色，大多数语言不支持，要拆成两个比较用 and 连。比如判断年龄在某范围：\`18 <= age <= 60\`，比 \`age >= 18 and age <= 60\` 清楚多了。

## 逻辑运算符

逻辑运算符把多个布尔值组合成一个新的布尔值：

| 运算符 | 含义 | 真值表 |
|--------|------|--------|
| \`and\` | 且：两边都真才真 | True and True = True，其余 False |
| \`or\` | 或：一边真就真 | False or False = False，其余 True |
| \`not\` | 非：取反 | not True = False，not False = True |

\`\`\`python
age = 20  # 定义数值 age
age >= 18 and age <= 60    # 成年且未老年 → True
age < 18 or age > 60       # 未成年或老年 → False
not True                   # False
\`\`\`

理解口诀：
- \`and\` 像"串联开关"——两个开关都按才通电
- \`or\` 像"并联开关"——任一开关按就通电
- \`not\` 像"反相器"——开变关，关变开

### 短路求值

这是逻辑运算的"性能优化"特性，理解它很重要：

\`and\` 如果左边是假，右边就不算了（结果已定，必定 False）。\`or\` 如果左边是真，右边也不算了（结果已定，必定 True）。这叫**短路**。

\`\`\`python
False and (1/0)    # 不会报错，右边没算
True or (1/0)      # 不会报错
\`\`\`

如果没短路，\`1/0\` 会报 ZeroDivisionError。但短路了，右边根本没执行，所以不报错。

短路的实际用途——做"默认值"：

\`\`\`python
name = ""  # 定义字符串 name
display = name or "匿名"     # name 为空（假），取后面的 "匿名"
\`\`\`

这里 \`name or "匿名"\`：\`name\` 是空字符串（假），所以 \`or\` 短路返回右边的 "匿名"。如果 \`name\` 非空（真），就返回 name。这比 \`if name: ... else: ...\` 简洁。

## 真值与假值

这是 Python 一个很"灵活"的特性：不是只有 \`True\`/\`False\` 能判断，其他类型的值也能当布尔用。Python 把这些当作"假"：

- \`False\`、\`0\`、\`0.0\`（零值）
- 空字符串 \`""\`、空列表 \`[]\`、空字典 \`{}\`、空元组 \`()\`（空容器）
- \`None\`（表示"没有"）

其他都是"真"。记住口诀："**空的、零的、没有的**是假，其余是真"。

\`\`\`python
if "":        # 假
if "hello":   # 真
if []:        # 假
if [0]:       # 真（列表非空，即使元素是0）
if 0:         # 假
if -1:        # 真（非零就是真）
\`\`\`

这个特性很常用。判断"列表非空"直接 \`if my_list:\`，判断"字符串非空"直接 \`if name:\`，比 \`if len(my_list) > 0:\` 简洁，这叫 Pythonic 写法。

⚠️ **注意**：\`if [0]:\` 是真——因为列表非空（里面有个 0），列表本身的"真假"看的是"空不空"，不看元素。同理 \`if [False]:\` 也是真。

## 逐行解读本章 demo

**第 1 部分布尔值**：演示 True/False 和它们的类型。强调首字母大写。

**第 2 部分比较**：演示 \`==\`、\`!=\`、\`>\`、\`<=\`，以及字符串比较（字典序）。注意 \`'a' < 'b'\` 是 True，按 ASCII 码比。

**第 3 部分链式比较**：\`0 < x < 10\` 等价 \`0 < x and x < 10\`，Python 特色写法。

**第 4 部分逻辑运算**：演示 and、or、not。注意优先级：\`not\` > \`and\` > \`or\`，不确定就加括号。

**第 5 部分短路求值**：\`False and (1/0)\` 不报错，\`True or (1/0)\` 不报错。还演示 \`name or "匿名"\` 做默认值。

**第 6 部分真值假值**：用 \`bool()\` 函数把各种值转成布尔，看哪些是 False。注意 \`[0]\`、\`[None]\`、\`{"k": 0}\` 都是真（非空容器）。

**第 7 部分判断空**：演示 Pythonic 写法——\`if items:\` 判断列表非空，\`if not name:\` 判断字符串为空。比 \`if len(items) > 0:\` 简洁。

## ⚠️ 初学者常见坑

1. **\`==\` 和 \`=\` 混**：\`if age = 18\` 是赋值，会报错或产生意外。判断相等用 \`==\`。
2. **布尔首字母小写**：\`true\`/\`false\` 会报 NameError，要写 \`True\`/\`False\`。
3. **以为 \`0\` 是真**：\`0\` 是假，\`0.0\` 也是假。但 \`[0]\` 是真（列表非空）。
4. **浮点数用 ==**：\`0.1 + 0.2 == 0.3\` 是 False（精度问题）。用 \`abs(a-b) < 1e-9\`。
5. **\`and\`/\`or\` 返回的不一定是布尔**：\`name or "匿名"\` 返回的是 name 或 "匿名"，不是 True/False。这叫"短路返回操作数"。

## 小结

- 布尔只有 \`True\`/\`False\`，首字母大写，是程序做决定的基础
- 比较运算符产生布尔值，\`==\` 判断相等，\`=\` 赋值，别混
- \`and\`（且）、\`or\`（或）、\`not\`（非）组合布尔值
- 短路求值：\`and\` 左假就不算右，\`or\` 左真就不算右
- 真值假值：空、零、None 是假，其余是真。\`if my_list:\` 判断非空

## 常见疑问 Q&A

**Q：为什么 \`if [0]:\` 是真？**
A：因为列表非空（里面有 0 这个元素）。容器的真假看"空不空"，不看元素内容。\`[]\` 是假，\`[0]\`、\`[False]\`、\`[None]\` 都是真。

**Q：\`and\`/\`or\` 返回的是什么？**
A：返回的是"决定结果的那个操作数"，不一定是布尔。比如 \`name or "匿名"\`，name 非空就返回 name，name 为空就返回 "匿名"。利用这点能做默认值。

**Q：怎么判断两个浮点数相等？**
A：用 \`abs(a - b) < 1e-9\`（差值足够小）。直接 \`==\` 会因精度问题出错。

**Q：\`not\`、\`and\`、\`or\` 优先级？**
A：\`not\` 最高，\`and\` 次之，\`or\` 最低。比如 \`not a and b\` 是 \`(not a) and b\`。不确定就加括号，永远没错。`,
    code: `# ============================================
# 第 9 章：布尔、比较、逻辑
# ============================================

# --- 1. 布尔值 ---
print("=== 1. 布尔值 ===")
t = True
f = False
print(f"t = {t}, 类型 {type(t)}")
print(f"f = {f}, 类型 {type(f)}")
print(f"注意首字母大写，不是 true/false")

# --- 2. 比较 ---
print("\\n=== 2. 比较 ===")
print(f"5 == 5 : {5 == 5}")
print(f"5 != 3 : {5 != 3}")
print(f"5 > 3  : {5 > 3}")
print(f"5 <= 5 : {5 <= 5}")
print(f"'a' < 'b' : {'a' < 'b'}    ← 字符串也能比（按字典序）")
print(f"'apple' < 'banana' : {'apple' < 'banana'}")

# 链式比较（Python 特色）
x = 5
print(f"\\nx = {x}")
print(f"0 < x < 10 : {0 < x < 10}    ← 链式比较，等价 0<x and x<10")

# --- 3. 逻辑运算 ---
print("\\n=== 3. 逻辑运算 ===")
age = 25
print(f"age = {age}")
print(f"age >= 18 and age <= 60 : {age >= 18 and age <= 60}")
print(f"age < 18 or age > 60    : {age < 18 or age > 60}")
print(f"not (age > 30)          : {not (age > 30)}")

# --- 4. 短路求值 ---
print("\\n=== 4. 短路求值 ===")
# and 左边假，右边不算（所以 1/0 不会执行，不报错）
result = False and (1 / 0)
print(f"False and (1/0) = {result}    ← 右边没算，不报错")
result = True or (1 / 0)
print(f"True or (1/0) = {result}      ← 右边没算，不报错")

# 利用短路做默认值
name = ""
display = name or "匿名"     # name 为空（假），取后面的
print(f"name or '匿名' = '{display}'")

# --- 5. 真值假值 ---
print("\\n=== 5. 真值假值 ===")
print("以下都被当作'假':")
for v in [False, 0, 0.0, "", [], {}, None]:
    print(f"  bool({v!r}) = {bool(v)}")
print("以下都被当作'真':")
for v in [True, 1, -1, "a", [0], [None], {"k": 0}]:
    print(f"  bool({v!r}) = {bool(v)}")

# --- 6. 实用：判断空 ---
print("\\n=== 6. 判断空（推荐写法）===")
items = []
# 推荐：直接判断真假
if items:
    print("列表非空")
else:
    print("列表空 ← items 是 []，被当作假")

name = ""
if not name:    # 等价 name == ""，但更 Pythonic
    print("名字是空的")`
  },

  // -----------------------------------------------------------
  // 第 10 章：列表基础
  // -----------------------------------------------------------
  {
    id: "py9-10",
    group: "数据：Python 里的东西",
    icon: "📋",
    title: "列表（一）：有序容器的诞生",
    content: `## 为什么需要列表

前面我们学的变量只能存一个值：\`age = 18\`、\`name = "小明"\`。但现实里数据常常是"一堆"：一个班 30 个成绩、购物车 5 件商品、通讯录 100 个联系人。一个个变量 \`score1\`、\`score2\`、\`score3\`… 太蠢了——你不知道到底有多少个，也没法批量处理。

**列表**就是装一堆值的容器，**有序、可变**：

\`\`\`python
scores = [90, 85, 78, 92, 88]                    # 数字列表
names = ["张三", "李四", "王五"]                  # 字符串列表
mixed = [1, "hello", True, 3.14]   # 可以混装不同类型
\`\`\`

打个比方：变量像"一个口袋"，只能装一个东西；列表像"一排抽屉"，每个抽屉装一个东西，按顺序排列，能增删改。这就是为什么列表这么重要——它让你能处理"批量数据"。

列表的两个核心特性：
- **有序**：元素有先后顺序，有下标（第 1 个、第 2 个…）
- **可变**：能增删改里面的元素（这是和字符串最大的区别，字符串不可变）

## 索引和切片（和字符串一样）

列表的索引切片规则和字符串**完全一样**——这是 Python 设计的统一性，学过字符串，列表的索引切片就免费会了。

\`\`\`python
scores[0]      # 90，第1个
scores[-1]     # 88，最后1个
scores[1:3]    # [85, 78]，切片得到新列表
\`\`\`

字符串的索引切片规则在列表上完全适用，复习一下：包含起、不包含止，支持负数，支持步长。\`scores[::-1]\` 同样能反转列表。

| 操作 | 字符串 | 列表 |
|------|--------|------|
| 索引 | \`s[0]\` 返回字符 | \`lst[0]\` 返回元素 |
| 切片 | \`s[0:3]\` 返回子串 | \`lst[0:3]\` 返回子列表 |
| 长度 | \`len(s)\` | \`len(lst)\` |
| 不可变 | ✅ 不可变 | ❌ 可变 |

## 修改元素

列表可变，能直接改（这点和字符串不同）：

\`\`\`python
scores[0] = 95    # 把第1个改成95
scores[1:3] = [80, 70]   # 切片赋值，一次改一段
\`\`\`

切片赋值很强大——你可以用一段替换另一段，甚至长度不同：\`scores[0:2] = [1, 2, 3, 4]\`（用 4 个替换 2 个，列表变长）。这是因为切片赋值是"删除一段，插入新内容"。

为什么字符串不能改而列表能改？因为字符串不可变（设计选择，安全+优化），列表可变（方便批量操作）。理解"可变 vs 不可变"是 Python 的核心概念，后面学字典、集合都要用到。

## 遍历

遍历列表就是"挨个看一遍元素"，用 for 循环：

\`\`\`python
for s in scores:        # 直接遍历元素
    print(s)  # 打印输出到屏幕
for i, s in enumerate(scores):   # 同时要下标和元素
    print(i, s)  # 打印输出到屏幕
\`\`\`

两种写法看需求：只要元素用第一种，要下标用 \`enumerate\`。\`enumerate\` 是 Python 的"神器"——它同时给你下标和元素，比 \`for i in range(len(scores))\` 优雅得多。

⚠️ **注意**：遍历时不要边遍历边增删元素，会导致跳过或重复。要改元素可以，要增删元素先复制一份或用别的方式。

## 常用操作

| 操作 | 例子 | 含义 |
|------|------|------|
| 长度 | \`len(scores)\` | 元素个数 |
| 是否包含 | \`90 in scores\` | 90 在不在列表里 |
| 拼接 | \`[1,2] + [3,4]\` | [1,2,3,4] |
| 重复 | \`[0] * 3\` | [0,0,0] |
| 最大最小 | \`max(scores)\` / \`min(scores)\` | |
| 求和 | \`sum(scores)\` | 所有元素加起来 |
| 计数 | \`scores.count(90)\` | 90 出现几次 |

\`in\` 运算符很常用——判断"某东西在不在列表里"，返回布尔值。比如 \`if 90 in scores:\` 判断 90 是不是成绩之一。\`sum\` 和 \`len\` 配合能算平均：\`avg = sum(scores) / len(scores)\`。

## 嵌套列表（列表里装列表）

列表的元素可以是任何东西，包括列表。这就形成了"嵌套列表"，常用来表示矩阵、表格：

\`\`\`python
matrix = [  # 定义列表 matrix
    [1, 2, 3],  # 执行操作
    [4, 5, 6],  # 执行操作
    [7, 8, 9],  # 执行操作
]
matrix[0]       # [1, 2, 3]  第1行
matrix[1][2]    # 6  第2行第3列
\`\`\`

\`matrix[1][2]\` 是"两次索引"——先 \`matrix[1]\` 拿到第 2 行 \`[4,5,6]\`，再 \`[2]\` 拿到那个列表的第 3 个元素 6。理解这个"层层取"的逻辑，就能处理多维数据。

遍历嵌套列表用"双层 for"：

\`\`\`python
for row in matrix:        # 外层遍历每一行
    for v in row:         # 内层遍历行里的每个元素
        print(v, end=" ")  # 打印输出到屏幕
    print()  # 打印输出到屏幕
\`\`\`

这是处理二维数据的标准套路——外层走行，内层走列。学完这个，你就能处理电子表格、图像像素这类二维数据。

## 逐行解读本章 demo

**第 1 部分创建**：演示数字列表、字符串列表、混合列表、空列表。注意 \`mixed\` 能混装不同类型——这是 Python 列表的灵活性。

**第 2 部分索引**：\`scores[0]\` 第 1 个，\`scores[-1]\` 最后 1 个。规则和字符串一样。

**第 3 部分切片**：\`scores[1:3]\`、\`scores[:2]\`、\`scores[-2:]\`、\`scores[::-1]\`（反转）。

**第 4 部分修改**：\`scores[0] = 95\` 改单个，\`scores[1:3] = [80, 70]\` 改一段。注意改的是原列表，不是新列表。

**第 5 部分遍历**：演示直接遍历和 \`enumerate\` 遍历。enumerate 同时给下标和元素。

**第 6 部分常用操作**：\`len\`、\`sum\`、\`max\`、\`min\`、\`in\`、\`+\`、\`*\`。算平均用 \`sum/len\`。

**第 7 部分嵌套列表**：3x3 矩阵，\`matrix[0]\` 取行，\`matrix[1][2]\` 取元素。双层 for 遍历。

## ⚠️ 初学者常见坑

1. **列表赋值是"贴标签"不是"复制"**：\`b = a\` 后改 \`a\`，\`b\` 也变。要复制用 \`a.copy()\` 或 \`a[:] \`。这是第 4 章讲过的"变量是标签"在列表上的体现。
2. **索引越界**：\`scores = [1,2,3]\`，\`scores[3]\` 报 IndexError。长度 3，索引 0-2。
3. **遍历时增删**：边遍历边删会跳过元素。要先复制或用别的方式。
4. **\`max\`/\`min\` 对空列表报错**：\`max([])\` 报 ValueError。先判断非空。
5. **混合类型不能排序**：\`[1, "a"]\` 排序会报 TypeError，因为 int 和 str 不能比。

## 小结

- 列表是"有序、可变"的容器，能装一堆值，可混装不同类型
- 索引切片规则和字符串完全一样（左闭右开、支持负数和步长）
- 列表**可变**，能直接改元素（\`lst[0] = x\`），这是和字符串的关键区别
- 遍历用 for，要下标用 \`enumerate\`
- 嵌套列表表示二维数据，\`matrix[i][j]\` 取元素，双层 for 遍历

## 常见疑问 Q&A

**Q：列表和字符串有什么区别？**
A：列表可变（能改元素），字符串不可变；列表能装任何类型，字符串只能装字符。但索引切片规则一样。

**Q：\`b = a\` 为什么改 a 会影响 b？**
A：因为 \`b = a\` 是"贴标签"，b 和 a 贴在同一份列表上。要独立复制用 \`b = a.copy()\` 或 \`b = a[:]\`。

**Q：列表能装不同类型吗？**
A：能，\`[1, "a", True]\` 合法。但一般建议装同类型（方便处理），混装是 Python 的灵活性，不一定好用。

**Q：怎么复制列表？**
A：三种方式：\`a.copy()\`（最直观）、\`a[:]\`（切片复制）、\`list(a)\`（构造函数）。三者效果一样，都是"浅复制"。涉及嵌套列表要"深复制"用 \`copy.deepcopy\`，后面会讲。`,
    code: `# ============================================
# 第 10 章：列表基础
# ============================================

# --- 1. 创建 ---
print("=== 1. 创建列表 ===")
scores = [90, 85, 78, 92, 88]
names = ["张三", "李四", "王五"]
mixed = [1, "hello", True, 3.14]
empty = []                       # 空列表
print(f"成绩: {scores}")
print(f"姓名: {names}")
print(f"混合: {mixed}")
print(f"空列表: {empty}, 长度 {len(empty)}")

# --- 2. 索引 ---
print("\\n=== 2. 索引 ===")
print(f"scores = {scores}")
print(f"scores[0]  = {scores[0]}    ← 第1个")
print(f"scores[-1] = {scores[-1]}   ← 最后1个")
print(f"scores[2]  = {scores[2]}")

# --- 3. 切片 ---
print("\\n=== 3. 切片 ===")
print(f"scores[1:3] = {scores[1:3]}   ← 取下标1,2")
print(f"scores[:2]  = {scores[:2]}    ← 前2个")
print(f"scores[-2:] = {scores[-2:]}   ← 最后2个")
print(f"scores[::-1] = {scores[::-1]} ← 反转")

# --- 4. 修改（列表可变！）---
print("\\n=== 4. 修改 ===")
print(f"修改前: {scores}")
scores[0] = 95                    # 改第1个
print(f"scores[0]=95 后: {scores}")
scores[1:3] = [80, 70]            # 切片赋值，一次改一段
print(f"scores[1:3]=[80,70] 后: {scores}")

# --- 5. 遍历 ---
print("\\n=== 5. 遍历 ===")
print("方式1：直接遍历元素")
for s in scores:
    print(f"  {s}", end=" ")
print()
print("方式2：用 enumerate 拿下标+元素")
for i, s in enumerate(scores):
    print(f"  第{i}个: {s}")

# --- 6. 常用操作 ---
print("\\n=== 6. 常用操作 ===")
nums = [90, 85, 78, 92, 88]
print(f"nums = {nums}")
print(f"len(nums)   = {len(nums)}")
print(f"sum(nums)   = {sum(nums)}")
print(f"max(nums)   = {max(nums)}")
print(f"min(nums)   = {min(nums)}")
print(f"avg         = {sum(nums) / len(nums):.1f}")
print(f"90 in nums  = {90 in nums}")
print(f"100 in nums = {100 in nums}")
print(f"[1,2] + [3,4] = {[1,2] + [3,4]}")
print(f"[0] * 5 = {[0] * 5}")

# --- 7. 列表里装列表（嵌套）---
print("\\n=== 7. 嵌套列表 ===")
matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9],
]
print(f"矩阵: {matrix}")
print(f"matrix[0]    = {matrix[0]}     ← 第1行")
print(f"matrix[1][2] = {matrix[1][2]}  ← 第2行第3列")
# 遍历二维
print("遍历矩阵:")
for row in matrix:
    for v in row:
        print(v, end=" ")
    print()`
  },
];
