// =============================================================
// 后端开发必备知识 - 第 13 批章节（高可用与云原生 8 章）
// =============================================================

export const chapters = [
  {
    id: "backend-13-0",
    group: "高可用与云原生",
    icon: "🛡️",
    title: "高可用架构设计",
    content: `
# 高可用架构设计

## 一、高可用基本概念

### 1.1 什么是高可用

高可用（High Availability, HA）是系统在面对各种异常时仍能持续提供服务的能力。通常用"几个9"来衡量：

| 可用性等级 | 年 downtime | 适用场景 |
|-----------|------------|---------|
| 99%（2个9） | 3.65天 | 内部系统、非核心业务 |
| 99.9%（3个9） | 8.76小时 | 一般企业级应用 |
| 99.99%（4个9） | 52.56分钟 | 核心业务系统 |
| 99.999%（5个9） | 5.26分钟 | 金融、电信级系统 |

### 1.2 高可用核心手段

- **冗余**：单点是高可用的大敌，所有关键组件都要冗余部署
- **故障转移**：自动检测故障并切换到备用节点
- **降级**：非核心功能暂时关闭，保证核心功能可用
- **限流**：防止流量突增打垮系统
- **熔断**：快速失败，避免雪崩效应

---

## 二、常见高可用架构模式

### 2.1 主从架构（Master-Slave）

\`\`\`
   ┌─────────┐
   │  Client │
   └────┬────┘
        │ Write/Read
        ▼
   ┌─────────┐
   │ Master  │ ← 写操作
   └────┬────┘
        │ 数据同步
   ┌────┴────┐
   ▼         ▼
┌──────┐  ┌──────┐
│Slave1│  │Slave2│ ← 读操作
└──────┘  └──────┘
\`\`\`

典型应用：MySQL主从复制、Redis主从

### 2.2 主备切换（Failover）

主节点故障时，自动提升备节点为主节点：

1. **健康检查**：持续检测主节点状态
2. **故障判定**：多次失败确认主节点真的挂了
3. **选举/切换**：通过一致性协议（如Raft）选举新主
4. **流量切换**：更新路由，将流量切到新主

### 2.3 集群与负载均衡

多节点同时提供服务，通过负载均衡分发流量：

- **无状态服务**：可以任意水平扩展，节点间无差异
- **有状态服务**：需要考虑数据一致性，通常用分片+副本

---

## 三、降级、限流与熔断

### 3.1 服务降级

当系统压力过大时，有策略地关闭非核心功能：

- **开关降级**：配置中心控制功能开关
- **自动降级**：根据超时、错误率自动降级
- **降级策略**：返回默认值、返回缓存数据、返回友好提示

### 3.2 限流算法

| 算法 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| 计数器固定窗口 | 统计单位时间内请求数 | 实现简单 | 临界问题 |
| 滑动窗口 | 将时间窗口分小格，滑动统计 | 解决临界问题 | 实现稍复杂 |
| 漏桶算法 | 请求进入漏桶，以固定速率流出 | 平滑流量 | 无法应对突发流量 |
| 令牌桶算法 | 固定速率往桶里放令牌，取到令牌才能通过 | 允许一定突发 | 实现稍复杂 |

### 3.3 熔断器模式（Circuit Breaker）

三种状态：

1. **关闭（Closed）**：正常调用，统计错误率
2. **打开（Open）**：错误率超阈值，直接返回错误，不调用下游
3. **半开（Half-Open）**：超时后放少量请求试探，成功则关闭，失败则继续打开

---

## 四、本章小结

高可用是后端架构的核心目标之一，需要从冗余、故障转移、限流熔断降级等多个维度综合设计。
`
  },
  {
    id: "backend-13-1",
    group: "高可用与云原生",
    icon: "📊",
    title: "监控告警与可观测性",
    content: `
# 监控告警与可观测性

## 一、可观测性三大支柱

### 1.1 指标（Metrics）

数值型的、可聚合的数据，反映系统状态：

- **黄金指标**：延迟（Latency）、流量（Traffic）、错误（Errors）、饱和度（Saturation）
- **RED 方法**：Rate（请求速率）、Errors（错误率）、Duration（延迟）
- **USE 方法**：Utilization（利用率）、Saturation（饱和度）、Errors（错误）

**常见指标类型：**

| 类型 | 说明 | 示例 |
|------|------|------|
| Counter | 只增不减的计数器 | 请求总数、错误总数 |
| Gauge | 可上下波动的瞬时值 | CPU使用率、内存使用、队列长度 |
| Histogram | 分布统计，分位数 | 请求延迟分布（p50/p95/p99） |
| Summary | 类似Histogram，客户端计算分位数 | 同上 |

### 1.2 日志（Logging）

记录离散的事件，用于问题排查：

- **日志级别**：TRACE < DEBUG < INFO < WARN < ERROR < FATAL
- **结构化日志**：JSON格式，便于检索分析
- **最佳实践**：
  - 不要打印敏感信息（密码、密钥、身份证号）
  - 关键路径打印足够上下文（traceId、userId、orderId）
  - 不要在循环中打印大量日志
  - 异常要打印完整栈信息

### 1.3 链路追踪（Tracing）

分布式环境下追踪一个请求经过的所有服务：

核心概念：
- **Trace**：一次完整请求链路，唯一traceId
- **Span**：链路中的一个环节，有开始结束时间
- **Context Propagation**：跨进程传递trace上下文

---

## 二、主流监控栈

### 2.1 Prometheus + Grafana

- **Prometheus**：时序数据库，拉模式采集指标，PromQL查询
- **Grafana**：可视化仪表盘
- **Alertmanager**：告警发送（邮件、钉钉、企业微信等）

### 2.2 ELK Stack (Elasticsearch, Logstash, Kibana)

- **Logstash/Fluentd/Filebeat**：日志采集
- **Elasticsearch**：日志存储与检索
- **Kibana**：日志查询与可视化

### 2.3 Jaeger / Zipkin / SkyWalking

分布式链路追踪系统：
- **Jaeger**：Uber开源，CNCF毕业项目
- **Zipkin**：Twitter开源
- **SkyWalking**：Apache开源，国产，对Java支持好，无侵入

---

## 三、告警设计

好的告警应该：
1. **可行动**：收到告警知道要做什么
2. **低噪声**：避免告警轰炸，只报真正需要处理的问题
3. **分级**：P0（紧急，立即处理）、P1（高优，工作时间处理）、P2（低优，有空再看）
4. **带上下文**：告警内容包含问题描述、影响范围、排查链接、预案

---

## 四、本章小结

完善的监控可观测体系是系统稳定运行的保障，没有监控的系统就是在裸奔。
`
  },
  {
    id: "backend-13-2",
    group: "高可用与云原生",
    icon: "☁️",
    title: "云原生基础",
    content: `
# 云原生基础

## 一、什么是云原生

云原生（Cloud Native）是一套构建和运行应用的方法论，充分利用云计算模型的优势，让应用天然适合在云上运行。

CNCF（云原生计算基金会）定义的云原生代表技术：
- **容器化**（Containers）
- **服务网格**（Service Mesh）
- **微服务**（Microservices）
- **不可变基础设施**（Immutable Infrastructure）
- **声明式API**（Declarative APIs）

---

## 二、容器化与Docker

### 2.1 Docker核心概念

- **镜像（Image）**：只读模板，包含运行应用所需的一切（代码、运行时、依赖、配置）
- **容器（Container）**：镜像的运行实例，隔离的进程
- **仓库（Registry）**：存储镜像的地方，如Docker Hub、阿里云镜像服务

### 2.2 Dockerfile示例

\`\`\`dockerfile
# 多阶段构建
FROM maven:3.8-openjdk-17 AS builder
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline
COPY src ./src
RUN mvn package -DskipTests

# 运行阶段
FROM openjdk:17-jre-slim
WORKDIR /app
COPY --from=builder /app/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
\`\`\`

### 2.3 常用Docker命令

\`\`\`bash
docker build -t myapp:v1 .           # 构建镜像
docker run -d -p 8080:8080 myapp:v1 # 运行容器
docker ps                            # 查看运行中的容器
docker logs -f <container-id>        # 查看容器日志
docker exec -it <container-id> sh    # 进入容器
docker stop/start/restart            # 停止/启动/重启
docker rm <container-id>             # 删除容器
docker rmi <image-id>                # 删除镜像
\`\`\`

---

## 三、Kubernetes (K8s) 基础

### 3.1 核心组件

- **Master节点**：控制平面
  - kube-apiserver：API入口，所有组件交互的枢纽
  - etcd：分布式键值存储，保存集群状态
  - kube-scheduler：调度Pod到合适节点
  - kube-controller-manager：各种控制器
- **Node节点**：工作节点
  - kubelet：与Master通信，管理本节点Pod
  - kube-proxy：网络代理，实现Service网络
  - 容器运行时：Docker/containerd

### 3.2 核心资源对象

| 资源 | 作用 |
|------|------|
| Pod | 最小调度单元，包含一个或多个容器 |
| Deployment | 管理无状态应用，声明式更新、扩缩容 |
| StatefulSet | 管理有状态应用（如数据库），稳定网络标识、持久存储 |
| Service | 为Pod提供固定访问端点，负载均衡 |
| ConfigMap | 配置管理，不用打包进镜像 |
| Secret | 敏感信息管理（密码、密钥） |
| Ingress | HTTP路由，七层负载均衡 |

---

## 四、CI/CD 持续集成持续部署

典型流程：

1. 开发人员提交代码到Git
2. 触发CI流水线：
   - 代码检查（SonarQube）
   - 单元测试
   - 构建打包
   - 构建Docker镜像
   - 推送到镜像仓库
3. 触发CD流水线：
   - 部署到测试环境
   - 自动化测试
   - 手动确认/自动发布到生产
   - 金丝雀发布/蓝绿发布

常用工具：Jenkins、GitLab CI、GitHub Actions、Argo CD（GitOps）

---

## 五、本章小结

云原生是现代后端开发的趋势，容器化和K8s已经成为后端工程师的必备技能。
`
  },
  {
    id: "backend-13-3",
    group: "高可用与云原生",
    icon: "🔄",
    title: "蓝绿部署与金丝雀发布",
    content: `
# 蓝绿部署与金丝雀发布

## 一、发布策略对比

| 策略 | 说明 |  downtime | 回滚速度 | 风险 | 适用场景 |
|------|------|-----------|---------|------|---------|
| 停机发布 | 停服更新，再启动 | 有，较长 | 慢 | 高 | 非核心、可维护窗口 |
| 滚动发布 | 逐个替换实例 | 无 | 中 | 中 | 一般场景 |
| 蓝绿部署 | 两套环境，切流量 | 极少 | 极快 | 低 | 核心业务 |
| 金丝雀发布 | 小流量验证，逐步放量 | 无 | 快 | 低 | 高风险变更、大版本 |

---

## 二、蓝绿部署（Blue-Green Deployment）

### 2.1 原理

准备两套完全相同的环境：
- **蓝环境**：当前运行的旧版本
- **绿环境**：部署新版本

绿环境部署好并测试通过后，将流量一次性从蓝切换到绿。出问题立即切回蓝。

\`\`\`
切换前：                 切换后：
  ┌──────┐                ┌──────┐
  │ 流量 │───┐            │ 流量 │───┐
  └──────┘   │            └──────┘   │
             ▼                         ▼
        ┌─────────┐              ┌─────────┐
        │ 蓝环境  │              │ 蓝环境  │
        │ v1.0    │              │ v1.0    │
        └─────────┘              └─────────┘
        ┌─────────┐              ┌─────────┐
        │ 绿环境  │              │ 绿环境  │
        │ v2.0    │              │ v2.0    │
        └─────────┘              └─────────┘
\`\`\`

### 2.2 优缺点

**优点：**
- 零 downtime
- 回滚极快（切流量即可）
- 新版本可以在绿环境充分测试

**缺点：**
- 需要双倍资源
- 数据库变更需要兼容新旧版本
- 不适合长连接场景（切流量连接断开）

---

## 三、金丝雀发布（Canary Release）

### 3.1 原理

先让一小部分流量（比如1%、5%）到新版本，观察没问题再逐步扩大流量比例，直到全量。

名字由来：矿工下井前先放金丝雀，金丝雀对毒气敏感，它没事矿工再下井。

\`\`\`
阶段1：5%流量到v2        阶段2：30%流量到v2       阶段3：100%流量到v2

  ┌──────┐ 95%           ┌──────┐ 70%           ┌──────┐
  │ 流量 │──────┐        │ 流量 │──────┐        │ 流量 │──────────┐
  └──────┘      │        └──────┘      │        └──────┘          │
           5%   ▼                     ▼                            ▼
         ┌──┐ ┌─────┐             ┌─────┐                      ┌─────┐
         │v1│ │ v2  │             │ v2  │                      │ v2  │
         └──┘ └─────┘             └─────┘                      └─────┘
\`\`\`

### 3.2 流量切分方式

- **按比例**：Nginx/Ingress权重配置
- **按用户特征**：只让内部员工、白名单用户先用
- **按地区**：先在某个城市/地区放量
- **灰度放量 + 监控对比**：对比新旧版本的错误率、延迟等指标

### 3.3 优缺点

**优点：**
- 风险极低，影响面小
- 可以用真实流量验证新版本
- 可以进行A/B测试
- 无 downtime

**缺点：**
- 发布周期长，需要观察
- 需要流量控制能力
- 数据库兼容问题
- 需要完善的监控来判断新版本是否正常

---

## 四、回滚策略

无论哪种发布方式，都要有回滚预案：

1. **应用回滚**：切回旧版本流量（蓝绿最快）
2. **数据回滚**：
   - 尽量做向前兼容，避免回滚数据
   - 数据变更先备份
   - DDL变更要兼容新旧版本（比如先加字段，再部署代码，再删旧字段）

---

## 五、本章小结

选择合适的发布策略能有效降低上线风险，核心系统建议使用蓝绿或金丝雀发布。
`
  },
  {
    id: "backend-13-4",
    group: "高可用与云原生",
    icon: "🔒",
    title: "数据安全与备份恢复",
    content: `
# 数据安全与备份恢复

## 一、数据备份策略

### 1.1 备份类型

| 类型 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| 全量备份 | 备份所有数据 | 恢复简单 | 耗时久、占用空间大 |
| 增量备份 | 只备份上次备份后变化的数据 | 快、空间小 | 恢复麻烦（需要全量+所有增量） |
| 差异备份 | 备份上次全量后变化的数据 | 恢复比增量简单 | 备份比增量大 |

### 1.2 常见备份方案

- **MySQL**：
  - 逻辑备份：mysqldump（适合小数据量）
  - 物理备份：XtraBackup（热备，适合大数据量）
  - 主从复制也算实时备份
- **Redis**：RDB快照 + AOF持久化
- **定期备份策略**：
  - 每日全量（或增量）
  - 每周全量
  - 备份文件保留30天以上
  - 异地备份（不要只存在同一台机器/机房）

---

## 二、数据安全

### 2.1 传输安全

- 全站HTTPS/TLS加密
- 内部服务间通信也加密（mTLS）
- 敏感数据传输额外加密

### 2.2 存储安全

- **密码**：bcrypt/Argon2哈希，绝对不能明文存储
- **敏感字段**：身份证号、手机号、银行卡号等加密存储（AES）
- **密钥管理**：不要硬编码在代码里，用配置中心或KMS
- **数据库访问**：最小权限原则，业务账号不要用root
- **脱库防护**：禁止公网访问数据库，IP白名单

### 2.3 数据脱敏

日志、测试环境、数据导出时要脱敏：

- 手机号：138****1234
- 身份证：110***********1234
- 邮箱：z***@example.com

---

## 三、容灾与多活

### 3.1 灾备等级

- **同城灾备**：同城两个机房，RTO（恢复时间）分钟级
- **异地灾备**：不同城市，RTO小时级
- **两地三中心**：生产中心 + 同城灾备 + 异地灾备
- **异地多活**：多个机房同时提供服务（最高级别）

### 3.2 多活架构挑战

- 数据一致性：多机房数据同步延迟
- 流量调度：根据地理位置、健康状态调度流量
- 全局唯一ID：不能用数据库自增ID
- 故障切换：单机房故障自动切走流量

---

## 四、本章小结

数据是企业最宝贵的资产，做好备份和安全是底线，任何时候都不能掉以轻心。
`
  },
  {
    id: "backend-13-5",
    group: "高可用与云原生",
    icon: "📝",
    title: "API设计最佳实践",
    content: `
# API设计最佳实践

## 一、RESTful API 设计

### 1.1 REST核心原则

- **资源导向**：URL表示资源，用名词不用动词
- **HTTP方法表示操作**：GET（查询）、POST（创建）、PUT（全量更新）、PATCH（部分更新）、DELETE（删除）
- **HTTP状态码表示结果**：2xx成功，4xx客户端错误，5xx服务端错误
- **无状态**：每个请求包含所有需要的信息，服务端不保存会话状态

### 1.2 URL设计规范

| 操作 | 错误示例 | 正确示例 |
|------|---------|---------|
| 查询用户列表 | /getUsers | GET /users |
| 查询单个用户 | /getUser?id=1 | GET /users/1 |
| 创建用户 | /createUser | POST /users |
| 更新用户 | /updateUser?id=1 | PUT /users/1 |
| 删除用户 | /deleteUser?id=1 | DELETE /users/1 |
| 查询用户订单 | /getOrders?userId=1 | GET /users/1/orders |

### 1.3 常用状态码

| 状态码 | 含义 | 使用场景 |
|-------|------|---------|
| 200 OK | 成功 | GET/PUT/PATCH成功 |
| 201 Created | 创建成功 | POST创建成功，返回Location头指向新资源 |
| 204 No Content | 成功无返回 | DELETE成功 |
| 400 Bad Request | 请求参数错误 | 参数校验失败 |
| 401 Unauthorized | 未认证 | 没登录/token无效 |
| 403 Forbidden | 无权限 | 登录了但没权限操作 |
| 404 Not Found | 资源不存在 | URL或资源ID不对 |
| 409 Conflict | 资源冲突 | 重复创建（如用户名已存在） |
| 429 Too Many Requests | 请求限流 | 被限流了 |
| 500 Internal Server Error | 服务端错误 | 代码bug |

---

## 二、请求响应格式

### 2.1 统一响应格式

\`\`\`json
{
  "code": 0,           // 业务状态码，0表示成功，非0表示错误
  "message": "success",// 提示信息
  "data": { ... },     // 响应数据
  "requestId": "xxx"   // 请求ID，用于排查问题
}
\`\`\`

分页列表：
\`\`\`json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [ ... ],   // 当前页数据
    "total": 100,      // 总条数
    "page": 1,         // 当前页码
    "pageSize": 20     // 每页条数
  }
}
\`\`\`

### 2.2 错误处理

\`\`\`json
{
  "code": 40001,
  "message": "参数错误：用户名不能为空",
  "data": null,
  "requestId": "abc123"
}
\`\`\`

---

## 三、API设计技巧

1. **版本化**：/api/v1/users，/api/v2/users，兼容旧版本
2. **字段过滤**：支持 fields 参数只返回需要的字段
3. **分页**：大数据量一定要分页，page+pageSize 或 cursor（游标，适合大数据）
4. **排序**：?sort=createTime,desc
5. **幂等性**：GET/PUT/DELETE 要幂等，重复调用结果一样
6. **限流**：接口要有限流，防止被刷
7. **接口文档**：Swagger/OpenAPI 自动生成文档
8. **向后兼容**：字段只加不删，改字段含义要谨慎

---

## 四、GraphQL 简介

REST的缺点：
- 多个端点，前端要多次请求
- 返回数据要么太多要么太少（字段冗余或不够）

GraphQL解决：
- 单一端点 /graphql
- 前端精确指定需要什么字段
- 一次请求获取所有需要的数据

适用场景：
- 客户端需要灵活获取数据
- 多端（Web/App）字段需求差异大

---

## 五、本章小结

好的API设计易理解、易使用、易维护，是前后端协作的基础。
`
  },
  {
    id: "backend-13-6",
    group: "高可用与云原生",
    icon: "💼",
    title: "后端工程师职业发展",
    content: `
# 后端工程师职业发展

## 一、能力层级模型

### 1.1 初级工程师（0-2年）

- 能在指导下完成模块开发
- 熟悉常用框架的基本用法
- 能写CRUD，了解基本的SQL
- 代码能跑，但质量一般，边界情况考虑不周
- 需要别人Review代码

### 1.2 中级工程师（2-5年）

- 能独立负责一个子系统/模块
- 熟练使用技术栈，了解一些底层原理
- 能写出高质量代码，考虑异常、边界、性能
- 能做数据库设计、接口设计
- 能排查线上问题
- 对系统稳定性、性能有概念

### 1.3 高级工程师（5-8年）

- 能独立负责一个中型系统/服务
- 深入理解技术原理，知其然知其所以然
- 能做架构设计，选择技术方案，权衡trade-off
- 能解决复杂技术问题，性能调优、疑难杂症排查
- 有项目全局观，能推动项目落地
- 能指导初中级工程师

### 1.4 资深/专家工程师（8年+）

- 能负责大型分布式系统架构设计
- 在某个技术领域有深入研究（如数据库、中间件、JVM）
- 能做技术选型、架构演进规划
- 能解决跨系统的复杂问题
- 有技术影响力（技术分享、开源贡献等）
- 能推动团队技术升级

---

## 二、核心硬技能

### 2.1 基础扎实

- 数据结构与算法（不用刷竞赛题，但常用的要会）
- 操作系统（进程线程、内存、IO）
- 网络（TCP/IP、HTTP）
- 数据库（SQL、索引、事务、锁、优化）

### 2.2 技术深度

- 至少精通一门语言（Java/Go/C++等）
- 深入理解使用的框架（Spring等），知道原理
- 熟悉常用中间件（Redis、MQ、Nginx等）
- 分布式系统理论与实践

### 2.3 技术广度

- 了解前端知识，能写点JS/HTML
- 了解DevOps，Docker/K8s会用
- 了解云服务（AWS/阿里云等）
- 对新技术保持敏感，但不盲目追新

---

## 三、软技能

### 3.1 沟通协作

- 能清晰表达自己的想法
- 能和产品、前端、测试有效沟通
- 开会有准备，讨论有结论

### 3.2 问题解决能力

- 遇到问题先自己排查，而不是直接问
- 会Google/StackOverflow，会看文档
- 线上问题冷静处理，先止损再排查根因
- 总结复盘，不犯同样的错

### 3.3 学习能力

- IT技术更新快，要持续学习
- 看官方文档、经典书籍、优秀博客
- 读源码是提升的好方法
- 动手实践，写Demo、做项目

---

## 四、学习建议

1. **打好基础**：基础决定上限，不要上来就只会调API
2. **深入原理**：多问几个为什么，源码读起来
3. **项目实战**：理论结合实践，做项目才能真正掌握
4. **总结输出**：写博客、做分享，输出倒逼输入
5. **关注质量**：写代码认真点，不要图快堆屎山
6. **线上思维**：写代码时就考虑异常、性能、并发、安全

推荐书籍：
- Java：《深入理解Java虚拟机》、《Java并发编程实战》、《Effective Java》
- 通用：《代码整洁之道》、《设计模式》、《数据密集型应用系统设计》（DDIA神书）
- 分布式：《从Paxos到Zookeeper》

---

## 五、本章小结

技术这条路，持续学习是关键。打好基础，深度广度结合，加上项目实战，一定能成长为优秀的工程师。
`
  },
  {
    id: "backend-13-7",
    group: "高可用与云原生",
    icon: "🎯",
    title: "后端面试指南",
    content: `
# 后端面试指南

## 一、面试考察点

| 方面 | 权重 | 考察内容 |
|------|------|---------|
| 项目经验 | ⭐⭐⭐⭐⭐ | 做过什么，怎么做的，遇到什么问题，怎么解决的，有什么思考 |
| 基础知识 | ⭐⭐⭐⭐ | 语言基础、JVM/Go runtime、集合、并发、网络、OS、数据库 |
| 算法题 | ⭐⭐⭐ | 中等难度为主，LeetCode 100-200题足够，考察思维和编码习惯 |
| 系统设计 | ⭐⭐⭐⭐ | 中高级岗位重点，给一个场景设计系统，考察架构思维 |
| 软素质 | ⭐⭐⭐ | 沟通、逻辑、学习能力、态度 |

---

## 二、简历怎么写

1. **真实**：不要造假，面试官一追问就露馅
2. **突出亮点**：不要写流水账，写你做得好的、有技术含量的
3. **用数据说话**：
   - 不好："优化了接口性能"
   - 好："通过缓存+SQL优化，将订单列表接口QPS从500提升到5000，延迟从500ms降到50ms"
4. **STAR法则描述项目**：Situation（背景）→ Task（任务）→ Action（你做了什么）→ Result（结果）
5. **技术栈写你真正会的**：不要写精通一堆，一问三不知

---

## 三、项目经验怎么讲

准备1-2个拿得出手的项目，按这个思路讲：

1. **背景**：这是个什么项目？解决什么问题？用户规模/量级多大？
2. **架构**：整体架构是什么样的？用了什么技术？你负责哪部分？
3. **难点/亮点**：遇到什么技术挑战？你是怎么解决的？为什么选这个方案？对比过其他方案吗？
4. **成果**：取得了什么效果？QPS、延迟、稳定性数据？
5. **思考**：如果让你重构，你会怎么改进？有什么遗憾？

**常见追问准备：**
- 遇到的最大的坑是什么？怎么解决的？
- 系统QPS多少？数据量多大？
- 这个地方为什么这么设计？有没有考虑过XX方案？
- 线上出过什么故障？怎么排查和解决的？

---

## 四、算法题准备

1. **刷 LeetCode Hot 100 + 剑指Offer**，足够应对大部分公司
2. **按专题刷**：数组、链表、树、动态规划、回溯、二分查找...
3. **不要背题**：理解思路，能自己写出来
4. **刷题时注意**：
   - 先想思路，说给面试官听，再写代码
   - 考虑边界情况（空、0、1、最大值、异常输入）
   - 写完自己走几个测试case
   - 代码风格要好，变量命名清晰
5. **常见必会算法**：
   - 排序：快排、归并、堆排的思想和复杂度
   - 二分查找及其变体
   - 链表操作（反转、合并、环检测）
   - 二叉树遍历（前中后层序）、BST
   - DFS/BFS
   - 动态规划（背包、最长子序列等常见题）
   - 设计LRU缓存

---

## 五、系统设计题思路

经典题：设计短链接、设计秒杀系统、设计消息队列、设计秒杀、设计Feed流、设计限流框架...

回答框架：

1. **问清楚需求**：
   - QPS多少？数据量多大？
   - 功能边界是什么？有哪些核心功能？
   - 有什么约束？（一致性、延迟要求）
2. **高层设计**：画整体架构图，有哪些组件，怎么交互
3. **核心组件设计**：
   - 数据模型怎么设计？表结构？
   - 接口怎么设计？
   - 关键流程是什么？
4. **深入讨论**：
   - 性能瓶颈在哪？怎么优化？
   - 怎么保证高可用？故障怎么处理？
   - 怎么扩展？
   - 有什么trade-off？

---

## 六、面试反问环节

面试最后一般让你问问题，好问题能加分：
- 团队负责的业务是什么？技术栈是什么？
- 团队目前面临的最大技术挑战是什么？
- 这个岗位的新人期望是什么样的？
- 团队的技术氛围怎么样？有没有技术分享、Code Review？

不要问：
- 你们加班多吗？（可以问团队节奏怎么样）
- 工资多少？（和HR谈）

---

## 七、本章小结

面试是双向选择，充分准备，自信表达，展示真实的自己。祝大家拿到心仪的Offer！
`
  }
];
